# PR-42 review

Date: 2026-09-11

## Identity

- PR: 42
- Target: `main`
- Base: `5455e5d137091f4947428550737052621cd90656`
- Merge base: `5455e5d137091f4947428550737052621cd90656`
- Head: `b616357f37bb90bc22c6b210a6148ec1a308adc0`
- Branch: `chore/night-time`

## Provider gate

Session 105 identifies Claude Code as the provider of the substantive change. The active reviewer is Codex. The gate passes under T-4 and D-101.

## Intended behavior and scope

PR #42 changes the scheduled night from 03:00 UTC to 08:00 UTC. The workflow comment must state 02:00 Central Standard Time and the summer time. The shape test must check the cron, the hand trigger, and the comment. The review inspected the complete diff, D-284, the Phase 1 roadmap, the workflow, the shape test, the handoff files, the agent files, and all PR comments.

No area remains uninspected.

## Findings

No finding.

The workflow uses the cron in D-284. The shape test checks the cron, the hand trigger, and the Central time comment. The metadata changes stay inside the D-184 allowlist. `AGENTS.md` and `CLAUDE.md` remain identical.

## Out of scope

None.

## PR comments

- The first automated pass approved the change and reported the expected missing review file. The author explained that the cross-provider review creates the file. The review verified the workflow and test independently.
- The second automated pass approved the change and reported that the review-gate check saw the prior `Blocked` verdict. The author explained that the verdict remained blocked only until the macOS result landed. The repeat review verifies that the macOS result now passes.

## Description edits

None.

## Verification

- `git diff b616357..bc7c457 --check`: passed. The new diff contains only review and handoff metadata.
- `dotnet test WhatYouCarry.slnx --no-build -m:1 --filter FullyQualifiedName~NightWorkflowRunsAtTwoCentralStandardTime`: passed. 1 test, 0 failures, 0 skips.
- The author response records 533 passed tests, 0 failures, and 0 skips on the effective head. Linux and Windows CI independently pass the full suite on the branch.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- ste-check --root .`: passed. 0 findings in 15 files.
- Remote checks at PR tip `bc7c457c91a55164f8b39f30e89ebf4876ff5c9b`: Linux, Windows, and macOS CI, three-platform bit identity, bots, det-lint, night-gate, STE check, and Gitar passed. The review-gate check failed only because the prior record still named `Blocked`.
- After the repeat review push, the review-gate check passed. Duplicate platform jobs from the metadata push remained pending at the final refresh.
- `git fetch origin`: passed. `git status --short --branch`: passed with no ahead count.
- Push: `7d3994ad11b7eb2ecac49d621e0186727906d4c6` is the head of `origin/chore/night-time`, verified with `gh pr view`.

## Open questions and accepted risks

OQ-99 remains open, but it blocks no work in this review.

## Earlier verdicts

**Blocked.** This verdict applied to effective head `b616357f37bb90bc22c6b210a6148ec1a308adc0`. The macOS CI result was pending, and the local full suite did not complete in the review execution context.

## Verdict

**Ready for owner merge.** This verdict applies to effective head `b616357f37bb90bc22c6b210a6148ec0`. The provider gate passes, the macOS CI result and the full-suite evidence now pass, and no finding remains. The review-gate check will rerun against this updated verdict.
