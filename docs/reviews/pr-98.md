# PR-98 review

Date: 2026-09-23

## Identity

- PR: 98
- Target: `main`
- Base: `2071cb66c8de7978bf8f302b20cf06d19358bedb`
- Merge base: `2071cb66c8de7978bf8f302b20cf06d19358bedb`
- Head: `bc51ddebf76dce49933c184aa9234eb6b2774bc9`
- Branch: `chore/pr-82-template-gitar-notice`

## Provider gate

The author provider is Claude Code, as Session 235 of the session handoff states. The reviewer provider is Codex. The providers differ, so the review meets T-4 and D-101.

## Intended behavior and scope

The PR changes the gitar line in the PR template to match the PR gate in `AGENTS.md` and adds a test for equality. The review read both changed implementation paths, the PR-82 roadmap entry and exit tests, the D-550 to D-554 decisions, all PR comments, and the supporting design and handoff changes. The test also checks that each file has exactly one matching line. No affected contract remains uninspected.

## Findings

No finding.

## Out of scope

The fixed seeds of the night belong to the next PR, as the PR-82 roadmap states (D-551).

## PR comments

- Gitar posted a free-plan notice with no item to address. It has no author answer and makes no review claim. D-550 says to ignore such notices.

## Description edits

None.

## Verification

- `git diff origin/main...HEAD`: reviewed all seven changed paths. The effective head is `bc51dde`; later commits change documents only.
- `dotnet test WhatYouCarry.slnx --filter FullyQualifiedName~PullRequestTemplateGitarLineMatchesThePrGate`: passed, 1 of 1.
- `dotnet test WhatYouCarry.slnx --filter Category=Documents`: passed, 141 of 141.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: passed, zero findings.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- doc-gate --root . --base origin/main --head HEAD --body /tmp/pr-98-body.md --title "chore: the gitar line of the PR template takes D-550 (PR-82)" --branch chore/pr-82-template-gitar-notice`: passed, zero problems.
- GitHub checks at head `d158af71`: `asset-qa`, both completed bit-identity jobs, `det-lint`, `documents`, `ste-check`, `doc-gate`, and Windows smoke passed. `night-gate` failed because the base night record is failed and no branch-night record exists. Linux smoke showed failed, but its workflow run was still in progress and its failure log was unavailable. The macOS bit-identity job, bots, platform CI jobs and sweeps, and macOS smoke were pending. The full required evidence is incomplete.
- The author handoff reports 1586 of 1586 full-suite tests passed at `bc51dde`; this review did not rerun the full suite.
- `evaluate` failed while required checks failed or remained pending. `review-gate` failed before this review record existed.
- Push: `3efc26604ba982d02ee4c57241afdc1029fedd51` was the head of `origin/chore/pr-82-template-gitar-notice`, verified with `gh pr view` after the first push. This record and handoff are in the amended metadata commit.

## Open questions and accepted risks

No open question or accepted risk affects this change.

## Verdict

**Blocked.** This verdict applies to head `bc51ddebf76dce49933c184aa9234eb6b2774bc9`. The changed behavior and focused tests pass review, but the required night-gate failed and other required CI evidence remains failed or pending.
