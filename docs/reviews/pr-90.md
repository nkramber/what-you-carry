# PR-90 review

Date: 2026-09-22

## Identity

- PR: 90
- Target: `main`
- Base: `ea8447355f158f231c593f96f7dc9f92e3002895`
- Merge base: `ea8447355f158f231c593f96f7dc9f92e3002895`
- Head: `09926b6461b65838abd23b6e747f4c18e3afe628`
- Branch: `fix/pr-72-enemy-movement`

## Provider gate

Session 211 names Claude Code as the author of PR #90. This review runs in Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

PR-72 fixes F-107 and F-108 under the owner decision D-484. The owner approved this two-concern exception to G-10. I inspected all changed files, pathfinder callers, the movement tests, the documents matrix, the decisions, the design text, and the focused roadmap. No changed file remains uninspected.

The diagonal rule checks both side-move orders, limits the rise in rows and floor height, and tests the open cells along the corner. The step-up rule requires both side columns to stay open under D-489. The path search uses costs 10 and 14 with an octile estimate. `PathWalk.NeedsAJump` compares the target floor with the higher of the feet and the ramp slope at the entry point. The shared follower serves enemies, the Overseer, and bots. `Reachability` keeps side moves. The simulation version rises to 15.

## Findings

No finding.

## Out of scope

None.

## PR comments

- Owner issue comment 5788160648 says `evaluate` waits for this review record and states that head `09926b6` was approved after the gitar finding closed. The absent-record failure was present before publication. This review independently assessed `09926b6`.
- Gitar issue comment 5788208590 reports one closed finding and no remaining issue. The comment matches the resolved thread below and its check passed on the metadata tip.
- Gitar thread `PRRT_kwDOUQs8G86k_lq5` found that a body supported by a block edge over a ramp could get a needless jump. The author added `ABodyOnTheEdgeOfABlockOverARampNeedsNoJumpOntoIt` and changed the ground to the higher of the feet and the slope. The review verified the trigger and regression test. The focused test run passed.

## Description edits

None.

## Verification

- `git diff --check origin/main...HEAD`: passed at tip `b2c7f41`.
- `dotnet test WhatYouCarry.Tests/WhatYouCarry.Tests.csproj --no-restore --filter 'FullyQualifiedName~EnemyWalkTests|FullyQualifiedName~EnemyTests|FullyQualifiedName~BitIdentityTests' --logger 'console;verbosity=minimal'`: passed, 53 tests, at effective head `09926b6`.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: passed with 0 findings in 34 files.
- `dotnet test WhatYouCarry.slnx --no-build`: passed, 1385 tests, with no skips, at metadata tip `b2c7f41`.
- CI run `35811714673` on `751431d`: all CI jobs passed. The run is after effective head `09926b6` and changes only metadata.
- Smoke run `35811714727` on `751431d`: Linux, Windows, and macOS passed.
- Bit-identity run `35811714728` on `751431d`: all three platforms and the compare job passed with the new answer.
- Bots run `35811714674` on `751431d`: passed.
- Metadata tip `b2c7f41`: `ci-skip` passed under Rule 2 and cites successful run `35811714673`. `documents`, `ste-check`, `doc-gate`, `det-lint`, `asset-qa`, `night-gate`, and Gitar passed. `evaluate` failed because this review record was absent. `review-gate` was skipped before publication.
- Full local test suite and STE check passed with this review record present.
- After publication at `cb103bf`, the check wait passed `evaluate`, `review-gate`, `documents`, `ste-check`, `doc-gate`, `det-lint`, `asset-qa`, `night-gate`, and Gitar. The heavy jobs skipped under Rule 2, and the earlier code-head results remain current.
- Push: `cb103bf` was the head of `origin/fix/pr-72-enemy-movement`, verified with `gh pr view`.

## Open questions and accepted risks

No open OQ blocks this review. D-480 accepts that a reduced seed count on a pull request can miss a fault that a full run on `main` or in the night later finds.

## Verdict

**Ready for owner merge.** This verdict applies to effective head `09926b6461b65838abd23b6e747f4c18e3afe628`. The provider gate passes, the complete scope has review coverage, and the required checks pass.
