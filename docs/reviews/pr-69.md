# PR-69 review

Date: 2026-09-14

## Identity

- PR: 69
- Target: `main`
- Base: `d65823ced37a880963bac54dd991f4f39456a500`
- Merge base: `d65823ced37a880963bac54dd991f4f39456a500`
- Head: `80ee5d90b333c777cf814584a149cbdd4106d3a5`
- Branch: `feat/pr-67-dig-restart`

## Provider gate

The PR author provider is Claude Code, as recorded in Session 164 of `docs/session-handoff.md`. The reviewer provider is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

The PR replaces the 10000-job dig cap with a 1000-job budget. It retries a failed dig on a fresh grid and plan, from the next draws of the Procgen stream, for at most four digs. It raises the simulation version and updates the known bit-identity value. The review inspected the complete code and test diff, affected callers, replay version use, the roadmap, design, decisions D-359 to D-361, questions, prior records, the PR description, and every PR comment. The review checked determinism, retry state, error context, regression coverage, document consistency, and the required remote gates. No changed area remains uninspected.

## Findings

No finding.

## Out of scope

The ramp and tier behavior belongs to PR-66, as the Phase 2 roadmap states.

## PR comments

- Gitar approved the effective head and summarized the restart behavior. The review verified the summary against the implementation and the passing checks. The comment also reports that automatic reviews were paused, but it did not request a correction.

## Description edits

None.

## Verification

- `dotnet test WhatYouCarry.slnx --no-build --filter FullyQualifiedName~DigRestartTests -m:1`: 4 passed, 0 failed, on the reviewed checkout. The first attempt hit a test-host socket permission error before execution.
- `dotnet test WhatYouCarry.slnx --no-build -m:1`: started in the review environment but produced no completion result. This local attempt is unverified.
- `dotnet build WhatYouCarry.slnx`: started in the review environment but produced no completion result. Session 164 reports 0 warnings and 0 errors on the effective head.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: started in the review environment but produced no completion result. Session 164 reports 0 findings.
- `git diff --check d65823c..80ee5d9`: passed.
- `cmp -s AGENTS.md CLAUDE.md`: passed.
- Revision-matched CI on `80ee5d9`: CI, smoke, bit identity on all three platforms, compare, bots, det-lint, asset QA, night gate, and STE check passed. No macOS leg ended `not acquired`.
- Revision-matched Gitar check: passed with approval and no issue comment.
- `evaluate`: failed because this review record did not exist on the PR head before this review. `review-gate`: neutral before this record, as D-251 requires. Both checks must refresh after the review commit reaches the PR.
- The 675000-floor sweep recorded in Session 164: 0 generator errors, 674877 first digs, 123 second digs, and a largest total of 1019 jobs. The local bot sweep recorded 0 softlocks and 0 crashes over seeds 1 to 100.
- Push: pending until the review record and Session 165 handoff commit.

## Open questions and accepted risks

None blocks PR #69. The four-dig error probability and stop-time estimate are accepted by D-360.

## Verdict

**Ready for owner merge.** This verdict applies to head `80ee5d90b333c777cf814584a149cbdd4106d3a5`. The provider gate passes, the complete changed scope has review coverage, the focused regression and revision-matched required checks pass, and no blocking finding remains. The review-gate check must refresh after this record reaches the PR.
