# PR-40 review

Date: 2026-09-10

## Identity

- PR: 40
- Target: `main`
- Base: `a2799f2f80d94af9cb9042f9ec0afda1f2404510`
- Merge base: `a2799f2f80d94af9cb9042f9ec0afda1f2404510`
- Head: `d943cb3475228783c82ab8ef7f81deef8b7ec58f`
- Branch: `feat/pr-58-night-gate`

## Provider gate

Session 100 identifies Claude Code as the provider of the substantive PR change. The active reviewer is Codex. The gate passes under T-4 and D-101.

## Intended behavior and scope

PR #40 adds the `night-gate` command and workflow. The gate must reject absent, malformed, stale, foreign, cancelled, and failed records. It must pass the current success record from a commit on the base branch. The review inspected the complete diff, the PR-58 roadmap entry and exit tests, the night workflow, the parser, the ancestry checks, the affected tests, the decisions, and all PR comments and replies.

## Findings

### P1-1. The workflow can use a `night.json` supplied by the pull request when the fetch fails

- Reviewed head: `2253e534dc52b4685fd33bb6a257a9b8100d060f`
- Location: `.github/workflows/night-gate.yml:26-31`
- Trigger: a pull request checkout contains a root `night.json`, and `git fetch --quiet origin night-results` returns nonzero, including when the branch is absent or the fetch has a transient failure.
- Expected: the absent-record case must fail the gate because only the record from `night-results` is valid (D-177, D-273, D-275, and PR-58 exit test 2).
- Actual: the `else` branch only prints a message. It does not remove a checked-out `night.json`. The later command reads that file. A contributor can add a fresh success record whose commit is on `origin/main`, and the gate then passes without a night result from `night-results`.
- Consequence: a required merge gate can be bypassed during the bootstrap case or any fetch failure. The workflow also treats an authentication or network error as an absent branch without preserving the required failure source.
- Correction: read the record into a path outside the pull request checkout, or remove `night.json` before the fetch and create it only after a successful `git show FETCH_HEAD:night.json`. Add a workflow test that places a valid `night.json` in the PR checkout and verifies that an absent or failed fetch still reaches the absent case and exits 1.

## Repeat review

The repeat review covers effective head `d943cb3475228783c82ab8ef7f81deef8b7ec58f`. The later commit `bcb934ed73ba16a1a01bd4b88b3b05d751b008ab` changes review and handoff metadata only.

P1-1 has full merit at the prior head and is corrected at this head. `NightGateFacts.Gather` checks the remote branch, fetches it, and reads `night.json` from `FETCH_HEAD`. The workflow no longer reads or writes a record in the pull request checkout. A fetch failure other than an absent branch throws with the git command, exit code, and error text.

The prior trigger reproduces as a failure at the correction boundary. A planted success record in the checkout fails when the remote has no `night-results` branch. A branch without `night.json` reaches the absent case. A failed record on the remote wins over the planted success record. An unreachable remote throws an error that names `ls-remote` and the remote.

The adjacent boundary also passes. The parser accepts the byte-order mark in the existing night records. The command returns exit 1 for the planted checkout record and exit 0 for the published remote record. The workflow shape test confirms that the workflow contains no shell fetch and no checkout record path.

## Out of scope

None.

## PR comments

The automated pass approved the code and reported the expected missing review file. The author explained that the cross-provider review creates this file. The review accepts the code approval as a claim and independently verifies the changed behavior. The automated configuration comment has no additional finding after this review identifies the workflow boundary.

## Description edits

None.

## Verification

- `git diff a2799f2..d943cb3 --check`: passed.
- `dotnet build WhatYouCarry.slnx -m:1`: passed. 0 warnings and 0 errors.
- `dotnet test WhatYouCarry.slnx --no-build -m:1`: passed. 532 tests, 0 failures, 0 skips.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- det-lint --root .`: passed. 0 findings.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- ste-check --root .`: passed. 0 findings in 15 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- bit-identity`: passed. `6ec00e90c1c85cdb`.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --editor --path WhatYouCarry.Game --build-solutions --quit`: passed with Godot 4.7.2.
- `night-gate` against the real `origin/night-results` record: passed for commit `a2799f2` at `2026-09-10T22:00:00Z`.
- Remote PR checks at head `2253e534dc52b4685fd33bb6a257a9b8100d060f`: CI, bit identity, bots, det-lint, STE check, night-gate, and Gitar passed. The review-gate check was neutral before this review record existed.
- The first real-remote probe failed because the sandbox could not resolve `github.com`. The approved retry passed. The first result was an execution-context failure, not a product result.
- The focused source trace verified the parser cases, exact 48-hour boundary, commit ancestry, command exit codes, full-history checkout, and the corrected remote-read boundary.

## Open questions and accepted risks

OQ-99 remains open, but it blocks no work in this review.

## Previous verdict

**Changes required.** This verdict applied to effective head `2253e534dc52b4685fd33bb6a257a9b8100d060f`. P1-1 blocked the required absent-record gate at that head.

## Verdict

**Ready for owner merge.** This verdict applies to effective head `d943cb3475228783c82ab8ef7f81deef8b7ec58f`. The provider gate passes, P1-1 is corrected, its regression tests pass, and the applicable local and remote checks pass.
