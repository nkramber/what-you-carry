# PR-10 review

Date: 2026-09-07

## Identity

- PR: 10
- Target: `main`
- Base: `94aadc59aba58d3688c2344e4f43c446f53f8fa4`
- Merge base: `94aadc59aba58d3688c2344e4f43c446f53f8fa4`
- Head: `b9bc6cd`
- Branch: `feat/pr-2-ste-check`

## Provider gate

The handoff identifies Claude Code as the author provider for PR-10. The active review provider is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

This repeat review covers the P2-4 correction after the prior review. The review inspected the changed masking code, regression tests, response, handoff, and the complete PR diff. The prior P2-1, P2-2, P2-3, and P2-4 findings are resolved. The review also ran the build, tests, repository checker, and Godot check.

## Findings

### P2-1: Resolved. The colon rule matches the documented contract

The checker now documents and tests the rule that a colon ends a sentence when a space or the line end follows it. A colon inside a word stays within that word.

### P2-2: Resolved. Nested parentheses remain one opaque word

`FindSpanEnd` counts nested parentheses, and the regression test fails on the old implementation.

### P2-3: Resolved. The command rejects trailing arguments

The command now rejects arguments outside a `--root <value>` pair. The regression test covers trailing and unknown arguments.

### P2-4: Resolved. Masked spans protect their delimiter characters

`MaskChar` now masks parentheses, straight and curly quotes, and backticks inside an opaque span. `Unmask` restores each character. The regression tests cover quoted and code delimiters inside parentheses, plus unbalanced spans. The old implementation fails the new quoted-parenthesis assertion.

No finding.

## Verification

- `git diff --check 94aadc5...HEAD`: passed.
- `dotnet build WhatYouCarry.slnx -m:1`: passed with 0 warnings and 0 errors.
- `dotnet test WhatYouCarry.slnx --no-build -m:1`: passed, 63 tests and 0 failures.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: passed, 0 findings in 15 files.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --editor --path WhatYouCarry.Game --build-solutions --quit`: passed with Godot 4.7.2.

## Open questions and accepted risks

OQ-12 remains open for PR-9. No open question blocks PR-10. The passive and participle rules use heuristics, as documented in the handoff and the `ste-writing` skill.

## Verdict

**Ready for owner merge.** This verdict applies to head `b9bc6cd`. The four prior findings are resolved, the applicable local checks pass, and no blocking finding remains.
