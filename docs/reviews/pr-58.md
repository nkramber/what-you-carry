# PR-58 review

Date: 2026-09-12

## Identity

- PR: 58
- Target: `main`
- Base: `f3f0bc0cf58ce09bca67916b4a355f60a3e86a24`
- Merge base: `f3f0bc0cf58ce09bca67916b4a355f60a3e86a24`
- Head: `9803a8bc70903e7bd897aad8f6b8d585132120c8`
- Branch: `feat/pr-60-fullscreen-test-exit`

## Provider gate

The handoff identifies Claude Code as the author of the substantive PR commits. The active reviewer is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

The PR sets borderless fullscreen mode without a fixed window size. It adds Escape and first-controller Start exits before each simulation intent. The exit writes an end line and quits with success when no error exists (D-310, D-311, D-312).

The review inspected the new effective diff, the response file, the affected input and session code, the project settings, the roadmap, the decisions, the questions register, the existing review records, and all PR comments. The review also checked the Core boundary, determinism boundary, error path, presentation settings, test coverage, and document consistency.

The review did not run a visual fullscreen check or a complete local test suite. The local full test stalled without output after discovery.

## Findings

### P2-1: Exit tests do not verify session termination

Status: fixed in `39a0c02`.

File: `WhatYouCarry.Tests/TestExitTests.cs:15-120`.

Trigger: Run the new Escape or Start test with an implementation that returns the expected Boolean from `TestExit.IsPressed` but does not call `Main.Quit`, write `TestExitMessage`, or return exit code 0.

Expected: `EscapeEndsTheSession` and `StartButtonEndsTheSession` must establish that the corresponding input ends a session with exit code 0 and an end line (D-311; Phase 2 roadmap exit tests 2 and 3).

Actual: The correction adds two Smoke tests that drive the headless Game process and assert the exit code, end line, and absence of the smoke end line. The old trigger fails with the new tests, and the correction passes.

Consequence: The original integration gap no longer passes the exit tests.

Correction: `SmokeSessionTests` now drives Escape and Start through the headless Game process and asserts the required result.

Regression check: The two engine tests failed with the Game code from `3dfbf03` and passed with `39a0c02`. Each corrected run ended at tick 101 with the end line and exit code 0.

### P2-2: Press parsing ignores trailing arguments

Status: fixed in `9803a8b`.

File: `WhatYouCarry.Game/Input/TestExit.cs:78-100`.

Trigger: Run the documented test session with `--press escape 100 unexpected`.

Expected: The press parser must reject an argument after the input name and tick with a contextual error. Its `NoArguments` contract says that the arguments end after those two values (T-2).

Actual: `PressOf` now rejects a plain word after the tick and includes that word in a `ContextException`. A flag after the tick remains available to its own parser.

Consequence: The reported plain-word trigger no longer passes in silence.

Correction: `PressOf` rejects a plain word after the press tick and `PressOfRejectsATrailingWord` checks the error and the word.

Regression check: `PressOfRejectsATrailingWord` failed at `39a0c02` and passed at `9803a8b`. The command exits at boot with code 1, names `unexpected`, and writes no successful test-exit line.

## Out of scope

The escape menu and removal of this temporary test exit belong to PR-53 (D-311). Deck visual verification belongs to PR-54, as the Phase 2 roadmap states. Whole-list validation for all Game flags belongs to OQ-170.

## PR comments

- Gitar approved the correction head and reported no issue. The review verified the correction and the accepted OQ-170 scope.

## Description edits

None.

## Verification

- `git fetch origin`: passed with elevated repository access.
- `git diff --check f3f0bc0..9803a8bc70903e7bd897aad8f6b8d585132120c8`: passed.
- `dotnet build WhatYouCarry.slnx -m:1`: passed, 0 warnings and 0 errors, as reported in the Session 146 handoff.
- `dotnet test WhatYouCarry.slnx --no-build -m:1 --filter "FullyQualifiedName~SmokeSessionTests|FullyQualifiedName~TestExitTests"`: passed, 15 tests and 0 failures.
- `dotnet test WhatYouCarry.slnx --no-build -m:1`: passed, 770 tests and 0 failures, as reported in the Session 146 handoff.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --path WhatYouCarry.Game --fixed-fps 60 -- --smoke --press escape 100 unexpected`: passed the corrected failure path with exit code 1 and contextual error text.
- `dotnet WhatYouCarry.Tools/bin/Debug/net10.0/WhatYouCarry.Tools.dll det-lint --root .`: passed, 0 findings.
- `dotnet WhatYouCarry.Tools/bin/Debug/net10.0/WhatYouCarry.Tools.dll ste-check --root .`: passed, 0 findings in 15 files.
- `dotnet WhatYouCarry.Tools/bin/Debug/net10.0/WhatYouCarry.Tools.dll bit-identity`: passed, `6ec00e90c1c85cdb`.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --editor --path WhatYouCarry.Game --build-solutions --quit`: passed, exit code 0.
- Revision-matched CI for `9803a8b` passed CI, smoke, bit identity, bots, det-lint, asset QA, STE check, night gate, and Gitar. `evaluate` and `review-gate` fail because this record still has the earlier `Changes required` verdict.
- Push: pending until the updated review record and handoff commit.

## Open questions and accepted risks

OQ-170 remains open and blocks no work in this PR. OQ-5 and OQ-46 remain open and block PR-15. OQ-161 remains open and blocks exit test 7 of PR-13 and M-3. OQ-159, OQ-160, OQ-44, and OQ-99 do not block this PR.

## Earlier verdicts

- `Changes required` for head `3dfbf03`, because P2-1 lacked end-to-end regression tests.
- `Changes required` for head `39a0c02`, because P2-2 accepted a plain trailing word without an error.

## Verdict

**Ready for owner merge.** P2-1 and P2-2 are fixed and verified. Whole-list validation remains an accepted out-of-scope question under OQ-170.
