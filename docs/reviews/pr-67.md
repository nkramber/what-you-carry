# PR-67 review

Date: 2026-09-14

## Identity

- PR: 67
- Target: `main`
- Base: `5b85b70743aceb89f87c9b6983d21726a53320d8`
- Merge base: `5b85b70743aceb89f87c9b6983d21726a53320d8`
- Head: `d774ab9628998eb0b644dafc2f92cc01d89e6d06`
- Branch: `chore/pr-run-concurrency`

## Provider gate

The handoff identifies Claude Code as the author of the substantive PR changes. The active reviewer is Codex. The provider gate passes under T-4 and D-101. No substantive change from the reviewer provider appears in the PR.

## Intended behavior and scope

The PR adds one concurrency group to each workflow that runs on a pull request. The group uses the workflow name and the pull request number. The PR event cancels an older run. The night workflow remains without a group.

The review inspected the complete diff, all ten workflow files, the repository shape tests, the affected documents, the decisions and questions, the roadmap entry, the PR description, and every PR comment. The review also checked the effective head rule of D-184 and the evidence rule of D-357. No affected area remains uninspected.

## Findings

No finding.

## Out of scope

- The dig restart in the Phase 2 roadmap is a separate roadmap item. The GitHub PR #67 concurrency change does not implement it.

## PR comments

- The owner comment requested a Gitar pass. The pass approved the effective code head with no issue.
- The Gitar pause comment reported a budget pause. The owner ran the requested pass, and its result approved the head with no issue.
- The completed Gitar review states that the nine workflow groups have no issue. The workflow diff and focused tests support that claim.

## Description edits

None.

## Verification

- Focused regression: `dotnet test WhatYouCarry.Tests/WhatYouCarry.Tests.csproj --no-restore --filter 'FullyQualifiedName~RepositoryShapeTests.EveryPullRequestWorkflowCancelsItsOlderRuns|FullyQualifiedName~RepositoryShapeTests.TheNightNeverCancels'` passed 2 tests on `d774ab9`.
- Old-head regression evidence: the new workflow test fails on `main` at `5b85b70` because `asset-qa.yml` lacks the required group. The night test passes on that base.
- Build: `dotnet build WhatYouCarry.slnx` passed with 0 warnings and 0 errors.
- STE check: passed with 0 findings in 16 files.
- Determinism lint: passed with 0 findings.
- Asset QA: passed with 0 findings for 2 models, 0 overlays, and 3 animations.
- Bit identity: passed with known answer `b00814dbf25e61e8`.
- Full local test: `dotnet test WhatYouCarry.slnx --no-build -m:1` did not produce a completion result because the local test host did not emit a result. This execution-context limit does not replace the revision-matched CI result.
- Revision-matched CI: CI, smoke, bit identity on Linux, Windows, and macOS, asset QA, bots, determinism lint, night gate, and STE check passed. The later tip commits are metadata commits under D-184, so D-357 applies.
- Review gate: the check is neutral or skipped before this review record exists. This is the expected pre-record state under D-251.
- Provider pass: Gitar approved the reviewed code head with no issue comment.
- Push: `origin/chore/pr-run-concurrency` is the remote head, verified with `gh pr view`.

## Open questions and accepted risks

OQ-173 is resolved by D-358. No open question or accepted risk blocks this review.

## Verdict

**Ready for owner merge.** This verdict applies to head `d774ab9628998eb0b644dafc2f92cc01d89e6d06`. The provider gate passes, the changed workflow surface has full review coverage, and the required revision-matched checks pass.
