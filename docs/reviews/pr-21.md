# PR-21 review

Date: 2026-09-09

## Identity

- PR: 21
- Target: `main`
- Base: `01e68c34e157b2dbe531214bf49ef567403834ce`
- Merge base: `01e68c34e157b2dbe531214bf49ef567403834ce`
- Head: `4207548c7cf4caf4d0994df824cc30c177c0038a`
- Branch: `feat/pr-7-world-collision`

## Provider gate

Session 63 identifies Claude Code as the provider for the substantive PR-7 change. The active review provider is Codex. The gate passes under T-4 and D-101.

## Intended behavior and scope

PR-21 adds the Core voxel grid, swept axis-aligned box collision, player movement, button validation, and the simulation version 2 state. The review inspected the complete code, test, tool, and document diff, affected callers, replay paths, Core boundaries, decisions D-231 to D-240, questions OQ-100 to OQ-109, and the PR-7 roadmap exit tests. No area remains uninspected.

The review checked open-interval overlap, contact skin, negative and positive motion, grid edges, axis order, jump and landing state, reserved buttons, yaw-relative movement, replay inputs, state-hash order, and the intentional bit-identity change under G-20.

## Findings

No finding.

## Out of scope

- PR-9 owns procedural generation and derives the grid and spawn from the seed.
- PR-8 owns the camera and aim ray.
- PR-10 owns projectile collision.
- Later gameplay PRs own health, weapons, enemies, and rendering.

## Description edits

None.

## Verification

- `git diff --check 01e68c3..4207548`: passed.
- `dotnet test WhatYouCarry.slnx --no-build -m:1`: passed 398 tests with 0 failures and 0 skipped tests.
- `dotnet build WhatYouCarry.slnx -m:1`: passed with 0 warnings and 0 errors.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- ste-check --root .`: passed with 0 findings in 15 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- det-lint --root .`: passed with 0 findings. Core 0 in 37 files, Game 0 in 0 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- bit-identity`: passed with `e8ef2b1fad938845`. The value differs from `283aa4b8cd1281be` because PR-7 adds body state to the sweep, as required by G-20.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --editor --path WhatYouCarry.Game --build-solutions --quit`: passed with Godot 4.7.2.
- GitHub checks read for PR #21 at the published tip `4207548`: Linux, macOS, and Windows CI and bit-identity jobs passed, as did det-lint, STE, and evaluate. `review-gate` was neutral before this review record.
- `git fetch origin`: passed with elevated execution after the local `.git/FETCH_HEAD` permission error.
- Push: `bd57f3407520d503e3a441cc8145c0ca83a528a2` is the head of `origin/feat/pr-7-world-collision`, verified with `gh pr view`.

## Open questions and accepted risks

OQ-99 remains open, but it blocks no work in this review. D-236 accepts caller-supplied grid and spawn inputs until PR-9 derives them from the seed.

## Verdict

**Ready for owner merge.** This verdict applies to effective head `4207548c7cf4caf4d0994df824cc30c177c0038a`. The provider gate passes, the complete scope has review coverage, all required checks pass, and no blocking finding remains.
