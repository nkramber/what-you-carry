# PR-81 review

Date: 2026-09-19

## Identity

- PR: 81
- Target: `main`
- Base: `ea338f449f68865869df52206cc1c60cefd02d25`
- Merge base: `ea338f449f68865869df52206cc1c60cefd02d25`
- Head: `be07a421e9a4e59113a41795265e9e92003458c1`
- Branch: `feat/skill-port`

## Provider gate

The session handoff identifies Claude Code as the author of PR-70. The active reviewer is Codex. The provider gate passes under T-4 and D-101.

## Intended behavior and scope

This PR splits the repository skills into focused reference files, adds the session runbook and code-conventions skill, updates the STE front-matter rule, and lowers the per-file skill ceiling. The review inspected all 34 changed paths, the PR description, the roadmap entry and exit tests, the cited decisions and questions, the automated comments, the moved skill sections, the checker implementation, and the focused tests.

The review covered skill pointers and scope, front-matter parsing, byte ceilings, document consistency, provider and review gates, and the required CI evidence. Core, gameplay, persistence, content, and presentation behavior do not apply to this documentation and tooling change.

## Findings

No finding.

## Out of scope

The branch-protection setting from D-387 remains an owner GitHub setting and is outside this repository PR.

## PR comments

- The owner states that the missing review record and failed `evaluate` check are the expected pre-review state. The review verified the check was neutral and that the other required checks passed.
- Gitar reports no issues and identifies the missing review record as the only CI failure. The review verified the changed behavior with focused tests and the remote check results.

## Description edits

None.

## Verification

- `git diff --check origin/main...HEAD`: passed with two trailing blank-line warnings in added reference files.
- `dotnet test WhatYouCarry.Tests/WhatYouCarry.Tests.csproj --no-build --filter 'FullyQualifiedName~ContextBudgetTests|FullyQualifiedName~SteCheckTests'`: 19 passed, 0 failed.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: 0 findings in 34 files.
- `dotnet test WhatYouCarry.slnx --no-restore --filter 'Category!=Smoke'`: incomplete. The process produced no result after compilation and was canceled after a bounded wait.
- Remote PR checks at tip `64bc3daabffaf09bd21615af81413dbecf18bdca`: CI, smoke, bit identity, compare, bots, asset-qa, det-lint, ste-check, doc-gate, night-gate, and Gitar passed. `evaluate` failed because the review record was absent. `review-gate` was neutral for the same reason.
- Push: `d08e8d9` is the head of `origin/feat/skill-port`, verified with `gh pr view`.

## Open questions and accepted risks

The PR records OQ-177 as resolved by D-386 and OQ-178 as resolved by D-387. No unresolved question affects this review.

## Verdict

**Ready for owner merge.** This verdict applies to head `be07a421e9a4e59113a41795265e9e92003458c1`. The provider gate passes, the changed scope has review coverage, focused contract tests pass, and no blocking finding remains.
