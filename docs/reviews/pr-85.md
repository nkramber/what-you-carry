# PR-85 review

Date: 2026-09-21

## Identity

- PR: 85
- Target: `main`
- Base: `ad7589a`
- Merge base: `ad7589a`
- Head: `46c4b9d`
- Branch: `feat/pr-18-stairwell-and-transition`

## Provider gate

The handoff for session 200 identifies Claude Code as the author of PR #85. The active reviewer is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

The PR adds the stairwell prompt, the seeded next-floor worker, the hidden chunk upload and floor swap, the early ascend state, the coward policy, the transition frame log, and the extended smoke session. The review inspected the complete effective diff, Core state transitions, Game ownership and recovery paths, bot and night workflows, changed tests, the focused roadmap, decisions D-427 to D-437, and the existing automated review comment.

The review inspected determinism, the Core boundary, gameplay transition rules, presentation integration, error paths, content loading, test coverage, workflow changes, and document consistency. The Steam Deck exit test remains uninspected because it requires the owner's Deck run.

## Findings

No finding.

## Out of scope

None.

## PR comments

- Gitar reported no issues and no author reply exists. The review verified the comment against the effective head and found no actionable claim. Gitar did not enable functional validation.

## Description edits

None.

## Verification

- `git fetch origin`: passed. The PR head is `14f0378`.
- `git merge-base origin/main origin/feat/pr-18-stairwell-and-transition`: passed with `ad7589a`.
- Effective-head calculation: passed. `46c4b9d` is the newest commit outside the metadata set. `14f0378` changes only metadata paths.
- `dotnet build WhatYouCarry.slnx`: passed with 0 warnings and 0 errors.
- Focused tests for `TransitionTests`, `MeasureTests`, and `SmokeSessionTests`: passed, 31 of 31.
- Full `dotnet test WhatYouCarry.slnx --no-build`: incomplete. The command produced no result after the build and was interrupted. The focused tests and CI provide the available test evidence.
- `det-lint --root .`: passed with 0 findings.
- `asset-qa --root .`: passed with 0 findings.
- `ste-check --root .`: passed with 0 findings in 34 files.
- Local Godot transition session with `--bot --frame-log /tmp/pr85-frames.txt --transitions 3`: passed with exit code 0. The log recorded 3 transitions and `transitionMicrosMax` of 16667 microseconds.
- PR checks at head `14f0378`: CI, Smoke, Bit identity, Bots, Night gate, Asset QA, Determinism lint, Doc gate, STE check, and Gitar passed. Review gate is neutral because this record did not exist when that run evaluated.
- Steam Deck exit test 6: not run. It requires the owner's Deck and the command in `CLAUDE.md` under D-428 and D-435.
- Push: pending for this review record and handoff entry.

## Open questions and accepted risks

- Exit test 6 remains an accepted unresolved gate requirement under D-427, D-428, and D-435. The review does not approve the PR until the owner records the Deck result.

## Verdict

**Blocked.** This review found no code defect in the inspected effective head, but the required Steam Deck transition exit test remains unresolved. The review applies to head `46c4b9d`.
