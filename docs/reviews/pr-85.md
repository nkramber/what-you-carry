# PR-85 review

Date: 2026-09-21

## Identity

- PR: 85
- Target: `main`
- Base: `ad7589a`
- Merge base: `ad7589a`
- Head: `bab19cc`
- Branch: `feat/pr-18-stairwell-and-transition`

## Provider gate

The handoff for session 200 identifies Claude Code as the author of PR #85. The active reviewer is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

The PR adds the stairwell prompt, the seeded next-floor worker, the hidden chunk upload and floor swap, the early ascend state, the coward policy, the transition frame log, the worker-side mesh preparation, the full-clearer timeout recovery, and the extended smoke session. The repeat review inspected the complete diff through effective head `bab19cc`, including the changes after `46c4b9d`, Core state transitions, Game ownership and recovery paths, bot and night workflows, changed tests, the focused roadmap, decisions D-427 to D-440, and the current automated review comment.

The review inspected determinism, the Core boundary, gameplay transition rules, presentation integration, error paths, content loading, test coverage, workflow changes, and document consistency. The Steam Deck exit test now has owner evidence at `c3ca60a` and remains valid because later changes do not alter the transition-test path.

## Earlier verdicts

- Head `46c4b9d`: **Blocked** because the Steam Deck transition exit test had no result.

## Findings

No finding.

## Out of scope

None.

## PR comments

- Gitar reported no code issues. Its metadata claims referred to the prior review verdict and an earlier document-matrix evaluation. The current document gate passed, and the verdict is updated in this repeat review. Gitar did not enable functional validation.

## Description edits

None.

## Verification

- `git fetch origin`: passed. The PR head is `e32c6cb`.
- `git merge-base origin/main origin/feat/pr-18-stairwell-and-transition`: passed with `ad7589a`.
- Effective-head calculation: passed. `bab19cc` is the newest commit outside the metadata set. `e32c6cb` changes only metadata paths.
- `dotnet build WhatYouCarry.slnx`: passed with 0 warnings and 0 errors.
- Focused transition, measurement, smoke, game-shape, and new full-clearer tests: passed, 48 of 48.
- Broader local test command: incomplete. It produced no result after the build and was interrupted. The focused tests passed 11 of 11. The author-reported full result is 1229 of 1229.
- `det-lint --root .`: passed with 0 findings.
- `asset-qa --root .`: passed with 0 findings.
- `ste-check --root .`: passed with 0 findings in 34 files.
- Local Godot transition session with `--bot --frame-log /tmp/pr85-rereview-frames.txt --transitions 10`: passed with exit code 0. The log recorded 10 transitions and `transitionMicrosMax` of 16667 microseconds.
- Steam Deck exit test 6: passed on the owner Deck at `c3ca60a`, with exit code 0 and 11.1 milliseconds as the slowest frame near each transition, below the 22 millisecond budget.
- Current PR checks at `e32c6cb`: CI, Smoke, Asset QA, Bots, Bit identity, Determinism lint, Doc gate, STE check, and Gitar passed. Night gate is red on the prior base-branch record and has the owner override of D-440. Review gate fails until this record reaches the PR.
- `gh pr view 85 --json body`: passed. The documents matrix has all eight required categories and valid reasons.
- Push: pending for this repeat review record and handoff entry.

## Open questions and accepted risks

- D-440 accepts the red night gate for this merge. The first night on `main` after merge must restore a green night gate.

## Verdict

**Ready for owner merge.** This repeat review found no code defect, the focused regression checks pass, the required CI checks pass, and the owner accepted the red night gate under D-440. The verdict applies to head `bab19cc`.
