# PR-54 review

Date: 2026-09-12

## Identity

- PR: 54
- Target: `main`
- Base: `5848bda`
- Merge base: `5848bda`
- Head: `ecdc95a`
- Branch: `feat/pr-57-asset-qa-gate`

## Provider gate

The Session 134 handoff identifies Claude Code as the author of the substantive commits `73ba35b` and `0cb62c7`. The reviewer is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

This PR adds the engine-free Assets project, the animation format and loader, the asset QA command, and its CI job. It also records the manual automated-pass procedure. The review inspected the complete diff, all affected callers, content paths, loaders, pose and overlap math, tests, workflow, roadmap, decisions, questions, and every PR comment. No area remains uninspected.

## Findings

No finding.

## Out of scope

Polygon budget, pivot placement, and UV coverage remain for the later asset-QA work named by the Phase 2 roadmap. Texture quality, armor attachment, and animation playback remain for PR-14, PR-22, and PR-15.

## PR comments

- The owner explained that the missing review file caused the review-gate failure. This agrees with D-251 and the review-gate passed only after the record is published.
- The automated pass found duplicate body-body clip reports and duplicate unknown-bone reports across overlays. The author corrected both in `0cb62c7`, added two regression tests, and the focused suite verifies the correction.
- The author requested another automated review. Gitar approved the effective head after the correction.

## Description edits

None.

## Verification

- Focused asset, animation, pose, and overlap tests: passed, 80 tests, 0 failures.
- `dotnet build WhatYouCarry.slnx`: local attempt hung without output after several minutes and was cancelled. Remote CI passed the build and test jobs on Linux, macOS, and Windows for the reviewed head.
- `dotnet test WhatYouCarry.slnx --no-build`: the focused tests passed. The local full-suite attempt was not completed because concurrent .NET commands held the build process without output.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- asset-qa --root .`: author-reported 0 findings. The remote `asset-qa` job passed.
- Remote `det-lint`, `ste-check`, `bit-identity`, `bots`, `night-gate`, and `smoke` checks: passed on the reviewed head. The three-platform CI jobs also passed.
- `git diff --check 5848bda..0cb62c7`: passed.
- The fresh `evaluate` check failed because `docs/reviews/pr-54.md` did not exist yet. The fresh `review-gate` check skipped for the same expected pre-review state.
- Push: `632e281` is the current head of `origin/feat/pr-57-asset-qa-gate` before this correction. The corrected record requires another commit and push.

## Open questions and accepted risks

OQ-159 and OQ-160 remain open but bind no work in this PR. OQ-161 remains open and blocks exit test 7 of PR-13 and M-3. No accepted risk changes this verdict.

## Verdict

**Ready for owner merge.** This verdict applies to head `ecdc95a`. The implementation satisfies the stated asset-QA contracts, the correction has regression coverage, and the process change has the required decision and handoff evidence. The review-gate must refresh after this record reaches the PR head.
