# PR-89 review

Date: 2026-09-22

## Identity

- PR: 89
- Target: `main`
- Base: `a2473ec8eec370db00be2fb8a552afc61b58fa0c`
- Merge base: `a2473ec8eec370db00be2fb8a552afc61b58fa0c`
- Head: `05aa78ad3e4e2e1faed779c17a7a0bd6cdcaaaa8`
- Branch: `chore/ci-skip-for-document-heads`

## Provider gate

The author provider is Claude Code, as Session 209 of the session handoff states. The reviewer provider is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

PR-71 adds a CI skip for document-only pull request changes, a test category for tests that read checkout documents, and a faster test split. The owner approved the two concerns in D-471. The review covered the skip rules and GitHub Actions inputs, fallback behavior, workflow conditions, changed-path handling, document test coverage, seed scaling, the `ProcgenTests` class split, all changed tests, and the related decisions, design text, and focused roadmap. Exit test 6 runs after the merge.

## Findings

No finding.

## Out of scope

F-107 and F-108 enemy path fixes belong to a later PR, as the PR-71 roadmap states.

## PR comments

- Gitar issue comment 5787053026 approves the fix for code moved into `docs/` and reports that `evaluate` failed because this review record was absent. The author cites `05aa78a` and `CodeMovedIntoTheSkipSetRunsEveryJob`; the review confirmed both paths and the regression test. The missing-record failure matched the branch before publication, and the check must refresh after this record reaches the PR.
- Author issue comment 5787059448 reports exit tests 4 and 5. The review verified the stated CI run results for `05aa78a` and the Rule 2 skips on the later documents push.
- Gitar thread `PRRT_kwDOUQs8G86k-Qwy` reports that a source file moved into the skip set could hide its old path. The author fixed it in `05aa78a`, and Gitar closed the thread. The review confirmed the implementation and regression test.

## Description edits

None.

## Verification

- `git diff --check origin/main...HEAD`: passed at tip `34109e6`.
- `WYC_PR_SWEEP=1 dotnet test WhatYouCarry.slnx --filter "Category=Documents|Category=Sweep"`: passed, 216 tests, 0 failed, at tip `34109e6`.
- `env -u WYC_PR_SWEEP dotnet test WhatYouCarry.slnx --no-build`: passed, 1351 tests, 0 failed, with the full local seed counts.
- CI run `35802856019` on effective head `05aa78a`: passed, including Linux, Windows, macOS, and the documents job.
- Smoke run `35802856009` on effective head `05aa78a`: passed on Linux, Windows, and macOS.
- Bit identity run `35802856053` on effective head `05aa78a`: all three platform hashes and the compare job passed.
- Bots run `35802856069` on effective head `05aa78a`: all five policies passed.
- CI on documents tip `34109e6`: each of the four `ci-skip` jobs passed by Rule 2. The heavy jobs skipped. `documents`, `ste-check`, `doc-gate`, `det-lint`, `asset-qa`, and `night-gate` passed. `evaluate` failed because this review record was absent; `review-gate` had no review record yet.
- Post-publication checks at metadata tip `1f06e28`: `evaluate`, `review-gate`, `documents`, `ste-check`, `doc-gate`, `det-lint`, `asset-qa`, and `night-gate` passed. The four `ci-skip` jobs passed by Rule 2, and the heavy jobs skipped. Gitar passed again at 2026-09-23 01:13:28 UTC.
- Exit test 6, the full workflow run on `main`, remains after the merge, as the roadmap requires.
- Push: the review record and Session 210 handoff are published to `origin/chore/ci-skip-for-document-heads`, verified with `gh pr view`.

## Open questions and accepted risks

No open question blocks this review. D-480 accepts that a fault found only by the full seed count can pass a pull request and appear on `main` or in the night run.

## Verdict

**Ready for owner merge.** This verdict applies to effective head `05aa78ad3e4e2e1faed779c17a7a0bd6cdcaaaa8`. The provider gate passes, the complete changed scope has review coverage, the effective head checks pass, and no blocking finding remains.
