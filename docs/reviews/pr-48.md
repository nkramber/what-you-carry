# PR-48 review

Date: 2026-09-11

## Identity

- PR: 48
- Target: `main`
- Base: `b7002965e928cc95dad857d35be42f2468001b79`
- Merge base: `b7002965e928cc95dad857d35be42f2468001b79`
- Head: `4786cfa5142d044870a434fa0ed902ee488f9156`
- Branch: `chore/night-back-to-0807`

## Provider gate

The PR author is Claude Code. Session 122 in `docs/session-handoff.md` identifies the provider of the substantive change. The reviewer is Codex. The providers differ, so the cross-provider gate passes under T-4 and D-101.

## Intended behavior and scope

The night workflow must return to `7 8 * * *`, which runs at 08:07 UTC. It must keep `workflow_dispatch`. Its comment must record D-288, F-94, and F-95. The shape test must enforce the cron, the hand trigger, the return time, and both findings. The review inspected the complete diff, the workflow, the shape test, the handoff files, D-286, D-288, D-290, F-94, F-95, the Phase 1 roadmap, and every PR comment. Core, Game, persistence, replay, and presentation behavior do not apply to this workflow-only change.

## Findings

No finding.

## Out of scope

None.

## PR comments

- Gitar reported the expected missing `docs/reviews/pr-48.md` file and approved the change. The owner answered that the record comes from this cross-provider review. The review verified both claims.

## Description edits

None.

## Verification

- `git diff --check b7002965e928cc95dad857d35be42f2468001b79..80eca7a7ea7c602651e6ac76dd7a3ff57ae3b092`: passed.
- `dotnet test WhatYouCarry.slnx --filter FullyQualifiedName~RepositoryShape`: passed with 14 tests, 0 failures, and 0 skips.
- `dotnet build WhatYouCarry.slnx`: passed with 0 warnings and 0 errors.
- `dotnet test WhatYouCarry.slnx --no-build`: did not complete after 120 seconds with no output and was stopped. This is an execution-context limitation. The revision-specific remote Linux and macOS CI jobs passed. The Windows CI job remained pending when this record was written.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- det-lint --root .`: passed with 0 findings.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: passed with 0 findings in 15 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- bit-identity`: passed with `6ec00e90c1c85cdb`.
- The shape test fails on the `21 17 * * *` workflow at the merge base. The new test passes on the reviewed head.
- Remote checks observed at `80eca7a7ea7c602651e6ac76dd7a3ff57ae3b092`: Linux and macOS CI, three-platform bit identity, bots, det-lint, STE check, night-gate, and Gitar passed. The Windows CI job remained pending. The evaluate job failed because this review file did not exist. The review-gate result skipped for the same expected reason. Both jobs must refresh after this record is pushed.
- `git status --short --branch`: clean, with the local branch at `80eca7a` and the remote branch at the same head.

## Open questions and accepted risks

OQ-99 remains open and does not block this review. No accepted risk covers the changed workflow.

## Verdict

**Blocked.** This verdict applies to effective head `4786cfa5142d044870a434fa0ed902ee488f9156`. No finding remains, but the required Windows CI result is pending. The review-gate result must also refresh after this record reaches the PR head.
