# PR-27 review

Date: 2026-09-09

## Identity

- PR: 27
- Target: `main`
- Base: `3e5cbd3`
- Merge base: `3e5cbd3`
- Head: `bd1366e`
- Branch: `feat/pr-9-dig-plan`

## Provider gate

Session 76 identifies Claude Code as the provider for the substantive PR-9 change and its correction. The active review provider is Codex. The gate passes under T-4 and D-101.

## Intended behavior and scope

PR-27 adds deterministic mine floor generation, chamber content, reachability search, floor transitions, replay generation from content, and the simulation version 4 state fields. This review inspected the complete diff from `main` to `bd1366e`, the affected Core callers, content files and validators, replay and stairwell paths, the PR-9 roadmap exit tests, D-159, D-164 to D-167, D-210, D-228, D-236, D-252 to D-257, G-20, the automated comments and replies, and all applicable local gates. No area remains uninspected.

## Findings

No finding.

## Out of scope

- Mine detail blocks, collapses, and band-specific block ids belong to PR-59.
- Enemies, loot, the timer, and the second biome belong to later roadmap items.

## PR comments

- The automated pass found that source enumeration order reached typed content lists. The author sorted paths by ordinal order and added `TheSourceOrderDoesNotReachTheLists`. The review verifies that the loader and content hash now use the same stable order.
- The automated pass reported the Windows failure caused by the same source-order defect. The correction removes that cause, and the local suite passes.
- The automated pass reported that no review record existed. The author explained that the neutral review-gate result is the designed pre-record state under D-251. The review record now supplies the required verdict for this head.

## Description edits

None.

## Verification

- `git diff main...bd1366e --check`: passed.
- `dotnet build WhatYouCarry.slnx -m:1`: passed with 0 warnings and 0 errors on macOS arm64.
- `dotnet test WhatYouCarry.slnx --no-build -m:1`: passed 469 tests with 0 failures and 0 skipped tests.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- det-lint --root .`: passed with 0 findings. Core 0 in 53 files, Game 0 in 0 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- ste-check --root .`: passed with 0 findings in 15 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- bit-identity`: passed with `036df5c08e2682e3`. The simulation version is 4, as required by G-20.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --editor --path WhatYouCarry.Game --build-solutions --quit`: passed with Godot 4.7.2, as recorded in Session 76.
- `gh pr view 27` verifies base `3e5cbd3`, remote head `32bb724`, the automated comments and replies, and new CI, bit-identity, STE, Gitar, and review-gate runs in progress.
- Push: `32bb724` is the head of `origin/feat/pr-9-dig-plan`, verified with `gh pr view`.

## Open questions and accepted risks

OQ-99 remains open, but it blocks no work in this review. The review accepts no risk.

## Verdict

**Ready for owner merge.** This verdict applies to effective head `bd1366e`. The provider gate passes, the complete PR-9 scope has review coverage, and all applicable local gates pass.
