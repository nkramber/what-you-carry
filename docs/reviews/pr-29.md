# PR-29 review

Date: 2026-09-09

## Identity

- PR: 29
- Target: `main`
- Base: `45dbaf5ca4aca39f2741f47a1c85071c849e0fdb`
- Merge base: `45dbaf5ca4aca39f2741f47a1c85071c849e0fdb`
- Head: `923e2a4`
- Branch: `feat/pr-59-detail`

## Provider gate

The handoff identifies Claude Code as the author of the substantive commits `787c851` and `923e2a4`. Codex is the reviewer in this session. The provider gate passes under T-4 and D-101.

## Intent and scope

PR #29 adds the mine detail pass from PR-59. It adds the declared block ids, band-specific wall blocks, pools, pillars, collapses, water movement, and the simulation version change to 5.

The review inspected the complete diff and the affected callers, content, reachability search, replay version use, bit-identity sweep, tests, decisions, roadmap, and PR comments. The review covered determinism, reachability, block solidity, water physics, failure context, test quality, and document agreement.

No area remains uninspected.

## Findings

No finding.

## Out of scope

- Enemy behavior, combat, and later floor content remain in their roadmap PRs.
- Player presentation and visual asset quality remain in the later Game-layer work.

## PR comments

- The automated pass claimed that the water probe needed an owner decision. The author recorded D-264 and OQ-132 in `923e2a4`. The review verified that `IsInWater` probes the column under the feet and that the decision revises only the probe wording of D-262.
- The automated pass claimed that air-over-water gravity needed a direct assertion. The author added the contrasting quarter-gravity and full-gravity assertions in `923e2a4`. The review verified the test and its result.
- The automated pass reported the absent review file as a CI failure. The author correctly treated this as the normal pre-review state under D-251. The review verified that the neutral `review-gate` result is expected until this record exists.

## Description edits

None.

## Verification

- `dotnet build WhatYouCarry.slnx -m:1`: passed with 0 warnings and 0 errors at `923e2a4`.
- `dotnet test WhatYouCarry.slnx --no-build -m:1`: passed with 476 tests and 0 failures at `923e2a4`.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- det-lint --root .`: passed with 0 findings. Core has 0 findings in 54 files. Game has 0 findings in 0 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: passed with 0 STE, reference, and session-number findings in 15 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- bit-identity`: passed with `62c5e1d152fe94fe`.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --editor --path WhatYouCarry.Game --build-solutions --quit`: passed with Godot 4.7.2.
- GitHub CI, both bit-identity platform jobs and the compare job, determinism lint, STE check, evaluate, and Gitar passed at the PR head. The review-gate result is neutral because this review record did not exist before this review.
- Push: `923e2a4` is the head of origin/feat/pr-59-detail, verified with gh pr view.

## Open questions and accepted risks

OQ-132 is resolved by D-264. No accepted risk remains.

## Verdict

**Ready for owner merge.** This verdict applies to head `923e2a4`. The provider gate passes, the complete PR scope has review coverage, all required local and remote checks pass except the expected pre-review neutral gate, and no blocking finding remains.
