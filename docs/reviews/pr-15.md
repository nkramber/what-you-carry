# PR-15 review

Date: 2026-09-08

## Identity

- PR: 15
- Target: `main`
- Base: `51b3de75e8df018b383969724a1c5017c8f64706`
- Merge base: `51b3de75e8df018b383969724a1c5017c8f64706`
- Head: `d530f4c3656cf950b0e7aab136881bfe16943b67`
- Branch: `feat/pr-4-logging`

## Provider gate

Sessions 46, 48, and 50 identify Claude Code as the provider for the substantive change and its corrections. The active review provider is Codex. The gate passes under T-4 and D-101.

## Intended behavior and scope

PR-15 adds the Core logger, the error context type, and the assertion helper. It also corrects three member forms in `det-lint` under D-216.

The second repeat review inspected the response, the diff since `e42a0a8`, each correction, the changed review skill, and every new test. It reran the prior triggers and checked the complete PR diff. No area remains uninspected.

## Findings

### P2-1: Caller fields can replace the required level or message

Status: fixed in `e42a0a8`.

File: `WhatYouCarry.Core/Logging/JsonlLogger.cs:91-109`.

Trigger: add a field named `level` to valid run fields, then write an error line.

Expected: D-212 requires one level and one message on every line. `LogFields` also rejects equal names because they have no defined reading.

Actual: the logger writes its `level`, then writes the caller field with the same name. The JSON object contains two `level` properties.

Consequence: a reader can get either severity from one line. A caller can also replace the meaning of the required message.

Correction: reserve `level` and `message`, and reject a caller field with either name before the sink receives the line.

Regression check: write a line with each reserved field name. Require a contextual error and no sink output.

Repeat-review result at `e42a0a8`: `LogFields.AddField` rejects both names before a line exists. `AReservedFieldNameIsAnError` covers both names.

### P2-2: A safe assertion changes the caller context and breaks the next assertion

Status: fixed in `e42a0a8`.

File: `WhatYouCarry.Core/Logging/Invariant.cs:51-65`.

Trigger: call two failed safe assertions with the same valid `LogFields` object.

Expected: D-112 says a safe assertion writes its report and continues. The caller context remains usable after that return.

Actual: the first call adds `assertFile`, `assertLine`, and `assertMember` to the caller object. The second call throws because `assertFile` is already present.

Consequence: a safe failure can turn a later safe failure into an unexpected exception. It can also add assertion fields to a later ordinary log line.

Correction: build the report with a separate field set. Do not change the caller field set.

Regression check: use one context for two safe failures. Require two report lines and no exception.

Repeat-review result at `e42a0a8`: the report uses a copy. The original trigger writes two reports, and the caller fields stay unchanged.

### P2-3: The string encoder emits JSON that the standard parser rejects

Status: fixed in `d530f4c`.

File: `WhatYouCarry.Core/Logging/JsonlLogger.cs:156-187`.

Trigger: add a string field that contains an unpaired high surrogate, such as `\uD800`, then write and parse the line.

Expected: the PR-4 JSONL contract and exit test 2 require each emitted line to contain one valid JSON object.

Actual: `AppendQuoted` writes the surrogate unchanged. `System.Text.Json.JsonDocument.Parse` rejects the line because it cannot transcode the invalid UTF-16 text.

Consequence: one accepted message, path, or field value can make the log file invalid at that line.

Correction: emit a valid JSON escape for unmatched surrogates, or reject the value with a contextual error before the sink receives it.

Regression check: test unmatched high and low surrogates in names and values. Require valid JSON or a contextual error with no sink output.

Repeat-review result at `e42a0a8`: the implementation escapes an unmatched surrogate, and the original parser trigger passes. The regression check does not pass completely. xUnit gives the high- and low-surrogate rows one test id and skips one row. Keep this finding open until both rows execute.

Second repeat-review result at `d530f4c`: all four labeled rows run, and each covers five positions in a field value, a field name, and the message. `AppendQuoted` replaces an unmatched surrogate with U+FFFD, so both `JsonDocument.Parse` and `GetString` succeed. The focused high- and low-surrogate value checks pass.

### P2-4: The logger helper chain exceeds one level

Status: fixed in `e42a0a8`.

File: `WhatYouCarry.Core/Logging/JsonlLogger.cs:91-196`.

Trigger: trace `BuildLine` through `AppendText` to `AppendQuoted`, and trace `AppendQuoted` to `HexDigit`.

Expected: D-110 permits helpers one level deep, and the review must check that depth.

Actual: a top-level operation calls a helper that calls another helper. The Unicode path adds one more nested helper call.

Consequence: the complete JSON write path requires several nested helper contracts. A later change can miss an escape or a name path.

Correction: keep each helper call one level below the operation that owns the JSON line.

Regression check: inspect the corrected call graph and require no helper-to-helper call in the logger.

Repeat-review result at `e42a0a8`: `BuildLine` calls `LevelName` and `AppendQuoted`. Both methods are leaves, and the prior nested helpers are absent.

### P2-5: Caller fields can prevent every assertion report

Status: fixed in `d530f4c`.

File: `WhatYouCarry.Core/Logging/Invariant.cs:53-57`.

Trigger: add `assertFile`, `assertLine`, or `assertMember` to valid run fields, then fail an assertion.

Expected: D-112 requires every assertion failure to write a full report. The report must carry the compiler call site under D-215.

Actual: the report copy keeps the caller field. The helper then adds the same name and throws before `logger.Write` runs.

Consequence: one accepted caller field prevents the mandatory report. A safe assertion also becomes an exception.

Correction: reserve the three assertion field names, or build the trusted call-site fields without a duplicate-name failure.

Regression check: test all three names. Require one report with the compiler call site, and require the selected safe action.

Second repeat-review result at `d530f4c`: `LogFields.AddField` rejects all five reserved names. `CopyWithCallSite` appends the trusted call site without calling the public duplicate-name path. All three regression rows write the report and return from the safe assertion.

### P2-6: The correction records contradict the reviewed revision

Status: fixed in `d530f4c`.

File: `docs/decisions.md:237`.

Trigger: compare D-214, the response file, and the PR description with effective head `e42a0a8`.

Expected: D-118 requires current document dispositions. The response procedure also requires the final PR head.

Actual: D-214 still says PR-4 adds 16 members, then adds `String.this[]` as another member. The response omits the final head. The PR description still names `41206bb`, 219 tests, and 16 members.

Consequence: the owner receives conflicting allowlist scope, test evidence, and revision identity for the correction.

Correction: make D-214 state the current member count. Add the final head to the response, and update the PR description for `e42a0a8`.

Regression check: compare the three records with the allowlist and `gh pr view 15`. Require one head, one test count, and one member count.

Second repeat-review result at `d530f4c`: D-214 states 9 added types, 18 added members, and totals of 18 types and 26 members. The response identifies its final head as its containing commit. The PR description names `d530f4c`, 233 tests, and the same allowlist counts.

### P2-7: Distinct accepted field names collapse to one JSON property name

Status: open.

File: `WhatYouCarry.Core/Logging/JsonlLogger.cs:194-200`.

Trigger: add one field named with the lone high surrogate `\uD800` and another named with the lone low surrogate `\uDC00`, then build and parse the line.

Expected: `LogFields` promises that a repeated name is an error because a JSON object with two equal names has no defined reading (T-2). Distinct accepted names must stay distinct in the emitted object, or the add must reject them with context.

Actual: `LogFields` accepts both raw names. `AppendQuoted` replaces each surrogate with U+FFFD, so the object contains two properties with the same replacement-character name.

Consequence: a reader can take either value from one property name. The P2-3 correction reintroduces the same ambiguous-object failure that P2-1 removed.

Correction: reject invalid UTF-16 in a field name before it enters `LogFields`, or compare names by the exact representation that the serializer emits. Keep replacement behavior for message and value text if that is the intended F-73 policy.

Regression check: add distinct unmatched high- and low-surrogate field names. Require a contextual rejection with no line, or require one parsed property per distinct accepted name.

## Out of scope

- PR-31 and PR-55 own the Game-layer file sink and its disk-failure behavior under D-211.
- PR-7 creates the simulation version constant. This PR cannot apply the G-20 version increment yet.

## Description edits

- Review pass count: `Two passes so far` to `Three passes so far`.
- Decision range in the Documents section: `D-211 to D-216` to `D-211 to D-217`.
- Finding range in the Documents section: `F-71` to `F-71 to F-76`.
- Handoff disposition in the Documents section: `session 46` to `session 51`.

## Verification

- `git diff --check origin/main...HEAD`: passed at `d530f4c`.
- `dotnet build WhatYouCarry.slnx --no-restore -m:1`: passed with 0 warnings and 0 errors on macOS arm64.
- `dotnet test WhatYouCarry.slnx --no-build --no-restore -m:1`: passed 233 tests with 0 failures and 0 skipped tests.
- Focused second repeat-review probe: distinct fields named `\uD800` and `\uDC00` produced two parsed properties named U+FFFD. Its contract assertion expected one and failed with actual 2. The temporary probe was removed after the run.
- Prior finding checks: all four labeled P2-3 rows execute and read the replacement text; all three P2-5 rows reject the caller name, write the assertion report, and continue safely.
- `det-lint --root .`: passed with 0 findings in 11 Core files.
- `ste-check --root .`: passed with 0 findings in 15 files.
- `bit-identity`: passed with `4d6385bb92454694`.
- Godot build: passed with Godot 4.7.2.
- GitHub checks: all CI, lint, STE, and bit-identity jobs passed at `d530f4c`. The review gate failed against the prior verdict and head.
- Push: the review commit is the head of `origin/feat/pr-4-logging`, verified after publication.

## Open questions and accepted risks

No open question blocks this review. The review accepts no risk.

## Verdict

**Changes required.** This verdict applies to effective head `d530f4c`. P2-3, P2-5, and P2-6 are fixed. P2-7 remains open because the surrogate correction can collapse two accepted field names into one ambiguous JSON property name despite the green broad gates.
