# PR-44 review

Date: 2026-09-11

## Identity

- PR: 44
- Target: `main`
- Base: `557568a`
- Merge base: `557568a`
- Head: `0bfafbd`
- Branch: `chore/night-minute`

## Provider gate

The PR author is Claude Code. Session 113 in `docs/session-handoff.md` identifies the author of the substantive change. The reviewer is Codex. The providers differ, so the cross-provider gate passes under T-4 and D-101.

## Intended behavior and scope

The scheduled night must run at 08:07 UTC, which is 02:07 Central Standard Time, and it must remain available through `workflow_dispatch`. The workflow comment and shape test must record the reason for the seven-minute offset under D-285 and F-94.

The review inspected the complete diff, the workflow, the repository-shape test, D-284, D-285, F-94, the Phase 1 roadmap, all changed documents, all PR comments, and the remote checks. No changed area remains uninspected. Core, Game, persistence, replay, and presentation behavior do not apply to this workflow-only change.

## Findings

No finding.

## Out of scope

None.

## PR comments

- The owner said that the missing review file is expected before the cross-provider review. The review verified that `docs/reviews/pr-44.md` was absent at the PR tip and that the review-gate failure named that absence.
- Gitar reported the missing review file as a configuration failure and approved the workflow change. The review verified the workflow, test, and register diff and found no defect.
- The owner repeated that the missing review file is expected and named `0bfafbd` as the effective head. The review verified that the handoff-only commit `838c544` does not change the effective head under D-184.

## Description edits

None.

## Verification

- `git diff --check 557568a..838c544`: passed.
- `dotnet test WhatYouCarry.slnx --no-build --filter FullyQualifiedName~RepositoryShape`: passed with 14 tests, 0 failures, and 0 skips.
- `dotnet test WhatYouCarry.slnx --no-build`: did not complete after about 30 seconds in the elevated execution context and was stopped. This is an execution-context limitation. Revision-specific remote CI passed the full suite on Linux, Windows, and macOS.
- `dotnet build WhatYouCarry.slnx`: produced no output and did not complete in the local execution context. This is an execution-context limitation. Revision-specific remote CI passed the platform builds.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- det-lint --root .`: passed with 0 findings.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- ste-check --root .`: passed with 0 findings in 15 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- bit-identity`: passed with `6ec00e90c1c85cdb`.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --editor --path WhatYouCarry.Game --build-solutions --quit`: passed on Godot 4.7.2.
- Remote checks for `838c544`: CI Linux, Windows, and macOS; bit identity on all three platforms; bots; det-lint; STE check; night-gate; and Gitar passed. The review-gate evaluation failed because this review file did not exist, and the review-gate result was skipped for the same expected reason. A fresh review-gate result is required after this file is pushed.
- Push: `632026c` published the review record and handoff to `origin/chore/night-minute`, verified with `gh pr view`.

## Open questions and accepted risks

OQ-99 remains open and does not block this review. No accepted risk covers the changed workflow.

## Verdict

**Ready for owner merge.** This verdict applies to head `0bfafbd`. The workflow, shape test, decision, finding register, roadmap, and handoff agree, and no blocking finding remains. The review-gate check must pass after this record is pushed.
