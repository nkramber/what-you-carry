# PR-86 review

Date: 2026-09-21

## Identity

- PR: 86
- Target: `main`
- Base: `32e7909b18264ea8de271550256dad809d73653e`
- Merge base: `32e7909b18264ea8de271550256dad809d73653e`
- Head: `c64e1e21bae6f92385af55dda9ee9f718a27b04e`
- Branch: `feat/pr-19-hud-and-navigation`

## Provider gate

The session handoff names the author as Claude Code. The reviewer is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

PR-86 adds the HUD, damage numbers, the stairwell tap and hold input, UI scale, controller focus navigation, the navigation fixture, and the HUD shot. The review inspected the complete implementation and test diff, the affected callers, the string content, the Phase 2 roadmap, the design guardrails, decisions D-441 to D-449, and the existing PR comment. No area remains uninspected.

## Findings

No finding.

## Out of scope

None.

## PR comments

- Gitar reported the pre-review missing review record and approved the implementation without findings. The review verified that the required record was absent before this review, that the comment is not a product finding, and that the reported implementation areas pass the focused checks.

## Description edits

None.

## Verification

- `dotnet build WhatYouCarry.slnx`: passed with 0 warnings and 0 errors at `5ea0566`.
- Focused HUD, navigation, input, and stairwell tests: passed, 30 tests.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- det-lint --root .`: passed, 0 findings.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- asset-qa --root .`: passed, 0 findings.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: passed, 0 findings.
- Godot headless smoke session: passed with HUD, navigation fixture, prompt, floor transition, and end-state log records.
- Full `dotnet test WhatYouCarry.slnx --no-build`: incomplete. The command produced only the test assembly header during the bounded run and did not return a result.
- CI at tip `5ea0566`: platform builds and tests, asset QA, det-lint, doc-gate, night-gate, STE check, and Gitar passed. The `evaluate` job failed only because the review record did not yet exist. The review-gate check was neutral for the same reason.
- Push: `11dc48c` is the head of `origin/feat/pr-19-hud-and-navigation`, verified with `gh pr view`.

## Open questions and accepted risks

None.

## Verdict

**Ready for owner merge.** This verdict applies to head `c64e1e2`. The cross-provider review found no in-scope defect, and the focused implementation and repository checks passed. The full suite remains an execution limitation because it stalled without a result.
