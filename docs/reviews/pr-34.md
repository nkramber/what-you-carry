# PR-34 review

Date: 2026-09-10

## Identity

- PR: 34
- Target: `main`
- Base: `d3093cf76bdebc2c0a0da0eae5c9471f5ee2b652`
- Merge base: `d3093cf76bdebc2c0a0da0eae5c9471f5ee2b652`
- Head: `4e1ea9ee3bc130e3911defe60d5c1ed6c4572ee0`
- Branch: `feat/pr-11-bots`

## Provider gate

The Session 89 and Session 91 handoffs identify Claude Code as the author of the substantive PR change and its correction. The substantive commits `8a7367f`, `0536f7e`, and `f6ca5f6` match those handoffs. The active reviewer is Codex. The provider gate passes under T-4 and D-101.

## Intended behavior and scope

The PR adds the deterministic bot policies, the headless runner, the night record command, and the PR and scheduled workflows. It adds four run end states, bounded bot budgets, the Bot RNG stream, run logs, and the required tests under D-115, D-117, D-127, D-149, D-157, and D-270 to D-273.

The repeat review inspected the new correction diff, the affected test classes, the console collection and shape test, the response record, the PR-11 roadmap exit tests, applicable decisions and questions, and every new PR comment. The prior review coverage of the complete implementation remains valid. No changed area remains uninspected.

## Findings

### P1-1: Bot tests race with global console capture

Status: fixed in `f6ca5f6`.

Files: `WhatYouCarry.Tests/BotTests.cs:78-79`, `WhatYouCarry.Tests/BitIdentityTests.cs:52-72`.

Trigger: Run `dotnet test WhatYouCarry.slnx --no-build -m:1` on the effective head. The bot tests call `Program.Main`, which writes to the process-global console, while `TheCommandPrintsTheHashAlone` redirects that same console to a test-local `StringWriter`.

Expected: The required test suite passes reliably, and the bit-identity command test captures only the hash that it invokes (T-3, G-9).

Actual: At the prior head, the full local run failed 1 of 517 tests. `BitIdentityTests.TheCommandPrintsTheHashAlone` expected `6ec00e90c1c85cdb` but captured `bot-run: policy greedy-descender, seeds 3...` from `BotTests.RunLogHasRequiredFields`.

Consequence: The PR adds a nondeterministic failure to the repository test gate. A green CI run does not establish that the suite is isolated.

Correction: `f6ca5f6` places the four existing console-touching test classes in one xUnit collection. It adds `ConsoleCollectionTests.EveryConsoleTestIsInTheCollection` as a repository shape check. Commit `4e1ea9e` extends that check to every test directory.

Regression check: Three independent full runs at the corrected head passed 518 tests with 0 failures and 0 skips. The extra test is the collection shape check.

## Out of scope

- The `night-gate` job belongs to PR-58 under D-177.
- The coward, full-clearer, and timer-tester policies belong to PR-16 to PR-18.
- Tier 3 bot coverage belongs to the later roadmap work.

## PR comments

- Gitar reported that a broken build could prevent the night record from reaching `night-results`. The author accepted the finding. Commit `0536f7e` adds a shell failure-record fallback, and the review verified that correction in the workflow.
- Gitar reported that a seed loop could wrap at `ulong.MaxValue`. The author accepted the finding. Commit `0536f7e` drives the loop by a bounded span, and the review verified the maximum-seed and oversized-range tests.
- Gitar approved the corrected head and confirmed both findings closed. The author replied that the missing review record is expected before the cross-provider review. The review verified that the neutral review-gate result is expected until this record reaches the PR head.
- Gitar reported that the first shape test scanned only the top test directory. The author accepted the suggestion, and `4e1ea9e` changes the scan to `SearchOption.AllDirectories`. The repeat review verified the correction and the author reply.

## Description edits

None.

## Verification

- `git diff --check 0536f7e...4e1ea9e`: pass.
- `dotnet build WhatYouCarry.slnx -m:1`: 0 warnings, 0 errors.
- `dotnet test WhatYouCarry.slnx --no-build -m:1`: 518 passed, 0 failed, 0 skipped, on three independent runs.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- det-lint --root .`: 0 findings. Core 0 in 61 files, Game 0 in 0 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- ste-check --root .`: 0 findings in 15 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- bit-identity`: `6ec00e90c1c85cdb`. The simulation version is 6.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --editor --path WhatYouCarry.Game --build-solutions --quit`: pass with exit code 0.
- `gh pr view 34`: base `d3093cf76bdebc2c0a0da0eae5c9471f5ee2b652`, PR head `e24cc2f1759281986a85df91d839abb4d55a1450`, effective head `4e1ea9ee3bc130e3911defe60d5c1ed6c4572ee0`, and complete author replies and Gitar comments.
- GitHub checks pass for CI on all three platforms, bit identity on all three platforms, the bit-identity comparison, bots, determinism lint, STE check, and Gitar. Evaluate and review-gate fail only because this record still has the prior verdict and head.
- Push: `945004a` is the head of `origin/feat/pr-11-bots`, verified with `gh pr view`. The review-gate check must rerun for the new verdict.

## Open questions and accepted risks

OQ-99 remains open and blocks no work. D-273 accepts the scheduled workflow permission to push the single `night.json` record to `night-results`. No accepted risk covers a changed area.

## Earlier verdicts

The first review gave the verdict `Changes required` for effective head `0536f7e6bb4582fce940b5b615ddf2fce087bdb3` because P1-1 was open.

## Verdict

**Ready for owner merge.** This verdict applies to effective head `4e1ea9ee3bc130e3911defe60d5c1ed6c4572ee0`. P1-1 is fixed, the correction has repeated regression coverage, and the complete changed scope has no remaining blocking finding.
