# PR-56 review

Date: 2026-09-12

## Identity

- PR: 56
- Target: `main`
- Base: `163742e`
- Merge base: `163742e`
- Head: `9236744`
- Branch: `feat/pr-14-texture-generator`

## Provider gate

The Session 137 handoff identifies Claude Code as the author of the substantive commit `9236744`. The reviewer is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

This PR adds the palette, the texture rules, the deterministic atlas generator, atlas loading, body UV updates, and the contact sheet. The review inspected the complete diff, affected callers, content paths, loaders, atlas sampling, model geometry, tests, workflow, roadmap, decisions, questions, and every PR comment. The review also ran focused falsifiers for the generator and command error paths. No area remains uninspected.

## Findings

### P2-1: Unreadable input files crash the texture command

Status: open.

File: `WhatYouCarry.Tools/TextureGen/TextureGenCommand.cs:102-105`.

Trigger: Make a rule file unreadable, then run `texture-gen --root <checkout>`.

Expected: The command reports the file path with context and returns exit code 1 for a bad rule or read failure. This is the command contract at line 23 and the zero-silent-failure rule T-2.

Actual: `File.ReadAllBytes(file)` throws `UnauthorizedAccessException` outside the `ContextException` handler. A direct probe returned exit code 134 and printed an unhandled exception instead of the command error.

Consequence: A damaged or inaccessible texture input can terminate the tool without its documented failure result. CI and an owner workflow that depend on the command cannot classify the failure as a normal texture-generation error.

Correction: Read the palette and each rule inside a contextual read boundary. Convert `IOException` and `UnauthorizedAccessException` into a `ContextException` that names the input path, or catch those exceptions in `Run` with the same contextual result. Apply the same rule to the palette read.

Regression check: Add unreadable palette and unreadable rule cases that assert exit code 1, a named path, and no unhandled exception. Run the focused texture tests and the full suite.

## Out of scope

None.

## PR comments

- The automated pass reported approval after the manual `Gitar review` request. The review verified the pass and found the independent P2 input-recovery defect above.
- The owner supplied the `Gitar review` request. The review treated it as process evidence under D-303.

## Description edits

None.

## Verification

- `dotnet test WhatYouCarry.slnx --no-build --filter 'FullyQualifiedName~TextureGenTests|FullyQualifiedName~ContactSheetTests'`: passed, 54 tests, 0 failures.
- `dotnet build WhatYouCarry.slnx`: local attempt produced no output and was cancelled. The Session 137 handoff reports 0 warnings and 0 errors on the same effective head.
- `dotnet test WhatYouCarry.slnx --no-build`: local attempt did not complete after the smoke portion started. The Session 137 handoff reports 759 tests and 0 failures.
- `dotnet run --no-build --no-restore --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: passed, 0 findings in 15 files.
- `dotnet run --no-build --no-restore --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- det-lint --root .`: passed, 0 findings.
- `dotnet run --no-build --no-restore --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- asset-qa --root .`: passed, 0 findings over 1 model.
- `dotnet run --no-build --no-restore --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- texture-gen --root .`: passed, wrote 65,973 bytes. The worktree stayed clean.
- `dotnet run --no-build --no-restore --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- bit-identity`: passed, `6ec00e90c1c85cdb`.
- Unreadable-rule probe against the compiled command: reproduced the unhandled `UnauthorizedAccessException` and exit code 134.
- `git diff --check main...9236744`: passed.
- Remote `asset-qa`, `bots`, `compare`, `det-lint`, bit identity, smoke, macOS, Windows, STE check, and night-gate checks: passed for the PR tip. The Linux CI check was pending when checked.
- The remote `evaluate` check failed because the review record did not exist yet. The remote `review-gate` check skipped for the same pre-review state.
- Push: not completed. The review record and the Session 138 handoff entry require a commit and push.

## Open questions and accepted risks

OQ-5 and OQ-46 block PR-15. OQ-161 blocks exit test 7 of PR-13 and M-3. OQ-159 and OQ-160 bind constants but block no work. OQ-44 blocks PR-18. OQ-99 remains open but blocks no work. No accepted risk changes this verdict.

## Verdict

**Changes required.** This verdict applies to head `9236744`. The provider gate passes, but the unreadable-input defect needs correction and the required Linux CI evidence was pending at review time.
