# PR-97 review

Date: 2026-09-23

## Identity

- PR: 97
- Target: `main`
- Base: `582f346b60a2edccad5dbc2d710c83595d808f69`
- Merge base: `582f346b60a2edccad5dbc2d710c83595d808f69`
- Head: `ba7f44869165cff3db20a68bfb6fa2f1bbb0a271`
- Branch: `fix/pr-81-night-softlocks`

## Provider gate

The PR author is Claude Code, as Session 232 and the author handoff state. The reviewer is Codex. This passes T-4 and D-101.

## Intended behavior and scope

The PR corrects the two causes of F-111 and adds branch night records and their night-gate read. I inspected the complete diff, all changed files, the PR comments, the PR-81 roadmap tests, the design guardrails, and the affected decision entries. The review traced diagonal movement, waypoint progress, simulation versioning, branch record publication, gate validation, and workflow permissions under D-545 to D-548, D-534, D-538, G-1, G-9, G-20, and T-2. No area remains uninspected.

## Findings

No finding.

## Out of scope

Old `night-branch/` record cleanup and bot policy changes remain outside PR-81 scope (PR-81 roadmap).

## PR comments

Issue comment 5801169319 says only that Gitar is working. No review thread or feedback exists in the exported comments. The author posted no reply. The comment states no claim about the change to verify (D-250, D-542).

## Description edits

None.

## Verification

- `git diff --check origin/main...HEAD`: passed.
- `cmp -s AGENTS.md CLAUDE.md`: passed; both files match.
- `dotnet build WhatYouCarry.slnx`: passed with zero warnings and errors.
- Focused regression and gate tests for diagonal drops, waypoint detours, five greedy-descender seeds, branch night rules, branch record history, and workflow shape: 12 passed, 0 failed, 0 skipped.
- CI run 35908069054 at effective head `ba7f448`: passed on Linux, Windows, and macOS, including the hosted sweeps and documents job.
- Smoke run 35908069124 at effective head `ba7f448`: passed. Bit identity, bots, asset QA, determinism lint, and STE also passed at that head.
- At tip `c7f46745`, `doc-gate`, documents, STE, determinism lint, and asset QA passed. Code-only jobs skipped under the documents-only rule; the prior effective-head results above cover the same code.
- Night run 35909827024 at `ba7f448` was still in progress. Its first policy passed; the other four policy runs and the 100,000-seed reachability sweep had not completed. The current `night-gate` failed because neither `night-branch/fix/pr-81-night-softlocks` nor its record exists yet. Exit test 8 and the required night-gate evidence remain incomplete.
- `review-gate` failed at tip `c7f46745` because this review record did not yet exist. The published record must cause a fresh review-gate run.
- Push: effective head `ba7f44869165cff3db20a68bfb6fa2f1bbb0a271` remains the PR code head after the metadata push, verified with `gh pr view`.

## Open questions and accepted risks

No open question blocks this review. D-545 to D-548 record the owner choices for this scope.

## Verdict

**Blocked.** This verdict applies to head `ba7f44869165cff3db20a68bfb6fa2f1bbb0a271`. The code review found no defect, but the branch night and its green `night-gate` result are required exit evidence and remain incomplete.
