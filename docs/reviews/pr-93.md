# PR-93 review

Date: 2026-09-23

## Identity

- PR: 93
- Target: `main`
- Base: `e069e1637f5a88593d93d05ba7f9b4134690fd57`
- Merge base: `e069e1637f5a88593d93d05ba7f9b4134690fd57`
- Head: `e28ecd21ad35ad1f5e2cc0bcd58bea6d8ee22c98`
- Branch: `feat/pr-78-codex-review`

## Provider gate

The author is Claude Code, as the response file records. This review runs in Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

PR-78 adds the Codex review command, the three-strike finding count, a ruleset for `main`, unique workflow check names, and their procedures. This round verified the fixes to P1-1 and P2-1, the later malformed-heading cases, and the affected parser, tests, and skill text. The review also checked the full PR diff, its response, comments, roadmap entry, guardrails, and the required platform checks. No affected area remains uninspected.

## Findings

### P1-1: Approval accepts an open blocking finding

Status: fixed in `2dca4fea1c26befed4e94491b303f9fb6190e0af`.

Open at: `b7623f4a33ef78c5b64dccc0716a51b104d198fa`.

File: `WhatYouCarry.Tools/CodexReview/ReviewOutcome.cs:90-103`.

Trigger: An approving review record has an open P0, P1, or P2 finding.

Expected: An approval has no open blocking finding. The command must return a fault for a record that violates this rule.

Actual: The first round returned approval before it checked open findings. Commit `2dca4fe` now faults on open P0 to P2 findings. The regression test covers P0, P1, and P2.

Consequence: The command could report approval while a blocking finding remained open.

Correction: The command checks open blocking findings before it returns approval.

Regression check: `AnApprovalWithAnOpenBlockingFindingIsAFault` passes for P0, P1, and P2. `AnApprovalWithAnAcceptedRiskApproves` also passes.

### P2-1: An unsupported or malformed finding heading can pass as nonblocking

Status: fixed in `e28ecd21ad35ad1f5e2cc0bcd58bea6d8ee22c98`.

Open at: `4e850b9dc73aac612f96ca9d044d64feb204ac3e`, `e28ecd21ad35ad1f5e2cc0bcd58bea6d8ee22c98`.

File: `WhatYouCarry.Tools/CodexReview/ReviewFindings.cs:50-65`.

Trigger: An approving review record has an open finding with a severity outside P0 to P3, or a malformed level-three-or-deeper heading such as `#### P1-1:` or `###P1-1:`.

Expected: Finding severities are P0 to P3, and each heading in the Findings section must match the finding format. The command must fault on any other severity or heading (T-2, D-515).

Actual: At `4e850b9`, the parser accepted severities P4 to P9 and ignored headings outside its pattern. Commit `7159ad2` rejected unsupported severities and malformed headings. Gitar then showed that headings with four hashes or no space still escaped. Commit `e28ecd2` faults on each line that starts with `###` when the line does not match the finding format.

Consequence: The command could ignore an open blocking finding and report approval.

Correction: The parser rejects unsupported severities and every malformed finding heading.

Regression check: `AFindingHeadingOutsideTheFormatIsAFault` passes for `P4-1`, `P10-1`, `P1:`, `### Notes`, `#### P1-1:`, and `###P1-1:`. The P3 approval case still passes.

## Out of scope

None.

## PR comments

- The initial PR comment said the review record would come from the review session. This record now exists.
- Gitar found that the login check read stdout while the CLI wrote to stderr. The author fixed it in `4e850b9`, and the real-child-process regression test passes.
- Gitar found that P4 and malformed headings could be skipped. The author fixed the cases in `7159ad2` and `e28ecd2`; all six parser cases pass.
- Gitar noted that the previous review record said round 2 approved while its verdict required changes. The new record gives the correct history and current verdict.
- Gitar's latest code review approved the fixes at `e28ecd2`. The automated pass found no remaining code issue. Its threads have replies and are resolved.
- The earlier `evaluate` and `review-gate` failures read the previous `Changes required` record. Fresh results must report after this record is pushed.

## Description edits

None.

## Verification

- `git fetch origin`; the base is `e069e1637f5a88593d93d05ba7f9b4134690fd57`. The detached head matches PR #93 at `e28ecd21ad35ad1f5e2cc0bcd58bea6d8ee22c98`.
- `dotnet test WhatYouCarry.Tests/WhatYouCarry.Tests.csproj --filter 'FullyQualifiedName~CodexReview|FullyQualifiedName~RulesetTests|FullyQualifiedName~ReviewGateTests'`: passed, 70 tests, 0 failed, 0 skipped. The build succeeded as part of this command.
- `gh pr checks 93`: code and platform checks passed at `e28ecd2`. After the record commit `a91b075`, `evaluate`, `review-gate`, `Gitar`, `asset-qa`, `det-lint`, `doc-gate`, `documents`, `night-gate`, and `ste-check` passed. Code-only jobs skipped on the metadata head under the documents-only rule.
- `git diff 4e850b9..HEAD --check`: passed.
- The complete prior diff, new commits, response, comments, PR-78 roadmap entry, design guardrails, and affected contracts: reviewed.
- Push: the review record and Session 222 are published to `origin/feat/pr-78-codex-review`, verified with `gh pr view`.

## Open questions and accepted risks

None.

## Earlier verdicts

**Changes required.** This verdict applied to `b7623f4a33ef78c5b64dccc0716a51b104d198fa`. P1-1 showed that approval could pass with an open P0 to P2 finding.

**Changes required.** This verdict applied to `4e850b9dc73aac612f96ca9d044d64feb204ac3e`. P2-1 showed that unsupported severity and malformed headings could pass as nonblocking.

## Verdict

**Ready for owner merge.** This verdict applies to head `e28ecd21ad35ad1f5e2cc0bcd58bea6d8ee22c98`. P1-1 and P2-1 now pass their regression checks, and the code and platform checks passed. The fresh `evaluate` and `review-gate` results must confirm publication of this record.
