# PR-93 review

Date: 2026-09-23

## Identity

- PR: 93
- Target: `main`
- Base: `e069e1637f5a88593d93d05ba7f9b4134690fd57`
- Merge base: `e069e1637f5a88593d93d05ba7f9b4134690fd57`
- Head: `b7623f4a33ef78c5b64dccc0716a51b104d198fa`
- Branch: `feat/pr-78-codex-review`

## Provider gate

The handoff names Claude Code as the author of PR #93. This session is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

PR-78 adds the Codex review command, finding round counts, the `main` ruleset, unique workflow check names, and their procedures. I read all 40 changed paths, the PR comments, the PR-78 roadmap entry, and the affected review and GitHub workflow contracts. The live ruleset setup in exit test 4 waits until after merge, as the roadmap states.

## Findings

### P1-1: Approval accepts an open blocking finding

Status: open.

Open at: `b7623f4a33ef78c5b64dccc0716a51b104d198fa`.

File: `WhatYouCarry.Tools/CodexReview/ReviewOutcome.cs:87-90`.

Trigger: A review record names `Ready for owner merge` and has an open P0, P1, or P2 finding whose `Open at:` line names the current head.

Expected: The review record rules require no blocking finding for `Ready for owner merge`. The command must not report approval while a P0 to P2 finding remains open.

Actual: `Judge` returns `Approve` as soon as it sees the approved verdict. It does not reject an open blocking finding. The test at `WhatYouCarry.Tests/CodexReviewTests.cs:148-153` covers only an open P3 finding.

Consequence: `codex-review` exits 0 for a record that does not meet the review verdict contract. The review gate then accepts the same verdict and head, so the automation can report the PR ready while a blocking finding remains unresolved.

Correction: Reject an approving record that has any open P0, P1, or P2 finding. Keep the approved verdict behavior for open P3 findings.

Regression check: Add a case to `CodexReviewTests` with an approved verdict and an open P2 finding at the current head. It must not return `CodexReviewExit.Approve`. Run `dotnet test WhatYouCarry.slnx --no-build --filter FullyQualifiedName~CodexReviewTests`.

## Out of scope

None.

## PR comments

- Gitar reported a missing review record and approved the code review. The author said the review session would add the record. The exported comments show no review threads. At review time, the Gitar check on `b7623f4` passed, while `evaluate` failed because the record was absent and `review-gate` was skipping. This is expected before this publication; the fresh checks after the metadata push remain to be read.

## Description edits

None.

## Verification

- `git fetch origin`; `origin/main` was `e069e1637f5a88593d93d05ba7f9b4134690fd57`, matching the PR base.
- PR #93 was open at `b7623f4a33ef78c5b64dccc0716a51b104d198fa`; the detached checkout and diff matched that head.
- `dotnet build WhatYouCarry.slnx`: passed, 0 warnings and 0 errors.
- `dotnet test WhatYouCarry.slnx --no-build`: passed, 1,491 passed, 0 failed, 0 skipped, duration 5 m 41 s.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: passed, 0 findings in 37 files.
- `dotnet test WhatYouCarry.slnx --filter Category=Documents`: passed, 131 passed, 0 failed, 0 skipped.
- `gh pr checks 93 --watch --interval 60`: on metadata commit `c06f6ce`, Gitar, `documents`, `doc-gate`, `ste-check`, and all other reported applicable checks passed. Heavy jobs skipped on the metadata-only head. `evaluate` and `review-gate` failed because this record requires changes.
- `gh api .../commits/b7623f4a33ef78c5b64dccc0716a51b104d198fa/check-runs`: Gitar completed with `success` at 07:26:13 UTC.
- Review of all changed paths and the exported comments: complete. No PR review threads exist.
- Push: `c06f6ce` was the head of origin/feat/pr-78-codex-review when verified with gh pr view.

## Open questions and accepted risks

None.

## Verdict

**Changes required.** This verdict applies to head `b7623f4a33ef78c5b64dccc0716a51b104d198fa`. An open P0 to P2 finding can pass the command and the review gate as an approval. The publication checks confirm the expected red review gates for this verdict.
