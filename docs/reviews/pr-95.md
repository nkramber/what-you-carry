# PR-95 review

Date: 2026-09-23

## Identity

- PR: 95
- Target: `main`
- Base: `2c4e6d5f07d01e210730d3363f839d0c906fe26c`
- Merge base: `2c4e6d5f07d01e210730d3363f839d0c906fe26c`
- Head: `25ad21edce71d8b350500d80cdebe39138db732c`
- Branch: `chore/pr-79-review-process`

## Provider gate

The handoff names Claude Code as the author of PR #95. Codex is the reviewer. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

PR-79 changes the review and override heads, the review command, the merge summary, and their tests and documents. Round 1 inspected all 25 changed paths, the PR description and comments, the roadmap entry, and affected callers. Round 2 inspected the author response, the changes since `d13f73c`, the override rule and tests, the shared CI document set, and the live checks. No changed area remains uninspected. The review focuses on D-533, D-534, D-539, D-540, D-541, D-190, and the four exit tests.

## Findings

### P2-1: README and LICENSE overrides cannot pass

Status: fixed in `25ad21edce71d8b350500d80cdebe39138db732c`.

Open at: `d13f73c6d94adf2c8ebd7e4fd22376ac64c48eec`.

File: `WhatYouCarry.Tools/ReviewGate/ReviewGateRules.cs:36-39` at the round 1 head.

Trigger: A PR changes only the root `README.md` or `LICENSE`, then receives the `review-override` label.

Expected: The review gate provides the supported approval path for each documents-only PR in D-475, under D-188, D-190, D-475, and D-540.

Actual: At `d13f73c`, the label's eligible set omitted both root files, while the effective-head skip set included them.

Consequence: A PR that changes only either root file could not pass the review gate by review or by label.

Correction: `ReviewGateRules` now uses `CiSkipRules.IsDocument` for label eligibility. The shared list covers both root files, and it also supplies the effective-head skip paths.

Regression check: `ReviewGateGitTests.ReviewGatePassesOnOverrideLabelForARootDocument`, `ReviewGateRulesTests.ReviewGatePassesOnOverrideLabelForEachPathOfTheSkipSet`, and `ReviewGateRulesTests.ReviewGateFailsOnOverrideLabelWithADocumentOutsideTheSkipSet`; all passed in the focused suite. The response records that all four README and LICENSE cases failed against the old rule.

## Earlier verdicts

- Round 1 at `d13f73c6d94adf2c8ebd7e4fd22376ac64c48eec`: **Changes required**, because P2-1 remained open. The night gate failed on the D-538 record, and Smoke evidence was absent at that head.

## Out of scope

- The night fix and branch-night behavior belong to D-538 and the next PR. The current night-gate failure is the D-538 record described below.

## PR comments

- `gitar-bot` issue comment 5799013917 says the automated pass is in progress. No review threads or actionable feedback exist.

## Description edits

None.

## Verification

- `git diff --check origin/main...HEAD`: passed at `25ad21e`.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- handoff-rotate --root .`: passed; moved Session 219 to the archive and confirmed Session 230 as the next number.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: passed with 0 STE findings, 0 reference findings, and 0 session-number findings.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- doc-gate --root . --base origin/main --head HEAD --body /tmp/pr-95-body.md --title "chore: the merge summary form, and a green review gate after a documents commit (PR-79)" --branch chore/pr-79-review-process`: passed with 0 problems over 25 changed paths.
- `dotnet test WhatYouCarry.slnx --filter 'FullyQualifiedName~ReviewGateRulesTests|FullyQualifiedName~ReviewGateGitTests|FullyQualifiedName~CodexReviewTests|FullyQualifiedName~CodexReviewGitTests' -m:1`: passed, 119 tests, at `25ad21e`.
- `dotnet test WhatYouCarry.slnx --filter Category=Documents -m:1`: passed, 140 tests, after the review record and handoff edits.
- GitHub checks at code head `25ad21e`: `asset-qa`, `det-lint`, `doc-gate`, `ste-check`, `documents`, `bots`, the Linux and macOS CI platform jobs, all three Smoke jobs, and all three bit-identity jobs passed. `ci-windows-x64` remained pending after the bounded wait; it has no result.
- `night-gate` failed at `25ad21e` on night record commit `e069e1637f5a88593d93d05ba7f9b4134690fd57`, which ended at `2026-09-23T14:00:41Z`; this is the known D-538 failure.
- `review-gate` and `evaluate` failed at `25ad21e` because the published review record still held the round 1 `Changes required` verdict.
- The only PR issue comment is the in-progress status above; the PR has no review threads.
- Push: the review record and Session 229 handoff are published to `origin/chore/pr-79-review-process`, verified with `gh pr view`.

## Open questions and accepted risks

No open question blocks this review. D-538 holds the known night failure; D-537 keeps the ruleset bypass for its manual merge. The Windows CI result was pending when this record was written.

## Verdict

**Blocked.** The code correction is sound and P2-1 is fixed. The required night-gate check failed on D-538, and `ci-windows-x64` had no result, so the required evidence is incomplete.
