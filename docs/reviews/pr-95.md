# PR-95 review

Date: 2026-09-23

## Identity

- PR: 95
- Target: `main`
- Base: `2c4e6d5f07d01e210730d3363f839d0c906fe26c`
- Merge base: `2c4e6d5f07d01e210730d3363f839d0c906fe26c`
- Head: `d13f73c6d94adf2c8ebd7e4fd22376ac64c48eec`
- Branch: `chore/pr-79-review-process`

## Provider gate

The handoff names Claude Code as the author of PR #95. Codex is the reviewer. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

PR-79 changes the review and override heads, the review command, the merge summary, and their tests and documents. I inspected all 23 changed paths, the PR description and comments, the roadmap entry, and the affected tool and test callers. The review focuses on D-533, D-534, D-539, D-540, D-190, and the four exit tests. No changed area remains uninspected.

## Findings

### P2-1: README and LICENSE overrides cannot pass

Status: open.

Open at: `d13f73c6d94adf2c8ebd7e4fd22376ac64c48eec`.

File: `WhatYouCarry.Tools/ReviewGate/ReviewGateRules.cs:36-39`.

Trigger: A PR changes only the root `README.md` or `LICENSE`, then receives the `review-override` label.

Expected: The review gate must provide a passing review or override path for each documents-only PR that D-534 removes from the review path, under D-188, D-190, D-475, and D-540.

Actual: D-475 places `README.md` and `LICENSE` in `SkipPaths`, so the PR has no effective head. D-190's `EligiblePaths` omits both files. The review path points to the label, but the label path rejects either changed path. `make codex-review` also refuses the PR because it has no effective head.

Consequence: A PR that changes only either root file cannot pass `review-gate` by review or by label.

Correction: Reconcile the review skip set and the label eligibility set. Add a Git regression case for each root file that proves the supported review or override path passes.

Regression check: Add `ReviewGateGitTests` cases for README-only and LICENSE-only PRs with the chosen approval path. Both must produce `success`; the current implementation rejects the label path.

## Out of scope

The failed night record and branch-night change belong to D-538, which the roadmap assigns to a later PR.

## PR comments

- `gitar-bot` issue comment 5799013917: the dashboard says the review is in progress. No review threads exist. The dashboard makes no code claim to verify.

## Description edits

None.

## Verification

- `git diff --check origin/main...HEAD`: passed.
- `dotnet test WhatYouCarry.slnx --filter 'FullyQualifiedName~ReviewGateRulesTests|FullyQualifiedName~ReviewGateGitTests|FullyQualifiedName~CodexReviewTests|FullyQualifiedName~CodexReviewGitTests' -m:1`: passed, 111 tests. The command built all projects.
- GitHub checks at PR head `b019737e5000bdf74d6c1dff081256cd80648a2f`: `asset-qa`, `det-lint`, `doc-gate`, `ste-check`, `documents`, `bots`, all CI platform jobs, and all bit-identity jobs passed.
- GitHub checks at PR head `b019737e5000bdf74d6c1dff081256cd80648a2f`: `night-gate` failed on night record commit `e069e1637f5a88593d93d05ba7f9b4134690fd57`, as D-538 records. The three Smoke platform jobs were skipped, so they provide no Smoke session evidence.
- `evaluate` and `review-gate` failed because this review record was absent from the PR head when those checks ran. The review verdict below requires a correction, so it does not approve that head.
- Push: the review record and Session 227 are published to `origin/chore/pr-79-review-process`, verified with `gh pr view`.

## Open questions and accepted risks

No open question blocks this review. D-538 records the known night failure.

## Verdict

**Changes required.** This verdict applies to head `d13f73c6d94adf2c8ebd7e4fd22376ac64c48eec`. The README and LICENSE override path cannot pass. The night gate also fails, and the Smoke jobs provide no session evidence.
