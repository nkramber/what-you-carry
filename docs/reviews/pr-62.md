# PR-62 review

Date: 2026-09-13

## Identity

- PR: 62
- Target: `main`
- Base: `aeb91df1a905ef1f316cc2c2acd73d97e0ade20d`
- Merge base: `aeb91df1a905ef1f316cc2c2acd73d97e0ade20d`
- Head: `38d1fac5ce23059ccac2231ab4f8ed53914cc298`
- Branch: `feat/pr-15-player-first-weapon`

## Provider gate

The session handoff identifies Claude Code as the author provider. Codex is the reviewer provider. The provider gate passes under T-4 and D-101.

## Intended behavior and scope

This PR adds the player state, the first weapon, the sword content, player animation, the sword attachment, and launch documentation. The repeat review inspected the correction diff, the original trigger, the regression tests, affected callers, the response file, PR comments, and the revision-matched remote checks. The owner play test remains open as exit test 7.

## Findings

### P2-1: Weapon asset paths accept invalid locations and file types

Status: fixed in `38d1fac`.

File: `WhatYouCarry.Core/Content/WeaponDefinition.cs:181-225`.

Trigger: Give `model` the value `models/../floors/a.json`, or give `animation` the value `models/player.bbmodel`.

Expected: The weapon validator accepts a model path only under `models/` with the model extension and accepts an animation path only under `models/` with the animation extension. It rejects dot segments. This follows D-298, D-302, and D-330.

Actual: `AssetPath` now checks the asset kind, path separators, path segments, and the file name extension. The original invalid values fail `WeaponDefinition.FromMembers`.

Consequence: The original invalid content no longer passes Core validation. A valid asset path under a `models/` subdirectory still loads.

Correction: Complete. The validator uses separate model and animation extensions, rejects rooted and malformed paths, and keeps valid subdirectory paths.

Regression check: `ContentTests` rejects the original triggers and accepts valid model and animation paths under a `models/` subdirectory. The focused correction suite passed.

## Out of scope

- The art quality pass remains in roadmap PR-62 after PR-20, as recorded by D-339.
- The owner play test remains exit test 7 of PR-15.

## PR comments

- Gitar reported a degenerate wedge when active ticks exceeded arc hundredths. The author rejected zero-turn wedges and constrained the arc in `6e35bc5`. The added tests cover zero, clockwise, and over-half-turn wedges, and the revision-matched automated pass approved the fix.
- The owner posted `Gitar review` after the automatic review pause notice. The follow-up reported that the review was running. No additional code claim appeared.
- The correction pass of Gitar approved `38d1fac` with no new issue. The review verified the correction against the original trigger and tests.

## Description edits

None.

## Verification

- `dotnet test WhatYouCarry.slnx --no-build -m:1 --filter 'FullyQualifiedName~PlayerTests|FullyQualifiedName~AnimationTests|FullyQualifiedName~ContentTests'`: passed 149 tests, including the correction cases.
- `dotnet build WhatYouCarry.slnx --no-restore -m:1`: the local run did not produce a final result in the execution window. Session 154 reports 0 warnings and 0 errors on this head.
- `dotnet test WhatYouCarry.slnx --no-build -m:1`: the local run did not produce a final result in the execution window. Session 154 reports 855 tests passed, including five Smoke tests.
- `git diff --check aeb91df..38d1fac`: passed.
- Revision-matched remote checks for `38d1fac`: build-and-test, bit identity, smoke on all three platforms, bots, determinism lint, asset QA, STE check, night gate, and Gitar passed.
- Fresh `evaluate` and `review-gate` checks pass after this record reached the PR. Duplicate platform CI jobs remain in progress or queued after the metadata push.
- Push: the repeat review update is published in commit `123e910`, verified with `gh pr view`.

## Open questions and accepted risks

OQ-171 is resolved by D-339 and does not block this PR. Exit test 7 remains an owner play test. No accepted risk remains for P2-1.

## Earlier verdicts

- At head `6e35bc5`, the verdict was `Changes required` because P2-1 was open.

## Verdict

**Ready for owner merge.** This verdict applies to head `38d1fac5ce23059ccac2231ab4f8ed53914cc298`. The original trigger fails with contextual validation, the regression tests pass, and no blocking finding remains. Fresh review-gate evidence must still publish for this record.
