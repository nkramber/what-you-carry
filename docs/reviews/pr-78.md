# PR-78 review

Date: 2026-09-16

## Identity

- PR: 78
- Target: `main`
- Base: `58e4fc864cd5b316588e10a0d6be398710e23cd6`
- Merge base: `58e4fc864cd5b316588e10a0d6be398710e23cd6`
- Head: `337d70632cef1f2d6b341ca2fc9820120d783c14`
- Branch: `feat/context-budget`

## Provider gate

Session 182 names Claude Code as the author of the substantive changes. This Codex review is independent and passes T-4 and D-101.

## Intended behavior and scope

The PR reduces repeated context reads, adds handoff rotation, and sets size limits for agent files and skills (D-377 to D-382). The prior review inspected the full diff, tests, changed tools, agent instructions, skills, decisions, design text, handoff history, PR description, comments, and checks. This repeat review inspected the correction diff, response, lookup command, regression tests, registers, and current PR comments. No affected area remains uninspected.

## Findings

### P2-1: The register lookup command searches fixed decision ids

Status: fixed in `337d706`.

File: `AGENTS.md:20-28`.

Trigger: Start a task whose relevant decisions include D-379 through D-382, then run the command shown in the instructions.

Expected: The command finds each D-# and OQ-# of the current task and each revision of those decisions (D-378, D-186).

Actual: The correction defines `d` and `q` variables, tells the reader to replace the example numbers, and uses those variables in all three grep patterns. `CLAUDE.md` matches `AGENTS.md` byte for byte.

Consequence: The command now supports task-specific register lookup and revision lookup.

Correction: The correction commit `337d706` adds the template instruction and the regression tests.

Regression check: `RegisterLookupTests` passes all three cases. The documented command also prints D-379 through D-382, the D-377 and D-381 revision lines, and OQ-9 and OQ-44.

## Earlier verdicts

- **Changes required** for `3eb7142ebbbd9b3f6e59a43ed7b60449599f086d`: P2-1 was open because the lookup command held fixed ids.

## Out of scope

None.

## PR comments

- The PR has no inline review comments. Its issue comments show two Gitar review requests, bot acknowledgements, and an approval dashboard comment for `337d706` with no finding. The effective head remains `337d706`, since later commits change only metadata.

## Description edits

None.

## Verification

- `git fetch origin`, `gh pr view 78`, and the branch refs: base `58e4fc864cd5b316588e10a0d6be398710e23cd6`; remote tip `2bdc070a7f7ef7991d0a17b4b4cc4f5c07df9e7e`; effective head `337d706`.
- `git diff origin/main...337d706`: complete PR scope inspected. Commits after `337d706` change only `docs/reviews/`, `docs/session-handoff.md`, or `docs/session-handoff-archive.md` (D-184).
- `git diff --check origin/main...337d706`: passed.
- `dotnet test WhatYouCarry.Tests/WhatYouCarry.Tests.csproj --no-restore --filter 'FullyQualifiedName~ReviewGateRulesTests|FullyQualifiedName~RegisterLookupTests|FullyQualifiedName~ContextBudgetTests' -m:1`: 27 passed, 0 failed.
- Documented D-378 command: printed D-379 through D-382, the D-377 and D-381 revision rows, OQ-9, and OQ-44.
- `cmp -s AGENTS.md CLAUDE.md`: passed.
- CI status: excluded from the review verdict at the owner's direction.
- Push: `00c29f6852990e0a05a063a45a99f25b1343c41d` was the head of `origin/feat/context-budget`, verified with `gh pr view`.

## Open questions and accepted risks

No open question or accepted risk blocks this review.

## Verdict

**Ready for owner merge.** This verdict applies to head `337d70632cef1f2d6b341ca2fc9820120d783c14`. P2-1 is fixed with regression coverage, the provider gate passes, and the complete PR scope has review coverage.
