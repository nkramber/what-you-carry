# PR-78 review

Date: 2026-09-16

## Identity

- PR: 78
- Target: `main`
- Base: `58e4fc864cd5b316588e10a0d6be398710e23cd6`
- Merge base: `58e4fc864cd5b316588e10a0d6be398710e23cd6`
- Head: `3eb7142ebbbd9b3f6e59a43ed7b60449599f086d`
- Branch: `feat/context-budget`

## Provider gate

Session 182 names Claude Code as the author of the substantive changes. This Codex review is independent and passes T-4 and D-101.

## Intended behavior and scope

The PR reduces repeated context reads, adds handoff rotation, and sets size limits for agent files and skills (D-377 to D-382). I inspected the complete diff, tests, changed tools, agent instructions, skills, decisions, design text, handoff history, PR description, comments, and current checks. No affected area remains uninspected.

## Findings

### P2-1: The register lookup command searches fixed decision ids

Status: open.

File: `AGENTS.md:20-28`.

Trigger: Start a task whose relevant decisions include D-379 through D-382, then run the command shown in the instructions.

Expected: The command finds each D-# and OQ-# of the current task and each revision of those decisions (D-378, D-186).

Actual: The command searches only D-146, D-375, OQ-9, and OQ-44. It omits the D-379 through D-382 decisions that define this PR's new rules. `CLAUDE.md` repeats the same command.

Consequence: A session can follow the new instruction and still miss the task's governing decision or a later revision. The lookup rule then does not prevent stale guidance.

Correction: Make the command a clear template, and tell the reader to replace its id lists with every id for the task. Keep `AGENTS.md` and `CLAUDE.md` identical.

Regression check: Run the documented command for D-379 through D-382 and the task's OQ ids. It must print each row and each revision line.

## Out of scope

None.

## PR comments

- The initial Gitar dashboard said automatic reviews were paused and showed an approval from before the final implementation push. The owner requested a manual review at 21:31:21 UTC; Gitar replied "On it" at 21:31:40 UTC, and the dashboard updated at 21:32:03 UTC with an approval and no finding. GitHub reports no Gitar review threads. The current Gitar check passes, and the review is fresh for effective head `3eb7142`; later commits changed only metadata.

## Description edits

None.

## Verification

- `git fetch origin`, `git rev-parse origin/main`, and `gh pr view 78`: base and merge base `58e4fc8`; remote tip `77c6f66`; local and remote branch refs match.
- `git diff origin/main...HEAD`: the effective head is `3eb7142`; the later `77c6f66` commit changes only `docs/session-handoff.md` (D-184).
- Focused tests `dotnet test WhatYouCarry.Tests/WhatYouCarry.Tests.csproj --no-restore --filter 'FullyQualifiedName~HandoffRotateTests|FullyQualifiedName~ContextBudgetTests'`: 12 passed, 0 failed.
- The repeat filtered run with `ReviewGateRulesTests` aborted before tests started with `SocketException (13): Permission denied`. An earlier identical run passed 33 tests, including the review record parser.
- `dotnet test WhatYouCarry.slnx --filter 'Category!=Smoke'`: no completion after about one minute. The run was cancelled and is not a pass.
- `gh pr checks 78 --watch --interval 60 > /dev/null 2>&1; gh pr checks 78` at tip `77c6f66`: asset-qa, bots, compare, det-lint, doc-gate, night-gate, ste-check, and platform checks passed. `evaluate` failed before this review record, and `review-gate` skipped before it existed.
- CI, smoke, and bit identity passed at effective head `3eb7142`, as the latest PR handoff records. Later commits change metadata only (D-357).
- Gitar: the manual request and response times, updated dashboard, passing check, and empty review-thread list prove a current approved pass for `3eb7142`.
- `cmp -s AGENTS.md CLAUDE.md`: passed.
- Push: the review record and Session 183 are published to `origin/feat/context-budget`, verified with `gh pr view`.

## Open questions and accepted risks

No open question or accepted risk blocks this review.

## Verdict

**Changes required.** This verdict applies to head `3eb7142ebbbd9b3f6e59a43ed7b60449599f086d`. P2-1 leaves the required task-specific decision lookup incomplete.
