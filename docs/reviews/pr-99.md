# PR-99 review

Date: 2026-09-24

## Identity

- PR: 99
- Target: `main`
- Base: `811c1c94e5efa0b274e441bb4fb7a5cb85190e2c`
- Merge base: `811c1c94e5efa0b274e441bb4fb7a5cb85190e2c`
- Head: `2b872f9942031e4c6c3d579926eb808673986777`
- Branch: `feat/pr-83-night-record-promotion`

## Provider gate

Session 239 names Claude Code as the author. Codex is the reviewer. The providers differ, so the review meets T-4 and D-101.

## Intended behavior and scope

The PR promotes a recent successful branch night to the record of main when the merge changes only paths in the skip set. It keeps newer records, re-runs the gate after each new main record, and preserves a later record when a main night ends after a promotion. I reviewed all changed paths, the PR description, the PR-83 roadmap entry and exit tests, the related decisions D-555 to D-563 and their revisions, the design guardrails, and the session handoff. I traced the git reads, tree comparison, ancestry checks, record writes, leases, workflow permissions, and gate re-runs. No area remains uninspected.

## Findings

No finding.

## Out of scope

The fixed seeds of the night belong to the next PR, as PR-83 states (D-560).

## PR comments

- Gitar posted “Gitar is working” with no item to address. D-550 defines this as a notice. It needs no answer and does not block this review.

## Description edits

None.

## Verification

- `git diff origin/main...HEAD` and the per-path diff: reviewed all 19 changed paths. Effective head `2b872f9` is the last commit outside the skip set. Later commits change only skipped documentation paths.
- Focused tests for the promotion, publication, and workflow rules: passed, 11 of 11.
- GitHub checks at head `3236111`: all three platform build and test jobs, both sweep jobs, asset QA, bit identity, bots, det-lint, CI skip, documents, doc-gate, night-gate, all smoke jobs, and ste-check passed. `evaluate` and `review-gate` failed because the review record did not exist on that head yet.
- `git diff --check origin/main...HEAD`: passed.
- `gh pr checks 99`: read the check state above.
- Push: `d7f90c51d2e06ac73c516b4c61b2cf5ef3adaa6b` was the head of `origin/feat/pr-83-night-record-promotion`, verified with `gh pr view`. The final amended metadata commit was also pushed and verified as the PR head.

## Open questions and accepted risks

No open question or accepted risk affects this change.

## Verdict

**Ready for owner merge.** This verdict applies to head `2b872f9942031e4c6c3d579926eb808673986777`. The changed behavior and focused tests pass review. The failed `evaluate` and `review-gate` checks read the prior state without a review record and must rerun against this metadata commit.
