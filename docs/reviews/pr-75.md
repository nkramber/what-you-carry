# PR-75 review

Date: 2026-09-16

## Identity

- PR: 75
- Target: `main`
- Base: `7345c9ce7de2450d755d2607edeeb2cdd002d569`
- Merge base: `7345c9ce7de2450d755d2607edeeb2cdd002d569`
- Head: `cb3c2e21e3c9221804f9fe14a4fa4c22f51d91ad`
- Branch: `feat/pr-68-shaft-landing`

## Provider gate

The substantive commits are authored by Claude Code, as recorded in Session 174 of `docs/session-handoff.md` and the handoff for PR-68. The reviewer is Codex. The provider gate passes under T-4 and D-101.

## Intended behavior and scope

The PR prevents detail-pass pillars from filling shaft columns, checks every shaft landing after generation, and raises the simulation version for the changed floor output. The review inspected the complete diff, affected procgen and reachability callers, replay-version and bit-identity tests, the PR roadmap entry, design and decision records, all existing PR comments, and the required CI evidence. The review covered determinism, construction errors, reachability, regression coverage, and metadata consistency. No affected area remains uninspected.

## Findings

No finding.

## Out of scope

None.

## PR comments

- The owner comment `Gitar review` requested a fresh automated pass. The resulting pass approved `cb3c2e2` and reported one closed Quality finding.
- Gitar claimed that `CheckShaftLandings` named only the unreachable cause when the landing was not a floor cell. Commit `cb3c2e2` now gives the two conditions separate messages and adds the `cause` context key. The corrected behavior and the hand night evidence were verified.

## Description edits

None.

## Verification

- `git diff --check 7345c9c..cb3c2e2`: passed.
- `cmp -s AGENTS.md CLAUDE.md`: passed.
- Focused regression `ShaftOfSeed79146LandsOnAReachableFloor`: passed in the author-reported local suite and revision-matched CI.
- `dotnet build WhatYouCarry.slnx`: no completion result after about 90 seconds without output in this local execution context. The attempt was interrupted and is not counted as a pass.
- Local `dotnet test WhatYouCarry.slnx --no-build`: not run after the local build hang. The author-reported full suite passed 1116 tests with 0 failures, including 5 Smoke tests.
- Author-reported local `det-lint`, `asset-qa`, `ste-check`, Godot build check, and `bit-identity`: passed at `cb3c2e2`.
- Revision-matched CI at `cb3c2e2`: CI, Smoke on Linux, macOS, and Windows, Bit identity on all three platforms, compare, Bots, Asset QA, determinism lint, and STE check passed. No self-hosted job reported `not acquired`.
- Hand night run `35067529373` at `cb3c2e2`: passed. The local 100000-seed night sweep also passed, as recorded in Session 174.
- `night-gate`: red because the successful feature-branch night commit is not an ancestor of `main`. D-372 explicitly accepts this state until the first scheduled night after merge.
- `evaluate` and `review-gate`: failed or neutral before a review record existed. They must refresh after this record reaches the PR.
- Push: the review record and Session 175 handoff are published to `origin/feat/pr-68-shaft-landing`, verified with `gh pr view`.

## Open questions and accepted risks

- D-372 accepts the red `night-gate` for PR-68. The first scheduled night on `main` carries the required base-branch evidence.
- OQ-175 is resolved by D-372.

## Verdict

**Ready for owner merge.** This verdict applies to head `cb3c2e21e3c9221804f9fe14a4fa4c22f51d91ad`. The provider gate passes, the changed behavior has focused regression coverage, revision-matched required checks pass apart from the accepted D-372 night-gate state, and no blocking finding remains.
