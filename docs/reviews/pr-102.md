# PR-102 review

Date: 2026-09-25

## Identity

- PR: 102
- Target: `main`
- Base: `a3590baba7549692621fab49fea4c41d7d78b083`
- Merge base: `a3590baba7549692621fab49fea4c41d7d78b083`
- Head: `69f530818aaecded6c23740880ca1db16a1e4f5d`
- Branch: `feat/pr-85-night-hosted-linux`

## Provider gate

The session handoff names Claude Code as the PR author. The reviewer is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

The PR moves the scheduled night to hosted Linux at 07:07 UTC. Round 1 read every changed path, the full workflow and gather script, the changed tests, the PR-85 roadmap entry, the changed decisions, and the related skills and runbook. It traced seed planning, sweep artifacts, status aggregation, failure records, record publication, reruns, and job permissions under D-571 to D-573, D-567, D-569, T-2, T-3, and G-10. Round 2 read the response and the complete diff since `69f5308`. It verified the unrelated audit prompt is absent from the PR tip and checked every remaining changed path. No area remains uninspected.

## Findings

### P2-1: Keep the repository audit prompts out of the night workflow PR

Status: fixed in `784583a37ed05b91516f876ee14aaf4450ac7e81`.

Open at: `69f530818aaecded6c23740880ca1db16a1e4f5d`.

File: `docs/reviews/repository-review-prompts.md:1-3`.

Trigger: PR #102 includes a new, repository-wide audit and remediation prompt while its stated concern and PR-85 roadmap scope cover the night workflow.

Expected: G-10 and the PR description limit this PR to one concern, the night schedule and runner.

Actual: The new file directs a full repository audit and a later implementation session. The PR description does not list this work in its scope or documents matrix.

Consequence: The night workflow change carries an independent process artifact, which expands review scope and couples unrelated work to this PR.

Correction: Remove this file from PR #102. Submit it with a PR whose single concern is the repository audit prompts.

Regression check: Read `git diff origin/main...HEAD --name-status`. Confirm every remaining path serves the night workflow move or its required records and tests.

## Out of scope

None.

## Earlier verdicts

- Round 1 at `69f530818aaecded6c23740880ca1db16a1e4f5d`: **Changes required.** The PR included a repository-wide audit prompt outside its stated night workflow concern.

## PR comments

- The owner explained that `evaluate` and `review-gate` wait for this review record under D-251 and D-577. The check run at `784583a` confirms both failed before publication.
- Gitar approved code head `69f5308` with no finding. Its summary flagged the permissions change as high risk. The workflow gives write permissions only to the record job, as the PR description states.

## Description edits

None.

## Verification

- `git fetch origin`: passed before review. `origin/main` was `a3590baba7549692621fab49fea4c41d7d78b083`.
- Effective-head calculation: passed. `69f530818aaecded6c23740880ca1db16a1e4f5d` is the newest commit outside the D-475 skip set.
- Round 1 code checks: Documents passed 147 of 147, STE passed with zero findings, and the gather-script fixtures and shell syntax passed. The full suite did not run.
- Round 2 regression check: `test ! -e docs/reviews/repository-review-prompts.md` passed at PR tip `784583a`; `git diff origin/main...HEAD --name-status` lists no audit prompt.
- `dotnet test WhatYouCarry.slnx --filter Category=Documents`: passed, 147 of 147, at `784583a`.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- ste-check --root .`: passed, 0 findings in 38 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- doc-gate --root . --base origin/main --head HEAD --body /tmp/pr-102-body.md --title "feat: the night runs on hosted Linux at 07:07 UTC, one job for each sweep (PR-85)" --branch feat/pr-85-night-hosted-linux`: passed, 0 problems over 13 changed paths.
- `git diff --check origin/main...HEAD`: passed.
- `gh pr checks 102` at `784583a`: Gitar, documents, doc-gate, STE, det-lint, asset QA, and night gate passed. Code, smoke, and bit-identity jobs skipped by the documents-only rule; their checks at effective head `69f5308` passed. `evaluate` and `review-gate` failed while this record was absent.
- Full suite: not run in round 2 because every change since reviewed head `69f5308` is in the D-475 skip set.
- Push: verified after the metadata commit with `gh pr view` for `origin/feat/pr-85-night-hosted-linux`.

## Open questions and accepted risks

None.

## Verdict

**Ready for owner merge.** The earlier finding is fixed: the PR tip deletes the unrelated audit prompt, and the round-two regression check confirms it is absent. Required code evidence applies to effective head `69f530818aaecded6c23740880ca1db16a1e4f5d`.
