# PR-102 review

Date: 2026-09-25

## Identity

- PR: 102
- Target: `main`
- Base: `a3590baba7549692621fab49fea4c41d7d78b083`
- Merge base: `a3590baba7549692621fab49fea4c41d7d78b083`
- Head: `69f530818aaecded6c23740880ca1db16a1e4f5d`
- Branch: `feat/pr-85-night-hosted-linux`

## Provider gate

The session handoff names Claude Code as the PR author. The reviewer is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

The PR moves the scheduled night to hosted Linux at 07:07 UTC. The review read every changed path, the full workflow and gather script, the changed tests, the PR-85 roadmap entry, the changed decisions, and the related skills and runbook. It traced seed planning, sweep artifacts, status aggregation, failure records, record publication, reruns, and job permissions under D-571 to D-573, D-567, D-569, T-2, T-3, and G-10. No area remains uninspected.

## Findings

### P2-1: Keep the repository audit prompts out of the night workflow PR

Status: open.

Open at: `69f530818aaecded6c23740880ca1db16a1e4f5d`.

File: `docs/reviews/repository-review-prompts.md:1-3`.

Trigger: PR #102 includes a new, repository-wide audit and remediation prompt while its stated concern and PR-85 roadmap scope cover the night workflow.

Expected: G-10 and the PR description limit this PR to one concern, the night schedule and runner.

Actual: The new file directs a full repository audit and a later implementation session. The PR description does not list this work in its scope or documents matrix.

Consequence: The night workflow change carries an independent process artifact, which expands review scope and couples unrelated work to this PR.

Correction: Remove this file from PR #102. Submit it with a PR whose single concern is the repository audit prompts.

Regression check: Read `git diff origin/main...HEAD --name-status`. Confirm every remaining path serves the night workflow move or its required records and tests.

## Out of scope

None.

## PR comments

- Gitar’s dashboard reported a missing review record and approved head `69f5308` with no code finding. The author explained that `evaluate` and `review-gate` wait for this record under D-251 and D-577. The current checks confirm that the product checks pass and those two checks fail before publication.

## Description edits

None.

## Verification

- `git fetch origin`: passed before review. `origin/main` was `a3590baba7549692621fab49fea4c41d7d78b083`.
- Effective-head calculation: passed. `69f530818aaecded6c23740880ca1db16a1e4f5d` is the newest commit outside the D-475 skip set.
- `dotnet test WhatYouCarry.slnx --filter 'Category=Documents'`: passed, 147 of 147.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- ste-check --root .`: passed, 0 findings in 38 files.
- `bash .github/scripts/night-gather.sh` fixture checks: passed. Success inputs returned success, failed and skipped inputs returned failure, and cancelled input returned cancelled.
- `bash -n .github/scripts/night-gather.sh`: passed.
- `git diff --check origin/main...HEAD`: passed.
- `gh pr checks 102`: product CI, bit identity, smoke, asset QA, night gate, doc gate, STE, det-lint, and Gitar passed. `evaluate` and `review-gate` failed because this review record was not yet present. One older doc-gate run was cancelled; the latest doc-gate run passed.
- Full suite: not run in this review. The PR head has green CI on its required test jobs, and this review ran the focused Documents suite.
- Push: verified after the metadata commit with `gh pr view` for `origin/feat/pr-85-night-hosted-linux`.

## Open questions and accepted risks

None.

## Verdict

**Changes required.** This review found one in-scope violation of G-10: the PR includes a repository-wide review prompt outside its stated night workflow concern. Remove the unrelated file and request a repeat review.
