# PR-43 review

Date: 2026-09-11

## Identity

- PR: 43
- Target: `main`
- Base: `f19fe2e`
- Merge base: `f19fe2e`
- Head: `d89338c`
- Branch: `chore/night-logs-artifact`

## Provider gate

The PR author is Claude Code. Session 109 in `docs/session-handoff.md` identifies the author field and the substantive branch work. The reviewer is Codex. The providers differ, so the cross-provider gate passes under T-4 and D-101.

## Intended behavior and scope

The workflow must upload `bot-logs` when a night fails. It must warn when the directory does not exist. The upload step must follow the bot steps. The review inspected the workflow, the shape test, the D-280 decision, the PR-11 roadmap contract, the complete diff, all PR comments, and the affected night-record steps. The local full build and test runner could not complete because the execution context denied the VSTest socket. Remote platform checks passed. No Core, Game, persistence, replay, or presentation behavior changed.

## Findings

### P2-1: The shape test does not require the upload step to follow the reachability sweep

- Status: open.
- Reviewed head: `d89338c`.
- Location: `WhatYouCarry.Tests/RepositoryShapeTests.cs:173`.
- Trigger: move the upload step between `Greedy descender, five thousand seeds` and `Reachability sweep, one hundred thousand seeds`.
- Expected: the upload step stays after every bot step, including the reachability sweep. This is the behavior stated by the PR and the D-280 workflow purpose.
- Actual: the assertion checks only that the upload step follows the greedy-descender step. The test remains green after the move described above, so a reachability failure can run without uploading the available logs.
- Evidence: the current workflow has the reachability step at line 34 and the upload step at line 38. The assertion searches only for the greedy-descender name at line 173.
- Correction: assert that the upload step follows `Reachability sweep, one hundred thousand seeds`, or assert order against the last bot step. Add a regression case that moves the upload step before the sweep and expects the shape test to fail.

## Out of scope

None.

## PR comments

- The owner said that the missing review file is expected until this review creates it. The review verified that `docs/reviews/pr-43.md` was absent at `d89338c` and that the review gate reported that absence.
- Gitar approved the change and reported that the original step slice reached end of file. The owner answered that the slice was bounded at `d89338c`. The review verified the slice ends at the next `- name:` line and that the cited issue is fixed.
- The owner repeated the missing-review-file answer and named `d89338c` as the effective head. The review verified the effective-head calculation and the gate behavior.

## Description edits

None.

## Verification

- `git diff --check origin/main...origin/chore/night-logs-artifact`: passed.
- `NightWorkflowKeepsTheLogsOfAFailedNight`: local run blocked before test execution by `System.Net.Sockets.SocketException: Permission denied` when VSTest bound its socket.
- `dotnet build WhatYouCarry.slnx`: local run produced no output and did not complete in the execution context. I stopped it after the timeout.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- det-lint --root .`: passed, 0 findings.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- ste-check --root .`: passed, 0 findings in 15 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- bit-identity`: passed, `6ec00e90c1c85cdb`.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --editor --path WhatYouCarry.Game --build-solutions --quit`: passed on Godot 4.7.2.
- Remote CI at `d89338c`: Linux, Windows, macOS, bots, det-lint, STE check, night-gate, and Gitar passed. The evaluate job failed because this review file was absent before this review. The review-gate check skipped for the same missing record. Fresh gate results remain pending after this record is pushed.
- Push: `8f55419` is the head of origin/chore/night-logs-artifact, verified with gh pr view.

## Open questions and accepted risks

OQ-99 remains open and does not block this review. No accepted risk resolves P2-1.

## Verdict

**Changes required.** This verdict applies to head `d89338c`. The workflow behavior is correct at this revision, but the new regression test does not enforce the required order after the reachability sweep.
