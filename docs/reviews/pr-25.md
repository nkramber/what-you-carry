# PR-25 review

Date: 2026-09-09

## Identity

- PR: 25
- Target: `main`
- Base: `a78e759155b28f8e5782e4c4f94d6062df38b774`
- Merge base: `a78e759155b28f8e5782e4c4f94d6062df38b774`
- Head: `3e1524915f3f1a17d236747b57da017d183fd8d8`
- Branch: `fix/review-gate-red-on-neutral`

## Provider gate

Session 69 identifies Claude Code as the provider for the substantive PR-25 change. The active review provider is Codex. The gate passes under T-4 and D-101.

## Intended behavior and scope

PR-25 changes the review-gate workflow so the job fails unless the published check run has a `success` conclusion. It adds a regression test for the workflow guard and updates the related decisions, design, roadmap, skill, agent files, and handoff records. The review inspected the complete diff, the workflow inputs and permissions, the base-branch execution boundary, the test, the automated review comment and answer, and all affected documents. No area remains uninspected.

The change preserves the three check-run conclusions from D-181. It makes a neutral check run produce a failed workflow job, as D-251 requires. It also fails for a missing or unexpected conclusion, which prevents a silent pass under T-2.

## Findings

No finding.

## Out of scope

None.

## PR comments

- Gitar identified a guard that passed missing or unexpected conclusions. The author changed the guard at `3e15249` to pass on `success` alone. The review verified the neutral, null, unexpected, and success cases with a shell probe, and the automated pass approved the final head.

## Description edits

None.

## Verification

- `git diff --check a78e759..3e15249`: passed.
- `dotnet build WhatYouCarry.slnx -m:1`: passed with 0 warnings and 0 errors.
- `dotnet test WhatYouCarry.slnx --no-build -m:1`: passed 399 tests with 0 failures and 0 skipped tests.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: passed with 0 findings in 15 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- det-lint --root .`: passed with 0 findings. Core 0 in 37 files, Game 0 in 0 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- bit-identity`: passed with `e8ef2b1fad938845`.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --editor --path WhatYouCarry.Game --build-solutions --quit`: passed with Godot 4.7.2.
- Shell falsifying probe: the new guard returned status 1 for null and neutral conclusions, and status 0 for success. The base guard matched only `failure`.
- GitHub checks read for PR #25 at head `3e15249`: CI, bit identity, determinism lint, STE, evaluate, and Gitar passed. The `review-gate` check was neutral because the base branch still holds the previous workflow, as D-197 predicts. The first post-merge PR must prove the new job result, as D-251 states.
- Push: `3451c24` is the head of `origin/fix/review-gate-red-on-neutral`, verified with `gh pr view`.

## Open questions and accepted risks

OQ-99 remains open, but it blocks no work in this review. D-197 and D-251 accept that a workflow change cannot exercise its new behavior on the same PR. The first post-merge PR must provide that proof.

## Verdict

**Ready for owner merge.** This verdict applies to effective head `3e1524915f3f1a17d236747b57da017d183fd8d8`. The provider gate passes, the complete scope has review coverage, all applicable checks pass, and no blocking finding remains.
