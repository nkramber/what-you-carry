# PR-83 review

Date: 2026-09-20

## Identity

- PR: 83
- Target: `main`
- Base: `e1c20ead7d81e87a6a29f424fce05646e4570f72`
- Merge base: `e1c20ead7d81e87a6a29f424fce05646e4570f72`
- Head: `f698cd9b14c43416c4092944de3eaa4858b2dcba`
- Branch: `feat/pr-16-enemies-and-pathfinder`

## Provider gate

The handoff identifies Claude Code as the author of PR #83. Codex is the reviewer for this session. The provider gate passes T-4 and D-101.

## Intended behavior and scope

The PR adds the first enemy family, shared combat state, grid pathfinding, humanoid AI, enemy placement, bot support, rendering, the F-104 and F-105 movement corrections, and the D-406 handoff rotation correction. The repeat review inspected the new handoff tool and tests, the changed decisions and roadmap, the complete earlier scope, PR comments, and the response file.

## Findings

No finding.

## Out of scope

Ranged attacks, additional enemy families, enemy models and clips, and armor-weight numbers remain with the roadmap items named by PR-83. The handoff tool does not resolve duplicate session numbers, which remains its stated error case.

## PR comments

- Gitar reported no code finding and no rules evaluated. The owner answered the earlier evidence limits in `docs/reviews/pr-83-response.md`. The repeat review verified the response, the handoff correction, the new D-406 scope, and the current automated comment.

## Description edits

None.

## Verification

- `git fetch origin`: passed. Base is `e1c20ead7d81e87a6a29f424fce05646e4570f72`.
- `gh pr view 83`: passed. The effective head is `f698cd9b14c43416c4092944de3eaa4858b2dcba`. The D-406 tool and test correction changes substantive paths, so this is the new effective head.
- Focused tests for `PathfinderRepeatsAndReportsNoPath`, `YawTowardFacesThePoint`, and `EnemyUsesPlayerRules`: passed, 6 tests.
- `dotnet test WhatYouCarry.slnx --no-build --filter 'FullyQualifiedName~HandoffRotateTests'`: passed, 9 tests.
- `gh pr checks 83`: all required implementation checks pass, including the three build legs, three smoke legs, bit identity, compare, bots, asset-qa, det-lint, STE, doc-gate, and night-gate. `evaluate` and `review-gate` fail only because this record has not yet published the approving verdict.
- The response reports 1172 passed and 0 failed at `0075b92`, 1171 passed and 1 failed at `a151849`, and 5 of 5 Smoke tests. The failed local test and the three failed CI legs match the handoff-order defect. The correction is present at the tip, and the focused handoff test passes.
- Gitar: passed on the effective head with no code finding.
- Push: `8c385ce9fe94a985a03ff37133526bbab69d5d83` is the head of `origin/feat/pr-16-enemies-and-pathfinder`, verified with `gh pr view`.

## Open questions and accepted risks

No open question affects this review. Fresh required CI remains pending.

## Earlier verdicts

- `Blocked` at head `8452dbd7c7d26ed5135901f8ac3a381b59b42f10`: the initial review lacked a completed local broad run and had pending required CI. The response supplied the run results and corrected the handoff-order defect. No finding was opened.
- `Blocked` at head `8452dbd7c7d26ed5135901f8ac3a381b59b42f10`: the repeat review verified the response and focused correction, but fresh CI was pending. The later D-406 correction moved the effective head.

## Verdict

**Ready for owner merge.** This verdict applies to head `f698cd9b14c43416c4092944de3eaa4858b2dcba`. The earlier evidence limits are resolved, the D-406 correction has focused regression coverage, all required implementation checks pass, and no finding remains.
