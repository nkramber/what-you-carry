# PR-52 review

Date: 2026-09-11

## Identity

- PR: 52
- Target: `main`
- Base: `52ab6ca`
- Merge base: `52ab6ca`
- Head: `9085a95`
- Branch: `feat/pr-13-model-loader-and-mesher`

## Provider gate

The handoff identifies Claude Code as the author of the substantive commits `9a7fb96` and `9085a95`. The reviewer is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

This PR adds the Blockbench model loader, model mesh construction, chunk greedy meshing, ambient occlusion, wall fade, and the M-3 bot session and frame log. The review inspected the complete diff, the model file, affected Game callers, Core bot and simulation contracts, tests, roadmap, decisions, questions, and every PR comment. Visual feel on the Steam Deck remains for M-3 and Gate 2.

## Findings

No finding.

## Out of scope

Visual feel, Deck readability, final texture quality, armor overlays, and animation remain for M-3, PR-14, PR-15, and PR-22 as the roadmap states.

## PR comments

The automated pass reported that a frame-log permission failure escaped the `IOException` catch. The author corrected the catch in `9085a95`; the focused and full suites, local bot run, and diff inspection verify the correction. The automated pass also reported the expected missing review file; the author explained that this review creates it, which agrees with D-251.

## Description edits

None.

## Verification

- `dotnet build WhatYouCarry.slnx -m:1`: passed with 0 warnings and 0 errors.
- `dotnet test WhatYouCarry.slnx --no-build -m:1`: passed, 622 tests, 0 failures.
- Focused PR-13 tests: passed, 38 tests, 0 failures.
- `det-lint --root .`: passed, 0 findings.
- `ste-check --root .`: passed, 0 findings.
- `bit-identity`: passed, `6ec00e90c1c85cdb`.
- Godot editor build: passed.
- Headless smoke session: passed with exit code 0.
- Headless bot session with frame log: passed with exit code 0; seed 1 reached floor 2 at tick 421, with 1,001 frames and `frameMicrosP99=7402`.
- Remote required checks on the reviewed effective head: build and test, bots, bit identity, determinism lint, night gate, STE check, and smoke passed. After the metadata push, the review-gate passed. Duplicate platform checks from that metadata push were still pending at the observation, so they remain unverified for the new tip.
- `git diff --check 52ab6ca..9085a95`: passed.
- Push: `42e5a46` is the head of `origin/feat/pr-13-model-loader-and-mesher`, verified with `gh pr view`.

## Open questions and accepted risks

OQ-161 remains open and blocks exit test 7 and M-3. OQ-159 and OQ-160 remain open but block nothing. The local frame log measures this machine; the Steam Deck measurement remains an owner action.

## Verdict

**Ready for owner merge.** This verdict applies to head `9085a95`. The implementation and required local and remote checks pass, and no finding remains. The review-gate check needs its normal refresh after this review record reaches the PR head.
