# PR-19 review

Date: 2026-09-09

## Identity

- PR: 19
- Target: `main`
- Base: `c11fb417dac25ea26bf9347d1471fa134e3097de`
- Merge base: `c11fb417dac25ea26bf9347d1471fa134e3097de`
- Head: `f7bec9f32cff8087c730d9a71f86e05d80b32172`
- Branch: `feat/pr-6-loop-record`

## Provider gate

Session 60 identifies Claude Code as the provider for the substantive PR-6 change. The active review provider is Codex. The gate passes under T-4 and D-101.

## Intended behavior and scope

PR-19 adds the fixed-step Core loop, the 16-byte intent frame, the run record, the recorder, and the replay. The review inspected the complete implementation diff, affected callers, tests, bit-identity sweep, content-reader changes, lint allowlist, design, decisions D-224 to D-230, questions, and the Phase 1 roadmap. It checked determinism, replay state, version and content mismatches, torn tails, error context, Core boundaries, and document consistency. No area remains uninspected.

## Findings

No finding.

## Out of scope

- PR-31 owns the Game-layer run-record sink, profile persistence, and replay resume behavior.
- Later world and camera PRs own movement simulation and the remaining initial-state types.

## Description edits

None.

## Verification

- `git diff --check c11fb41..f7bec9f`: passed.
- `dotnet test WhatYouCarry.slnx --no-build -m:1`: passed 336 tests with 0 failures and 0 skipped tests.
- `dotnet build WhatYouCarry.slnx`: stalled without compiler output in the local execution context. CI build and test jobs passed on Linux x64, macOS arm64, and Windows x64.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- det-lint --root .`: passed with 0 findings. Core 0 in 29 files, Game 0 in 0 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- ste-check --root .`: passed with 0 findings in 15 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- bit-identity`: passed with `283aa4b8cd1281be`. The value differs from `4d6385bb92454694` because the sweep now replays a fixed record, as required by G-20.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --editor --path WhatYouCarry.Game --build-solutions --quit`: passed with Godot 4.7.2.
- GitHub checks for PR #19 passed for the published tip `0850d32`, including Linux, macOS, and Windows build and test, compare, det-lint, STE, and evaluate. `review-gate` was neutral before this review record.
- `git fetch origin`: passed before this review.
- Push: `1247769d2d675e6b1e7df29d3a875ea854832209` is the head of `origin/feat/pr-6-loop-record`, verified with `gh pr view`.

## Open questions and accepted risks

OQ-99 remains open, but it blocks no work in this review. The review accepts no risk.

## Verdict

**Ready for owner merge.** This verdict applies to effective head `f7bec9f32cff8087c730d9a71f86e05d80b32172`. The provider gate passes, the complete review scope has coverage, the intentional bit-identity change is confirmed, and no blocking finding remains.
