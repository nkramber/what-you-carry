# PR-94 review

Date: 2026-09-23

## Identity

- PR: 94
- Target: `main`
- Base: `6ee836d017d7a0b571bdecee0cc70a51389a7983`
- Merge base: `6ee836d017d7a0b571bdecee0cc70a51389a7983`
- Head: `df900d52e4bf2049682c2531911258beb400e779`
- Branch: `feat/pr-74-body-art`

## Provider gate

The author is Claude Code, as Session 224 records. This review runs in Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

PR-74 adds the approved face and body art, shade and grain recipe support, the umber ramp, and explicit ruleset fields. I reviewed the full diff, changed files, tests, texture consumers, focused roadmap exit tests, design guardrails, cited decisions, the PR description, and all PR comments. No affected area remains uninspected.

## Findings

No finding.

## Out of scope

None.

## PR comments

- The only issue comment is Gitar's in-progress dashboard. No review threads exist. D-536 records the owner's waiver of the automated pass for this PR. The dashboard makes no code claim to verify.

## Description edits

None.

## Verification

- `git fetch origin`; base and merge base are `6ee836d017d7a0b571bdecee0cc70a51389a7983`. The detached head matches PR #94 at `df900d52e4bf2049682c2531911258beb400e779`.
- `dotnet build WhatYouCarry.slnx`: passed, 0 warnings and 0 errors.
- `dotnet test WhatYouCarry.slnx --no-build --filter 'FullyQualifiedName~TextureGenTests|FullyQualifiedName~RecipeTests|FullyQualifiedName~BlockbenchLoaderTests|FullyQualifiedName~RulesetTests'`: passed, 152 tests, 0 failed, 0 skipped.
- The targeted checks verify exit test 6: `RulesetTests` passes, and the live ruleset matches `.github/rulesets/main.json` with an empty diff.
- `gh pr checks 94`: `night-gate` failed on the known softlock issue recorded in D-538. `ci-linux-x64`, `ci-windows-x64`, `ci-macos-arm64`, `ci-windows-x64-sweeps`, `ci-linux-x64-sweeps`, `ci-windows-x64`, `ci-macos-arm64`, `smoke-macos-arm64`, `smoke-windows-x64`, `bit-identity-macos-arm64`, `bit-identity-windows-x64`, and `bots` were pending. Exit tests 1, 2, 3, and 5 therefore lack complete required platform evidence. `evaluate` and `review-gate` failed because this record was absent at the checked head.
- `asset-qa`, `det-lint`, `doc-gate`, `documents`, `ste-check`, `smoke-linux-x64`, and `bit-identity-linux-x64` passed on the reviewed head.
- The full test suite and the three-platform Godot smoke sessions did not run locally. This checkout has no configured Godot binary. Required CI jobs remain the evidence source for those checks.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- handoff-rotate --root .`: passed; Session 215 moved to the archive.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: passed, 0 findings in 37 files.
- `git diff --check`: passed for the metadata changes.
- `git diff --check origin/main...HEAD`: passed.
- Push: effective head `df900d5` remains the PR code head after the metadata push, verified with `gh pr view`.

## Open questions and accepted risks

No open question blocks this review. OQ-181 applies to PR-77 alone. D-538 records the known night failure and its planned correction; the required `night-gate` remains red on this PR.

## Verdict

**Blocked.** The provider gate passes and no in-scope defect was found. Required platform evidence remains pending, and the required `night-gate` failed. Reassess after the checks report on the published metadata head and the owner resolves the night-gate block.
