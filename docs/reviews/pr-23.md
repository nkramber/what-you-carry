# PR-23 review

Date: 2026-09-09

## Identity

- PR: 23
- Target: `main`
- Base: `1d8f8bdf5e4c584f7847fe63362dff92553a60f5`
- Merge base: `1d8f8bdf5e4c584f7847fe63362dff92553a60f5`
- Head: `4e98470`
- Branch: `feat/pr-8-camera`

## Provider gate

Session 71 identifies Claude Code as the provider for the substantive PR-23 change. The active review provider is Codex. The gate passes under T-4 and D-101.

## Intended behavior and scope

PR-23 adds the camera, grid ray march, aim ray, controller aim assist, the pitch limit, and the simulation version 3 change. The repeat review inspected the correction diff, the response file, the replay observer, the bit-identity sweep, the related decisions D-241 to D-249, the Phase 1 PR-8 exit tests, and all existing PR comments and replies. It also reran the Core boundary, deterministic math, replay, error, and test checks. No area remains uninspected.

## Findings

### P2-1: The bit-identity camera fold does not replay the record

Status: fixed in `4e98470`.

Original file: `WhatYouCarry.Tools/BitIdentity/BitIdentitySweep.cs:208-235`.

Correction files: `WhatYouCarry.Core/Replay/IReplayObserver.cs:1-18`, `WhatYouCarry.Core/Replay/RunReplayer.cs:52-101`, and `WhatYouCarry.Tools/BitIdentity/BitIdentitySweep.cs:192-273`.

Trigger: Run the `bit-identity` sweep. The record is replayed at lines 208-210, but `AddCamera` creates a new `SimulationLoop` at line 219 and steps the original intent list at lines 220-222. It then hashes that live loop's camera pose and aim ray.

Expected: The bit-identity job must replay a record with camera motion on all three platforms and assert one hash (Phase 1 PR-8 exit test 5, D-151, G-20).

Actual: At the original head, the replay result contributed only its final simulation hash. At `4e98470`, `RunReplayer` calls `AfterTick` after each complete frame, and `CameraFold` hashes the camera pose and aim ray from that replayed loop. The separate live loop is gone.

Consequence: The required cross-platform replay proof now covers the new camera and aim behavior for every complete replayed frame.

Correction: The author added the smallest replay observer needed to visit each complete replayed tick. The five-argument replay overload keeps its existing behavior through a silent observer.

Regression check: `TheReplayVisitsEveryCompleteFrame` checks complete frames, torn tails, and an empty record. `AimRayIsDeterministic` compares the replay fold with the live fold over one hundred seeds. `TheSweepReadsEveryFunctionAndStream` rejects a second live loop. All pass. The bit-identity job returns one hash on all three platforms in CI.

## Out of scope

None.

## PR comments

- Gitar approved the original implementation and the author answered the later pass. The later automated comment suggested a null guard and asked for a verdict edit. The author gave no-merit replies. The review verifies that the observer is non-nullable under `Directory.Build.props`, which enables nullable references and treats warnings as errors, and that the review verdict belongs to the reviewing provider under T-4 and D-101.

## Description edits

None.

## Verification

- `git diff --check 1d8f8bd...4e98470`: passed.
- `dotnet build WhatYouCarry.slnx -m:1`: passed with 0 warnings and 0 errors.
- `dotnet test WhatYouCarry.slnx --no-build -m:1`: passed 426 tests with 0 failures and 0 skipped tests. The first restricted run hit a `SocketException (13): Permission denied`, so the command ran again in the permitted execution context.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- det-lint --root .`: passed with 0 findings. Core 0 in 43 files, Game 0 in 0 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- ste-check --root .`: passed with 0 findings in 15 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- bit-identity`: passed with `afed0063a6cf8a50`.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --editor --path WhatYouCarry.Game --build-solutions --quit`: passed with Godot 4.7.2.
- `gh pr view 23` verified base `1d8f8bd`, head `f1af5db`, and the existing gitar comments and replies. The GitHub checks still show the earlier review-gate and evaluate failures because this approved record is not yet on the PR head.
- Push: pending review commit. The session end gate will verify the pushed head with `gh pr view`.

## Open questions and accepted risks

OQ-99 remains open, but it blocks no work in this review. D-151 and G-20 require replay compatibility and the simulation version bump. No accepted risk covers the missing replay camera fold.

## Verdict

**Ready for owner merge.** This verdict applies to effective head `4e98470`. P2-1 is fixed, the provider gate passes, the complete scope has review coverage, and all applicable local checks pass. The review-gate check will update after this record reaches the PR head.
