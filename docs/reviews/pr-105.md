# PR-105 review

Date: 2026-09-25

## Identity

- PR: 105
- Target: `main`
- Base: `0d99e2c50d861ce41db3d09eb0c39d11c6be5713`
- Merge base: `0d99e2c50d861ce41db3d09eb0c39d11c6be5713`
- Head: `1678ef5d65b0cde57be30e7c8f7951101f0a6b9d`
- Branch: `feat/pr-86-hosted-macos`

## Provider gate

The author provider is Claude Code, as Session 259 records. The reviewer provider is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

PR-86 moves the macOS CI, smoke, and bit-identity jobs to `macos-latest`, checks for arm64 before checkout, and retires the self-hosted runner (D-572 to D-585). The first review read the full diff and affected contracts. This round checked the response, the corrected roadmap text, the round-to-round diff, the PR comments, and the current checks. No area remains uninspected.

## Findings

### P2-1: Roadmap summary contradicts the runner retirement

Status: fixed in `1678ef5d65b0cde57be30e7c8f7951101f0a6b9d`.

Open at: `1678ef5d65b0cde57be30e7c8f7951101f0a6b9d`.

File: `docs/roadmaps/phase-2-first-playable.md:1241`.

Trigger: A reader reads the PR-86 plain-English summary.

Expected: The focused roadmap states that the three macOS checks leave the owner’s Mac and that the runner retires (T-5, D-572, D-584).

Actual at the open revision: The summary said the three checks still ran on the owner’s Mac, then said that they moved to hosted Macs and the runner was removed.

Consequence: The focused roadmap gives conflicting accounts of where PR checks run.

Correction: Rewrite the summary to state that the three checks move to hosted Macs and no PR code runs on the owner’s Mac.

Regression check: The corrected summary states that the owner’s Mac ran the checks before the change, hosted Macs run them now, and no PR code runs on the owner’s Mac. This agrees with D-572 and D-584.

## Out of scope

None.

## PR comments

- Gitar posted one approval summary with no specific item to address. The review verified the architecture steps and hosted job results; no actionable comment remains (D-550).

## Description edits

None.

## Earlier verdicts

- `Changes required` for effective head `1678ef5d65b0cde57be30e7c8f7951101f0a6b9d`, due to P2-1.

## Verification

- `gh pr view 105` and `git rev-parse`: PR #105 targets `main`; base and merge base are `0d99e2c50d861ce41db3d09eb0c39d11c6be5713`; PR tip is `817dad3461246fbbc24e128c5df8abcd8264f2b5`; effective head is `1678ef5d65b0cde57be30e7c8f7951101f0a6b9d` under D-475 and D-534.
- `git diff 1678ef5..HEAD`: only documentation, review, skill-reference, and handoff paths changed after the prior review. No code or workflow behavior changed.
- The PR-86 roadmap summary at `docs/roadmaps/phase-2-first-playable.md` now states the pre-change and current runner locations. The correction agrees with D-572 and D-584.
- `gh pr checks 105` after the review and handoff push: Gitar, asset QA, det-lint, doc-gate, documents, night-gate, ste-check, evaluate, and review-gate pass. Heavy jobs skip after the documents-only change under D-474.
- `gh api` comment export: one Gitar approval summary has no specific item to address; no review threads exist (D-550).
- `dotnet test WhatYouCarry.slnx --filter Category=Documents`: passed 196 of 196 tests on this worktree.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: passed with 0 findings in 38 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- doc-gate --root . --base origin/main --head HEAD --body /tmp/pr-105-body.md --title "feat: the macOS legs run on hosted macos-latest, and the self-hosted runner retires (PR-86)" --branch feat/pr-86-hosted-macos`: passed with 0 problems over 29 changed paths and 7 commits.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- handoff-rotate --root .`: passed and moved Session 252 to the archive.
- `git diff --check`: passed.
- `gh run view 36181584076`, `36181584256`, and `36181584096`: the earlier round records the passing hosted CI, bit-identity, and smoke jobs at `6dfe7f8`; each macOS job logged the arm64 check before checkout, and the bit-identity comparison passed.
- The earlier round ran the full suite: 1,820 of 1,820 tests passed on macOS arm64 with .NET 10.0.400. The hosted runner removal checks returned zero registered runners and no launch agent.
- Push: commit `3f5edebffdb6b88666d1e99aae197fc5d42b387b` pushed the review record and Session 262 to `origin/feat/pr-86-hosted-macos`. `gh pr view 105` confirmed that head, and the required gates then passed.

## Open questions and accepted risks

None.

## Verdict

**Ready for owner merge.** This verdict applies to effective head `1678ef5d65b0cde57be30e7c8f7951101f0a6b9d`. The earlier finding is fixed, and the current checks cover the documents-only correction. The existing workflow evidence covers the implementation head.
