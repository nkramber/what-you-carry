# PR-105 review

Date: 2026-09-25

## Identity

- PR: 105
- Target: `main`
- Base: `0d99e2c50d861ce41db3d09eb0c39d11c6be5713`
- Merge base: `0d99e2c50d861ce41db3d09eb0c39d11c6be5713`
- Head: `1678ef5d65b0cde57be30e7c8f7951101f0a6b9d`
- Branch: `feat/pr-86-hosted-macos`

## Provider gate

The author provider is Claude Code, as Session 259 records. The reviewer provider is Codex. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

PR-86 moves the macOS CI, smoke, and bit-identity jobs to `macos-latest`, checks for arm64 before checkout, and retires the self-hosted runner (D-572 to D-585). The review read the full diff, each changed file in context, the PR-86 roadmap and exit tests, design guardrails, cited decisions, the questions register entries, both agent files, the changed skills, runbooks, all workflow files, and focused test code. It checked the PR comments and the current CI evidence. No area remains uninspected.

## Findings

### P2-1: Roadmap summary contradicts the runner retirement

Status: open.

Open at: `1678ef5d65b0cde57be30e7c8f7951101f0a6b9d`.

File: `docs/roadmaps/phase-2-first-playable.md:1241`.

Trigger: A reader reads the PR-86 plain-English summary.

Expected: The focused roadmap states that the three macOS checks leave the owner’s Mac and that the runner retires (T-5, D-572, D-584).

Actual: The summary says the three checks still run on the owner’s Mac, then says that they move to hosted Macs and the runner is removed.

Consequence: The focused roadmap gives conflicting accounts of where PR checks run.

Correction: Rewrite the summary to state that the three checks move to hosted Macs and no PR code runs on the owner’s Mac.

Regression check: Compare the corrected summary with D-572 and D-584. It must state that the checks run on hosted macOS and no PR code runs on the owner’s Mac.

## Out of scope

None.

## PR comments

- Gitar posted one approval summary with no specific item to address. The review verified the architecture steps and hosted job results; no actionable comment remains (D-550).

## Description edits

None.

## Verification

- `gh pr view 105` and `git rev-parse`: PR #105 targets `main`, base and merge base are `0d99e2c50d861ce41db3d09eb0c39d11c6be5713`, tip is `d063bae3f51db8880e9e49008e6265a1b77a9dda`, and effective head is `1678ef5d65b0cde57be30e7c8f7951101f0a6b9d` under D-475 and D-534.
- `gh run view 36181584076`, `36181584256`, and `36181584096`: CI, bit identity, and smoke passed at `6dfe7f8`; each macOS job passed its architecture step before checkout. The bit-identity comparison passed.
- `gh run list --branch feat/pr-86-hosted-macos`: the later document-only runs at `7707110` and `d063bae` completed. The current head has successful applicable checks, including documents, STE, doc gate, asset QA, bots, night gate, det-lint, smoke, and bit identity; heavy code jobs skip by D-474 rule 2 after the successful code run at `6dfe7f8`.
- `dotnet test WhatYouCarry.slnx`: passed 1,820 of 1,820 tests on macOS arm64 with .NET 10.0.400.
- `dotnet test WhatYouCarry.slnx --no-build --filter Category=Documents`: passed 196 of 196 tests after the review record and handoff edits.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: passed with 0 findings in 38 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- handoff-rotate --root .`: passed and moved Session 249 to the archive.
- `gh api repos/nkramber/what-you-carry/actions/runners -q '.total_count'`: returned `0`.
- `launchctl list`: no runner agent appeared.
- `git diff --check origin/main...HEAD`: passed.
- Local review-gate validation: not run; the GitHub review gate checks the pushed record.
- Push: the metadata commit is pushed to `origin/feat/pr-86-hosted-macos` and verified with `gh pr view`.

## Open questions and accepted risks

None.

## Verdict

**Changes required.** This verdict applies to effective head `1678ef5d65b0cde57be30e7c8f7951101f0a6b9d`. The workflow changes and their checks pass, but the PR-86 roadmap summary contradicts the runner retirement.
