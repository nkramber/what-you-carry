# PR-39 review

Date: 2026-09-10

## Identity

- PR: 39
- Target: `main`
- Base: `736e466b24a5bc8b3fe96367015a3dcfa9eae331`
- Merge base: `736e466b24a5bc8b3fe96367015a3dcfa9eae331`
- Head: `34cbc39768611d795fb656b6fa6d65a17432931e`
- Branch: `fix/dig-plan-job-budget`

## Provider gate

Session 97 identifies Claude Code as the provider of the substantive PR change. The active reviewer is Codex. The gate passes under T-4 and D-101.

## Intended behavior and scope

PR #39 raises the dig-plan job cap to the measured tail of the first failed night. It returns the dig-job count and adds regression coverage for the three measured floors. The review inspected the complete diff, `FloorGenerator`, the PR-9 contracts, D-253, D-279, F-92, the automated comments and reply, and the affected tests.

No area remains uninspected.

## Findings

No finding.

The cap check still rejects a floor before it runs more than `MaxJobs` jobs. The new cap accepts all three measured floors. `FloorGenerator` ignores the new return value without changing its behavior. The regression test reads the returned count, checks the old cap boundary, checks the measured count, and runs the full generator. The old implementation fails these seeds at the 400-job guard.

## Out of scope

- A retry lane after a failed cap belongs to the alternative in D-279 and is not required by this PR.
- Run-artifact upload for failed night logs belongs to the separate work bound by D-280.

## PR comments

- The automated pass approved the code and reported the expected missing review file. The author explained that the cross-provider review creates this file and that the neutral review-gate result applies before the record exists.
- The review accepts the automated code approval as a claim and verifies the cap, the measured seeds, the caller, and the regression behavior independently.

## Description edits

None.

## Verification

- `git diff 736e466...0685c4e --check`: passed.
- `dotnet build WhatYouCarry.slnx -m:1`: passed with 0 warnings and 0 errors.
- `dotnet test WhatYouCarry.slnx --filter FullyQualifiedName~DigPlanJobCapTests --no-restore -m:1`: passed 2 tests.
- `dotnet test WhatYouCarry.slnx --no-build -m:1`: passed 520 tests with 0 failures and 0 skipped tests.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- det-lint --root .`: passed with 0 findings. Core has 0 findings in 61 files, and Game has 0 findings in 0 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- ste-check --root .`: passed with 0 STE, reference, and session-number findings in 15 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj --no-build -- bit-identity`: passed with `6ec00e90c1c85cdb`. The simulation version is 6.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --editor --path WhatYouCarry.Game --build-solutions --quit`: passed with Godot 4.7.2.
- The GitHub status observed at PR head `90ab13e` showed Gitar success, bit-identity compare success, and the expected pre-record review-gate neutral result. A later API request failed, so the complete remote status is unverified here.

## Open questions and accepted risks

OQ-99 remains open, but it blocks no work in this review. The cap of 10000 accepts the measured tail and bounds a stuck floor as D-279 records.

## Head correction

2026-09-10, after the review, by the author under D-282, one time. The head field read `0685c4e078d96a4cba3fafd333cbd9e0ece738f5`, the fix commit. The effective head under D-184 was `90ab13ece853a8775211beafefe7af6c41a22178`, the commit that adds D-279 to D-281 to the registers, because the register files lie outside the metadata set. The review read that commit as the tip of the branch. The head field names `34cbc39768611d795fb656b6fa6d65a17432931e` now, the commit that adds D-282 and OQ-150 to the registers and changes no other file. The verdict stands on the diff the review read, and no code changed after it.

## Verdict

**Ready for owner merge.** This verdict applies to effective head `34cbc39768611d795fb656b6fa6d65a17432931e`. The provider gate passes, the complete changed scope has review coverage, all applicable local gates pass, and no blocking finding remains. The owner must confirm the pending remote checks before merge.
