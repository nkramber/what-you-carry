# PR-65 review

Date: 2026-09-14

## Identity

- PR: 65
- Target: `main`
- Base: `d2ef347dcd53a6e46d3c602bc4227f9a4bd58da0`
- Merge base: `d2ef347dcd53a6e46d3c602bc4227f9a4bd58da0`
- Head: `6f0d6f247553fabc0a74eb74b783aca7bd43af33`
- Branch: `feat/pr-63-dig-sizes`

## Provider gate

The substantive commit `6f0d6f2` belongs to the Claude Code session recorded in `docs/session-handoff.md` Session 158. The reviewer is Codex. The provider gate passes under T-4 and D-101.

## Intended behavior and scope

This PR moves gallery, drift, and chamber dig sizes into each floor template. It validates the new fields, uses them in the dig plan and detail pass, records tunnel stamps, updates content, and raises the simulation version. The review inspected the complete non-metadata diff, the affected callers, content validation, generation and detail paths, tests, replay identity, design and roadmap text, and all PR comments. No area remains uninspected.

## Findings

No finding.

## Out of scope

- Dig restart after the job budget belongs to PR-67 under D-353.
- Ramp cells and ramp meshes belong to PR-64 to PR-66 under D-351.
- The owner play test of floor 1 remains exit test 6 for this PR.

## PR comments

- gitar approved the head and reported no issues. The review verified the described template validation, per-walker dimensions, collapse height, simulation-version update, and 873-test result. The pause note does not add a review claim.

## Description edits

None.

## Verification

- `git fetch origin`: passed before review.
- `git status --short --branch`: clean at `feat/pr-63-dig-sizes`.
- `git diff --find-renames --find-copies origin/main...6f0d6f2`: complete diff inspected.
- `git diff --check origin/main...6f0d6f2`: passed.
- `dotnet test WhatYouCarry.Tests/WhatYouCarry.Tests.csproj --no-build --filter FullyQualifiedName~ContentTests`: aborted because the test host could not bind its socket in this execution context. This result is not a product failure.
- Revision-matched CI on `6f0d6f2`: build-and-test passed on Linux, Windows, and macOS. Bit identity, smoke, asset QA, bots, det-lint, STE check, and night gate passed.
- Revision-matched bit identity: `b00814dbf25e61e8`, with the compare job passed.
- Automated pass: approved with no issue comment.
- Review gate before this record: neutral or skipping because no review record existed. The failed `evaluate` result had no review record to evaluate.
- Push: `7028406bcce383a45294eb70887bd1c8fcdf7c72` is the head of origin/feat/pr-63-dig-sizes, verified with gh pr view.

## Open questions and accepted risks

- OQ-172 is resolved by D-353. The dig tail remains accepted until PR-67.
- Exit test 6 needs the owner play test of floor 1.

## Verdict

**Ready for owner merge.** This verdict applies to head `6f0d6f247553fabc0a74eb74b783aca7bd43af33`. The provider gate passes, the inspected scope has no blocking finding, and the revision-matched required automated checks pass. The owner play test remains separate exit evidence.
