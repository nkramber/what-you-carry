# PR-49 review

Date: 2026-09-11

## Identity

- PR: 49
- Target: `main`
- Base: `e1cf847abd422c51df69fab71423668000535eb6`
- Merge base: `e1cf847abd422c51df69fab71423668000535eb6`
- Head: `066ce0eb0d4be0c2645da307e35732f02ba4bc8`
- Branch: `feat/pr-12-game-skeleton`

## Provider gate

The handoff identifies Claude Code as the author provider for the substantive PR-12 commit. Codex is the reviewer provider for this session. The providers differ, so the gate passes under T-4 and D-101.

## Intended behavior and scope

This PR adds the Game root, fixed-step input, render interpolation, content source, placeholder scene, smoke session, and smoke CI workflow. The review inspected the complete diff, affected Core callers, input state, CI boundaries, tests, documentation, and the existing PR comments. The review did not inspect visual quality on a Steam Deck.

## Findings

No finding.

## Earlier verdicts

- At head `91d1b6f`, `P2-1` required changes because a held controller stick overrode a later mouse event.
- The correction at head `066ce0e` tracks the latest look event and adds `HeldStickThenMouseMotionReadsTheMouse`, which fails on the old behavior and passes on the correction.

## Out of scope

Visual feel and Steam Deck readability remain for M-3 and the presentation reviews of later Phase 2 work.

## PR comments

- The owner said that the review gate is red until this record exists on the PR head. The review verified that `evaluate` failed because the file was absent, and `review-gate` was neutral or skipped for that expected condition.
- Gitar reported no code issue and the missing review file. The review verified the no-finding claim as an automated result, but found P2-1 through an independent input-state trace.
- The owner reported the correction commit and its regression test. The review verified both against the new effective head.

## Description edits

None.

## Verification

- `dotnet test WhatYouCarry.slnx --no-build --filter 'FullyQualifiedName~InputReaderTests' -m:1`: passed, 8 tests.
- `dotnet test WhatYouCarry.slnx --no-build --filter 'FullyQualifiedName~SmokeSessionPasses' -m:1`: passed, 1 test.
- The author reported `dotnet test WhatYouCarry.slnx`: passed, 584 tests, 0 failures. Remote CI independently passed the full suite on Linux, Windows, and macOS.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- det-lint --root .`: passed, 0 findings.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- ste-check --root .`: passed, 0 findings in 15 files.
- `dotnet run --project WhatYouCarry.Tools/WhatYouCarry.Tools.csproj -- bit-identity`: passed, `6ec00e90c1c85cdb`.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --editor --path WhatYouCarry.Game --build-solutions --quit`: passed.
- `/Applications/Godot_mono.app/Contents/MacOS/Godot --headless --path WhatYouCarry.Game --fixed-fps 60 -- --smoke`: passed, exit code 0 with start and end lines.
- `dotnet build WhatYouCarry.slnx`: did not complete locally after more than 60 seconds without output. The Godot editor build and remote Linux, Windows, and macOS build jobs passed on the reviewed head.
- `gh pr checks 49`: remote Linux, Windows, and macOS CI, smoke, bit identity, det-lint, STE, night-gate, bots, and Gitar passed. `evaluate` and `review-gate` failed because the prior review record still said `Changes required`. These checks must refresh after this record reaches the branch.
- Open questions: OQ-157 remains open but blocks nothing. OQ-158 blocks the merge because the cache action needs an owner decision.
- Push: 949fbfa is the head of origin/feat/pr-12-game-skeleton, verified with gh pr view before the final metadata update.

## Open questions and accepted risks

OQ-157 remains open and blocks nothing. OQ-158 blocks the merge under G-16. No accepted risk remains from the earlier review.

## Verdict

**Ready for owner merge.** This verdict applies to head `066ce0eb0d4be0c2645da307e35732f02ba4bc8`. The correction fixes P2-1 and adds a regression test that fails on the old behavior. OQ-158 remains a separate merge prerequisite.
