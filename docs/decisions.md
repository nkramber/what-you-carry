# Decisions

Status: active register. Owner: Nate. Started 2026-09-06. Written in ASD-STE100.

This file records each owner decision. Each decision has an id (D-#). The numbers never change. A reversed decision stays in this file with a dated note in the "Effect" column. These decisions are the source of the design doc (`docs/design.md`).

How to read this file:

- The "Decision" column gives the answer.
- The "Effect" column gives what the decision changes, binds, or supersedes.
- A date in the "Effect" column marks a later revision.

## Game design

| Id | Date | Topic | Decision | Effect |
|---|---|---|---|---|
| D-1 | 2026-09-06 | Project purpose | Commercial Steam release. | Steam features, audio, UI polish, controller support, and notarization are in scope. |
| D-2 | 2026-09-06 | Banked gear in runs | The player selects a loadout from the bank. Gear that enters the dungeon is lost on death. | Needs a hub and a loadout screen. D-34 gives the amulet exception. |
| D-3 | 2026-09-06 | Ascension result | The run ends. The next run starts at floor 1. | Early floors must be short. |
| D-4 | 2026-09-06 | Run length | 30 to 45 minutes to floor 15. | Two to three minutes per floor. |
| D-5 | 2026-09-06 | Dungeon bottom | Floor 15 is the end of v1. An endless mode comes after v1. | The floor-15 boss is the final boss. |
| D-6 | 2026-09-06 | Floor bosses | Bosses only on floors 5, 10, and 15. Other floors end at a stairwell. | Replaces the v1 cadence table. v1 has three bosses. |
| D-7 | 2026-09-06 | World theme | Fantasy with black-powder guns. | Guns are muzzle-loaders. Reload time is a weapon stat. |
| D-8 | 2026-09-06 | Tone | Dark with dry humor. | A consistent voice for text. |
| D-9 | 2026-09-06 | Hub | A small physical hub. Equipment is modeled on the player body. | The model loader needs armor overlays. |
| D-10 | 2026-09-06 | Suspend | Suspend at stairwells only. | Superseded by D-97 on 2026-09-06. |
| D-11 | 2026-09-06 | Name | "What You Carry". | Replaces "Descent" in every file. |
| D-12 | 2026-09-06 | Co-op | Solo forever. No network code. | Pillar 3 stands. |
| D-13 | 2026-09-06 | Camera | Over-the-shoulder. The player controls the camera. | Procgen must guarantee a minimum corridor width. |
| D-14 | 2026-09-06 | Aim | Free aim. Controller aim assist. | Aim assist is a Core function (D-77). |
| D-15 | 2026-09-06 | Input | Keyboard and mouse, controller, and a Steam Deck verified target. | The Deck is the performance floor. Each UI screen must work without a mouse. |
| D-16 | 2026-09-06 | Visible loot | Enemies wear and wield what they drop. | Loot tables are enemy loadouts. |
| D-17 | 2026-09-06 | Character | One character. Gear is identity. | No classes. |
| D-18 | 2026-09-06 | Equipment slots | Head, chest, legs, feet, amulet, and shield are modeled. Two ring slots are not modeled. | Shield added by D-26. Rings are the one exception to the model rule. |
| D-19 | 2026-09-06 | Inventory | Equipped gear plus a small visible satchel. | Satchel slots compete: loot, potions, spare weapons, found amulets. |
| D-20 | 2026-09-06 | Weapon slots | One main weapon at a time. | Reload is exposure. Dodge is the counter. |
| D-21 | 2026-09-06 | Weapon swap | Spare weapons ride in the satchel. A swap is slow and cannot be canceled. | Swap time is tuned against reload time. |
| D-22 | 2026-09-06 | Throwables | Consumables in the satchel. A quick slot uses them with the main weapon equipped. | The thrown weapon class shrinks to wielded exotics. |
| D-23 | 2026-09-06 | Armor effect | Damage reduction plus weight. Weight slows movement and dodge recovery. | Two stats per armor piece. |
| D-24 | 2026-09-06 | Health | Potions in the satchel. No free heal. Health carries across floors. | Low health is an ascend signal. |
| D-25 | 2026-09-06 | Lethality | Fast and lethal. A few hits kill. | Telegraph readability moves up in priority. |
| D-26 | 2026-09-06 | Defense | Dodge, plus block with a shield. A shield needs a one-handed melee weapon. | Weapons have a handedness property. |
| D-27 | 2026-09-06 | Movement | Dodge roll, sprint, and jump. | Procgen, pathfinding, and Core collision are three-dimensional. |
| D-28 | 2026-09-06 | Stamina | None. Dodge has a cooldown. Armor weight extends the cooldown. | Sprint is free. |
| D-29 | 2026-09-06 | Block rules | Only shields block. Two-handed melee swings cannot be interrupted (hyper-armor). | A stagger system exists for the player. |
| D-30 | 2026-09-06 | Enemy shots | The same projectile simulation and rules as the player. | Enemy AI leads targets and reloads. |
| D-31 | 2026-09-06 | Enemy roster | Humanoid enemies use the player gear pool. Monsters have their own attacks. | Two enemy archetypes. |
| D-32 | 2026-09-06 | Self-damage | Full self-damage from the player's own bombs. | The first bomb needs a clear tell. |
| D-33 | 2026-09-06 | Magic | Mana is a resource for exotic weapons only. | Three resource models: reload, mana, cooldown. |
| D-34 | 2026-09-06 | Amulet | Every player always has an amulet. It is never lost. The tree upgrades it and assigns its ability. | Key feature. Exception to D-2. |
| D-35 | 2026-09-06 | Proficiency | Tree proficiencies only buff. No penalty for an unskilled class. | Found loot is good news for any build. |
| D-36 | 2026-09-06 | Numbers | Damage numbers always on. Bosses show a health bar with a number. | Style must not hide silhouettes. |
| D-37 | 2026-09-06 | Found amulets | Found amulets unlock skill orbs in the tree. A death forfeits the unlock. | A second reason to descend. |
| D-38 | 2026-09-06 | Amulet cost | Amulet abilities use cooldowns. | Mana stays with exotic weapons. |
| D-39 | 2026-09-06 | Amulet shape | One active ability plus one passive, assigned in the tree. | Two assignment screens. |
| D-40 | 2026-09-06 | Ammunition | Infinite ammunition. Reload and draw time are the only cost. | Replaces design doc v1 section 2.5. |
| D-41 | 2026-09-06 | Amulet carry | Found amulets are satchel items. | An unlock competes for satchel space. |
| D-42 | 2026-09-06 | Bows and guns | Bows have small area damage and a faster draw. Guns have single-target burst and a slow reload. | Arrows need an area mechanic. |
| D-43 | 2026-09-06 | Mana regeneration | Slow regeneration over time. Potions restore mana. | Exotic rhythm is "wait". |
| D-44 | 2026-09-06 | Skill points | Earned per kill with a boss bonus. Each floor has a time limit. | The timer is a new system. |
| D-45 | 2026-09-06 | Timer expiry | An unkillable hunter spawns and spawns escalate. The hunter accelerates until escape is impossible. | One hunter entity plus an escalation spawner. |
| D-46 | 2026-09-06 | Timer shape | A visible countdown. Deeper floors get more time. | Per-floor time in data. |
| D-47 | 2026-09-06 | Item rolls | Random affixes on drops. | Binds D-49. |
| D-48 | 2026-09-06 | Depth reward | Item tiers by depth band, plus a rare chance of a higher tier anywhere. | A property test checks the band distribution. |
| D-49 | 2026-09-06 | Affix visibility | Enemies show rarity color only. Enemies use their affixes. | Affixes are behaviors for any wielder. |
| D-50 | 2026-09-06 | Ascension | Ascend at every stairwell at no cost. The restart at floor 1 is the cost. | Reverses design doc v1 section 4.2. |
| D-51 | 2026-09-06 | Respec | Free at the hub. | Build identity comes from gear. |
| D-52 | 2026-09-06 | Death payout | The retained share of run skill points scales with the depth reached. | The curve is the sensitive number. |
| D-53 | 2026-09-06 | Modifiers | None in v1. Depth is the only difficulty dial. | Heat modifiers belong to the endless mode. |
| D-54 | 2026-09-06 | Tree shape | Several shallow branches. Found-amulet orbs gate the branch tips. | The tree UI must work on a controller. |
| D-55 | 2026-09-06 | Rings | Pure affix carriers. Not modeled. Lost on death. | Enemies use ring affixes. |
| D-56 | 2026-09-06 | v1 scope | Small: 15 floors, one biome, 3 bosses, about 12 weapons, about 8 enemy families, about 10 affixes, one hunter. | Early Access size. |
| D-57 | 2026-09-06 | First playable | One floor, one sword, one enemy family, the timer, the hunter, and a stairwell. No economy. | Feel before economy. |
| D-58 | 2026-09-06 | Timeline | About 12 months to the first public release. | Revised 2026-09-06 from "no deadline". |
| D-59 | 2026-09-06 | Look reference | Minecraft Dungeons light and material discipline, pushed darker, with torchlight. | Beat the clone comparison on silhouette and mood. |
| D-60 | 2026-09-06 | Feel reference | Hunt: Showdown gunplay. Risk of Rain 2 movement and camera. | Only the gunplay transfers from Hunt. |

## Technology

| Id | Date | Topic | Decision | Effect |
|---|---|---|---|---|
| D-61 | 2026-09-06 | Godot version | Pin the latest stable 4.x at scaffold time. Upgrade only by a decision entry with the determinism CI green. | A fixed API surface. |
| D-62 | 2026-09-06 | .NET version | The newest LTS release that the pinned Godot supports. | |
| D-63 | 2026-09-06 | Editor use | The Godot editor is never required. C# builds the scenes. A few small text scenes exist for the hub and UI roots. | Every workflow runs from the command line. |
| D-64 | 2026-09-06 | GDScript | Banned. C# only, tools included. | |
| D-65 | 2026-09-06 | Tool language | C# only for tools. | Replaces the Python texture generator. See D-130. |
| D-66 | 2026-09-06 | Test stack | xUnit. Property tests are seed loops with explicit assertions. Each failure names its seed. | No shrink library. |
| D-67 | 2026-09-06 | Lint | .NET analyzers, dotnet format, and a C# lint tool that scans Core syntax trees for banned symbols. | Runs in the PR gate. |
| D-68 | 2026-09-06 | Null and log | Nullable reference types on, warnings as errors. A hand-written JSONL logger with the mandatory fields. | |
| D-69 | 2026-09-06 | Determinism | Cross-platform bit-identity from day one. | DetMath, lint, and CI before gameplay. |
| D-70 | 2026-09-06 | Precision | float in Core. | Matches Godot vector types. |
| D-71 | 2026-09-06 | CI matrix | Linux x64, macOS arm64, and Windows x64 on every PR. | Needs a macOS arm64 runner (D-100). |
| D-72 | 2026-09-06 | Threads | The simulation is single-threaded. One worker generates the next floor. | The worker is pure and seeded. |
| D-73 | 2026-09-06 | Tick rate | 60 Hz fixed. The Game layer interpolates. | |
| D-74 | 2026-09-06 | Intent | One fixed-size record per tick. | The aim field is replaced by D-77. |
| D-75 | 2026-09-06 | Camera home | The camera is a Core system. | Camera collision is deterministic. |
| D-76 | 2026-09-06 | AI home | All AI and pathfinding in Core, on the voxel grid. Godot navigation is never used. | The pathfinder understands jumps and drops. |
| D-77 | 2026-09-06 | Look input | The intent carries quantized yaw and pitch deltas, movement, and buttons. Core derives the aim ray and applies aim assist. | Sensitivity applies in the Game layer before quantization. |
| D-78 | 2026-09-06 | World | A voxel grid of one-meter cubes. | Procgen writes blocks. Greedy meshing renders them. Revised in part by D-345 on 2026-09-14, the cell shape only: a ramp cell holds a slope. The one-meter grid, the blocks that procgen writes, and greedy meshing stand. |
| D-79 | 2026-09-06 | Destruction | Props only. Walls are permanent. | No re-mesh mid-floor. |
| D-80 | 2026-09-06 | Godot physics | Cosmetic only. Core owns all gameplay collision. | |
| D-81 | 2026-09-06 | Lighting | Ambient plus a small budget of dynamic point lights. No shadow maps. Vertex ambient occlusion. | Deck safe. |
| D-82 | 2026-09-06 | Proportions | A custom proportion set with one shared base body. | Armor overlays are shared. |
| D-83 | 2026-09-06 | Mojang | Avoid the tells. Keep the cuboid style. | A style rule in the art spec. |
| D-84 | 2026-09-06 | Textures | 32 or 64 px faces. | Pinned by D-85. |
| D-85 | 2026-09-06 | Atlas pin | 32 px faces. An own palette of about 32 colors, weighted dark. | The palette needs the owner's eye. |
| D-86 | 2026-09-06 | Blockbench | Review only. Every fix goes through the generator or the model JSON. | The QA gate is the source of truth. |
| D-87 | 2026-09-06 | Animation | JSON keyframes per bone, next to the model. Locomotion is procedural. Gameplay time values live in Core weapon data. | A test checks that both files agree. |
| D-88 | 2026-09-06 | Camera walls | The camera collides in Core. The Game layer fades walls between the camera and the player. | The mesher needs per-block visibility. Revised in part by D-292 on 2026-09-11, the effect note only: the world shader fades the walls, and the mesher needs no per-block visibility. The decision stands. |
| D-89 | 2026-09-06 | Audio | All generated, music included. | Music quality is a register risk. Revised in part by D-467: a pitched sound can ship as a CC0 recording. The rest stands. |
| D-90 | 2026-09-06 | UI | Godot Control nodes built in C#. Controller navigation and Deck screen sizes from the first screen. | |
| D-91 | 2026-09-06 | Data format | JSON for all content. No .tres files. | One loader, one schema set. |
| D-92 | 2026-09-06 | Schemas | One schema per content type. Validate at load and in tests. An absent field is an error. | |
| D-93 | 2026-09-06 | Audio source | Procedural C# synthesis from parameter files and a sequencer format. | |
| D-94 | 2026-09-06 | Save format | Three JSON files with schema versions and migrations from v1. Atomic writes. | Superseded by D-152 on 2026-09-07: one profile file plus one run record. |
| D-95 | 2026-09-06 | Mid-floor exit | Resume at floor start. | Superseded by D-97 on 2026-09-06. |
| D-96 | 2026-09-06 | Steamworks | Achievements and cloud saves at launch. | A dependency entry. Cloud conflicts are explicit. |
| D-97 | 2026-09-06 | Resume | Resume by replay of the recorded intent stream, to five seconds before the exit tick. A repeat crash falls back to floor start. | Supersedes D-10 and D-95. Suspend works anywhere. |
| D-98 | 2026-09-06 | Localization | English only. All strings in an ID-keyed table from day one. | The lint tool forbids inline strings. |
| D-99 | 2026-09-06 | Mods | Unsupported, not prevented. | |
| D-100 | 2026-09-06 | CI runners | GitHub-hosted Linux and Windows. The Mac Mini is a self-hosted macOS arm64 runner. | The Mac Mini stays on. |

## Process

| Id | Date | Topic | Decision | Effect |
|---|---|---|---|---|
| D-101 | 2026-09-06 | Review record | A reviews directory with one file per PR. | Exempt from D-137. |
| D-102 | 2026-09-06 | Merge | The owner merges every PR. | Revised in part by D-516 on 2026-09-23, the actor of the merge only: GitHub auto-merge merges a PR with the green light. The owner can still merge a PR. |
| D-103 | 2026-09-06 | Autonomy | The owner starts every session. No scheduled agents. | See D-117. Revised in part by D-511 on 2026-09-23, the start of a reviewer session only: the author session starts it with `make codex-review`. The rule of no scheduled agents stands. |
| D-104 | 2026-09-06 | Owner role | Owner decisions, a playtest at each milestone, and spot checks of code. | |
| D-105 | 2026-09-06 | Harness host | Locally on the Mac Mini. | An external SSD is mandatory. |
| D-106 | 2026-09-06 | Repository | Private until launch. | |
| D-107 | 2026-09-06 | Budget | Near full time. Generous tokens. | Twelve months is conservative. |
| D-108 | 2026-09-06 | Namespace | WhatYouCarry.Core, WhatYouCarry.Game, WhatYouCarry.Tools, WhatYouCarry.Tests. | |
| D-109 | 2026-09-06 | Optimized | Not wasteful. Tune only on measurement. | Concrete rules in the standards. |
| D-110 | 2026-09-06 | Helpers | Allowed, one level deep. | The reviewer checks depth. |
| D-111 | 2026-09-06 | Abstraction | Two concrete cases before any abstraction. | Single-instance systems stay concrete. |
| D-112 | 2026-09-06 | Assertions | On in shipped builds. A failure logs a full report and continues where safe. | A report flow in the UI. |
| D-113 | 2026-09-06 | Hub errors | Outside a run, errors carry save versions, screen, action, and file paths. | Two context schemas. |
| D-114 | 2026-09-06 | Game tests | A headless scripted smoke session per PR, plus contract tests for content. | Headless Godot in CI. D-149: the smoke grows with the systems. |
| D-115 | 2026-09-06 | Bot cadence | A few hundred bot runs per PR. Ten thousand each night. A night failure blocks the next merge. | |
| D-116 | 2026-09-06 | Seed sweeps | Thousands of seeds per PR. One hundred thousand each night. | Revised in part by D-480 on 2026-09-22, the count per PR alone: a pull request runs one fifth of the count on `main`. The night count stands. Revised in part by D-564 on 2026-09-24, the night count alone: each night adds a rotating slice beside the one hundred thousand fixed seeds. The count per PR stands. |
| D-117 | 2026-09-06 | Night jobs | Scheduled tests are allowed. Scheduled agents are not. | Consistent with D-103. |
| D-118 | 2026-09-06 | Doc cadence | The handoff every session. Other docs when relevant. The PR gate needs an explicit "no change needed" line per doc. | D-146 changes the handoff from a rewrite to an added entry. |
| D-119 | 2026-09-06 | Tenet order | Documentation, then no silent failures, then tests, then cross-provider review, then simplicity. The attribution tenet is absolute. | |
| D-120 | 2026-09-06 | Doc layout | All artifacts in docs. The v1 design doc moves to docs/archive. | Case revised by D-129. Shape revised by D-132. D-144 adds `docs/questions.md`. |
| D-121 | 2026-09-06 | Session | One harness invocation, one PR, one handoff rewrite. | D-146: one handoff entry, not a rewrite. Revised in part by D-297 on 2026-09-12, the count of PRs only: a documentation PR can follow the merge of the code PR in the same session. The one-invocation rule stands. Revised in part by D-375 on 2026-09-16, the count of PRs only: the documentation exception of D-297 ends, so one invocation holds one PR of any kind and one role. The one-invocation rule stands. |
| D-122 | 2026-09-06 | Agent files | CLAUDE.md and AGENTS.md are identical pointer files. A test asserts equality. | |
| D-123 | 2026-09-06 | Decision granularity | Record anything a fresh session could plausibly redo differently. | |
| D-124 | 2026-09-06 | Self-resolve | Never. Every open question belongs to the owner. A session stops and files the question. | Drain the questions file before each session. |
| D-125 | 2026-09-06 | Doc growth | The handoff is replaced each session. Decisions split per area with an index of current state. | D-146 keeps 10 sessions in the handoff. D-141 sets the split threshold. |
| D-126 | 2026-09-06 | Git | Trunk. Short branches. Squash merge by the owner. Conventional commit prefixes. No trailers. | Revised in part by D-516 on 2026-09-23, the actor of the merge only: GitHub auto-merge merges a PR with the green light. Trunk, short branches, squash merges, conventional prefixes, and no trailers stand. |
| D-127 | 2026-09-06 | Bot policy | A small family of simple deterministic policies. | Each policy is named in the run log. D-149: each policy lands with the system it exercises. |
| D-128 | 2026-09-06 | LLM play | Weekly on main, plus every economy PR. | Needs the socket and a compact state format. |
| D-129 | 2026-09-07 | File case | Lowercase file names in docs. | Revises D-120. |
| D-130 | 2026-09-07 | STE checker | Port the checker to C#. | A tool project. |
| D-131 | 2026-09-07 | Skills | Create ste-writing and design-doc-style in .claude/skills, adapted to this project. | |
| D-132 | 2026-09-07 | Doc shape | One combined docs/design.md per the template. Focused roadmaps are separate linked files. | Revises D-120. |
| D-133 | 2026-09-07 | Vision tier | Screenshots plus the intent socket. Milestone only. | A screenshot hook in the Game layer. |
| D-134 | 2026-09-07 | Playtests | The owner from the first playable. Friends from the hub loop. | |
| D-135 | 2026-09-07 | Pose check | No box interpenetration at animation extremes. Armor overlays enclose their limb. | D-149: the check lands as PR-57 before any armor PR. |
| D-136 | 2026-09-07 | Milestones | Five gated milestones. Each is a playable build with a written exit test. | Revised in part by D-150 on 2026-09-07, the Gate 1 condition only. The five phases and their order stand. |
| D-140 | 2026-09-07 | Timer pauses | The timer pauses at the stairwell choice. The timer runs in boss fights. | Boss floors need a larger time budget in data. |
| D-141 | 2026-09-07 | Decisions file | One file until it exceeds about 300 rows. Then split per area and make this file the index. | The D-125 mechanism. |
| D-142 | 2026-09-07 | Prerequisites | A GitHub repository exists. An Apple Developer account, an external SSD, and a Steam partner account do not exist yet. | Three purchases before Phase 5. The SSD comes before Phase 1. D-145: the SSD arrives 2026-09-08. |
| D-143 | 2026-09-07 | Interview close | No more topics before v2. The design doc v2 is written from D-1 to D-143. | The gaps that remain become open questions in the design doc. |
| D-144 | 2026-09-07 | Questions file | A separate `docs/questions.md` holds the open questions register. The design doc section 9 links to it. | Resolves OQ-24. Revises the D-120 file list. |
| D-145 | 2026-09-07 | External SSD | Ordered. Arrives 2026-09-08. | Updates D-142 and F-25. |
| D-146 | 2026-09-07 | Handoff history | `docs/session-handoff.md` keeps the last 10 sessions, newest first. A new session adds an entry. The oldest entry beyond 10 moves to `docs/session-handoff-archive.md`. | Revises D-118, D-121, and D-125. Revised in part by D-199 on 2026-09-08, the content of the state of the build only. The six parts and the ten-entry limit stand. |
| D-147 | 2026-09-07 | Audit first | Address every finding of the 2026-09-07 repository audit before the focused roadmaps start. | Sequence step 0. |
| D-148 | 2026-09-07 | Gate bootstrap | PR-1 adds the three-platform build-and-test workflow. A check that does not exist yet is named in the PR with the PR that creates it. The PR that creates a check must pass it. | Resolves audit R-1. Only PR-2 and PR-3 use the clause. |
| D-149 | 2026-09-07 | Gate prerequisites | Four fixes: a Core player box in PR-7 and the stairwell transition in PR-9, with PR-11 limited to two policies, and later PRs add theirs. The PR-12 smoke is boot, run, move, quit, extended in PR-18. A new PR-57, asset QA v1, follows PR-13. PR-32 moves before PR-27. | Resolves audit R-2. |
| D-150 | 2026-09-07 | Gate 1 | Gate 1 is a foundation gate with a written exit test and no playtest. Gates 2 to 5 are playable builds with the owner's sign-off. | Resolves audit R-7. Revises D-136. |
| D-151 | 2026-09-07 | Run record | The record header carries a format version, a simulation version constant, a content hash, the seed, and an immutable initial state: loadout items with rolls, tree state, amulet assignment. Replay ignores the live bank and tree. Resume is exact on a match, and floor start with a notice on a mismatch. | Resolves audit R-3 and OQ-26. Binds PR-6, PR-31. |
| D-152 | 2026-09-07 | Save protocol | One profile file holds the tree, the bank, and the suspended-run pointer, with a schema version and a generation number, written atomically. The run record is a separate append-only file with checksummed tick frames. One profile write commits ascension or death. A run id makes completion idempotent. | Resolves audit R-4 and OQ-27. Revises D-94. Binds PR-31. |
| D-153 | 2026-09-07 | Basic kit | The loadout screen always offers a tier-0 sword with no affixes at no cost. It is not a bank item. The first run uses it by default. The tree unlocks the amulet's first orb, a basic active, from the start. | Resolves audit R-5 and OQ-28. Binds PR-28, PR-30. |
| D-154 | 2026-09-07 | Economy test | M-5 uses matched trials in simulated time: one thousand fixed seeds, the basic kit and an empty tree, ascend and die policies at every depth plus shallow-repeat, full-clear, and early-death, simulated ticks over 60 plus a sixty-second hub cost per run start, and a bootstrap 95 percent interval. Pass: at every depth the ascend lower bound exceeds the die upper bound, and deep ascend beats shallow repeat. | Resolves audit R-6 and OQ-29. Binds PR-27, M-5. |
| D-155 | 2026-09-07 | Project skill location | All project skills live in `.claude/skills/`. Every new project skill must use that directory. Both agent files state the path and require direct access to each required `SKILL.md`. | Extends D-131 to all project skills. Applies even when a skill is absent from the automatic skill list. |
| D-156 | 2026-09-07 | Initial commit | Reset HEAD to `main`. Commit every file as "initial commit" and push to `origin/main`. Roadmap work happens on the branch `docs/roadmaps`. | Resolves OQ-30 and F-37. Adds `.gitignore` and `.gitattributes`. |
| D-157 | 2026-09-07 | macOS runner | The Mac Mini is a self-hosted GitHub Actions runner with the label `macos-arm64-self-hosted`. It runs as a launch agent. Its work directory is on the external SSD. | Resolves OQ-31 and F-39. Owner action before PR-1. |
| D-158 | 2026-09-07 | Branch protection | `main` requires a PR and the `build`, `ste-check`, and `bit-identity` checks as each one exists. Only the owner pushes. | Resolves OQ-32. Superseded by D-170 on 2026-09-07: deferred until launch, a convention until then. |
| D-159 | 2026-09-07 | RNG | xoshiro128** streams, seeded by SplitMix64 from the 64-bit run seed. One stream per subsystem, split by subsystem id. | Resolves OQ-33. Binds PR-3. |
| D-160 | 2026-09-07 | State hash | FNV-1a 64 over the raw bit patterns of every state field in a fixed declared order. A test asserts the field order. | Resolves OQ-34. Binds PR-3. |
| D-161 | 2026-09-07 | DetMath accuracy | Range reduction to [-pi, pi]. Degree-7 minimax polynomials for sine and cosine. Absolute error at most 1e-6 against a double reference. Atan2 at most 1e-6 radians. Sqrt wraps the IEEE square root. Tests cover [-4 pi, 4 pi]. | Resolves OQ-35. Binds PR-3. Revised in part by D-203 on 2026-09-08, the reduction range only. The degree, the 1e-6 target, the Sqrt rule, and the [-4 pi, 4 pi] test range stand. |
| D-162 | 2026-09-07 | Intent layout | A fixed 16-byte frame: tick uint32, yaw and pitch deltas int16 in hundredths of a degree, movement x and y int8, buttons uint16 bit mask, CRC32 uint32. | Resolves OQ-36. Binds PR-6. |
| D-163 | 2026-09-07 | Run record layout | One UTF-8 JSON header line, then the fixed frames of D-162. The content hash is SHA-256 over each content file's relative path and bytes in sorted path order. | Resolves OQ-37. Binds PR-5, PR-6. |
| D-164 | 2026-09-07 | Grid limits | Block ids are one byte in a flat array. A floor is at most 128 by 32 by 128 blocks. | Resolves OQ-38. Binds PR-7, PR-9. |
| D-165 | 2026-09-07 | Player box | A box of 0.6 by 1.8 by 0.6 meters. A jump clears exactly one block. No automatic step-up. A move is one block up or any drop, for the player and the pathfinder. | Resolves OQ-39. Binds PR-7, PR-16. Revised in part by D-345 on 2026-09-14, the move rule only: a move is one block up, any drop, or a walk along a ramp, for the player and the pathfinder. The box, the one-block jump, and no automatic step-up stand. Revised in part by D-486 on 2026-09-22, the move rule of the pathfinder only: the search also takes a diagonal move. The box, the one-block jump, no automatic step-up, and the move rule of `Reachability` stand. |
| D-166 | 2026-09-07 | Corridor | Every corridor is at least three blocks wide and three blocks high. | Resolves OQ-40. Binds PR-8, PR-9. |
| D-167 | 2026-09-07 | Difficulty budget | Each floor template names a budget number. Each room template names a weight. The sum of room weights is within 10 percent of the budget. PR-16 maps weights to enemy spawns. | Resolves OQ-41. Binds PR-9, PR-16. Revised in part by D-398 on 2026-09-20, the reach of the map only: the chamber of the player spawn takes no enemy, and the spawned weight matches the budget less the weight of that chamber. The room weights and the budget window stand. |
| D-168 | 2026-09-07 | Content validators | One hand-written C# validator per content type, with an explicit required-field list and an unknown-field check. Revisit past ten types. | Resolves OQ-42. Binds PR-5. |
| D-169 | 2026-09-07 | .NET pin | .NET 8 LTS. Godot 4.7's C# docs require .NET 8 or later, verified 2026-09-07. | Resolves OQ-2. Binds PR-1. Superseded by D-173 on 2026-09-07: .NET 10 LTS. |
| D-170 | 2026-09-07 | Branch protection deferred | No branch protection until launch. GitHub requires Pro or a public repository for it on a private repository, verified 2026-09-07 by a 403 from the API. D-158 becomes a convention: the owner merges, and no session pushes to `main`. | Revises D-158. Resolves F-46. The PR gate and the agent files enforce it by rule, not by GitHub. Revised in part by D-522 on 2026-09-23, the deferral only: a ruleset protects `main` from PR-78. The rule that no session pushes to `main` stands. |
| D-171 | 2026-09-07 | Runner registration date | Register the Mac Mini runner on 2026-09-08 after the SSD mounts, once, with the work directory on the SSD. The runbook is `docs/runbooks/macos-runner.md`. | Applies D-157. |
| D-172 | 2026-09-07 | Attribution option | `.claude/settings.json` in the repository sets `attribution.commit` and `attribution.pr` to false, verified against the Claude Code settings reference on 2026-09-07. Codex is unverified. The PR-1 trailer scan is the backstop. | Resolves OQ-16 and F-15 for Claude Code. Superseded by D-175 on 2026-09-07: empty strings, not booleans. |
| D-173 | 2026-09-07 | .NET pin revised | .NET 10 LTS, the current long-term-support release, supported until November 2028. Godot 4.7 accepts .NET 8 or later, so it qualifies under D-62. | Revises D-169. Resolves OQ-2. Binds PR-1. |
| D-174 | 2026-09-07 | Repository name | The GitHub repository is `nkramber/what-you-carry`. The local checkout is `/Users/nate/Repos/what-you-carry` until the SSD move (D-145). | Renamed from `roguelite-game`. GitHub redirects the old name. PR #1 stays open. Revised in part by D-195 on 2026-09-07, the checkout path only. The repository name stands. |
| D-175 | 2026-09-07 | Attribution option revised | `.claude/settings.json` sets `attribution.commit` and `attribution.pr` to the empty string. The Claude Code schema requires strings, and the empty string hides the attribution. A boolean fails validation, and the harness then ignores the whole file. Verified on 2026-09-07 against the settings reference, the schema in the VS Code extension, and the validator in the CLI. | Revises D-172. Resolves OQ-16 and F-15 for Claude Code. Codex stays unverified. |
| D-176 | 2026-09-07 | Attribution reading | T-6 and D-137 prohibit text that names an agent, harness, or model as the source of the work. A tool name that identifies a configured file, a schema, or a verified version is not attribution. | Resolves review finding P1-1 on PR #1. Binds PR-1 exit test 6. The registers keep the tool names that explain a decision. |
| D-177 | 2026-09-07 | Night gate bootstrap | PR-11 creates the scheduled night job only. PR-58 creates the `night-gate` job, after one real night result exists. The gate fails on an absent, stale, cancelled, or failed result. Stale means more than 48 hours old. | Resolves review finding P1-2 on PR #1. Revises the PR-11 scope in D-115 and D-149. Binds PR-11 and PR-58. |
| D-178 | 2026-09-07 | Reference check | The PR-2 checker adds a reference check. It reads the Effect column of this file. It fails on any file outside this file that cites a revised decision as a current answer. A line passes when it holds a revision word or names the revising decision. | Resolves review findings P2-1 and P2-2 on PR #1. Binds PR-2. |
| D-179 | 2026-09-07 | Review gate check | A `review-gate` CI job reads `docs/reviews/pr-<number>.md` for the PR number. It fails when the file is absent, when the verdict is not `Ready for owner merge`, or when the head that the file records is not the effective head. The effective head is the newest commit that changes a path outside `docs/reviews/`. A review-file commit therefore does not invalidate the review (`pr-review` SKILL.md, metadata commits). | Mechanizes T-4 and D-101. Binds PR-1. The response file stays a convention and the gate does not require it. Revised in part by D-181 on 2026-09-07, the conclusions only. Revised in part by D-184 on 2026-09-07, the metadata set only. Revised in part by D-197 on 2026-09-08, the event only. The three rules and the file name stand. |
| D-180 | 2026-09-07 | Review gate enforcement | The `review-gate` job is advisory until launch. GitHub returns 403 for both branch protection and rulesets on a private free repository, verified 2026-09-07. At launch the repository becomes public, and `review-gate` becomes a required status check on `main`. | Applies D-170. Binds PR-1 and the launch checklist. Resolves F-50. Until launch the owner reads the check and does not merge (D-126). D-181 adds the mode variable to the launch step. Revised in part by D-521 on 2026-09-23, the timing only: the check turns enforced and required with PR-78, before launch. The rest stands. |
| D-181 | 2026-09-07 | Review gate conclusions | `review-gate` publishes a check run with one of three conclusions. Success on an approved review of the effective head. Failure on a review that does not approve, or on a stale head. In advisory mode, a missing review file gives a neutral conclusion, which shows grey and keeps the PR rollup clean. In enforced mode, a missing review file gives a failure. The mode comes from the repository variable `REVIEW_GATE_MODE`, with the values `advisory` and `enforced`. An absent or unknown value fails the job and names the variable (T-2). | Revises D-179 and D-180. Binds PR-1 and Phase 5 step 11. GitHub treats a neutral conclusion as a success for a required check, so enforced mode must not use it (F-51). Revised in part by D-185 on 2026-09-07, the mode source only. The three conclusions and the two modes stand. Revised in part by D-251 on 2026-09-09, the job result only. The three conclusions and the two modes stand. |
| D-182 | 2026-09-07 | Commit the record | A session always commits the review record and its session handoff entry. The reviewer commits `docs/reviews/pr-<number>.md` with its handoff entry. The author commits the response file, the corrected files, and its handoff entry. One commit holds the record and the entry. | Binds every review and every answer to a review. Narrows the scope limit in the `pr-review` skill, which listed a commit as unauthorized. `review-gate` cannot read an uncommitted record (D-179, D-181). |
| D-183 | 2026-09-07 | Reviewer push | The reviewer pushes its own review commit to the PR branch. A push is the only way `review-gate` reads the record, because the gate reads the PR head. The reviewer never pushes to `main`, and a code fix, a merge, and an external message stay unauthorized. | Applies D-182. Narrows the scope limit in the `pr-review` skill further. Binds every review. |
| D-184 | 2026-09-07 | Metadata paths | The effective head is the newest commit that changes a path outside the metadata set. The metadata set is `docs/reviews/`, `docs/session-handoff.md`, and `docs/session-handoff-archive.md`. A commit that changes only those paths is a metadata commit. | Revises the effective head in D-179. Applies D-182 and D-183. Without it the required review commit invalidates the review that it publishes. Resolves review finding P1-4 on PR #1. Binds PR-1. Revised in part by D-534 on 2026-09-23, the paths that the review follows only, from the merge of the PR that builds it. The metadata set stands for the gitar pass. |
| D-185 | 2026-09-07 | Review gate mode source | The mode lives in the tracked file `.github/review-gate-mode`, and it holds `advisory` or `enforced`. The workflow reads the file from the base branch, never from the PR head, so a PR cannot change the mode that judges it. PR-1 creates the file with `advisory`. An absent file, an empty file, or an unknown value fails the job and names the file (T-2). | Revises the mode source in D-181. Resolves review finding P1-3 on PR #1. Phase 5 changes the file through a reviewed PR. Revised in part by D-196 on 2026-09-07, the G-19 claim only: the base branch needs the file before PR-1, so one owner action was necessary. The mode source and the rules stand. Revised in part by D-521 on 2026-09-23, the PR that changes the file only: PR-78 sets `enforced`. The mode source and the rules stand. |
| D-186 | 2026-09-07 | Revision granularity | The Effect column uses two markers. `Superseded by D-N` means the whole answer is replaced, and every citation outside this file must name D-N. `Revised in part by D-N` means one named part changed, the decision stays citable, and the Effect column names the part that changed and the parts that stand. The D-178 reference check flags a superseded citation only. | Revises the single marker in D-178. Partial revisions carried 33 of 48 citations and produced the churn. A decision should cover one contract, so a future revision is a whole one. |
| D-187 | 2026-09-07 | Session number | Fetch the remote and read `docs/session-handoff.md` again immediately before the handoff commit. Take the highest session number in the file and add one. Every session pushes (D-183), so a push serializes the two writers, and the second one sees the first number. The PR-2 checker fails on a duplicate session number. | Applies D-182 and D-183. Two providers picked the number 11 on 2026-09-07. Binds PR-2 and every session. Revised in part by D-377 on 2026-09-16, the second read of the whole file only: the fetch and the rule of the highest number plus one stand. |
| D-188 | 2026-09-07 | Documentation PR override | A PR that changes only documentation merges without a cross-provider review when the owner gives the override for that PR. The owner states the override, and it applies to one PR. Every other PR gate line still applies. | Narrows T-4 and D-101 for documentation only. A PR that changes code, a workflow, or a schema always needs the review. Binds `CLAUDE.md` and `AGENTS.md`. Revised in part by D-190 on 2026-09-07, the override signal and the eligible path set only. The per-PR owner override and the review requirement for code stand. |
| D-189 | 2026-09-07 | SDK on the CI runner | The CI workflow calls `actions/setup-dotnet` with the `global-json-file` input on all three platforms. The runner needs no .NET SDK on its path. | Applies D-173. Binds PR-1. A launch agent starts with a minimal path and does not read `~/.zshrc`, so a direct `dotnet` call on the self-hosted runner fails. The action supports a self-hosted runner and reads `global.json`, checked 2026-09-07. |
| D-190 | 2026-09-07 | Review override signal | The label `review-override` on the PR carries the owner override. The `review-gate` job gives a `success` conclusion when three conditions hold. The label is present. Every changed path is outside the code set. No commit outside the metadata set is newer than the label event. The eligible set is `docs/`, `CLAUDE.md`, `AGENTS.md`, and `.claude/skills/`. The code set is every other path, and it includes `.github/`, the source directories, the project files, the schemas, and the content files. The workflow computes the changed paths from the diff, so a PR cannot claim an eligibility that it does not hold. The label overrides a review file that does not approve. The workflow also runs on the `labeled` and `unlabeled` event types. The check run output names the label, the account that added it, and the effective head. | Revises in part D-188, the signal and the eligible path set only. The per-PR override and the review requirement for code stand. Binds PR-1. Mirrors the stale-head rule of D-184, so a push after the label needs the label again. The agents hold the owner GitHub token, so the label stops an accident and not an attack. Revised in part by D-541 on 2026-09-23, the eligible set only: it is the skip set of D-475. The label, the three conditions, and the label time rule stand. |
| D-191 | 2026-09-07 | SSD format | Format the external SSD as APFS (Case-sensitive), with the GUID Partition Map scheme and no encryption. The volume name is `SSD-1TB`. | Applies D-145. Binds D-171 and D-174. A case-sensitive volume makes the macOS CI leg agree with the Linux leg on file name case, which D-135 and PR-57 check. An unencrypted volume mounts at login, so the launch agent finds its work directory. The repository has no file name collision, checked 2026-09-07. |
| D-192 | 2026-09-07 | Runner registration | The Mac Mini is a registered runner. The name is `mac-mini-m4`, the label is `macos-arm64-self-hosted`, and the runner version is 2.337.0. The work directory is `/Volumes/SSD-1TB/actions-work`. | Revises in part D-171, the date only. The SSD arrived on 2026-09-07, one day early, so the registration ran on 2026-09-07. The runbook and the once-only rule stand. Gives the runner name and the volume name that D-171 asks for. Resolves OQ-31 in full. |
| D-193 | 2026-09-07 | Full Disk Access | `/bin/bash` and the runner `node` binary hold Full Disk Access on the Mac Mini. Without that grant macOS denies a launch agent every path on an external volume. | Applies D-157, which puts the work directory on the SSD. Every bash process on this machine now reads every file, which is the cost of the work directory on the SSD. The owner selected this on 2026-09-07 over a move to the internal drive. A machine rebuild repeats the grant, or the runner fails with `Operation not permitted`. Records F-54. |
| D-194 | 2026-09-07 | Solution format | The solution file is `WhatYouCarry.slnx`. The .NET 10 SDK creates that XML format by default. | Binds PR-1. The Phase 1 roadmap named `WhatYouCarry.sln`, and this decision changes that name. D-108 names the four projects and not the solution file, so D-108 stands. Godot 4.7.2 builds a project from a `.slnx` and creates no `.sln`, verified 2026-09-07. `dotnet build` and `dotnet test` accept a `.slnx`, verified on the runner 2026-09-07. The XML format holds one line for each project, so two providers rarely conflict. Records F-55. |
| D-195 | 2026-09-07 | Checkout path | The local checkout is `/Volumes/SSD-1TB/what-you-carry`. | Revises in part D-174, the checkout path only. The repository name stands. Applies D-145 and completes the SSD move in the Phase 1 sequence. The move ran on 2026-09-07 after the runner registration, and `git fsck` reported no corruption. A session now needs the SSD mounted. |
| D-196 | 2026-09-07 | Mode file bootstrap | The owner put `.github/review-gate-mode` with `advisory` on `main` before the PR-1 merge, in commit `4ec9708`, on the owner instruction. PR-1 then reads the file from the base branch and passes its own check. | Resolves OQ-72. Applies D-185 and G-19. The one commit went to `main` without a PR and without a review, and the owner selected that over a one-time G-19 exception and over a head read in the tool. A head read stays rejected, because it weakens D-185 for every later PR. PR-1 also holds the same file with the same content, so the merge has no conflict. |
| D-197 | 2026-09-08 | Trusted evaluator | The `review-gate` workflow runs on the `pull_request_target` event. GitHub then runs the workflow file and the tool from the base branch. The job fetches the PR head into the git object store as data, no step checks out the head, and the tool reads it through git. | Resolves PR #6 review finding P1-1. Revises in part D-179, the event only. The three rules stand. Applies D-185, which reads the mode from the base for the same reason. A PR cannot change the code that judges it. PR #6 has no gate check on itself, because `main` holds no workflow until PR-1 merges. GitHub triggers this event only when the workflow file exists on the default branch (F-58), so no run is possible before the merge. After the merge, a throwaway PR against `main` with an evaluator that approves every PR proves the trusted evaluator, and the PR #6 response file records the result. Binds Phase 5 step 11: the enforced mode needs this event. Records F-56. |
| D-199 | 2026-09-08 | Session end gate | Every session ends with a push and a check of the remote. After the last commit, the session runs `git push`, `git fetch`, and `git status --short --branch`, and the status shows no `[ahead N]`. A review session also compares `gh pr view --json headRefOid` with `git rev-parse HEAD`, and writes the result as the push line of the review record. The state of the build in every handoff entry names the remote head. A session that cannot push does not end. It asks the owner to approve the push, and says in the handoff that the work is committed and unpushed. | Applies D-182 and D-183. Revises in part D-146, the content of the state of the build only. The six parts and the ten-entry limit stand. Records F-59: the reviewer left its commit unpushed on the shared checkout twice on PR #10, and the author pushed it each time. The evidence must come from the remote, because a sandbox that blocks the network denies a push in silence. |
| D-198 | 2026-09-08 | Review record trust | The gate trusts the review file at the PR head. One shared GitHub identity cannot prove which provider wrote a commit. The check-run output names the commit that last changed the review file, with its subject, and the owner reads that line before a merge. | Resolves PR #6 review finding P1-2 as an accepted risk. Applies D-190: the gate stops an accident and not an attack. Every commit in the repository has one author identity and no signature, verified 2026-09-08. D-184 stands. A per-provider identity, such as a second account or a signing key, is a separate owner decision. Records F-57. |
| D-200 | 2026-09-08 | DetMath Pow | `Pow` takes a float value and an integer exponent, and it multiplies by repeated squaring. Every step is one multiply, so the result is exact and needs no accuracy target. A fractional exponent waits for its first caller and a new decision. | Resolves OQ-73. Binds PR-3. Completes D-161, which named no target for `Pow`. An absolute error of 1e-6 has no meaning for a result with no bound. `Sqrt` covers the exponent one half, and no Phase 1 caller needs another fractional exponent (D-111). `Pow` rejects `int.MinValue`, a value that is not finite, and zero to a negative exponent (T-2). |
| D-201 | 2026-09-08 | Bit-identity program | The sweep is the `bit-identity` command of `WhatYouCarry.Tools`. It prints the state hash on standard output and its context on standard error. `WhatYouCarry.Tools` references `WhatYouCarry.Core`. | Resolves OQ-74. Binds PR-3. The Phase 1 roadmap named `Tests/BitIdentity/`, and `WhatYouCarry.Tests` is a test library and not a program. This decision keeps the four projects of D-108 and adds no fifth one. G-1 binds what Core depends on, so a reference from Tools to Core holds it. The command follows the `review-gate` and `ste-check` pattern of PR-1 and PR-2. |
| D-202 | 2026-09-08 | Lint parser | The `det-lint` tool reads each Core source file with `Microsoft.CodeAnalysis.CSharp`, the C# compiler API. It compiles the sources and asks the compiler what each name means. | Resolves OQ-75. Binds PR-3. This is the dependency entry that G-16 requires. D-67 named the Core syntax trees and no package. A parse tells an identifier from a comment and from a string, so a banned name in prose gives no finding. A parse also reads a name behind a `using static`, which a text search cannot see. The package is the compiler that the SDK already ships, and it stays in the tool project. Clarified 2026-09-08, after the PR #12 repeat review: the tool reads symbols and not source words. A parse alone cannot tell `System.Type.GetMethods` from a Core member of the same name, and it cannot tell `System.Numerics.Vector3` from the Core vector of PR-7 (F-64). The package and the dependency entry stand. |
| D-203 | 2026-09-08 | DetMath reduction range | `Sin` and `Cos` reduce to the nearest multiple of pi/2, which leaves a remainder in [-pi/4, pi/4] and a quadrant. `Atan2` folds to the octant [0, tan(pi/8)]. The domain limit is 4096 radians, and a larger angle is an error (T-2). | Resolves OQ-76. Revises in part D-161, the reduction range only. The degree, the 1e-6 target, the `Sqrt` rule, and the [-4 pi, 4 pi] test range stand. Records F-60: a true Remez minimax fit of degree 7 on [-pi, pi] reaches 2.5e-4 for sine, 250 times the D-161 target, measured 2026-09-08. The three parts of D-161 could not hold together. The new range gives 1.2e-09 for sine, 4.7e-11 for cosine, and 1.1e-07 for atan. The measured error of the whole float path is 8.5e-08 for sine and 3.7e-07 for `Atan2`. The 4096 limit keeps a margin of 12 against the target, and 65536 radians would leave none. |
| D-204 | 2026-09-08 | No conditional compilation in Core | A Core source file holds no `#if`, `#elif`, or `#else`. The `det-lint` tool reports each `#if` as `L-CONDITIONAL`. A `#nullable`, `#region`, or `#pragma` directive stays legal, because none of them selects a branch. | Resolves OQ-77. Binds PR-3 and every later Core PR. Records F-65. A `#if` makes two programs from one file, and a lint that parses one symbol set can never read the other, so the alternative correction cannot be complete: `DEBUG` and `RELEASE` are both real builds, and the bit-identity job builds Debug. Core holds one simulation (D-69). Core held no conditional directive on this date, so the rule costs nothing now. A later Core file that needs a branch asks for an exemption in its own decision, as the `MathF` allowlist does. |
| D-205 | 2026-09-08 | Core namespace allowlist | Core may use only an approved namespace. The set is `System`, `System.Collections.Generic`, `System.Globalization`, `System.Numerics`, `System.Runtime.CompilerServices`, and any namespace under `WhatYouCarry.`. Each entry matches one namespace and never its children, so `System` does not approve `System.ComponentModel`. `det-lint` reports every other namespace as `L-NAMESPACE`. The type denylist stays for the cases inside an approved namespace, such as `System.Math` and `System.Type`. | Resolves OQ-78. Binds PR-3 and every later Core PR. Records F-66. Revised in part by D-206 on 2026-09-08, the `System` entry only. Superseded by D-207 on 2026-09-08: Core approves every type by name, and no namespace as a whole. The reason for the boundary stands, and D-207 states the rule. A denylist over the class library cannot be complete: the PR #12 review reopened the reflection finding four times, and each pass named one more type. This list turns the boundary around, so a surface that nobody named cannot enter. It applies G-16 to the class library, where a new dependency needs a decision. Core used two namespaces on this date, so the rule cost nothing. A later Core PR adds a namespace here with a decision that says why. |
| D-206 | 2026-09-08 | Core System type allowlist | `System` is approved by type and not as a whole. Core may use `ArgumentOutOfRangeException`, `BitConverter`, `IEquatable`, `Int32`, `InvalidOperationException`, `Object`, `Single`, `String`, and `UInt64`. `det-lint` reports every other `System` type as `L-SYSTEM`. It also reports `Object.GetHashCode` and `String.GetHashCode` as `L-IDENTITY`, because a member of an approved type can still read the machine. | Resolves OQ-79. Revises in part D-205, the `System` entry only. Superseded by D-207 on 2026-09-08: every namespace is approved by type, not only `System`. The other approved namespaces stand as whole namespaces. Binds PR-3 and every later Core PR. Records F-67. `System` holds the rest of the nondeterminism beside the denied types: `Guid` draws from the platform, `HashCode` takes a new seed in each process, and `GC`, `OperatingSystem`, `Console`, and `AppContext` read the machine. `Guid` and `HashCode` also join the type denylist, so each one reports its own rule. A measurement on this date found seven `System` types in Core, so the list starts at nine with `Object` and `String`. The rule binds the names that Core writes and never a return type, because every method that gives back a bool would need an entry. |
| D-207 | 2026-09-08 | Core type allowlist | Core approves every type outside this project by full name. The list is `System.ArgumentOutOfRangeException`, `System.BitConverter`, `System.IEquatable`, `System.Int32`, `System.InvalidOperationException`, `System.Object`, `System.Single`, `System.String`, `System.UInt64`, and `System.Globalization.CultureInfo`. No namespace is approved as a whole. `det-lint` reports every other type as `L-TYPE`, and it reports an import of a namespace that holds no approved type. A member of an approved type that reads the machine stays in the member denylist, such as `CultureInfo.CurrentCulture`. | Resolves OQ-80. Supersedes D-205 and D-206, which approved whole namespaces and then the `System` types. Binds PR-3 and every later Core PR. Records F-68. A whole namespace is too coarse: `System.Collections.Generic` holds `EqualityComparer`, whose string hash takes a new seed in each process, and `System.Runtime.CompilerServices` holds `RuntimeFeature`, which reads the runtime. A measurement on this date found one type outside `System` in Core, so the list holds ten entries. The rule binds the names that Core writes and never the type a method gives back. Core uses no array member and no collection yet, so `System.Array` and the collection types are absent. The PR that first needs one adds it with a decision (G-16). |
| D-208 | 2026-09-08 | Core member allowlist | Core approves every member of an outside type by name and overload arity. A method entry ends with a slash and the count of its parameters, a property and a field carry no count, and a constructor uses the member name `new`. The list is `ArgumentOutOfRangeException.new/2`, `BitConverter.SingleToUInt32Bits/1`, `CultureInfo.InvariantCulture`, `Int32.MinValue`, `InvalidOperationException.new/1`, `Single.IsFinite/1`, `Single.IsNegative/1`, and `UInt64.ToString/2`. `det-lint` reports every other member as `L-MEMBER`. A member of a type of this project needs no entry. | Resolves OQ-81. Extends D-207, which stands: the type list still binds every type name. Binds PR-3 and every later Core PR. Records F-69. An approved type is not an approved surface: `String` holds `Intern`, which reads the process intern pool, and `CultureInfo` holds a constructor that reads the user settings. The arity separates two overloads that do not share one behavior: `UInt64.ToString/2` takes a format provider, and `ToString/0` reads the current culture. The same split binds `Parse`, `TryParse`, and `Compare`. A measurement on this date found six members and two constructors in Core. A named argument and a local carry a containing type and are not members of it, so the rule reads a method, a property, a field, and an event alone. |
| D-209 | 2026-09-08 | Review scope | A review judges the change in front of it. The roadmap entry for the PR and its exit tests set the boundary. A concern outside that boundary goes under `## Out of scope` in the review record, with the PR that holds it, and it takes no severity and blocks no merge. A PR that creates a check must pass that check, and the check does not cover the whole platform on the first day. A finding closes when its stated trigger and its regression check pass. A new trigger of the same class takes a new id. The third assessment of one id stops, and the owner settles the scope. | Binds every review and every answer to a review. The `pr-review` skill carries the rules. Records F-70: the PR #12 review reopened one finding four times and another one three times, and each pass widened the surface that a new tool had to cover. Each pass found a real defect, and the sum went past the PR-3 scope, which names a lint tool for the banned symbols of G-2 and G-21. This decision does not lower the standard for the code that a PR changes. It moves the growth of a new tool to the PR that owns that growth (G-10). Revised in part by D-513 and D-514 on 2026-09-23, the count only: the stop counts the rounds in which one id is open, and `make codex-review` stops the loop. The scope rules stand. |
| D-210 | 2026-09-08 | The v1 biome | The one v1 biome is a collapsed deep mine. A mining company dug too far, and it did not come back. The block set is raw stone, hewn stone, timber beam, ore vein, still water, rubble, and plank. Floors 1 to 5 are the working mine, with timber, rails, and lamps. Floors 6 to 10 are older workings in cut stone, with flooding. Floors 11 to 15 hold what the miners reached. | Resolves OQ-12. Binds PR-9. Feeds OQ-1, OQ-61, OQ-64, PR-13, and PR-14. The descent is the logic of the biome, so 15 floors read as one place and not as fifteen rooms. Black powder needs no explanation, because a blasting charge is a mining tool (D-7). Torchlight is native, because a mine has no daylight (D-59). The dry humor sits in company signage, safety notices, and pay disputes (D-8). The three depth bands give a difficulty ramp and need no new block. The three props of OQ-64, a barrel, a crate, and a torch stand, belong in a mine with no change. The risk is a brown corridor, so the palette of OQ-1 carries the mood. |
| D-211 | 2026-09-08 | Log sink | Core builds a log line and hands it to an `ILogSink`. Core opens no file. The Game layer writes the line to the user directory, and a test collects the lines. | Resolves OQ-82. Binds PR-4, and PR-31 and PR-55 write the sink of the Game layer. Core stays pure, so a disk failure never reaches the simulation thread (D-72, G-1). It keeps `System.IO` out of the Core allowlist and keeps a path out of Core (OQ-57). The Game writer and the test collector are the two concrete callers that D-111 asks for. |
| D-212 | 2026-09-08 | Log line schema | Every line carries a level and a message beside the required fields of its context. The levels are debug, info, warning, and error. No line carries a wall-clock time. | Resolves OQ-83. Extends the required set of D-113, which names the context fields alone. Binds PR-4 and PR-55. The tick is the only time in Core, and a run line already names its tick (G-21, D-73). A caller that needs a wall-clock time adds it as one more field, and the Game layer supplies the value. A level makes a crash report and a night sweep readable at more than one weight. |
| D-213 | 2026-09-08 | Log line builder | The logger builds each line with `System.Text.StringBuilder`. | Resolves OQ-84. Binds PR-4. The escape pass appends one character at a time, and a string concatenation would make one string per character, which T-1 calls wasteful. A hand-written buffer would rewrite a platform type against T-1, and it would need its own tests. D-214 records the allowlist entries. |
| D-214 | 2026-09-08 | PR-4 allowlist | Core adds 9 types and 18 members to the lists of D-207 and D-208, and it removes 2 entries that no Core file uses. The lists then hold 18 types and 26 members. The types are `System.Exception`, `System.Int64`, `System.Text.StringBuilder`, `System.Collections.Generic.List`, `IReadOnlyList`, `IReadOnlyCollection`, and the three caller attributes of `System.Runtime.CompilerServices`. The members are the constructors of those types, `Append/1`, `ToString/0`, `Add/1`, `Count`, `this[]`, `Exception.Message`, `Int64.ToString/1`, `Single.ToString/2`, `String.Length`, and `String.this[]`. `System.Object` and `List.new/0` leave the lists. | Resolves OQ-85. Applies D-207 and D-208, which stand. Binds PR-4. This session verified each entry: it removed one entry at a time and ran the scan, and every entry that stays produces a finding when it goes. Two entries produced none, and they are gone. Nothing in the set reads the user, the process, or the machine. A dead entry widens the boundary in silence, so the removal is part of the rule and not a tidy-up. Corrected 2026-09-08 after the PR #15 repeat review: the first text said 8 types and 16 members, and it then named `String.this[]` as one more. The counts here are the counts in the code (F-75). |
| D-215 | 2026-09-08 | Assertion call site | The assertion report names the call site from the compiler, through `CallerFilePath`, `CallerLineNumber`, and `CallerMemberName`. It walks no stack. | Resolves OQ-85. Revises the PR-4 scope line in the Phase 1 roadmap, which said "the stack". Binds PR-4 and PR-55. A stack trace changes with the build, the inlining, and the platform, and a file, a line, and a member name do not (G-2, D-69). `System.Diagnostics.StackTrace` would also enter the allowlist for a value that no two builds share. |
| D-216 | 2026-09-08 | Lint reaches every member form | The `det-lint` member rule reads a target-typed `new`, a `base` or `this` constructor initializer, and an indexer, beside the explicit `new` and the member access that it already read. | Resolves OQ-86. Records F-71. Applies D-208, which stands. Binds PR-4 and every later Core PR. `CultureInfo c = new("en-US", true);` gave no finding while `new CultureInfo("en-US", true)` gave one, so the P2-9 case was reachable by another spelling. The owner chose the fix in the PR-4 branch over a separate PR, because a merge gate with a known way around it is worse than a second concern in one PR (G-10). |
| D-217 | 2026-09-08 | Reviewer corrects the PR description | A review corrects a stale fact in the PR description directly, and it needs no finding for that. The reviewer changes the head, a count, a check result, or a sentence that a later commit replaced. The reviewer never changes what the author says the PR does, a decision, a tradeoff, or a gate line that the owner ticks. Each edit takes one line in the review record, under `## Description edits`. A claim that is wrong in substance stays a finding. | Owner instruction, 2026-09-08. The `pr-review` skill carries the rules, and the review record skeleton gains the section. Narrows the scope limit, which named every external message. Records F-76: the PR #12 review reopened its description finding four times, and the PR #15 review reopened one, and each pass cost a whole round trip for a head, a count, or a superseded sentence. The author still owns what the description claims. |
| D-218 | 2026-09-08 | Log name and value text | A log field name is an identifier that the code writes. It must hold valid text, and a surrogate without its pair in one is an error at the add, with its position in the context. A log field value and a log message carry text from the run. Invalid text in either one takes the replacement character, and neither one throws. | Resolves OQ-87. Binds PR-4 and every later Core caller of the logger. Records F-77. A JSON object needs each name to stay its own, and two names that differ only in a lone surrogate reached the object as one property before this rule. A crash report writes the state that led to a failure, and text from a run is the kind that can be malformed, so a throw there would lose the report at the moment it is needed most (D-112, PR-55). The author chose the split while it answered PR #15 finding P2-7, and this entry ratifies it. |
| D-219 | 2026-09-08 | Content source | Core takes the content bytes from an `IContentSource` and opens no file. The Game layer reads the content directory of the build, and a test hands over a set that it holds in memory. Each file carries its path relative to the content root, with a forward slash on every platform. | Resolves OQ-88. Binds PR-5, and PR-31 writes the source of the Game layer. Applies D-211, which kept `System.IO` out of Core for the logger. A disk failure never reaches the simulation thread (D-72, G-1), no path lives in Core (OQ-57), and a directory enumeration order never reaches the content hash of D-163. The Game reader and the test source are the two concrete callers that D-111 asks for. |
| D-220 | 2026-09-08 | JSON reader | Core reads JSON with `System.Text.Json.Utf8JsonReader`, a forward-only reader over bytes. It uses no serializer and no reflection. The reader gives every value as text, and each validator turns it into the shape that its type needs. | Resolves OQ-89. Binds PR-5 and every later content type. D-68 asked for a hand-written JSONL logger, and a writer is far smaller than a parser: a correct JSON parser holds the number, escape, and surrogate rules, and PR-4 took three review passes on the escape rules alone. The reader reads no type at run time, so G-2 holds. It also gives a position on a malformed file, which D-92 needs for the error message. |
| D-221 | 2026-09-08 | Content hash | The content hash is SHA-256 over each file's relative path and bytes, in sorted path order, as 64 lowercase hexadecimal digits. Two files of one path are an error. | Resolves OQ-90. Applies D-163, which named SHA-256. Binds PR-5 and PR-6. The state hash of D-160 stays FNV-1a: that one needs speed on every tick, and this one guards a run record against a content set that somebody changed, so it needs resistance. The sort makes the answer the same whatever order the source gives, and the path in the input makes a rename a change. |
| D-222 | 2026-09-08 | Two lint rule sets | The `det-lint` command reads `WhatYouCarry.Core` with the determinism rules and `WhatYouCarry.Game` with the string rule of G-8. The two sets never mix. The report names the count of each set. | Resolves OQ-91. Binds PR-5 and every later Game PR. One command and one CI job keep one place to read the rules. Game has an engine dependency and needs no allowlist, and Core needs no string rule, because no player reads a Core string. The string rule permits a literal in an engine call such as `GetNode`, in a `const` declaration, and in an argument to `Strings.Get`, which is the id and not the text. |
| D-223 | 2026-09-08 | PR-5 allowlist | Core adds 10 types and 30 members to the lists of D-207 and D-208. The lists then hold 28 types and 56 members. The types are the five of `System.Text.Json`, `System.Text.Encoding`, `System.Security.Cryptography.SHA256`, `System.Globalization.NumberStyles`, `System.StringComparison`, and `System.Array`. | Resolves OQ-89, OQ-90, and OQ-91 in part. Applies D-207 and D-208, which stand. Binds PR-5. The set follows from D-220 and D-221: a JSON reader and a SHA-256 hash need their types, and the parse and compare members follow. Nothing in the set reads the user, the process, or the machine. `StringComparison.Ordinal` keeps a path compare away from the culture of the machine. The PR #17 review added `Encoding.GetString/1`, `Utf8JsonReader.ValueSpan`, `System.Array`, and `Array.Length` on 2026-09-08, for the P2-1 and P2-2 corrections. `System.Array` was absent after PR-4 because Core used no array member then, and D-207 says the PR that first needs one adds it (F-78). |
| D-224 | 2026-09-09 | CRC-32 in Core | Core holds a table-driven CRC-32 of IEEE 802.3 in `Crc32.cs`, next to DetMath. The text "123456789" gives 0xCBF43926, and a test pins it. | Resolves OQ-92. Applies D-162, which named CRC32 and no implementation. Binds PR-6. No new dependency (G-16) and no allowlist entry (D-207, D-208). The platform class that computes one lives in the package `System.IO.Hashing`, which needs a dependency decision and puts a `System.IO` namespace into Core for the first time. The function is thirty lines of integer arithmetic, so it is deterministic by construction. |
| D-225 | 2026-09-09 | Run record sink | Core hands the header line and each frame to an `IRunRecordSink` and opens no file. The Game layer appends the bytes to the record file, and a test collects them in memory. | Resolves OQ-93. Applies D-211 and D-219. Binds PR-6, and PR-31 writes the sink of the Game layer. A disk failure never reaches the simulation thread (D-72, G-1), and `System.IO` stays out of Core. The Game writer and the test collector are the two concrete callers that D-111 asks for. A recorder that gives back a buffer loses frames in a crash between two drains. |
| D-226 | 2026-09-09 | Fixed frames, no length prefix | The tick frames of a run record are the fixed 16 bytes of D-162, with no length prefix. Sections 3.9 and 7 of the design doc said "length-prefixed, checksummed tick frames", and both name the fixed frame now. | Resolves OQ-94. Applies D-162 and D-163, which stand. Records F-80. A fixed frame needs no length, and the torn tail of D-152 is the bytes after the last multiple of 16. The doc was stale, and the decision was not. |
| D-227 | 2026-09-09 | Phase 1 loop state | The Phase 1 simulation state is the seed, the tick, the yaw and pitch sums in hundredths of a degree, and the buttons of the last intent. The yaw wraps at 36000, and the pitch stops at plus or minus 9000. The state hash covers those five fields in that order. The loop reads no movement byte yet. | Resolves OQ-95. Applies D-73, D-77, and D-160. Binds PR-6, PR-7, and PR-8. PR-7 adds the world, and PR-8 derives the aim ray from the two sums. A position needs collision, so the movement bytes wait for PR-7. Every field is an integer, so the hash is the same on every platform by construction. Revised in part by D-241 on 2026-09-09, the pitch limit only. The five fields, the yaw wrap, and the hash order stand. |
| D-228 | 2026-09-09 | Torn-tail floor | The torn-tail log line names floor 1 until the state holds a floor. PR-31 reads the floor from the state. | Resolves OQ-96. Applies D-3, D-113, and D-152. Binds PR-6 and PR-31. Every run starts at floor 1, and a Phase 1 run never leaves it, so the line is true today and needs no schema change. |
| D-229 | 2026-09-09 | Empty list and null | The JSON reader of D-220 gives two more kinds, an empty list and a null. The run record header writes `loadout` as `[]`, `tree` as `[]`, and `amulet` as `null`. A list with an item is an error until Phase 3 reads one. | Resolves OQ-97. Extends D-220, which stands. Applies D-151. Binds PR-6 and PR-31. The schema is complete from the first record: a Phase 3 record adds items under the same names, and an older reader fails on a list with an item and never on an absent name. The shared error text says "file" and not "content file", because the header takes the same path. |
| D-230 | 2026-09-09 | PR-6 allowlist | Core adds 1 type and 6 members to the lists of D-207 and D-208. The lists then hold 29 types and 62 members. The type is `System.UInt32`. The members are `UInt32.MaxValue`, `UInt64.TryParse/4`, `UInt64.ToString/1`, and the `JsonTokenType` values `StartArray`, `EndArray`, and `Null`. | Resolves OQ-98. Applies D-207 and D-208, which stand. Binds PR-6. A removal check proved every entry in use: each entry gives a finding when it goes. `MaxValue` guards the tick counter against a silent wrap (T-2). The two `UInt64` members read and write the seed with the invariant culture. The three token values are the two kinds of D-229. Nothing in the set reads the user, the process, or the machine. |
| D-231 | 2026-09-09 | Motion constants | Gravity is 20 meters per second squared. Walk speed is 4 meters per second, sprint speed is 6.5, and jump velocity is 7. Each is a Core constant. | Resolves OQ-100. Applies D-27 and D-165. Binds PR-7. The apex of a jump is 1.23 meters, so a jump clears one block with a margin and never a second. Gravity above the real value gives a short arc that reads well in a one-meter grid. A tune happens on measurement after Gate 2 (D-109). Corrected 2026-09-09: 1.23 meters is the closed form. The fixed-step integration of PR-7 gives an apex of 1.17 meters, measured on this date, and the one-block clear stands (F-83). Revised in part by D-315 on 2026-09-12, the sprint speed only: 7 meters per second (D-319). Gravity, the walk speed, and the jump velocity stand. |
| D-232 | 2026-09-09 | Button bits | The button mask of D-162 assigns bit 0 to jump, bit 1 to sprint, bit 2 to dodge, bit 3 to attack, bit 4 to use, bit 5 to interact, bit 6 to quick slot next, and bit 7 to quick slot previous. Bits 8 to 15 are reserved, and a set reserved bit is an error. | Resolves OQ-101. Extends D-162, which stands. Binds PR-7 and every PR that reads a button. The whole list is one decision, so a later PR cites a bit and needs no decision of its own. Revised in part by D-243 on 2026-09-09, bit 8 only. The eight assigned bits and the reserved rule for bits 9 to 15 stand. Revised in part by D-257 on 2026-09-09, bit 9 only. The nine assigned bits and the reserved rule for bits 10 to 15 stand. |
| D-233 | 2026-09-09 | Movement mapping | The movement x of an intent is strafe, and y is forward. Core divides each by 127 to a fraction, clamps -128 to -127, rotates the pair by the yaw sum, and clamps the length to 1. The speed is the walk speed, or the sprint speed when the sprint bit is set. | Resolves OQ-102. Applies D-162, D-227, and D-231. Binds PR-7. The clamp keeps a diagonal from being faster than a straight line. The yaw lives in Core alone, so the Game layer rotates nothing before it quantizes. |
| D-234 | 2026-09-09 | Coordinate frame | Core uses the frame of Godot: right-handed, Y up, meters. Forward at yaw zero is minus Z, and the yaw turns counterclockwise seen from above. Block (x, y, z) fills the cube from (x, y, z) to (x + 1, y + 1, z + 1). | Resolves OQ-103. Applies D-78. Binds PR-7, PR-8, and PR-9. The Game layer copies a Core position into a Godot node with no conversion, and a swap is one more place for a sign error. |
| D-235 | 2026-09-09 | Body position and contact | The player position is the feet center in float meters: the middle of the box on X and Z, and the bottom of it on Y. The sweep stops a box face one contact skin before a block face, and never on it. The skin is 2^-10 meters. The body stands on the ground when a solid cell lies within two skins below its feet. | Resolves OQ-104. Applies D-70, D-165, and D-234, which stand. Binds PR-7, PR-8, PR-10, and PR-16. Records F-82: a float contact on a face is one ulp off for twelve integer faces below 130, so an exact contact is impossible by formula. The skin is exact in float and 128 times the ulp of a position below 128 meters, so rounding never puts a face inside a block. The Game layer copies the position with no conversion. Integer micrometers and a one-ulp correction after each contact were the other options. Revised in part by D-365 on 2026-09-14, the ground probe only: a ramp slope within two skins below the feet is ground too. The feet center, the contact skin, and the probe depth stand. |
| D-236 | 2026-09-09 | World input before procgen | The loop and the replayer take the grid and the spawn point from the caller. The loop constructor takes the seed, the grid, and the spawn, and the replay takes the record, the content hash, the grid, the spawn, and the logger. The grid is an input like the content and not state, so the state hash reads the body and not the blocks. | Resolves OQ-105. Applies D-151 and D-227, which stand. Binds PR-7, PR-8, and PR-9. The record does not carry the grid, so two callers with two grids replay two runs from one record until PR-9 derives both from the seed and the floor number, which closes the gap. The bit-identity sweep and every test build the same grid on each side. A constant Phase 1 grid in Core was the other option, and PR-9 would then remove it. |
| D-237 | 2026-09-09 | Grid edge | A cell outside the grid is solid for collision. The edge of the world is a wall, and a body can never leave the grid or fall below zero. A direct read or write outside the grid is an error. | Resolves OQ-106. Applies D-164 and D-234. Binds PR-7, PR-9, and PR-16. Only the collision query treats the outside as rock, so an absent block stays an error everywhere else (T-2). The sweep stops at the edge cell at the latest, so a move of any size ends inside an int. An error at the edge and air outside were the other options. |
| D-238 | 2026-09-09 | Air control | The horizontal velocity of the body comes from the intent on every tick, on the ground and in the air. The state holds one vertical velocity and no horizontal one. | Resolves OQ-107. Applies D-60 and D-233. Binds PR-7 and PR-15. A jump steers, and a jump onto a one-block step needs no run-up. The take-off velocity that holds until the body lands was the other option, and it adds two state fields to the hash. |
| D-239 | 2026-09-09 | PR-7 block ids | PR-7 defines two block ids: 0 is air and 1 is raw stone. Every block but air is solid. PR-9 adds the rest of the D-210 set with a decision that also says whether still water is solid. | Resolves OQ-108. Applies D-164 and D-210. Binds PR-7 and PR-9. A number here is part of the grid, so it never changes once a later PR reads it. The grid rejects an id that no PR declared (T-2). The whole D-210 set with still water as air was the other option. Revised in part by D-254 on 2026-09-09, the PR that adds the rest of the ids only: PR-59 and not PR-9. The two ids and the solid rule stand. Revised in part by D-259 on 2026-09-09, the ids beyond air and raw stone only, and D-258 gives the water exception to the solid rule. |
| D-240 | 2026-09-09 | Terminal velocity | A falling body has no terminal velocity. Gravity accumulates until the body lands. | Resolves OQ-109. Applies D-231. Binds PR-7. The sweep reads every cell in the path, so it never tunnels at any speed, and a fall of 32 blocks reaches 36 meters per second. A cap is a tune after Gate 2 (D-109). A cap of 30 meters per second was the other option. |
| D-241 | 2026-09-09 | Pitch limit | The pitch limit is 80 degrees up and 80 degrees down, for the loop and the camera alike. The loop stops the pitch sum at plus or minus 8000 hundredths of a degree. | Resolves OQ-110. Revises in part D-227, the pitch limit only. The five fields, the yaw wrap, and the hash order stand. Binds PR-8. One number and no dead band: a separate camera clamp would let the pitch sum rise while the camera stands still, and a look back down would answer late. A look straight up would put the camera under the floor. The loop clamp changes, so the simulation version rises in PR-8 (G-20). A tune happens on measurement after Gate 2 (D-109). |
| D-242 | 2026-09-09 | Camera geometry | The pivot sits 1.5 meters over the feet. The shoulder point is the pivot moved 0.6 meters right and 0.3 meters up in camera space. The boom runs 3 meters back from the shoulder point along the look direction. | Resolves OQ-111. Applies D-13, D-60, D-165, and D-166. Binds PR-8. The pivot sits near the eyes of the 1.8 meter body. With the player centered in a three-wide corridor the shoulder point stays 0.6 meters from the wall. Each number is a Core constant, and a tune happens on measurement after Gate 2 (D-109). A closer camera and a wider one were the other options. |
| D-243 | 2026-09-09 | Controller aim flag | Bit 8 of the button mask is set when a controller aims on that tick, and aim assist runs on that tick alone. Bits 9 to 15 stay reserved, and a set one is an error. | Resolves OQ-112. Revises in part D-232, bit 8 only. The eight assigned bits and the reserved rule for the rest stand. Applies D-14 and D-77. Binds PR-8, PR-12, and PR-54. The Game layer sets the bit from the device that gave the look input, so a Steam Deck player who changes the device inside a run gets the change on the next tick, and the record carries every change. The frame keeps its shape, so the format version stands. A header field per run and an assist strength in the header were the other options. |
| D-244 | 2026-09-09 | Aim assist shape | Aim assist pulls the aim ray toward the target with the smallest angle to the ray, when that angle is inside a cone of 5 degrees, by half the angle. No target inside the cone leaves the ray as it is. The cone and the strength are Core constants. | Resolves OQ-113. Applies D-14 and D-77. Binds PR-8 and PR-16. A target is a point in Phase 1, and PR-16 gives the enemy positions. The assist changes the ray and never the yaw and pitch sums, so the state and the hash stay as they are. A snap and a friction on the look deltas were the other options. A tune happens on measurement after Gate 2 (D-109). |
| D-245 | 2026-09-09 | Camera state | Core derives the camera on each tick from the body position, the yaw and pitch sums, and the grid. The camera holds no state and adds no hash field. Core smooths nothing, and the Game layer interpolates the render (D-73). | Resolves OQ-114. Applies D-75 and D-227. Binds PR-8. A stored camera with a smoothing rate was the other option, and it adds three hash fields and a rate to tune. |
| D-246 | 2026-09-09 | Boom sweep | The boom sweep walks the grid cells from the shoulder point toward the camera point along the line. It stops at the first solid cell and pulls the camera back by a camera radius of 0.25 meters, so the near plane stays out of the wall. | Resolves OQ-115. Applies D-88 and D-237. Binds PR-8. The per-axis sweep of PR-7 follows a staircase and not the line, so a diagonal boom would leave the camera beside the line. The camera radius is a Core constant. Short per-axis sweeps were the other option. |
| D-247 | 2026-09-09 | Aim ray origin | The aim ray starts at the camera position and runs along the look direction. It is the crosshair. | Resolves OQ-116. Applies D-77 and D-246. Binds PR-8 and PR-10. PR-10 fires a projectile toward the first hit of that ray. The boom sweep keeps the camera out of walls, so the ray never starts inside one. A ray from the shoulder point was the other option, and the crosshair and the shot could then disagree. |
| D-248 | 2026-09-09 | Pitch sign | A positive pitch sum looks up. At the limit of D-241 the forward vector has a Y component of the sine of 80 degrees. | Resolves OQ-117. Applies D-227, D-234, and D-241. Binds PR-8 and PR-12. This is the elevation convention of the right-handed frame. The Game layer applies any inversion before quantization (D-77), so a mouse whose forward push looks down inverts there and not in Core. |
| D-249 | 2026-09-09 | Shoulder march | The camera runs two ray marches. The first runs from the pivot to the shoulder point and pulls the shoulder point in by the camera radius at a wall. The second runs the boom from that shoulder point. | Resolves OQ-118. Extends D-246, which stands: the boom march is the second one. Applies D-242. Binds PR-8. The shoulder point sits 0.6 meters right of the pivot, and the body is 0.3 meters wide from its center, so a player who hugs a right wall puts the shoulder point inside rock, and a march from there has no start. The pivot is always in air, because it sits inside the body box 0.3 meters from every face. A dropped offset at a wall and an offset scaled by a probe were the other options. |
| D-250 | 2026-09-09 | Automated review pass | An automated reviewer, gitar, comments on every PR after a push. The author answers every comment before the hand-over to the other provider, or before the override request on a documentation PR. A comment with no merit gets a reply with the reason, and the author resolves its thread. A comment with merit gets the change, a commit, a push, and a reply. The PR is ready when gitar approves it, or when every comment has its answer and a new pass adds none. The reviewing provider reads the existing PR comments into its review and never addresses gitar. | Owner instruction, 2026-09-09. Applies before T-4 and D-101, which stand: the cross-provider review stays required for every code PR. The `pr-review` skill carries the two procedures, and the review record skeleton gains a `## PR comments` part. Binds every PR from PR #23 onward. A reply names no provider, harness, or model as the source of work (T-6, D-176), and a reply that gives a reason is not attribution. The PR template gains its gate line in the next code PR, because `.github/` is outside the override set (D-190). Revised in part by D-374 on 2026-09-16, the skill of the author procedure only: the `gitar-review` skill carries it. The rest stands. Revised in part by D-522 on 2026-09-23, the resolve of a comment with merit only: the author resolves its thread after the reply. The rest stands. Revised in part by D-542 on 2026-09-23, the requirement only: the automated pass stops as a gate until the owner ends the pause. The answer to each comment stands. Revised in part by D-550 on 2026-09-23, the answer to each comment only: a gitar notice needs no answer. The rest stands. |
| D-251 | 2026-09-09 | Review gate job result | The `evaluate` job of the review-gate workflow fails on a `failure` conclusion and on a `neutral` one. It passes on `success` alone. The check run keeps its three conclusions. | Resolves OQ-119. Revises in part D-181, the job result only. The three conclusions and the two modes stand. Records F-84: the job stayed green on a neutral verdict, and the workflow carries the name "Review gate", so the PR rollup read as a pass with no review record. A job cannot set a neutral conclusion by its exit code, so red is the one honest job result for the grey case. A valid override label gives `success`, so a documentation PR with the label stays green. The workflow runs from the base branch (D-197), so the change takes effect on the first PR after the merge, and `ReviewGateJobFailsOnNeutral` reads the file. A split into a publish job and a skipped verdict job, and a rename, were the other options. At launch the job can serve as the required check, because it is red where GitHub counts a neutral check run as a pass (F-51). |
| D-252 | 2026-09-09 | Floor size by band | Each floor template names `sizeX`, `sizeY`, and `sizeZ`. The working mine of floors 1 to 5 is 48 by 12 by 48, the older workings of floors 6 to 10 are 72 by 16 by 72, and the deep of floors 11 to 15 is 96 by 20 by 96. The validator rejects a size past D-164. | Resolves OQ-120. Applies D-91, D-164, and D-210. Binds PR-9. Content and not code, so a tune after Gate 2 changes a file (D-109). A formula in Core and a size per floor were the other options. Revised in part by D-343 on 2026-09-14, the three band sizes only: every band is 64 by 20 by 64. The size fields and the validator bound of D-164 stand. |
| D-253 | 2026-09-09 | The mine dig plan | A floor is a mine dig plan. A seeded random walk lays a main gallery and side drifts that jitter, slope by one-block steps, and branch. A chamber is a union of overlapping boxes, smoothed by a cellular pass, with pillars left standing. Shafts and ramps join levels. Collapses fill dead ends with rubble. Every tunnel is carved with a brush of at least three by three. | Resolves OQ-121. Applies D-78, D-159, D-165, D-166, D-167, and D-210. Binds PR-9, PR-59, and PR-16. The owner asked for a floor far more random, varied, and detailed than rectangles, whatever the implications. The rooms of D-166, D-167, and the roadmap are chambers, and the corridors are tunnels. D-166 holds by construction, because no brush is smaller than three by three. The generator reads the Procgen stream alone and steps in integers, so it is deterministic by construction. Cellular caves with detected rooms and rectangles with detail passes were the other options. Revised in part by D-345 and D-347 on 2026-09-14, the tunnel slope only: a tunnel changes height by a ramp or a shaft, and not by one-block steps. Revised in part by D-348 on 2026-09-14, the chamber shape only: some chambers hold a tier. The random walk, the chamber boxes, the collapses, the shafts, and the brush of at least three by three stand. |
| D-254 | 2026-09-09 | The PR-9 split | PR-9 carves the dig plan in raw stone and air, with the eight exit tests and the night sweep. PR-59 is a new roadmap entry that follows it: the detail pass with collapses, rubble, pillars, and the block ids of D-210 by band. | Resolves OQ-122. Revises in part D-239, the PR that adds the rest of the block ids: PR-59 and not PR-9. The two ids and the solid rule stand. Binds PR-9 and PR-59. One concern per PR (G-10), and the review of the carving does not wait on the detail. The decision on still water comes before the PR-59 code. |
| D-255 | 2026-09-09 | Chamber templates | A chamber kind is a content type, `content/chambers/*.json`, one file per kind: an id, a weight, a box count range, and a box size range. The generator draws kinds until the sum of weights lands inside the budget window of D-167, inside the room count range of the floor template. | Resolves OQ-123. Applies D-91, D-92, D-167, and D-168. Binds PR-9 and PR-16. One validator per type with an explicit field list. PR-16 maps the weight to enemy spawns. An inline list in the floor template and weights without kinds were the other options. Revised in part by D-350 on 2026-09-14, the field list only: a chamber kind also names a tier chance. The weight, the box ranges, and the draw stand. |
| D-256 | 2026-09-09 | Spawn and stairwell | The spawn is a standing cell at the center of the first chamber of the dig plan. The stairwell is one cell in the chamber with the longest walkable path from the spawn. | Resolves OQ-124. Applies D-165 and D-253. Binds PR-9. The reachability search gives the path lengths, so every floor asks for a walk across it. The last chamber and two random chambers were the other options. |
| D-257 | 2026-09-09 | Stairwell choice bits | At the stairwell, the interact bit (bit 5) descends, and bit 9 ascends. Both bits act at the stairwell alone. Bits 10 to 15 stay reserved, and a set one is an error. | Resolves OQ-125. Revises in part D-232, bit 9 only. The nine assigned bits and the reserved rule for the rest stand. Applies D-50, D-151, D-162, and G-5. Binds PR-9, PR-11, and PR-18. The choice reaches the record through the intent stream, so a replay reproduces the run with one frame kind. The prompt of PR-18 maps its two answers to the bits, and a policy of PR-11 sets them. An API call with a marker frame was the other option. |

## Rules set during the interview

| Id | Date | Topic | Decision | Effect |
|---|---|---|---|---|
| D-137 | 2026-09-06 | Attribution tenet | No work is attributed to an agent, harness, or model in code, the game, commits, PR descriptions, or GitHub comments. Exemptions: the session-handoff author field ("Claude Code" or "Codex") and the reviews directory. | Absolute. Not ranked. |
| D-138 | 2026-09-07 | Push-back rule | Ask the moment a question appears. Push back with evidence. Quote both statements on a conflict. Record each answer in this file. | |
| D-139 | 2026-09-07 | Text rules | ASD-STE100 for every doc, skill, and agent file. The design doc follows the design-doc-style template. | Load the ste-writing skill first. |
| D-258 | 2026-09-09 | Still water | Still water is not solid. A body walks through it as through air, and a pool is a one-block depression that the detail pass carves in a floor. While the feet stand in a water cell, the body walks and jumps more slowly. The reachability search reads water as air, so a pool is a drop of one and a step up of one. | Resolves OQ-126. Answers the decision that D-239 and D-254 call for. Applies D-165 and D-253. Binds PR-59. A solid block that reads as a painted floor, and a flat water block at floor level, were the other options. |
| D-259 | 2026-09-09 | Block ids | The block ids are 0 air, 1 raw stone, 2 hewn stone, 3 timber beam, 4 ore vein, 5 still water, 6 rubble, and 7 plank: the order of D-210. A number here is part of the grid, so it never changes once a floor holds it. | Resolves OQ-127. Revises in part D-239, the ids beyond air and raw stone only. The two ids and the solid rule stand, with the water exception of D-258. Binds PR-59 and PR-13. A numbering by band was the other option. |
| D-260 | 2026-09-09 | Version for a layout change | PR-59 raises the simulation version to 5. A floor with other blocks is another simulation, so a record of version 4 fails with the notice of D-151 and never replays to another floor in silence. | Resolves OQ-128. Applies D-151 and G-20. Binds PR-59 and every later PR that changes a dug floor. The reading that kept the version for changes to simulation numbers alone was the other option. |
| D-261 | 2026-09-09 | Water speed | While the feet stand in a water cell, the walk speed and the sprint speed of D-231 take the factor one half: 2 meters per second and 3.25 meters per second. | Resolves OQ-129. Applies D-231 and D-258. Binds PR-59. Two thirds and one third were the other options. A tune after Gate 2 changes the constant (D-109). Revised in part by D-319 on 2026-09-13, the sprint number only: 3.5 meters per second. The factor one half and the walk number stand. |
| D-262 | 2026-09-09 | Water jump | While the feet stand in a water cell, the jump velocity of D-231 takes the factor one half and gravity takes the factor one quarter, so the apex of a jump stays at one block and the rise takes twice as long. A body leaves every pool by the same jump that clears one block on land (D-165). | Resolves OQ-130. Applies D-165, D-231, and D-258. Binds PR-59. A lower jump velocity alone was the other option, and it would put the apex below one block, so every pool would need a ramp out and the search a pit rule. Corrected 2026-09-09 before the merge, on the automated pass of PR #28 (F-86): the first text gave both numbers one factor of one half, which halves the apex and keeps the rise time. Time doubles, so velocity halves and gravity quarters. Revised in part by D-264 on 2026-09-09, the probe only: the factors hold in the air over water too. The two factors and the apex stand. |
| D-263 | 2026-09-09 | Water constants | The three water factors of D-261 and D-262, one half for the speeds, one half for the jump velocity, and one quarter for gravity, are Core constants in `PlayerBody`, beside the motion constants of D-231. | Resolves OQ-131. Applies D-109 and D-231. Binds PR-59. Two fields per floor template were the other option, and the working mine has no water to name them for. |
| D-264 | 2026-09-09 | Water probe | The water factors of D-261 and D-262 apply while the column under the feet is water: the feet cell holds still water, or the body is in the air over still water with air alone between. A body that steps off a ledge over a pool falls slowly into it. | Resolves OQ-132. Revises in part D-262, the words "while the feet stand in a water cell" only. The two factors and the apex of one block stand. Binds PR-59. A probe of the feet cell alone loses the quarter gravity as soon as the feet rise out of the cell, and the apex then reaches 1.05 blocks at 1.6 times the ticks. The automated pass of PR #29 asked for the decision. |
| D-265 | 2026-09-09 | Phase 1 shot | Until PR-15 gives the loadout a weapon, the attack bit of D-232 fires the first projectile definition of the content set, in the ordinal path order of the typed lists. | Resolves OQ-133. Applies D-232 and G-6. Binds PR-10 and PR-15. The test-only set of PR-10 puts a plain definition first. A projectile id in the floor template, and no shot from the intent stream, were the other options. Superseded by D-320 on 2026-09-13: the attack bit swings the first weapon definition, and the shot ends. |
| D-266 | 2026-09-09 | Spread field | The projectile schema gains the required field `spreadHundredths`: the half angle of the spread cone in hundredths of a degree, a whole number. The Projectile stream of D-159 draws the spread of each shot. | Resolves OQ-134. Applies D-77, D-92, and D-159. Binds PR-10, PR-24, and PR-43 to PR-46. An absent field is an error and never a zero (T-2). An optional field, and no spread before PR-15, were the other options. |
| D-267 | 2026-09-09 | Fire timing | A shot fires on a tick where the attack bit is set and was clear on the tick before. A held bit fires once. Reload and draw time are PR-15. | Resolves OQ-135. Applies D-232. Binds PR-10 and PR-15. A shot every N ticks while held was the other option. Revised in part by D-320 on 2026-09-13, the shot of the attack bit only: the bit fires no shot from PR-15, and D-323 gives the swing the same press rule. The press rule stands for the ranged weapon of PR-24. |
| D-268 | 2026-09-09 | Shot origin | A shot starts at the shoulder point of D-242 and flies toward the first solid cell that the aim ray meets within 100 meters, or toward the point of the ray at 100 meters when it meets none. | Resolves OQ-136. Applies D-242 and D-247. Binds PR-10. The crosshair and the shot agree at the target, and a shot never starts inside the camera boom. The camera along the ray, and an eye point over the feet, were the other options. |
| D-269 | 2026-09-10 | One verdict per record | The Verdict section of a review record holds one verdict name. An earlier verdict goes in a section above it, under a heading that starts with another word. The review gate reads the section under the exact heading `## Verdict`, and it fails a section that names two verdicts and names both. A repository test reads every review record the same way. | Resolves OQ-137. Applies D-179 and D-181, which stand. Records F-89: the repeat review of PR #31 put a previous-verdict line inside the Verdict section, and the gate read the first name. A history section named "Verdict history" then matched the heading as a prefix. The owner asked for the three layers: the parser, the test, and the skill line. |
| D-270 | 2026-09-10 | Run end states | A bot run ends in one of four states. `bottom`: the run ascends at the stairwell of floor 15. `budget`: the tick budget of a policy that promises no progress runs out. `softlock`: a floor budget passes with no floor progress on a policy that promises progress. `crash`: an exception, and the log holds it. | Resolves OQ-138. Applies D-115 and D-127. Revises in part D-149, the end states of the PR-11 scope only. Binds PR-11 and PR-58. The random walker ends by budget, and the zero-softlock test of the night reads the greedy descender. Three states with the test on the descender alone was the other option. Revised in part by D-403 on 2026-09-20, the end states only: a death is the fifth end state `death`, and the four earlier states stand. Revised in part by D-430 on 2026-09-21, the end states only: an ascend on floors 1 to 14 is the sixth end state `ascend`, and `bottom` stays for floor 15. |
| D-271 | 2026-09-10 | Bot budgets | The greedy descender has 18000 ticks per floor before the run reads softlock. The random walker wanders 36000 ticks, then the run reads budget. Both are Core constants. | Resolves OQ-139. Applies D-73 and D-270. Binds PR-11. Ten thousand night runs then take on the order of ten minutes of wall time per policy. Twice and one third were the other options. Revised in part by D-420: the floor budget of a policy that promises progress is the length of the floor timer. The wander budget stands. |
| D-272 | 2026-09-10 | Bot stream | `RngStream.Bot` is the fifth value of D-159, at the end. A bot policy draws from the Bot stream of its run seed and never from a simulation stream, so a bot run replays like a player run. | Resolves OQ-140. Applies D-159 and G-21. Binds PR-11. The Enemy stream was the other option, and PR-16 would then share it. |
| D-273 | 2026-09-10 | Night record | The night job commits one file, `night.json`, with the commit, the end time, and the status, to the orphan branch `night-results`, which holds nothing else. A PR job reads it with one fetch. The scheduled workflow has write permission on contents. | Resolves OQ-141. Applies D-117 and D-177. Binds PR-11 and PR-58. An artifact of the scheduled run and a check run on the commit were the other options. |
| D-274 | 2026-09-10 | Night event | The gate of PR-58 reads every night record the same way, whatever event ran the night. A hand run of the night workflow writes the record of D-273 at the commit it ran on, and that record counts for the gate. | Resolves OQ-142. Applies D-177 and D-273, which stand. Binds PR-58. An `event` field in the record, with a pass on the scheduled event alone, was the other option. The gate rule in the design doc and the roadmap reads "night" now, and the sequence still waits for one scheduled night before PR-58 opens (G-19). Revised in part by D-278 on 2026-09-10, the sequence wait only: one night by hand satisfies it. The gate rule stands. Revised in part by D-373 on 2026-09-16, the ref only: a night writes a record on `main` alone. The rule that every night event counts stands, and a hand run on `main` still writes a record. |
| D-275 | 2026-09-10 | Night commit | The gate of PR-58 fails a record older than 48 hours, and a record whose commit is not on the base branch of the PR. The job fetches the base branch and runs one ancestry check, `git merge-base --is-ancestor`. The failure message names the case, the record commit, and the record time. | Resolves OQ-143. Applies D-113, D-115, and D-177. Binds PR-58. The time alone was the other option, and a hand run on a feature branch could then satisfy the gate for every PR within 48 hours. Revised in part by D-538 on 2026-09-23, the record of the PR itself only: a success record of a branch night at the effective head of the PR passes its gate (D-547). The 48-hour limit and the rule for the record of `main` stand. |
| D-276 | 2026-09-10 | M-1 timing | The M-1 table fills as soon as ten Phase 1 PRs since PR-3 merge, and before PR-58. Sequence item 22 marks the table complete, and the fill does not wait for it. | Resolves OQ-144. Applies D-147. Binds M-1. The wait for item 22 was the other option. The tenth PR merged on 2026-09-10 with PR-11, while the first scheduled night blocks PR-58. |
| D-277 | 2026-09-10 | PR seed counts | The PR reachability sweep keeps 5000 seeds. The M-1 table reads one Windows CI job at 601 seconds, one second over the bound, and the night sweep of 100000 seeds covers the difference. The bound reads again at Gate 1, and a job past eleven minutes files a new question. | Resolves OQ-145. Applies D-116, which stands. Binds M-1 and Gate 1. Records F-91. A cut to 2000 seeds, and a job of its own for the sweep, were the other options. Revised in part by D-479 and D-480 on 2026-09-22: the sweep runs in a job of its own on the hosted legs, and a pull request runs 1000 seeds. The 5000 seeds on `main` stand. |
| D-278 | 2026-09-10 | Night by hand | The first night runs by hand on `main` on 2026-09-10, and its record satisfies sequence item 20, so PR-58 opens after it. The scheduled nights continue at 03:00 UTC. The M-2 table counts the seven scheduled nights, and a note beside it records the hand run. | Resolves OQ-146. Revises in part D-274, the sequence wait only: one night by hand satisfies it, and the gate rule of D-274 stands. Applies D-177, D-273, and G-19: a real record exists before PR-58 opens. Binds PR-58 and M-2. The wait for the scheduled night was the other option, and it bought nothing that the hand run does not. Two nights then run before PR-58 merges. |
| D-279 | 2026-09-10 | Dig job cap | `DigPlan.MaxJobs` is 10000. The first night measured the tail: of 75000 floors, three needed 615, 1661, and 2817 jobs, and the rest under 300. With the cap lifted, those floors generate, play to bottom, and hold less air than a normal floor. No floor that dug inside 400 jobs changes, so the bit-identity hash and the simulation version stay. | Resolves OQ-147. Applies D-109 and D-253. Records F-92. Binds PR-9 and the night. A restart of the dig after 400 jobs, and a cap of 100000, were the other options. The restart needs a retry lane in the Procgen stream and more code, and the wide cap makes the error path of a stuck floor ten seconds. Superseded by D-359 on 2026-09-14: the PR-67 measurement found 7 of 675000 floors past the cap, and a job budget of 1000 with a restart replaces the cap. |
| D-280 | 2026-09-10 | Night logs | The night workflow uploads the bot run logs as a run artifact when a step fails, with `actions/upload-artifact`, so the crash text of a failed night survives the next checkout of the runner. | Resolves OQ-148. Applies D-117 and D-270. This entry is the dependency entry of the action (G-16). Binds the night workflow, in a PR of its own. The runner checkout cleans the logs at the next job, and the crash texts of the first night were gone before a read. A local reproduction was the other option. |
| D-281 | 2026-09-10 | Second night by hand | After the fix of F-92 merges, the night runs by hand on `main` a second time, so PR-58 opens with a success record the same day. | Resolves OQ-149. Applies D-274 and D-278. Binds PR-58. The scheduled night at 03:00 UTC was the other option. The macOS CI jobs of every open PR wait for the run, because one Mac runner serves both. |
| D-282 | 2026-09-10 | Head correction, one time | The author sets the head field of the PR #39 review record to the effective head of the PR. A dated note in the record names the change and the commits after the review. The verdict stands on the diff the review read. | Resolves OQ-150. Applies D-184. One time, PR #39 alone. On every other PR the reviewer owns the review record, as the `pr-review` skill states. The hand-over named the fix commit as the effective head, and the review recorded it, while the commit that added D-279 to D-281 was the effective head. The head names the commit that adds this decision, because a register entry moves the effective head, and that commit holds the register entries alone. A repeat review was the other option. |
| D-283 | 2026-09-10 | Gate 1 timing and the M-2 count | Four more nights run by hand on `main` on 2026-09-10, one after another. The M-2 table counts the two hand runs of D-278 and D-281, those four, and the first scheduled night, seven in all. Gate 1 signs on 2026-09-11 after the first scheduled night passes on its own, and Phase 2 starts then. | Resolves OQ-151. Revises in part D-278, the M-2 count only: hand runs count. The gate rule of D-274 and the sequence wait of D-278 stand. Applies D-115, D-116, and D-150. Binds M-2 and Gate 1. A week of scheduled nights, and a sign-off today on the two hand runs, were the other options. The runs are deterministic on one machine, so a scheduled night adds no measurement, and the first one proves the schedule alone. |
| D-284 | 2026-09-10 | Night time | The night runs at 08:00 UTC, which is 02:00 Central Standard Time. The cron of GitHub reads UTC alone, so the run is at 03:00 Central Daylight Time in summer. | Resolves OQ-152. Revises in part D-278, the time only. The owner lives in the Central time zone, and 03:00 UTC was the evening before. Binds the night workflow, in a PR of its own after this record. The first scheduled night of 2026-09-11 runs at 03:00 UTC when that PR merges after it, and at 08:00 UTC otherwise. |
| D-285 | 2026-09-11 | Night minute | The night runs at 08:07 UTC, which is 02:07 Central Standard Time. A cron at the start of an hour gets delayed or dropped under load, and the 03:00 UTC run of 2026-09-11 never came (F-94). | Resolves OQ-153. Revises in part D-284, the minute only. The hour and the summer shift stand. Binds the night workflow. To keep 08:00 UTC and watch one night, and to fold the change into the open night logs PR, were the other options. Revised in part by D-286 on 2026-09-11, the time only, until the first scheduled night passes: the night runs at 17:21 UTC for one test. The 08:07 UTC time stands as the goal, and the cron returns to it after that pass. |
| D-286 | 2026-09-11 | Schedule test at 17:21 UTC | The night runs at 17:21 UTC for one test of the schedule, which is 12:21 Central Daylight Time. After the cron change merges, `gh workflow disable` and `gh workflow enable` turn the workflow off and on again. Gate 1 signs after the first scheduled night passes, and D-283 stands. After that pass, a PR returns the cron to 08:07 UTC. | Resolves OQ-155. Revises in part D-285, the time only, until the first scheduled night passes. Applies D-283. Binds the night workflow in a PR of its own, and a return PR after the pass. The 08:07 UTC night of 2026-09-11 never came either, and the repository has no schedule run in two chances (F-95). The first test runs at 17:21 UTC on 2026-09-11 when the workflow is on again before then, and on 2026-09-12 otherwise. The owner first named 11:21 UTC and then 15:21 UTC, and set 17:21 UTC when each earlier slot of 2026-09-11 passed before a merge (D-287). A disable and an enable with no cron change, a wait for 2026-09-12 with no change, a `launchd` timer on the Mac Mini, and a Gate 1 sign-off today on a hand run were the other options. |
| D-287 | 2026-09-11 | PR #46 slot swap, no repeat review | The PR #46 test slot moves from 15:21 UTC to 17:21 UTC with no Codex repeat review. The author sets the head field of the PR #46 review record to the swap commit, and a dated note in the record names the change. The verdict stands on the diff the review read at `eebb605`. | One time, PR #46 alone, as D-282 was for PR #39. Applies D-184 and D-286. The owner waived the repeat review when the 15:21 UTC slot passed before a merge. The swap changes the cron hour, the matching test strings, and the times in the registers, and nothing else. The `review-override` label was no path, because the gate fails the label on a PR that changes a workflow or a test (D-190). A repeat review was the other option, and the 17:21 UTC slot left no time for it. On every other PR the reviewer owns the review record. |
| D-288 | 2026-09-11 | Gate 1 sign-off | Gate 1 signs on 2026-09-11. The owner: "Sign Gate 1, but we need to move all scheduled runs back to 08:07 UTC." The first scheduled night, run 34600758086 of the 08:07 UTC cron on `095ce5e`, started at 12:47 UTC and passed in 58 minutes. | Resolves OQ-154. Applies D-150 and D-283. Phase 2 starts, and PR-12 opens from `main`. The night returns to 08:07 UTC, the time of D-285, and D-286 bound that return to this pass. The run came 4 h 40 min after its cron, so F-95 read a late run as a miss. A wait for a further scheduled night was the other option. |
| D-289 | 2026-09-11 | Default bindings and curves | Keyboard and mouse: WASD move, mouse look, left button attack, right button block, Space jump, Left Shift sprint, Left Control dodge, E interact, Q throwable, R reload, Tab satchel, F amulet active. Controller: left stick move, right stick look, A jump, B dodge, right trigger attack, left trigger block, left bumper sprint, right bumper throwable, X interact, Y amulet active, D-pad satchel, view button reload. Curves: a linear mouse, and a cubic stick curve with a 15 percent dead zone. | Resolves OQ-47. Binds PR-12. The recommendation of OQ-47 stands, except that the owner swapped the two keyboard keys: sprint on Left Shift and dodge on Left Control. The owner first wrote "slide" for the Control key and then corrected it to dodge, so D-27 stands. |
| D-290 | 2026-09-11 | Three PRs in one run | The run of Sessions 116, 118, 120, and 121 opens three PRs: PR #46, the Gate 1 record, and the return of the night to 08:07 UTC. The return PR opens from `main` after the Gate 1 record merges, takes a Codex review, and merges before 08:07 UTC on 2026-09-12. The 17:21 UTC scheduled run of 2026-09-11 stays. | Resolves OQ-156. A one-time exception to D-121 for this run alone: one run, three PRs, and a handoff entry in each. D-121 stands for every other run. Applies T-4 to the return PR. A fresh session was the other option for the record. A fold into the record, or a return with no review, were the other options for the return. To turn Night off until the return merges was the other option for the 17:21 UTC run. |
| D-291 | 2026-09-11 | Chunk size and mesh budget | The mesher emits one mesh per chunk of 16 by 32 by 16 blocks, so a maximum floor gives 64 chunk meshes. The mesh budget is 64 world meshes plus one mesh per entity. | Resolves OQ-43. Applies D-164. Binds PR-13 and its `MeshBudgetTest`. Chunks let the Game layer upload the next floor over several frames (PR-18), each chunk is one draw, and the engine culls each chunk by its own bounds. One mesh per floor was the other option. |
| D-292 | 2026-09-11 | Wall fade approach | The world shader fades each fragment inside a capsule around the segment from the camera to the player. The mesher does not change for the fade. | Resolves OQ-49 (F-40). Revises in part D-88, the effect note only: the mesher needs no per-block visibility. The decision of D-88 stands. Binds PR-13. No CPU work runs per frame, and the fade follows the camera at any speed. Per-block visibility flags in the mesher were the other option. |
| D-293 | 2026-09-11 | Look sensitivity | One pixel of mouse motion turns the look a tenth of a degree. A stick at full deflection turns the look 180 degrees per second, three degrees per tick. | Resolves OQ-157. Applies D-289. Binds `MouseHundredthsPerPixel` and `StickHundredthsPerTick` in `IntentBuilder`. PR #49 merged both constants with these numbers, so no code change follows. A full turn takes 3600 pixels of mouse motion, or two seconds on the stick. Half of both, and double both, were the other options. |
| D-294 | 2026-09-11 | Godot binary in CI | Each job of the smoke workflow restores the pinned Godot .NET binary with `actions/cache`, and it downloads the binary from the GitHub release on a cache miss. | Resolves OQ-158. This entry is the dependency entry of the action (G-16), as D-189 is for `actions/setup-dotnet` and D-280 is for `actions/upload-artifact`. Binds the smoke workflow. PR #49 merged the workflow with this step, so no workflow change follows. A miss costs one download of about 110 MB on Linux and 200 MB on macOS. A download on every run, and an install on the self-hosted runner alone, were the other options. |
| D-295 | 2026-09-11 | Deck frame target | The frame target on the Steam Deck at 800p is 90 frames per second, the top refresh rate of the Steam Deck OLED panel. The fallback is 60 frames per second if M-3 misses 90. | Resolves OQ-15. Applies D-15. Binds M-3, every render PR (F-3), and PR-54. At 90 frames per second a frame takes 11.1 milliseconds, and the render interpolates between the 60 Hz ticks (D-73). The recommendation was 60 with 40 as the fallback, and 40 alone was the other option. |
| D-296 | 2026-09-11 | Deck unit for M-3 | The owner owns a Steam Deck OLED. M-3 measures on that unit, and exit test 7 of PR-13 runs on it as written. | Resolves OQ-50 (F-41). Applies D-15. Binds M-3 and PR-13. A Deck before PR-13 was the recommendation. A Windows stand-in with a frame cap, and a move of exit test 7 out of PR-13, were the other options. |
| D-297 | 2026-09-12 | Docs PR in the same session | A session can open a documentation PR after its code PR merges. The one-PR rule of D-121 binds code PRs alone. Each PR has its own handoff entry. | Owner instruction, 2026-09-12. Revises in part D-121, the count of PRs only: the one-invocation rule stands, and D-146 gives one handoff entry per PR. Applies D-188: a documentation PR changes no code and merges with the `review-override` label. D-290 was a one-time exception, and this decision makes the documentation case the rule. A fresh session for the merge record of PR #52 was the other option. Superseded by D-375 on 2026-09-16: no session opens a second PR, and no PR exists only to record the merge of an earlier PR. |
| D-298 | 2026-09-12 | Animation keyframe format | One JSON file per animation at `content/models/<model>.<animation>.json`, next to its model, with a `model` field that names the model file relative to the content directory. Each bone has a list of keyframes with a `tick` at 60 Hz and a `rotation` of three euler degrees. The euler order is the order of Blockbench: a vector turns about X, then about Y, then about Z. Interpolation is linear. The `phases` list covers tick 0 to the `length` with no gap and no overlap, and each phase has a tag of `windup`, `active`, `recovery`, or `idle`. Every content source skips the `models/` directory. | Resolves OQ-45. Applies D-87 and D-168. Binds PR-57, which reads every keyframe in its clip check, and PR-15, which plays the file. The recommendation of OQ-45 stood, and the owner gave the answer before PR-57, because exit test 1 of PR-57 names a keyframe. A rest-pose check in v1 was the other option for PR-57. The Core loader errors on a `.json` file in a directory that it does not claim, so the sources skip the model directory, and the animation stays out of the content hash of D-163. The Blockbench order keeps a keyframe copied from the editor. The `content/animations/` directory of the PR-15 roadmap line was the other location. |
| D-299 | 2026-09-12 | Assets project | A fifth project, `WhatYouCarry.Assets`, holds the model reader, the animation reader, and the pose math. It has no engine dependency, and it references Core alone. Game and Tools reference it. `det-lint` does not read it. | Resolves OQ-162. Applies D-108 and D-111. Binds PR-57 and every later PR that reads a model. Game and Tools are the two concrete users of one reader. Game converts each Core vector to an engine vector when it builds a node. A reader in Core needed the JSON types on the allowlists of D-207 and D-208, and a second reader in Tools was the third option. |
| D-300 | 2026-09-12 | Overlay rule | An armor overlay is a `.bbmodel` under `content/models/armor/`. Each overlay box carries the name of the body box that it covers, and it encloses that box on every axis at the rest pose. The body is `content/models/player.bbmodel`. A name that matches no body box is a finding. | Resolves OQ-163. Applies D-82 and D-135. Binds PR-57 and PR-22. The overlay box takes the bone of the body box that it names, so the clip check poses it with the body. A match by bone and a deferral to PR-22 were the other options. |
| D-301 | 2026-09-12 | Clip rule | Two boxes clip when they penetrate each other by a positive depth at the rest pose or at any keyframe. A shared face is not a clip. A box under a bone and a box under the parent of that bone are exempt at every pose, because that overlap is the joint. Every other pair counts, two boxes of one bone included. | Resolves OQ-164. Applies D-135. Binds PR-57 and PR-49. The first answer was zero tolerance for every pair, and the owner revised it in the same batch: a lower arm that bends at its elbow pivot swings into the upper arm box at every pose. The code holds a float noise epsilon of one hundredth of a millimeter, and an overlap under it is a touch. A depth tolerance for every pair was the other option. |
| D-302 | 2026-09-12 | File name reference rule | A string value in a content JSON file, a model file, or an animation file that ends in `.json`, `.bbmodel`, or `.png` is a file name reference. It resolves relative to the content directory, and each segment must equal one directory entry with the same case. | Resolves OQ-165. Applies D-135 and D-191. Binds PR-57. The check reads directory entries and never asks the file system whether the file exists, because a case-insensitive file system says yes to the wrong case. A named field list and a deferral of the check were the other options. |
| D-303 | 2026-09-12 | Manual automated pass | When gitar reports that automatic reviews are paused for the period, the author posts the comment `Gitar review` on the PR, and that runs the pass on demand. The pass then counts as the pass of D-250. | Owner instruction, 2026-09-12. Applies D-250. Binds every PR. The first case was PR #54, where the pass paused after the fix push and the comment ran one within a minute. The handoff entry records the manual trigger. Revised in part by D-374 on 2026-09-16, the trigger only: the author posts `Gitar review` when the review of the head is stale or not proven current. The on-demand pass and its count as the pass of D-250 stand. Revised in part by D-542 on 2026-09-23, the request only: the author posts no `Gitar review` comment during the pause. The rest stands. |
| D-304 | 2026-09-12 | Palette | The palette of the atlas is candidate A, "Lamp and Rock": eight ramps of four colors from dark to light. The ramps are rock, slate, timber, ochre, rust, water, lichen, and bone. `content/textures/palette.json` holds the 32 values. | Resolves OQ-1. Applies D-59, D-83, D-85, and D-210. Binds PR-14 and every later texture. The owner chose from a preview page of three candidates, each on the seven blocks and a miner at game zoom. 19 of the 32 colors are below 0.50 of OKLab lightness. Neutral rock, cool slate, and water answer the brown-corridor risk of D-210, and skin, rust, and lichen stay for the color roles of OQ-61. Candidate B, one stone ramp of eight steps and six ramps of four, and candidate C, eight cold ramps of low chroma, were the other options. Revised in part by D-528 on 2026-09-23, the count of colors only: each ramp gains nine shades between its four steps. The eight ramps and the 32 colors stand. Revised in part by D-530 on 2026-09-23, the count of ramps only: a ninth ramp, umber, follows the eight. The eight ramps and their colors stand. |
| D-305 | 2026-09-12 | Texture file home | The palette and the rules live under `content/textures/`, and every content source skips that directory, as it skips `content/models/`. The generator writes `content/textures/atlas.png`, the repository holds that file, and Game reads it at boot. A test fails when the committed atlas differs from the generator output. | Resolves OQ-166. Applies D-91, D-163, and D-298. Binds PR-14. The Core loader stops on a JSON file that no content type claims, and the simulation never reads a texture. A palette change leaves the content hash as it was, so a suspended run still resumes (D-97). The PNG holds stored deflate blocks, so the file has one byte form on the three platforms. Two Core content types, and an atlas that Game builds at boot, were the other options. |
| D-306 | 2026-09-12 | Contact sheet render | The Game flag `--contact-sheet <png>` renders the contact sheet with the world shader, the atlas, the scene light, and the camera of play, and then quits. Each shot stands at the boom length of 3 meters, at about 5.4 pixels per texel, the scale of play on the 800 rows of the Deck screen. The render needs a window, so it runs on a desktop and not in CI. | Resolves OQ-167. Applies D-83 and D-133. Binds exit test 5 of PR-14. The sheet shows the seven blocks and the body from two sides. A headless run is an error line and exit code 1, and a test of the Smoke category holds that. A software render in Tools was the other option, and it approximates the shader and the light. |
| D-307 | 2026-09-12 | Texture scope of PR-14 | PR-14 ships ten rule files: one for each of the seven blocks of D-259, and skin, cloth, and leather for `player.bbmodel`. The tile of a block is its id, and the three body tiles are 8, 9, and 10, the start of the second row of the atlas. | Resolves OQ-168. Applies D-85 and D-259. Binds PR-14 and PR-22. The roadmap line "model textures beyond the first three materials" names no material, and the three body materials fill it. Three rule files in all, and the seven blocks with no body texture, were the other options. Revised in part by D-330 on 2026-09-13, the rule count only: an eleventh rule paints the metal tile 11. The ten rules and their tiles stand. Revised in part by D-505 on 2026-09-22, the fixed tile places only: the layout of the generator places each block and each face. The materials stand. |
| D-308 | 2026-09-12 | Body texel density | Each face of `player.bbmodel` reads a rectangle of its body tile at the world density of 32 texels per meter, so a head face is 16 by 16 texels. The file resolution is 256, the side of the atlas. The head reads skin, the torso and the arms read cloth, and the legs read leather. | Resolves OQ-169. Applies D-82, D-83, and D-85. Binds PR-14, PR-22, and every later model. The old face map was the 64-pixel net of a Minecraft skin at 16 texels per meter, half the density of the world. One whole tile per face, and the old net in a region of four tiles, were the other options. Revised in part by D-506 on 2026-09-22, the file side only: the atlas is 512. The density of 32 texels per meter and the material of each box stand. Revised in part by D-505 on 2026-09-22, the rectangle of a body tile only: each face reads its own recipe canvas. |
| D-309 | 2026-09-12 | First contact sheet | The owner approves the first contact sheet of PR-14 as drawn. The ten rules stand as the sheet shows them, as base color index, noise, and edge: raw stone 1, 0.40, 0. Hewn stone 6, 0.20, 1. Timber beam 9, 0.30, 1. Ore vein 12, 0.55, 0. Still water 21, 0.15, 0. Rubble 0, 0.60, 0. Plank 10, 0.25, 1. Skin 29, 0.12, 0. Cloth 17, 0.12, 0. Leather 8, 0.12, 0. The seed of each rule is 1000 plus its tile. | Closes exit test 5 of PR-14. Applies D-83, D-304, D-306, D-307, and D-308. Binds PR-14 and every later change to a rule, which needs a new sheet. The sheet showed the seven blocks and the body from two sides at game zoom, and the tiles equal the preview of OQ-1 pixel for pixel. A noise amount is the chance that a pixel moves one step along its ramp, and the edge moves the outer ring of a tile down its ramp. A change to a material and a change to the sheet view were the other options. |
| D-310 | 2026-09-12 | Window mode | The game window opens in borderless fullscreen at the resolution of the display, on macOS, Windows, and Linux, and on the Steam Deck at its native 1280 by 800. So a 4K desktop display renders at 4K, and the Deck renders at 800p. | Owner instruction, 2026-09-12: the resolution on macOS must differ from the resolution of the Deck, because 800 rows are tiny on a 4K screen. Applies D-15. Binds PR-60. The project set no size before this decision, so the engine opened its default window of 1152 by 648. G-15 stands: the Deck at 800p stays the floor of readability and performance. A window at 80 percent of the display height, which was the recommendation, and a fixed window of 2560 by 1600 were the other options. |
| D-311 | 2026-09-12 | Test exit | During testing, the Escape key and the Start button of a controller end the game at once. The escape menu of PR-53 replaces this exit, and PR-53 removes the quit on both inputs. | Owner instruction, 2026-09-12. Binds PR-60 and PR-53. No roadmap item held an escape menu before this decision. The Escape key alone was the recommendation for the inputs. A new Phase 2 PR after PR-19 was the recommendation for the menu, and PR-19 was the other option. |
| D-312 | 2026-09-12 | Scope of PR-60 | One PR, PR-60, carries D-310 and D-311 before PR-15. The PR is an exception to G-10, for PR-60 alone. | Owner choice, 2026-09-12. Binds PR-60. Two PRs, PR-60 for the window and PR-61 for the exit, was the recommendation, because G-10 gives one concern to each PR. |
| D-313 | 2026-09-12 | User argument check | The Game layer reads its user arguments once at boot with one parser. The parser holds each flag and the count of words after it. An unknown word, an unknown flag, a repeated flag, and a flag with too few words each stop the boot with an error that names the word. PR-61 carries the parser, after PR-60 and before PR-15. | Resolves OQ-170. Owner choice, 2026-09-12. Applies T-2 and G-10. Binds PR-61 and every later PR that adds a flag. The five flags today are `--smoke`, `--bot`, `--frame-log`, `--contact-sheet`, and `--press`. The press flag of PR-60 rejects a plain word after its tick, and that check stays until PR-61 replaces it. The recommendation stood. A check in each flag, and no check, were the other options. |
| D-314 | 2026-09-12 | Stagger and weight | Heavy armor resists stagger, and light armor does not. | Resolves OQ-5 and F-21. Owner choice, 2026-09-12. Applies D-23, D-28, and D-29. Binds PR-15 and PR-22. The recommendation stood. No effect of weight on stagger was the other option. |
| D-315 | 2026-09-12 | Initial combat numbers | Player health is 100. The tier-0 sword has a windup of 12 ticks, an active phase of 6 ticks, a recovery of 18 ticks, and a damage of 34. The dodge cooldown is 45 ticks at zero weight, and a dodge moves 3 meters. The walk speed is 4 meters per second, and the sprint speed is 7. | Resolves OQ-46. Owner choice, 2026-09-12. Applies D-25, D-28, D-123, and D-153. Binds PR-15. At 60 Hz the windup is 0.2 seconds, the active phase 0.1, the recovery 0.3, and the cooldown 0.75. Three hits of the sword kill an enemy with 100 health, which matches D-25. The recommendation stood. A later change to a number needs a decision (D-123). |
| D-316 | 2026-09-12 | Weight in PR-15 | PR-15 builds the zero-weight case alone: the dodge cooldown of D-315 and the stagger rule of light armor. PR-22 sets the two numbers of weight with the first armor sets of OQ-10: the growth of the dodge cooldown with weight, and the weight at which armor resists stagger. | Owner choice, 2026-09-12. Applies D-23, D-28, D-123, D-314, and D-315. Binds PR-15 and PR-22. PR-15 has no armor, so the weight of the player is always zero there. Exit test 1 of PR-15 drops its weight clause, because PR-22 already holds `Weight.cs` and `WeightSlowsDodge`, and its armor sets give weight a scale. The recommendation stood. Both numbers before PR-15, with a new question that blocks PR-15, were the other option. |
| D-317 | 2026-09-12 | Ignored flags | The user argument parser of D-313 also stops the boot on a flag that the session ignores. The contact sheet flag takes no other flag, because the contact sheet starts no loop. The smoke flag and the bot flag exclude each other, because the smoke script gives every intent. The error names both flags. PR-61 carries the check with the parser. | Owner choice, 2026-09-12. Applies T-2 and D-313. Binds PR-61 and every later PR that adds a flag. D-313 stands whole, and this decision adds a fifth kind of bad argument to its four. Before PR-61, `--contact-sheet sheet.png --smoke` rendered the sheet and ignored the smoke flag, a frame log there wrote an empty file, and `--smoke --bot` never used the bot. The recommendation stood. A later PR, and no check of combinations, were the other options. |
| D-318 | 2026-09-13 | README in PR-15 | The root file `README.md` gives the steps to launch the game: the .NET SDK and the Godot .NET build, the build command, the play command, the controls, and the test exit. The PR-15 branch carries it, as an exception to G-10 for PR-15 alone. | Owner choice, 2026-09-13. Applies T-5. Binds PR-15. `README.md` is outside the eligible set of D-190, so a PR with that file alone needs a cross-provider review, and D-190 stands. A PR of its own with a Codex review was the recommendation. A change to the eligible set of D-190, and a file under `docs/`, were the other options. |
| D-319 | 2026-09-13 | Sprint speed | The sprint speed is 7 meters per second, as D-315 states. D-231 held 6.5, and neither decision marked the other. A sprint in water takes the factor one half of D-261, 3.5 meters per second. | Owner choice, 2026-09-13. Applies D-315, which stands. Binds PR-15. The recommendation stood. The 6.5 of D-231 with a correction of D-315 was the other option. |
| D-320 | 2026-09-13 | Attack bit after PR-15 | The attack bit swings the main weapon: the first weapon definition of the content set, in the ordinal path order of the typed lists, until the loadout of PR-30 (D-153). The Phase 1 shot of D-265 ends. The projectile tests fire through the projectile simulation, and the bit-identity sweep folds a projectile run of its own. PR-24 puts a ranged weapon on the attack bit. | Owner choice, 2026-09-13. Supersedes D-265, which ran until PR-15 gave the loadout a weapon. Revises in part D-267, the shot of the attack bit only. Binds PR-15, PR-24, and PR-30. T-2 makes a content set with no weapon definition an error. PR-10 exit test 5 replays no shot now, because no intent fires one. The recommendation stood. The shot as a fallback for a set with no weapon, and the shot on the use bit until PR-23, were the other options. Revised in part by D-422: the main weapon is the weapon with the id `sword-basic`. The rest stands. |
| D-321 | 2026-09-13 | Sword handedness | The tier-0 sword is one-handed. A hit staggers the player out of a swing of it, and the basic kit can take a shield (D-26). | Owner choice, 2026-09-13. Applies D-26, D-29, D-153, and D-314. Binds PR-15 and PR-22. A two-handed swing keeps the hyper-armor of D-29. The recommendation stood. A two-handed sword was the other option. |
| D-322 | 2026-09-13 | Death | At zero health the run ends with the end kind `death`. The loop takes no later intent, and the state hash reads the end kind. | Owner choice, 2026-09-13. Applies D-2, D-24, and T-2. Binds PR-15, PR-27, and PR-31. Exit test 4 of PR-15 reads this end as the death event. The end kind `ascend` of D-257 stands beside it. The recommendation stood. A death event with a run that goes on at zero health was the other option. |
| D-323 | 2026-09-13 | Swing input | A swing starts on a press of the attack bit: the bit is set, and it was clear on the tick before. A press during a swing, a roll, or a stagger does nothing, and no press waits for later. A held bit swings once. | Owner choice, 2026-09-13. Applies D-232 and D-315. Binds PR-15 and PR-16. The press rule of D-267 carries over from the shot. The recommendation stood. A press buffered in the last 8 ticks of recovery, and a held bit that repeats, were the other options. |
| D-324 | 2026-09-13 | Move during a swing | A swing leaves the walk, the sprint, and the jump free. The arc of the swing follows the camera yaw on each tick. | Owner choice, 2026-09-13. Applies D-60 and D-315. Binds PR-15 and PR-16. The recommendation was a walk at half speed with no sprint and no jump, and a direction locked at the windup start. A body rooted through the swing was the other option. |
| D-325 | 2026-09-13 | Sword arc | The blade of the tier-0 sword is a line from the body center out to 1.6 meters, from 0.5 meters to 1.7 meters over the feet. Across the 6 active ticks it turns 90 degrees, from the right of the camera yaw to the left. Each active tick sweeps one sixth of the arc from the yaw of that tick, and the tick tests the wedge that it swept. A target takes one hit per swing. | Owner choice, 2026-09-13. Applies D-25, D-234, D-315, and D-324. Binds PR-15 and PR-16. The numbers live in the weapon file (D-334). The recommendation stood. A fixed box in front of the body, and an arc of 150 degrees at 2 meters, were the other options. |
| D-326 | 2026-09-13 | Stagger and guard | A stagger lasts 20 ticks. It cancels a swing in any phase, and the body cannot walk, swing, dodge, or jump until it ends. Gravity still acts. For 30 ticks after a stagger ends, no hit staggers the player. A hit during a stagger or during that guard deals damage and staggers nothing. | Owner choice, 2026-09-13, in two answers. Applies D-29, D-314, D-316, and D-321. Binds PR-15 and PR-22. The owner took the 20 ticks and added: "there needs to be a cooldown so the player cant get stunlocked". The guard answers it, and the owner chose 30 ticks after the end, the recommendation. A guard of 60 ticks after the end, and a guard of 40 ticks from the hit, were the other options. |
| D-327 | 2026-09-13 | Roll shape | A roll moves 3 meters over 18 ticks, 10 meters per second. The direction is the direction of the movement input at the press, from the camera yaw, and backward with no input. The roll does not steer, and gravity acts. The dodge cooldown of D-315 counts 45 ticks from the press. | Owner choice, 2026-09-13. Applies D-27, D-28, D-233, D-315, and D-316. Binds PR-15 and PR-22. A small stick deflection gives the full roll, because the input sets the direction alone. The recommendation stood. Rolls of 12 and 24 ticks, and a cooldown from the end of the roll, were the other options. |
| D-328 | 2026-09-13 | Roll protection | No hit lands during a roll. It deals no damage and no stagger. | Owner choice, 2026-09-13. Applies D-26 and D-27. Binds PR-15, PR-16, and PR-22. The recommendation was no protection, where a roll avoids a hit only when it moves the body out of the hit. Protection for the first 10 ticks was the other option. |
| D-329 | 2026-09-13 | Roll start | A roll starts on a press of the dodge bit, on the ground, when the dodge cooldown is ready and no stagger holds. A roll during a swing cancels the swing. No swing starts during a roll, and the jump bit does nothing until the roll ends. | Owner choice, 2026-09-13, in two answers: the start rule, and then no jump in a roll. Applies D-27, D-232, and D-324. Binds PR-15. The recommendation stood in both. A roll that waits for the end of the swing, a roll in the air, and a jump during a roll were the other options. |
| D-330 | 2026-09-13 | Sword model | The model `content/models/sword-basic.bbmodel` is the tier-0 sword: a blade of a new metal texture rule on the slate ramp, a plank guard, and a leather grip. A `weapon` locator on the right lower arm of `player.bbmodel` holds it, and the attachment points of D-18 gain the main weapon of D-20. The atlas gains the metal tile, and the owner approves a new contact sheet before the merge (D-309). | Owner choice, 2026-09-13. Applies D-20, D-82, D-83, D-85, D-304, and D-309. Binds PR-15. The metal rule paints tile 11, the next tile after the body tiles of D-307. The recommendation was the same model with the hewn stone tile for the blade and no new rule. No sword model was the other option. Revised in part by D-505 on 2026-09-22, tile 11 only: the layout places the metal recipe. The model, the metal material, and the locator stand. |
| D-331 | 2026-09-13 | First clips | PR-15 ships three animation files next to `player.bbmodel`, in the format of D-298: `player.sword-swing.json` with the windup, active, and recovery phases of D-315, `player.dodge.json` over the 18 ticks of the roll, and `player.stagger.json` over the 20 ticks of the stagger. A test matches each length to Core. | Owner choice, 2026-09-13. Applies D-87, D-298, D-315, D-326, and D-327. Binds PR-15. The session writes the keyframes, and the asset QA reads each one (D-301). The recommendation stood. The swing alone, and the swing with the roll, were the other options. |
| D-332 | 2026-09-13 | Body facing | The body always faces the camera yaw. A walk sideways is a strafe, and the swing leaves along the look. | Owner choice, 2026-09-13. Applies D-13 and D-324. Binds PR-15. The body turned with the camera yaw before PR-15, and it keeps that. The recommendation stood. A body that turns toward the move direction was the other option. |
| D-333 | 2026-09-13 | Walk cycle | The procedural walk turns the upper legs 30 degrees each way, over one full cycle per 1.2 meters of distance traveled. The arms swing 20 degrees against the legs. The lower legs and the lower arms stay straight. | Owner choice, 2026-09-13. Applies D-87. Binds PR-15. The walk is a pose of the Game layer and never simulation state. The recommendation stood. A stride of 0.9 meters at 25 degrees, and one of 1.6 meters at 40 degrees, were the other options. |
| D-334 | 2026-09-13 | Weapon file | A weapon file holds `id`, `tier`, `handedness`, `windupTicks`, `activeTicks`, `recoveryTicks`, `damage`, the arc numbers `reachCentimetres`, `arcHundredths`, `lowCentimetres`, and `highCentimetres`, and the paths `model` and `animation`. `content/weapons/sword-basic.json` is the first one. The match test reads the swing clip from `animation`, and the file case check reads both paths (D-302). | Owner choice, 2026-09-13. Applies D-87, D-92, D-153, D-168, and D-315. Binds PR-15, PR-24, and PR-43 to PR-46. Every number is a whole number, as in the projectile type. The recommendation stood. Name rules for the model and the clip, with no path field, were the other option. |
| D-335 | 2026-09-13 | Descent | Health alone carries across a descent (D-24). The swing, the roll, the stagger, the guard, and the dodge cooldown clear at the spawn of the next floor. | Owner choice, 2026-09-13. Applies D-24 and D-257. Binds PR-15 and PR-18. The recommendation stood. Counts that carry to the next floor were the other option. |
| D-336 | 2026-09-13 | Sword on the contact sheet | The two body shots of the contact sheet show the sword at the `weapon` locator, as in play. The body cells grow when the sword needs more room. | Owner choice, 2026-09-13. Applies D-306 and D-330. Binds PR-15. The recommendation stood. Two cells with the sword alone were the other option. |
| D-337 | 2026-09-13 | No roll in water | A roll cannot start while the probe of D-264 reads still water: a press of the dodge bit there does nothing, and the dodge cooldown does not start. A roll that starts on land keeps its speed over water to its end. | Owner choice, 2026-09-13, in three answers. Applies D-258, D-264, and D-329. Binds PR-15. The first answer was a roll at half speed in water, with the probe at the press, the recommendation. The owner replaced it in the same session: "let's make it so you cannot roll in water". A roll that water also ends was the other option for a roll from land. |
| D-338 | 2026-09-13 | PR-15 contact sheet | The contact sheet of PR-15 stands as a first-pass look, and not as final art. The metal rule stands as drawn: tile 11, base 7, noise 0.10, edge 0, and seed 1011. PR-15 stays on the sword mechanics, and an art quality pass waits for OQ-171. | Owner choice, 2026-09-13. Closes exit test 8 of PR-15 as a first pass. Applies D-309, D-330, and G-10. The owner first asked: "Are these just test materials? They are not high enough quality to be finished product". The answer: the art is the current output of the pipeline of D-304, D-305, and D-309, and no roadmap item raises it. The recommendation stood. An art quality PR before the merge of PR-15, and the art inside PR-15, were the other options. |
| D-339 | 2026-09-13 | Art quality pass | A new PR-62, the art quality pass, follows PR-20 in the Phase 2 sequence, before Gate 2. It covers richer texture rules, a more detailed body and sword, the scene light, and the enemy models of PR-16, with a new contact sheet for the approval of the owner. | Owner choice, 2026-09-13. Resolves OQ-171 and F-96. Applies D-59, D-81, D-82, D-83, D-85, D-305, and D-338. Binds PR-62 and PR-16. The owner answers the rule kinds and the looks in the PR-62 session, before the code. The M-3 table and the Tier 4 pass keep their places after PR-20, so they follow PR-62. The recommendation was an art PR right after PR-15 and before PR-16, so the first enemy models need no second pass. An art pass in Phase 4 was the other option. Revised in part by D-504 on 2026-09-22, the scope of PR-62 only: PR-62 is the texture recipe system, and PR-74 to PR-77 hold the body, the sword, the enemy models, and the light with the edge smoothing. The pass and its place before Gate 2 stand. |
| D-340 | 2026-09-14 | PR-15 play test | The owner played the sword of PR-15 and confirms that it feels committed and readable. Exit test 7 of PR-15 passes on the first-pass art of D-338. | Owner confirmation, 2026-09-14, after the merge of PR #62. Closes exit test 7 of PR-15. Applies D-25, D-315, D-323 to D-329, and D-338. The owner first asked whether the roll was in the game, and it worked on Left Control. PR-62 still raises the art before Gate 2 (D-339). |
| D-341 | 2026-09-14 | Dig sizes | A main gallery is 7 blocks wide and 5 high. A side drift is 5 blocks wide and 4 high. A chamber is 5 to 8 blocks high. The box size ranges of the chamber kinds grow by one half, rounded up: crosscut junction 5 to 8, stope 6 to 11, cavern 8 to 14, great stope 9 to 15, lamp room 5 to 8, ore bin 8 to 12, powder magazine 5 to 8, and pump chamber 9 to 12. | Owner choice, 2026-09-14, the wide preset. Records F-97: the owner played floor 1 of PR-15 and found the tunnels very cramped, with drifts of 3 by 3, a gallery of 5 by 3, and chambers 3 to 4 high. Applies D-166, D-253, and D-255, which stand. D-166 stays the minimum. Binds PR-63 and PR-16. The preset named box sizes of 5 to 15 in all, about one and a half times the old ranges, and the range of each kind applies that factor. A trial build before the choice, the recommendation, and a vast preset of 7 by 5 drifts and 9 by 6 galleries were the other options. |
| D-342 | 2026-09-14 | Dig sizes in the floor template | Each floor template names the gallery width and height, the drift width and height, and the chamber height range. These sizes leave the Core constants of `DigPlan`, and the validator rejects a size under the minimum of D-166. | Owner choice, 2026-09-14. Applies D-91, D-166, D-168, and D-252. Binds PR-63. A band can then change its sizes with a content edit and no code change (D-109). The recommendation stood. One set of Core constants for every floor was the other option. |
| D-343 | 2026-09-14 | One floor size | Every band uses the dig sizes of D-341 and one floor size: floors 1 to 15 are 64 by 20 by 64. The floor no longer grows with depth. | Owner choice, 2026-09-14. Revises in part D-252, the three band sizes only. The size fields and the validator bound of D-164 stand. Binds PR-63. The recommendation was the same dig sizes on every band, with floors of 96 by 24 by 96 for floors 6 to 10 and 128 by 28 by 128 for floors 11 to 15. Dig sizes that grow toward the vast preset with depth were the other option. |
| D-344 | 2026-09-14 | One room count | Every floor template holds 5 to 9 rooms with a difficulty budget of 100, the numbers of floors 1 to 5. Depth raises the threat through enemy gear tiers (D-48), the timer, and the hunter, and not through more rooms. | Owner choice, 2026-09-14. Applies D-167, D-255, and D-343. Binds PR-63, PR-16, and PR-17. The older workings held 8 to 13 rooms with a budget of 220, and the deep held 11 to 17 with 400. Those counts are unlikely to fit a floor of 64 by 64 with the chambers of D-341. No measurement shows that 5 to 9 rooms fit either, so PR-63 files a question when the seed sweep fails. The recommendation stood. A count that PR-63 measures, and larger deep floors with the old counts, were the other options. |
| D-345 | 2026-09-14 | Ramps | A ramp is a cell with a sloped floor that rises along one of four directions. A body walks up and down a ramp with no jump, and the pathfinder reads a ramp as a walk. | Owner choice, 2026-09-14. Revises in part D-78, the cell shape only. Revises in part D-165, the move rule only: a move is one block up, any drop, or a walk along a ramp. Revises in part D-253 with D-347, the tunnel slope only. Binds PR-64, PR-65, PR-66, and PR-16. Revised in part by D-486 on 2026-09-22, the move rule of the pathfinder only: the search also takes a diagonal move. The ramp cell, the walk with no jump, and the move rule of `Reachability` stand. The owner wrote that "staircases are less preferable to ramps for smooth transitions between areas with height difference". The PR-64 session decides the encoding of a ramp cell inside D-164, or with a decision that revises it, and the owner answers the motion on a ramp there, before the code. The recommendation stood. Half-block steps with an automatic step-up, and a one-block step-up with no jump, were the other options. |
| D-346 | 2026-09-14 | Ramp slopes | Each floor template lists the slopes of its ramps, and the generator picks the slope of each ramp from the list with the Procgen stream (D-159). Every band lists 1:2, 1:3, and 1:4: a rise of one block over two, three, or four blocks, near 27, 18, and 14 degrees. | Owner choice, 2026-09-14. Applies D-342 and D-345. Binds PR-64, PR-65, and PR-66. The owner asked for "angled ramps with different slopes between areas depending on the level generator". The recommendation stood. Slopes that steepen with depth, and a slope of 1:1 on every band, were the other options. |
| D-347 | 2026-09-14 | No steps in tunnels | The generator digs no one-block step in a tunnel. A tunnel changes height by a ramp or by a shaft alone. A jump still clears one block for rubble, pools, and ledges (D-165, D-258). | Owner choice, 2026-09-14. Revises in part D-253 with D-345, the tunnel slope only. Applies D-254 and D-258. Binds PR-66. The recommendation stood. Ramps with a small share of one-block steps was the other option. |
| D-348 | 2026-09-14 | Chamber tiers | A chamber can hold a tier: a raised floor inside the chamber, and a ramp joins it to the chamber floor. Ramps serve tunnels and chambers both. | Owner choice, 2026-09-14. Revises in part D-253, the chamber shape only: some chambers hold a tier. Applies D-345. Binds PR-66 and PR-16. The owner asked for larger spaces "for more dynamic combat encounters", and a tier gives a fight two levels. D-349 sets the height, and D-350 sets which chambers get one. The recommendation was ramps in tunnels alone, with tiers in a later item. |
| D-349 | 2026-09-14 | Tier height | A tier rises 2 blocks over its chamber floor. A jump cannot reach it (D-165), so its ramp is the way up. A chamber of 5 blocks keeps 3 blocks of air over the tier (D-166). | Owner choice, 2026-09-14. Applies D-165, D-166, D-341, and D-348. Binds PR-66. The recommendation stood. A tier of 1 block that a jump reaches, and a tier of 2 or 3 blocks by chamber height, were the other options. |
| D-350 | 2026-09-14 | Tier chance | Each chamber kind names a tier chance in percent. The first values: great stope 50, cavern 40, stope 25, ore bin 25, pump chamber 25, and crosscut junction, lamp room, and powder magazine 0. | Owner choice, 2026-09-14. Revises in part D-255, the field list only. Applies D-348 and D-349. Binds PR-66. Large kinds get tiers often, and small rooms never. The owner answers the shape of a tier in the PR-66 session, before the code: its share of the chamber, and a chamber too small for a tier and its ramp. The recommendation stood. One chance in the floor template, and a tier in every great stope and cavern, were the other options. |
| D-351 | 2026-09-14 | Dig work before PR-16 | The dig sizes and the ramps come after PR-15 and before PR-16 in the Phase 2 sequence. | Owner choice, 2026-09-14. Applies D-121 and G-10. Binds PR-63 to PR-66 and PR-16. The roadmap splits the work into four items, one concern each: PR-63 the dig sizes, PR-64 the ramp cells in Core, PR-65 the ramp meshes in Game, and PR-66 the ramps and the tiers in the generator. The pathfinder and the spawns of PR-16 then build once, on the final shape of the floor. OQ-9 can take its answer at any time before PR-16. The recommendation stood. The dig work after PR-16, and after Gate 2, were the other options. |
| D-352 | 2026-09-14 | Odd tunnel widths | The validator rejects an even gallery or drift width, and the error names the field. It also rejects a tunnel width past the rock shell of the floor, and a dig height that leaves fewer than three rows of the floor: the base row, one floor row, and the top row. | Owner choice, 2026-09-14, in the PR-63 session. Applies D-164, D-166, and D-342, which stand. Binds PR-63. The brush of a walker has its center on the cell that the walker stands on, and only an odd width puts the same count of columns on each side. D-341 uses 7 and 5, so no content changes. The dig plan also stops on an even width in a template that no validator read, and it never digs a narrower tunnel (T-2). The recommendation stood. An even width with its extra column on the +X or +Z side, and a half width in the template, were the other options. |
| D-353 | 2026-09-14 | The dig tail | PR-63 merges with the job cap of D-279. A new roadmap item, PR-67, comes right after PR-63: when a dig passes a job budget, the generator digs the floor again from the next draws of the Procgen stream, in place of an error. | Owner choice, 2026-09-14, in the PR-63 session. Resolves OQ-172. Records F-98: on the sizes of D-341, D-343, and D-344, the two night sets of 175000 floors pass, and 7 of 500000 more floors ran the 10000 jobs with a chamber still in rock. Applies D-159, D-279, and G-10. D-279 stands until PR-67. Binds PR-63, PR-67, and PR-66. The PR-67 session asks the owner for the job budget and the count of digs before the code. The recommendation stood. The restart inside PR-63, a larger floor, and a larger cap were the other options. D-359 to D-361 give the job budget, the count of digs, and the draw on 2026-09-14. |
| D-354 | 2026-09-14 | PR-63 play test | The owner played floor 1 of PR-63 and confirms that the spaces no longer feel cramped. Exit test 6 of PR-63 passes on the dig sizes of D-341. | Owner confirmation, 2026-09-14, after the merge of PR #65. Closes exit test 6 of PR-63. Applies D-341, D-343, and D-344. F-97 stays open for the ramps and the tiers of PR-64 to PR-66. |
| D-355 | 2026-09-14 | Second code PR in one run | The run of Sessions 158 and 160 can open a second code PR: the concurrency groups of D-356. It opens from `main` after PR #66, the merge record of PR-63, merges, and it takes a Codex review. | Owner choice, 2026-09-14. A one-time exception to D-121, as D-297 revises it, for this run alone. D-121 and D-297 stand for every other run, and a fresh session that opens the concurrency PR needs no exception. Applies T-4 and D-188 to the concurrency PR. Records F-99. The recommendation stood. The merge record alone in this run, with the concurrency PR in a fresh session, was the other option. |
| D-356 | 2026-09-14 | Cancel older PR runs | Every workflow that runs on `pull_request`, and the review gate on `pull_request_target`, takes one concurrency group: the workflow name with the PR number, or with the commit when the event has no PR. The group cancels its older run on the two pull request events alone, so a newer event on a PR cancels the older run of that workflow for that PR. A push to `main` takes the group of its own commit, so no run of a merge commit cancels. The night takes no group. | Owner choice, 2026-09-14. Records F-99. Applies D-100 and D-157. Binds the nine PR workflows, `night.yml`, and the concurrency PR. The group keys on the PR number, because the ref of `pull_request_target` is the base branch for every PR. The night gate fails a cancelled record, so the night never cancels (D-274). The recommendation stood. A cancel of older `main` runs too was the other option. |
| D-357 | 2026-09-14 | CI evidence on the tip | CI on the tip of a PR counts as evidence for the effective head when every commit after the effective head is a metadata commit (D-184). The pr-review skill, `CLAUDE.md`, and `AGENTS.md` state the rule. | Owner choice, 2026-09-14. Applies D-184 and D-356, which stand. Binds the pr-review skill, `CLAUDE.md`, `AGENTS.md`, and the concurrency PR, which adds the rule to them. A metadata commit pushed right after a code commit cancels the CI of the code commit (D-356), and the code of the two commits is the same. The recommendation stood. A rule that the author pushes no metadata commit until CI on the code commit completes was the other option. |
| D-358 | 2026-09-14 | Re-run a lost leg | When a self-hosted job of a PR run ends with the annotation "not acquired", the author re-runs the failed jobs of that run. The re-run counts as CI for that head. The pr-review skill, `CLAUDE.md`, and `AGENTS.md` state the rule. | Owner choice, 2026-09-14, in the concurrency PR session. Resolves OQ-173. Records F-100: on 2026-09-14 the Mac runner ran 54 jobs and skipped 18 job messages after an acquire conflict, and GitHub cancelled two legs. Applies D-356 and D-357, which stand. Binds the pr-review skill, `CLAUDE.md`, `AGENTS.md`, and PR #67, which adds the rule to them. The recommendation stood. An automatic re-run workflow, a second runner on the Mac mini, and a re-run by hand with no rule were the other options. |
| D-359 | 2026-09-14 | Dig job budget | One dig of a floor runs at most 1000 jobs. A dig that reaches 1000 jobs with a chamber still in rock stops, and the floor generator digs the floor again. `DigPlan.JobBudget` holds the number, and the job cap of 10000 leaves Core. | Owner choice, 2026-09-14, in the PR-67 session. Supersedes D-279. Applies D-109, D-159, D-260, and D-353, which stand. Binds PR-67. Records the PR-67 measurement of 2026-09-14 over the 675000 floors of F-98: the median need is 1 job, one floor in 5500 needs more than 1000 jobs, and the largest need that passes under the old cap is 9952. Each floor over 1000 jobs digs on its second dig. A heavy floor digs 1000 jobs in about 0.55 s, and the next floor digs inside one tick, so the budget bounds the stop of the game at a descend. The floors that needed 1001 to 10000 jobs change, so the simulation version rises to 9 (D-260). The three floors of the bit-identity sweep need 7, 2, and 1 jobs, so they stay the same. The replay header holds the version, so the known answer moves from `b00814dbf25e61e8` to `efcce6816cec980e`, and the new code with the version at 8 gives the old answer. The recommendation stood. Budgets of 500, 2000, and 10000 jobs were the other options. |
| D-360 | 2026-09-14 | Count of digs | A floor gets at most 4 digs. When the fourth dig also reaches the job budget of D-359 with a chamber in rock, the floor is an error that names the digs, the budget, the chambers dug, the seed, and the floor (T-2). `FloorGenerator.MaxDigs` holds the number. | Owner choice, 2026-09-14, in the PR-67 session. Applies D-353 and D-359. Binds PR-67. In the PR-67 measurement, each floor that started again dug on its second dig, at each budget from 500 to 10000 jobs. When each dig fails on its own, four failures in a row come about once in 900 trillion floors at a budget of 1000. The longest stop is then about 2.2 s. The fourth dig covers digs that do not fail on their own. The recommendation stood. Two, three, and eight digs were the other options. |
| D-361 | 2026-09-14 | Draw on a restart | A restart draws the chamber kinds again and digs a new plan on an empty grid, from the next draws of the same Procgen stream. The floor still comes from the seed and the floor number alone. | Owner choice, 2026-09-14, in the PR-67 session. Applies D-159, D-167, D-255, and D-353, which stand. Binds PR-67. The restart runs the steps of the first dig again, so the rule of the chamber draw does not change. At a budget of 1000, the second dig needed at most 19 jobs with a new draw, and at most 194 with the same kinds. Each dig is then close to an independent try, which the estimate of D-360 assumes. The recommendation stood. A restart that keeps the chamber kinds of the first draw was the other option. |
| D-362 | 2026-09-14 | Speed on a ramp | On a ramp, the speed along the slope is the flat speed: 4 meters per second for a walk, 7 for a sprint, and 10 for a roll. On a tick that starts with the body on the ground over a ramp, the horizontal velocity takes the factor that keeps that speed along the slope. The ramp under the feet center gives the slope. A move across the slope keeps its full horizontal speed. | Owner choice, 2026-09-14, in the PR-64 session. Applies D-231, D-315, D-327, and D-345, which stand. Binds PR-64 and PR-16. Straight up or down a slope, the horizontal speed is 11 percent lower on 1:2, 5 percent on 1:3, and 3 percent on 1:4. A body in the air takes no factor (D-238). The recommendation was the flat horizontal speed on every slope. A slower climb with the full speed downhill was the other option. |
| D-363 | 2026-09-14 | Down a ramp | A walk and a sprint stay on the slope on the way down a ramp: a body on the ground follows the slope with no air time. A roll does not. On the way down, the roll leaves the slope, and gravity takes over. | Owner choice, 2026-09-14, in the PR-64 session. The owner wrote: "When walking or running you stay on the slope. If you roll, gravity takes over." Applies D-231, D-327, and D-345, which stand. Binds PR-64 and PR-16. After each step of a walk, the body moves down to the slope by at most the drop of a slope of 1:2 over that step, so a body still falls from a ledge of one block. The recommendation was a body that stays on the slope in a walk, a sprint, and a roll. A body that leaves the slope in each motion was the other option. |
| D-364 | 2026-09-14 | No slide on a ramp | A body does not slide on a ramp. At rest and during a stagger, it stays where it stands on every slope. | Owner choice, 2026-09-14, in the PR-64 session. Applies D-326 and D-345, which stand: gravity still acts in a stagger, and the slope holds the body. Binds PR-64 and PR-16. A staggered enemy of PR-16 also stays in place. The recommendation stood. A slide during a stagger alone, and a slide of every body with no walk input, were the other options. |
| D-365 | 2026-09-14 | Jump from a ramp | A body jumps from a ramp as from flat ground. The ground probe reads the slope of a ramp too, so a jump starts at any place on a ramp with the jump velocity of D-231. It clears one block over the take-off point (D-165). | Owner choice, 2026-09-14, in the PR-64 session. Revises in part D-235, the ground probe only: a slope within two skins below the feet is ground. The feet center, the contact skin, and the probe depth stand. Applies D-165 and D-231, which stand. Binds PR-64 and PR-16. The recommendation stood. No jump on a ramp was the other option. |
| D-366 | 2026-09-14 | Roll on a ramp | A roll starts on a ramp as on flat ground (D-329). On the way up, the roll climbs the slope at the speed along the slope of D-362. On the way down, the roll leaves the slope, and gravity takes over (D-363). | Owner choice, 2026-09-14, in the PR-64 session. Applies D-327, D-329, D-337, D-362, and D-363, which stand. Binds PR-64. A roll straight up a slope of 1:2 moves 3 meters along the slope and 2.68 meters across the ground. The recommendation stood. A roll that an uphill slope stops, and no roll start on a ramp, were the other options. |
| D-367 | 2026-09-14 | Ramp cell ids | A ramp cell is a block id from 8 to 43, one byte, inside D-164. Ids 8 to 15 are the ramps of 1:2, ids 16 to 27 the ramps of 1:3, and ids 28 to 43 the ramps of 1:4. Inside one slope, the id is the first id of the slope, plus the rise direction times the run, plus the place. The rise directions count +X as 0, -X as 1, +Z as 2, and -Z as 3, and place 0 is the low end. A number here never changes once a floor holds it. | Owner choice, 2026-09-14, in the PR-64 session. Applies D-164, D-259, D-345, and D-346, which stand. Binds PR-64, PR-65, and PR-66. A ramp has no block kind of its own, so PR-65 picks the tile of its faces. The solid rule of D-258 reads a ramp as solid, and the collision reads its slope. Ids 44 to 255 stay free for new blocks. The recommendation stood. A second shape byte per cell, which revises D-164, and 36 ramp ids per solid block kind were the other options. |
| D-368 | 2026-09-14 | Ramp face tile | Every face of a ramp cell takes the raw stone tile, tile 1, in every band: the slope, the two sides, the two ends, and the bottom. The mesher does not read the tile of a ramp from its id. | Owner choice, 2026-09-14, in the PR-65 session. Applies D-259, D-307, D-309, and D-367, which stand. Binds PR-65 and PR-66. The floor of each tunnel and chamber is raw stone in all three bands, because the detail pass dresses a rock cell only when rock stands over it. A ramp then reads as the floor that it joins. The atlas, the eleven rules, and the approved sheet of D-309 do not change. The recommendation stood. Hewn stone, the tile of the block under the ramp, and a new tile with a twelfth rule were the other options. |
| D-369 | 2026-09-14 | PR-65 contact sheet | The owner approves the ramps of the PR-65 contact sheet as drawn: a ramp of each slope, 1:2, 1:3, and 1:4, at game zoom. Every face takes the raw stone tile, the side of each run comes to a point at the foot, and the vertex occlusion reads the upper half of a ramp as a block. | Owner confirmation, 2026-09-14, in the PR-65 session. Closes exit test 4 of PR-65. Applies D-81, D-306, D-308, and D-368, which stand. Binds PR-65 and every later change to the ramp mesh, which needs a new sheet. Each ramp shot keeps the whole render of 800 pixels, and its camera looks at the foot of the ramp from the boom length. The cells of the blocks and the body stay as D-309 and D-338 approved them. The recommendation stood. A change to the tile, to the occlusion, and to the view of the sheet were the other options. |
| D-370 | 2026-09-15 | The shaft landing fix | A fix PR of its own, PR-68, comes before PR-66. It finds why the shaft of seed 79146, floor 7 lands on an unreachable floor, corrects the dig, and takes that seed as a regression test. | Owner choice, 2026-09-15, after the night of 2026-09-15. Resolves OQ-174. Records F-101. Applies D-341, D-343, D-344, and G-10. Binds PR-68 and PR-66. The floor comes from the dig sizes of PR-63. The same grid and the same landing stand at `e1076ca` and at `d65823c`, so PR-64 and PR-67 did not make them. `night.yml` takes a manual event, so PR-68 can run a night before it merges. The recommendation stood. The fix inside PR-66, a dig that starts the floor again on such a shaft, and a sweep that accepts the landing were the other options. |
| D-371 | 2026-09-15 | The PR-65 merge record merges with a red night gate | The merge record of PR-65 opens and merges at once, although the `night-gate` job is red. The record changes no code, and the night failure of F-101 belongs to PR-68. | Owner choice, 2026-09-15. Applies D-102, D-115, D-126, and D-297. Binds the PR-65 merge record. The record on the branch `night-results` holds a failure, so the `night-gate` job fails on every PR until a night passes (F-101). The `review-override` label carries the review of a documentation PR (D-188, D-190), and it does not cover the night gate, so the owner merges with that gate red. In the same session the owner first chose a merge after a green night, and then chose this merge. A hold of the record until PR-68 lands was the other option. |
| D-372 | 2026-09-16 | PR-68 merges with a red night gate | PR-68 merges although the `night-gate` job is red. The hand night on the branch and the local sweep of 100000 seeds carry exit test 2 of PR-68. The first scheduled night on `main` after the merge makes the gate green again. | Owner choice, 2026-09-16. Resolves OQ-175. Applies D-115, D-126, D-177, D-275, and D-370, which stand. Binds PR-68. D-275 fails a record whose commit is not an ancestor of the base branch, so a hand night on a feature branch cannot make the gate green. `main` holds the defect, and seed 79146 is deterministic, so every night on `main` fails until this fix merges. D-371 made the same choice for the PR-65 merge record. The recommendation stood. A revision of D-275 that accepts the PR head, and a hold of PR-68 until a green night on `main`, were the other options. |
| D-373 | 2026-09-16 | The night record guards the ref | The two publish steps of `night.yml` take the condition `github.ref == 'refs/heads/main'`. A night on another ref runs in full and writes no record, so it cannot replace the record of `main`. The run log carries the evidence of a branch night, and a PR body cites that run. | Owner choice, 2026-09-16, after the merge of PR #75. Resolves OQ-176. Revises in part D-274, the ref only. The rule that every night event counts stands, and a hand run on `main` still writes a record. Applies D-273 and D-275. Binds PR-69. D-275 guards the read of the record, and this decision guards the write. The hand night of PR-68 overwrote the record of `main` with a branch commit. That cost nothing over a failure, and the same write over a fresh success turns the gate of every PR red. The recommendation stood. One record for each commit, a second file for a branch, and a trap line alone were the other options. Revised in part by D-538 on 2026-09-23, the branch write only: a night on another branch writes its record to the branch `night-branch/<branch>`. The guard that `main` alone writes `night-results` stands. Revised in part by D-557 on 2026-09-23, the writer only: the promotion job also writes `night-results`, at a merge commit that a branch night tested. The ref guard of the night stands. |
| D-374 | 2026-09-16 | The gitar-review skill | The `gitar-review` skill holds the author procedure of the automated pass: the request, the proof that the review of the head is current, and the answer to each finding. The `pr-review` skill and `AGENTS.md` link to it and keep only the rules of this repo. The author posts `Gitar review` when the review of the head is stale or not proven current, and a pause note alone is not the trigger. | Owner instruction, 2026-09-16. Revises in part D-250, the skill of the author procedure only, and D-303, the trigger only. The rest of both decisions stands. Binds every PR. The rules of this repo win over the skill: the author alone answers gitar, a reply names no source of work (T-6), and a PR is ready for the other provider or the override, not for the merge. The old section of `pr-review` gave the pause note as the trigger, gave no proof that a review is current, and asked for a push after each fix. On 2026-09-16, four pull requests in two repos had a review older than the head. Revised in part by D-381 on 2026-09-16, the skill that holds the author rules of the automated pass and the link to `gitar-review`: the `review-response` skill holds them, and `pr-review` keeps the rules of the reviewer. The rest stands. Revised in part by D-542 on 2026-09-23, the push wait and the request only, during the pause. The skill and the answer to each finding stand. |
| D-375 | 2026-09-16 | One PR, one session | A session binds to one repository, one branch, one PR, and one role: author, reviewer, or correction author. It never works on a second PR. More than one clean session can work on one PR, one after the other. After the hand-over, the author session can answer the gitar findings and the review findings of its PR with no new session. The PR carries its code, tests, decisions, questions, design and roadmap state, review record, and handoff entry. No PR exists only to record the documents, the handoff, or the merge of an earlier PR. Before the merge, the documents record the complete state of the PR: the roadmap mark `Done in PR #N` and the handoff state "pending owner merge". Git and GitHub hold the merge commit and the merge time, and no document copies them. The PR names each exit test that only a run on `main` after the merge can prove, and the next session writes the result in its own handoff entry. Every PR session loads the `one-pr-one-session` skill. A session that holds the work of another PR stops with `Blocked: start a new clean session for this PR.` | Owner instruction, 2026-09-16, with the owner note of the same day that an answer to gitar or review findings needs no new session. Supersedes D-297. Revises in part D-121, the count of PRs only. The one-invocation rule stands. Applies D-146, D-182, D-184, D-187, and D-199, which stand. The handoff and the review record stay in the metadata set, so the review record stays in the PR and the effective head does not move. A roadmap or design mark moves the effective head, so the author writes it before the gitar pass and the review. D-188 and D-190 stand: a PR that changes no code and has a concern of its own still merges with the `review-override` label. D-290 and D-355 were one-time exceptions, and they stay history. Before this decision, 24 of the 76 PRs recorded the merge of an earlier PR, and each took a session, a gitar pass, and a full CI run. The harness exposes no session identity, so the clean-session rule is agent-enforced and owner-enforced. Revised in part by D-511 on 2026-09-23, the start of the reviewer session only: the author session starts it as a separate Codex process. One invocation, one PR, and one role stand. |
| D-376 | 2026-09-16 | Documentation gate | A `doc-gate` job runs on each PR on the events opened, edited, reopened, and synchronize. It fails when one of these holds. The PR does not change `docs/session-handoff.md`. The newest handoff entry does not name the PR branch in the form Branch and the name in backticks. The `## Documents` section of the PR description lacks a line for a category, holds more than one line for a category, or a line lacks the disposition `Changed:`, `Reviewed; no change needed:`, or `Not applicable:`. A reason has fewer than five words, or it says "no documentation impact". A line disagrees with the changed paths. The description or the newest handoff entry puts documents off to later work. The title or the branch names a merge record. The categories are `docs/design.md`, `docs/decisions.md`, `docs/questions.md`, `docs/roadmaps/`, `docs/runbooks/`, `docs/session-handoff.md`, `CLAUDE.md` with `AGENTS.md`, and `.claude/skills/`. The rules live in `WhatYouCarry.Tools`, command `doc-gate`. | Owner instruction, 2026-09-16, which asked for a machine check of each fact that CI can read. Applies D-118, D-356, and D-375. Binds the PR template, `CLAUDE.md`, `AGENTS.md`, the `one-pr-one-session` skill, and this PR, which creates the job (D-148, G-19). The matrix lives in the PR description and not in a file, so an edit of it moves no effective head (D-184). The job runs on `pull_request`, so a PR can change the rules that judge it. The cross-provider review of `WhatYouCarry.Tools` and `.github/` covers that change (T-4, D-190). The check reads text and paths alone. It cannot read the conversation of a session, and it cannot prove that a reason is true. The reviewer checks each reason (`pr-review`). The `review-gate` job keeps the review record rules for `docs/reviews/`. |
| D-377 | 2026-09-16 | Handoff read scope | The first action reads the newest entry of `docs/session-handoff.md`, and the read order adds the newest entry that names the branch of the session. A session reads an older entry only when one of those two entries points to it. At the end of a session, after a fetch, the session number comes from the top heading of the file, and the new entry goes in with one edit. The ten-entry limit of D-146 does not change. | Owner instruction, 2026-09-16, after the token audit of this repository. Revises in part D-187, the second read of the whole file only. The fetch and the rule of the highest number plus one stand. The audit measured 17.3% of Claude Code input and 27.5% of Codex input in the model calls after handoff and archive work. A full read of the file costs about 17800 tokens, and the newest entry about 1200. |
| D-378 | 2026-09-16 | Register lookup | A session never reads `docs/decisions.md` or `docs/questions.md` in full. It looks up every D-# and OQ-# of the task with one command, early in the session: the row of each id, and each line that revises or supersedes one of them. `AGENTS.md` holds the command. A session reads section 6 of `docs/design.md` in full, finds another section by its heading, and reads the roadmap entry of its PR. | Owner instruction, 2026-09-16, after the token audit. Applies D-186. A lookup late in a session costs a model call over the whole context, so the command puts all ids in one call. The audit measured 10.1% of Claude Code input in register text that later calls sent again. |
| D-379 | 2026-09-16 | Handoff rotation command | The `handoff-rotate` command of `WhatYouCarry.Tools` keeps the 10 newest entries of `docs/session-handoff.md` and moves each older entry, with its text intact, to the top of `docs/session-handoff-archive.md`. It prints the moved session numbers and the next session number. It fails and changes nothing when the handoff has no entry, holds a number two times, puts an older entry above a newer one, or when the archive top is not older than a moved entry. A session runs it after it adds its entry. | Owner instruction, 2026-09-16, after the token audit. Applies D-146 and D-187. No new dependency. The archive write comes before the handoff write, so a stop between the two writes leaves a state that the next run names. A seed-loop test proves that each entry keeps its text and its order. The audit measured 6.1% of Claude Code input and 12.0% of Codex input in the model calls after archive work. |
| D-380 | 2026-09-16 | One wait for checks | After each push, a session waits on the checks of the PR with one blocking command, `gh pr checks --watch` and then one `gh pr checks` for the final state, in the background when the harness permits that. It runs no other status command while the wait runs. The `one-pr-one-session` skill holds the command. Each wait of the `gitar-review` skill runs as one shell loop that prints only the final state. | Owner instruction, 2026-09-16, after the token audit. Applies D-251, D-356, D-357, D-358, and D-374. The command waits for every check and has no `--fail-fast`, because `evaluate` fails until a review record exists. The `gitar-review` skill does not change, because other repositories use the same file. The audit measured 17.2% of Claude Code input and 22.8% of Codex input in the model calls after a status poll or a wait. |
| D-381 | 2026-09-16 | Review skills by role | The `pr-review` skill holds the procedure of the reviewing provider. The new `review-response` skill holds the procedure of the author: the answer to review findings, the response file, and the automated pass of gitar. The author reads the two shared sections of `pr-review`, "Commit the record" and "Session end gate", with one bounded command. | Owner instruction, 2026-09-16, after the token audit. Revises in part D-374, the skill that holds the author rules of the automated pass and links `gitar-review`. The rest of D-374 stands. The author path falls from 35075 bytes to about 8500 bytes, and the reviewer path from 35075 to about 30500. T-4 and D-101 do not change. Revised in part by D-383, the read mechanism of the two shared sections only: the split of `pr-review` moves them to `references/commit-and-push.md`, and the author reads that file. The role split stands. |
| D-382 | 2026-09-16 | Context budget | A test fails when `CLAUDE.md` or `AGENTS.md` holds more than 15000 bytes, a skill more than 12000 bytes, `pr-review` more than 31000 bytes, `docs/session-handoff.md` more than 60000 bytes, or its newest entry more than 7000 bytes. The failure names the file, the size, the ceiling, and this decision. A larger ceiling needs a decision. | Owner instruction, 2026-09-16, after the token audit. The limit of 7000 characters on the `one-pr-one-session` skill (D-375) stands beside it. From 2026-09-08 to 2026-09-16, `CLAUDE.md` grew 57%, the handoff 53%, and `docs/decisions.md` 161%. Revised in part by D-384, the ceiling of a skill file alone. |
| D-383 | 2026-09-17 | The skill port | One PR ports the skill improvements of the second repository of the owner into this repository. The port holds seven items. The `gitar-review` skill takes the effective head and the metadata set of D-184, so a commit of metadata keeps a pass current. The `ste-writing` skill takes a glossary table of the process terms, and the terms of the project areas move to a reference file. It also takes a section that states each byte ceiling. The `pr-review` skill splits into a short skill file and one reference file for each case, and it takes the comment export command and the staged read of the diff. The `one-pr-one-session` skill takes the transitional prompt of the merge and the enforcement table. The `design-doc-style` skill takes the entry list of a focused roadmap. A new runbook, `docs/runbooks/session-context.md`, holds the targeted reads, the commit command, the wait for gitar, and the comment export. A new skill, `csharp-conventions`, holds the code rules of `CLAUDE.md`, and the two agent files keep a pointer to it. | Owner answers, 2026-09-17. Binds PR-70. Applies D-184, D-374, D-378, and D-381, which stand. The port keeps every rule of this repository, and it copies no rule of the other repository. PR-69 keeps this session, and the port starts in the next clean session (D-375). Revised in part by D-516 on 2026-09-23, the trigger of the transitional prompt only: the merge itself, or `Merged PR #N`. The rest stands. |
| D-384 | 2026-09-17 | The ceiling of a skill file | Each `.md` file under `.claude/skills/` holds no more than 12000 bytes, a reference file included, and the ceiling of 31000 bytes for `pr-review` ends. The failure names the file, the size, the ceiling, and this decision. | Owner answer, 2026-09-17. Revises in part D-382, the ceiling of a skill file alone. The four other ceilings of D-382 stand. Binds PR-70, which splits `pr-review` and changes `ContextBudgetTests`. D-382 holds until that merge, because the test carries the rule. |
| D-385 | 2026-09-17 | The reference files of the session skill | The `one-pr-one-session` skill file keeps the binding, the start gate, the documents matrix, and the completion gate, and it stays under 7000 characters. The transitional prompt of the merge and the enforcement table go to reference files of that skill. A session loads a reference file at the step that needs it. | Owner answer, 2026-09-17. Applies D-375, D-382, and D-384, which stand. Binds PR-70. The cap of 7000 characters holds, because a session reads this skill at the start of every PR. |
| D-386 | 2026-09-18 | The front matter and the STE rules | The STE checker reads the front matter of a Markdown file, and it holds that text to rule 6.3 alone. No grammar rule reads the front matter. A file that opens with a `---` line has front matter, which ends at the next `---` line. An open `---` with no close is a thematic break, and the file then has no front matter. | Owner answer to OQ-177, 2026-09-18. Answers F-102, the red trunk of 2026-09-17. Applies D-139 and D-130, which stand. A description names the triggers of a skill, so the grammar rules add no value over a trigger list. The sentence limit stays, because a model reads the description to pick the skill. `SteCheckTests` pins the rule and the thematic break case. |
| D-387 | 2026-09-18 | Branch protection on the trunk | A branch protection rule on `main` requires each check that the PR gate names, and the owner keeps an override for an emergency. The owner applies the rule in the GitHub settings. No code of this repository enforces it. | Owner answer to OQ-178, 2026-09-18. Answers the second part of F-102. GitHub PR #79 merged on 2026-09-16 with five red checks, and the trunk carried two faults into every later branch. The cost of a red trunk falls on the next session, which pays with a full suite run and an unplanned repair. D-170 and D-180 note that branch protection needs a paid plan on a private repository, so the owner states when the rule is live. |
| D-388 | 2026-09-19 | Tier shape | A tier takes one of four shapes: a rectangle against one side of the footprint, the far side of a cut line across the footprint, one raised box of the union that built the footprint, or an island that touches no wall. The generator tests all four at the drawn share, and the Procgen stream draws one of the shapes that fit with equal chance (D-159). | Owner answer, 2026-09-19. Resolves OQ-179 in part. Applies D-348, D-349, and D-350, which stand. Binds PR-66. The owner answered "All four can appear", and then chose the equal draw and the fit test first. `ChamberFootprint.Make` keeps no box list today, so the raised box shape needs that list. A rectangle against one side alone, a cut line alone, and a pick by footprint area were the other options. |
| D-389 | 2026-09-19 | Tier share | The Procgen stream draws the share of the chamber floor that a tier takes, from 25 to 40 percent (D-159). | Owner answer, 2026-09-19. Resolves OQ-179 in part. Applies D-348 and D-388. Binds PR-66. The chamber floor stays the larger space, and two chambers of one kind differ. A fixed quarter, a fixed third, and a fixed half were the other options. |
| D-390 | 2026-09-19 | The ramp of a tier | The generator tests the slopes that the floor template lists, and the Procgen stream draws one of the slopes that fit beside the tier (D-159, D-346). A rise of 2 blocks costs a run of 4, 6, or 8 cells at the slopes 1:2, 1:3, and 1:4. | Owner answer, 2026-09-19. Resolves OQ-179 in part. Applies D-346, D-349, and D-388. Binds PR-66. The owner answered "Test what fits, then randomly select one". A footprint of 6 cells fits the run of 4 alone, so the draw gives one value there. The steepest slope that fits, and always 1:2, were the other options. |
| D-391 | 2026-09-19 | A chamber too small for a tier | The generator shrinks the tier toward a floor of 2 by 3 cells when no shape fits at the drawn share. It skips the tier when no shape fits at that floor beside a ramp, and it counts the skip. | Owner answer, 2026-09-19. Resolves OQ-179 in part. Applies D-350, D-388, D-389, and D-390. Binds PR-66. The tier rate of a kind then holds near its chance, and exit test 4 of PR-66 reads the rate over the chambers that can hold a tier. A plain skip, a new footprint draw, and a minimum box size for each tiered kind were the other options. |
| D-392 | 2026-09-19 | One tier on every floor | A floor whose tier-chance draws build no tier takes one tier in the first chamber that fits, in dig order (D-350, D-391). The pass reads the chambers of a kind with a tier chance over zero alone, because a kind of chance zero is a small room. A floor with no such chamber that fits holds no tier. | Owner answer, 2026-09-19. Applies D-348, D-349, D-350, D-388, and D-391, which stand. Binds PR-66. The draws alone gave 184 tiers over 1000 floors, because a tunnel that crosses a chamber keeps its cross-section and takes no tier (D-342, D-352). The measured rate of the draws holds: great stope 22 percent of its chambers, cavern 14, and the small kinds under 2. The guarantee gives about 1.2 tiers of every floor, so exit test 8 of PR-66 reads a tier on floor 1. A rise of the tier chances, and a tier over a tunnel inside its chamber, were the other options. The second one walls the mouth of a gallery with 2 blocks of tier, which is the cramped feeling of F-97. |
| D-393 | 2026-09-19 | The width of a tier ramp | The ramp of a tier takes the widest cross-section that fits, from 3 cells down to 2 (D-166). A chamber with room for no ramp of 2 cells holds no tier (D-391). | Owner answer, 2026-09-19. Applies D-348, D-390, D-391, and D-392, which stand. Binds PR-66. The straight corridor of the ramp is what a chamber most often lacks, so the narrowest ramp decides how many floors hold a tier. The measurement of 2026-09-19 over 1000 floors gives 524 tiers and 51 percent of floors with one, against 41 percent at 3 cells and 70 percent at 1. A ramp of 1 cell reads as a plank and not as the ramp of D-345, and 54 percent of the ramps took that width. Of the 524 ramps, 329 are 3 cells wide and 195 are 2. |
| D-394 | 2026-09-20 | The shafts of a floor | The dig routes a drift under a chamber after the whole dig, so a shaft of that chamber has a landing (F-103). The drift starts at a cell of the trail, and it takes a ramp down when no cell lies low enough. The pass digs no chamber, so the budget draw of D-167 stands. A floor with no shaft from the random tries then reads every chamber column in scan order. | Owner answer, 2026-09-20. Resolves OQ-180. Applies D-253, D-167, D-346, and D-347, which stand. Binds PR-66. The measurement of 2026-09-20 over 3000 floors gives 76 shafts and 2.5 percent of floors with one, against 0.045 percent on the base `27db615`. One floor in 40 holds a shaft, and 82 percent hold a chamber that could take one. The drift of the route keeps the drift height of the template (D-342), and the hole of a shaft takes no column of a ramp or of an end of one (D-345). The dig puts walkable space 4 rows under a chamber too seldom to reach that ceiling, and a change to the row spread of the dig belongs to a PR of its own. The generation of one floor costs 29 milliseconds against 21 on the base. The owner took this option over a record of the finding and a PR of its own. |
| D-395 | 2026-09-20 | Enemy families | The v1 roster holds eight families, four humanoid and four monster (D-56). The humanoids are the scavenger on floors 1 to 5, the company guard on 1 to 10, the deep cultist on 6 to 15, and the foreman revenant on 11 to 15. The monsters are the cave crawler on 1 to 10, the powder tick on 1 to 15, the drowned miner on 6 to 15, and the stone maw on 11 to 15. | Owner answer, 2026-09-20. Resolves OQ-9. Applies D-31, D-56, and D-210, which stand. Binds PR-16 and PR-36 to PR-42. A humanoid reuses the player gear pool and the player melee rules, so four of them cost less art and less code than four monsters (D-31). Each floor band of D-210 gains two families. No family flies, so the ground move rule of D-165 and D-345 serves every one. Five humanoids with three monsters, and three humanoids with five monsters and a flying lamp moth, were the other options. |
| D-396 | 2026-09-20 | The first enemy family | PR-16 ships the scavenger, a melee-only humanoid of floors 1 to 5. | Owner answer, 2026-09-20. Applies D-395. Binds PR-16. Ranged enemy attacks are PR-24, so a melee family needs no change when PR-24 lands. The phase 2 gate plays floor 1, and the scavenger is the family of that floor. The company guard needs its crossbow, and the deep cultist starts at floor 6. |
| D-397 | 2026-09-20 | The gear of a humanoid spawn | The family file names one weapon, and every spawn of the family carries it. | Owner answer, 2026-09-20. Applies D-31 and D-168. Binds PR-16. The spawn is deterministic with no extra draw, and the validator of D-168 reads an explicit field list. D-111 asks for two concrete cases before an abstraction, so a pool of loadouts waits for the second family. A small pool per family, and a roll from the full player gear pool, were the other options. |
| D-398 | 2026-09-20 | The chamber of the player spawn | The chamber that holds the player spawn point takes no enemy. The spawned weight of a floor matches the floor budget less the weight of that chamber, inside the window of one tenth (D-167). | Owner answer, 2026-09-20. Revises in part D-167, the reach of the map only: every other chamber still takes enemies for its weight, and the budget window stands. Binds PR-16. Exit test 4 of PR-16 reads the budget less the weight of the first chamber. The first room of every floor is empty, and a floor opens with no enemy in reach. Enemies in every chamber with a clear radius of 8 meters around the spawn point, and enemies in every chamber with no clear radius, were the other options. |
| D-399 | 2026-09-20 | The scavenger | The scavenger has 40 health, the box of D-165, and the basic sword that D-31 gives every humanoid. Its weight against the floor budget is 10. | Owner answer, 2026-09-20. Applies D-31, D-165, D-167, D-315, and D-396. Binds PR-16. A floor of budget 100 then holds about 10 scavengers over 5 to 9 chambers, and a room holds 1 to 4. Two or three sword hits kill one. The count is small, so the cost of the paths stays low. 70 health at weight 20, and 25 health at weight 5, were the other options. |
| D-400 | 2026-09-20 | The wake of an enemy | An enemy holds its post until the player comes inside 20 meters and a clear ray joins the two eye points. It then hunts the player. An enemy that loses the line of sight for 5 seconds returns to its post. | Owner answer, 2026-09-20. Applies D-76 and D-246. Binds PR-16. An enemy paths only after it wakes, so the cost of the AI stays with the rooms that the player enters. A wall hides the player, and a player who breaks away can lose a hunt. A wake with no line of sight and no end, and a hunt from the first tick, were the other options. Revised in part by D-419: a wave enemy hunts from its spawn with no give-up. The rule stands for the enemies of the floor plan. |
| D-401 | 2026-09-20 | The drawing of an enemy | The Game layer draws each living enemy with the body model of PR-13 and the sword of PR-15, at the position of the simulation. PR-16 adds no animation clip for an enemy, and no model of its own. | Owner answer, 2026-09-20. Applies D-82, D-83, and D-396. Binds PR-16 and PR-62. An enemy that hits the player and draws nothing is a trap in every play session. PR-62 then has a model to improve. A later PR for the drawing, and a scavenger model of its own in PR-16, were the other options. |
| D-402 | 2026-09-20 | The cadence of the scavenger | The scavenger waits 30 ticks after a swing ends before it can swing again. It swings when the two feet centers come inside 140 centimetres. It moves at 5 meters per second. | Owner answer, 2026-09-20. Applies D-231, D-315, and D-399. Binds PR-16. A swing and its cooldown are 66 ticks, about 1.1 seconds, so a pair of scavengers can hold the player between swings. The attack range sits inside the 160 centimetre reach of the sword, and the margin of 20 centimetres covers the step of the body over the 12 windup ticks. The speed is above the player walk of 4 and under the player sprint of 7, so a sprint is the one escape (D-231). 60 ticks with 4 meters per second, and 90 ticks with 3, were the other options. |
| D-403 | 2026-09-20 | A bot death is its own end state | A bot run whose player reaches zero health ends as `death`, the fifth end state. A crash keeps its meaning: an exception out of the simulation. The bot gate and the night gate fail on a crash or a softlock alone, and the night record carries the count of deaths of each policy. | Owner answer, 2026-09-20. Revises in part D-270, the end states only: the four states stand, and `death` joins them. Binds PR-16 and PR-17. Before PR-16 nothing could kill the player, so `BotRun` read a death as a crash. With the scavengers live, 60 of 60 greedy-descender seeds ended as a crash on the measurement of 2026-09-20. The count of deaths per policy over 5000 seeds measures how hard a floor is. A gate that fails on a death, and a bot sweep with no enemy, were the other options. Revised in part by D-411: the night record also carries the count of each cause. The five end states and the count of deaths of each policy stand. Revised in part by D-430: an ascend on floors 1 to 14 is the sixth end state `ascend`. The five end states stand. |
| D-404 | 2026-09-20 | The greedy descender fights what blocks it | The greedy descender keeps its path to the stairwell, and it presses the attack bit at an enemy inside the reach of its weapon. It chases nothing. | Owner answer, 2026-09-20. Applies D-127, D-149, and D-403. Binds PR-16. A descender that ignores the scavengers dies on floor 1 or 2, and the night sweep then stops covering floors 3 to 15. The policy keeps one concern, the walk down. A sprint past every enemy, and no change at all, were the other options. |
| D-405 | 2026-09-20 | The ramp wedge of PR-16 | PR-16 fixes the wedge of a body against the slope of a ramp, and it does not leave the fix to a later PR. | Owner answer, 2026-09-20. Applies D-345, D-362, and D-363. Binds PR-16. The bots of PR-16 roll, fight, and chase, so a body meets a slope in ways that no earlier policy did. The measurement of 2026-09-20 gives about one bot run in 10 that comes no nearer its goal, and a jump after 4 seconds with no gain takes that to 3 runs in 2000. The night gate needs zero softlocks over 5000 seeds. The owner took this option over a softlock allowance with a register finding, and over a wedge fix in a PR of its own before PR-16. The session states that the ramp collision is the concern of PR-64 and PR-65, so this widens PR-16 past one concern (G-10). |
| D-406 | 2026-09-20 | The handoff rotation repairs the order | `handoff-rotate` puts an entry that sits under an older one back in its place by number, with its text intact, and it names every entry whose place it changed. Two entries of one number stay an error, and nothing moves (D-187). `RepositoryFilesHoldTheRule` asserts that the committed file needs no sort, so a file that breaks the order still fails the build legs of a branch. | Owner answer, 2026-09-20. Applies D-146 and D-379. Records F-106. Binds PR-16. Four sessions put an entry at the end of `docs/session-handoff.md`: Session 191 on the base of PR #83, and Sessions 193 and 194 on its branch, each of which failed the three build legs and held the review of PR #83 at `Blocked` for pending CI. The owner took this option over a verdict against the effective head alone, over a PR of its own for the tool, and over a merge on the evidence. It adds a third concern to PR-16, against G-10. |
| D-407 | 2026-09-20 | Timer lengths | The floor timer runs three minutes on floors 1 to 4, four minutes on floors 6 to 9, and five minutes on floors 11 to 14. A boss floor gets two more minutes than its band: floor 5 has five minutes, floor 10 has six, and floor 15 has seven (D-6). | Owner answer, 2026-09-20. Resolves OQ-4. Applies D-4, D-46, and D-140, which stand. Binds PR-17. The lengths sit in the floor template data. The timer is a limit above the two to three minutes per floor of D-4, so a careful player rarely meets the hunter. Four, five, and six minutes with three more on a boss floor, and two and a half, three and a half, and four and a half with two more, were the other options. |
| D-408 | 2026-09-20 | The hunter pace | The hunter moves at 3.5 meters per second at expiry. Its speed grows by 1 meter per second each 20 seconds after expiry, with no cap. It passes the sprint speed of 7 meters per second 70 seconds after expiry. | Owner answer, 2026-09-20. Resolves OQ-6 in part: the first speed and the speed curve. Applies D-45, which stands. Binds PR-17. The first speed is below the walk speed of 4 meters per second, so a player near the stairwell can escape. The numbers sit in data. A start at 5 meters per second with 1 more each 15 seconds, and a start at 4 that doubles each minute, were the other options. |
| D-409 | 2026-09-20 | The hunter look and sound | The hunter is the Overseer: a tall, faceless figure in a company coat and a lamp helmet. Its lamp is the only warning in the dark. Its sound is a slow boot tread and a pit whistle that comes nearer. The foreman revenants of D-395 are its dead crews, and the Overseer is taller and always alone. | Owner answer, 2026-09-20. Resolves OQ-6 in part: the look and the sound. With D-408, OQ-6 is resolved. Applies D-210 and D-395, which stand. Binds PR-14 and PR-20 for the model and the sound, and PR-17 for the id `overseer`. The name keeps the hunter apart from the foreman revenant family. The mine itself as a mass of rubble, a canary that went wrong, the name "the Foreman" for both, and a new name for the revenants were the other options. Revised in part by D-468: the spawn sound is a mine horn over falling stones, and the pit whistle goes. Revised in part by D-458: the pit whistle sounds at the spawn, and the step is a tread and a low thump. The look, the name, and the tread stand. D-460 supersedes D-458, so D-409 stands in full again. |
| D-410 | 2026-09-20 | The escalation after expiry | After expiry, a wave of enemies spawns each 30 seconds, out of the sight of the player. Wave n holds n enemies: one, then two, then three. No wave spawns while 12 escalation enemies are alive. Each spawn draws from the family list of the floor. Floors 6 to 15 spawn no escalation wave until PR-36 to PR-42 add their families (D-395). | Owner answer, 2026-09-20. Applies D-45, which stands. Binds PR-17 and PR-36 to PR-42. The numbers sit in the floor template data. On floors 6 to 15 the hunter alone gives the pressure for now, and no family appears outside its band. One spawn each 20 seconds, waves that double, and the scavenger as a stand-in on floors 6 to 15 were the other options. |
| D-411 | 2026-09-20 | The cause of a death | The `death` end state carries a cause: the id of the family whose hit took the last health, or `overseer` for the hunter. The night record carries the count of deaths of each cause for each policy. Exit test 5 of PR-17 runs the timer tester over one thousand seeds with a test switch that turns off the enemy spawns and the escalation waves. It asserts 1000 deaths of 1000, each with the cause `overseer`. | Owner answer, 2026-09-20. Revises in part D-403, the night record only: the count of deaths of each policy stands, and the count of each cause joins it. The five end states of D-403 stand. Binds PR-17. The switch is a test input, not a game option. Before this answer the run end recorded no cause, and a scavenger can kill the timer tester before the hunter. A test with the enemies live, and a test with no cause field, were the other options. |
| D-412 | 2026-09-20 | Exit test 6 of PR-17 | Exit test 6 counts the floors that the greedy descender completes or on which its timer expires. It asserts expiry on under 5 percent of those floors, and it asserts at least 500 of those floors. | Owner answer, 2026-09-20. Binds PR-17. After PR-16 the greedy descender dies before expiry in most runs, so a count over all runs can pass without a test of the timer. A floor on which the descender dies before expiry does not count. A run with the enemies off, and both assertions, were the other options. |
| D-413 | 2026-09-20 | The Overseer swing | The Overseer swings a two-handed weapon, `overseer-pick`, by the player melee rules (D-31). It deals 50 damage. The windup is 30 ticks, the active phase 6 ticks, and the recovery 30 ticks. The reach is 2 meters and the arc 90 degrees. The attack cooldown is 60 ticks. | Owner answer, 2026-09-20. Applies D-29 and D-45, which stand. Binds PR-17. Two hits kill a player at full health. The long windup can be read and rolled, so skill buys time but no escape. A touch that kills, and damage for each second of contact, were the other options. One hit that kills, and the numbers of `sword-basic`, were the other swing options. |
| D-414 | 2026-09-20 | The Overseer takes no damage | A hit takes no health from the Overseer. A hit staggers it by the rules of D-326, with the same guard. Its two-handed swing gives hyper-armor (D-29). | Owner answer, 2026-09-20. Applies D-29 and D-326, which stand. Binds PR-17. A skilled player can buy a second or two, and the guard stops a lock of staggers. No stagger at all was the other option. |
| D-415 | 2026-09-20 | The Overseer spawn | The Overseer spawns on the tick after expiry, at the reachable floor cell that the player cannot see and that is farthest from the player by path. A floor with no such cell is a fault: the run ends as a crash with its context (T-2), and the night sweep reports each such floor. | Owner answer, 2026-09-20, in two answers. Applies D-45, which stands. Binds PR-17. The cell is never in the sight of the player. A spawn at the floor entry, and a spawn at a fixed path distance, were the other options. A spawn at the farthest cell in sight, a spawn that waits, and a search that includes unreachable cells were the other options for a floor with no hidden cell. |
| D-416 | 2026-09-20 | The Overseer senses | The Overseer knows where the player is from the tick it spawns. It walks a path to the floor cell of the player, with no sight check and no give-up. | Owner answer, 2026-09-20. Applies D-45, which stands. Binds PR-17. The hunter never idles, and the player cannot hide from it. The humanoid senses of D-400 were the other option. |
| D-417 | 2026-09-20 | The stairwell after expiry | The countdown alone pauses while the player stands at the stairwell (D-140). The Overseer and the waves move and attack there. | Owner answer, 2026-09-20. Applies D-140, which stands. Binds PR-17. The stairwell is one button from an escape, so it needs no safe zone. A stairwell that the Overseer and the waves cannot attack was the other option. |
| D-418 | 2026-09-20 | The wave spawn cells | A wave enemy spawns at a post of the floor plan: an enemy spawn cell of D-398. The posts go in spawn order, and a cursor goes on from wave to wave. A post that the player can see is skipped. A floor with no post out of sight skips the spawn, and the run log records the skip. The choice draws no random number. | Owner answer, 2026-09-20. Applies D-398 and D-410, which stand. Binds PR-17. The Enemy stream of D-159 stays unused. Random reachable cells, and spawns at the Overseer, were the other options. |
| D-419 | 2026-09-20 | The wave brains | A wave enemy hunts from the tick it spawns. It walks a path to the floor cell of the player, with no sight check and no give-up, like the Overseer (D-416). | Owner answer, 2026-09-20. Revises in part D-400, for the wave enemies only: the wake and the give-up of D-400 stand for the enemies of the floor plan. Binds PR-17. A wave enemy that spawns asleep out of sight gives no pressure. A wave enemy that hunts with the give-up of D-400 was the other option. |
| D-420 | 2026-09-20 | Softlock at expiry | A run of a policy that promises progress ends as a softlock when the floor timer expires with no floor change. The floor budget of such a policy is then the length of the floor timer (D-407). The wander budget of a policy that promises no progress stands. | Owner answer, 2026-09-20. Revises in part D-271, the floor budget only: the wander budget of 36000 ticks stands. Binds PR-17. A bot that is stuck can die to the Overseer, and a death passes the gates (D-403), so a softlock hides behind it without this rule. The measurement of 2026-09-20: over 200 seeds of each policy, the greedy descender and the full clearer completed 1308 floors, and the slowest took 8266 ticks, under the 10800 of the shortest timer. Sweeps with the timer off, and a count of deaths of each cause alone, were the other options. |
| D-421 | 2026-09-20 | The timer tester | The timer tester stands at the floor entry through the expiry and after it. It never runs and never fights. It promises no progress, so its run ends by death or by the wander budget. | Owner answer, 2026-09-20. Applies D-149, which stands. Binds PR-17. The roadmap said the tester runs for the stairwell after expiry, and a sprint of 7 meters per second can escape the first pace of D-408, so exit test 5 could not hold. Exit test 5 now proves that a wait is never safe. A run 70 seconds after expiry, and a test that allows an escape, were the other options. |
| D-422 | 2026-09-20 | The main weapon by id | The attack bit swings the weapon with the id `sword-basic` until the loadout of PR-30. A content set with no weapon of that id fails to load. | Owner answer, 2026-09-20. Revises in part D-320, the choice of the main weapon only: the end of the Phase 1 shot and the rest of D-320 stand. Binds PR-17 and PR-30. The file `weapons/overseer-pick.json` sorts before `weapons/sword-basic.json`, so the first weapon in path order is then the pick. A pick inside the hunter file, and a file name that sorts last, were the other options. |
| D-423 | 2026-09-20 | The Overseer reach and body | The Overseer starts a swing when the two feet centers are 1.8 meters apart or nearer. Its speed rises on every tick: 3.5 meters per second plus 1 for each 1200 ticks after expiry (D-408). Its body is the box of D-165 until the model of PR-14. | Owner answer, 2026-09-20. Applies D-165, D-408, and D-413, which stand. Binds PR-14 and PR-17. The range keeps the margin of 20 centimeters under the reach that the scavenger has. Every tunnel of D-166 fits the box, so the pathfinder needs no change. A range of 2.0 or 1.5 meters, a speed that rises in steps, and a taller box were the other options. |
| D-424 | 2026-09-20 | The wave clock and the cap | The first wave spawns 30 seconds after expiry, and each later wave 30 seconds after the one before it. A wave that is larger than the room under the cap of 12 spawns up to the cap, and the wave number still rises. | Owner answer, 2026-09-20. Applies D-410, which stands. Binds PR-17. The Overseer alone holds the first 30 seconds after expiry. A first wave at expiry, and a wave that spawns only when it fits whole, were the other options. |
| D-425 | 2026-09-20 | The Overseer between swings | Inside its swing range, the Overseer stands still and faces the player while its attack cooldown runs. Outside the range, it walks toward the player. It never retreats. | Owner answer, 2026-09-20. Applies D-413 and D-416, which stand. Binds PR-17. The retreat of D-402 stays with the humanoid families. A retreat like D-402 was the other option. |
| D-426 | 2026-09-20 | The timer tester in the night | The night sweep runs the timer tester at five thousand seeds, after the three policies of PR-16. | Owner answer, 2026-09-20. Applies D-115 and D-421, which stand. Binds PR-17. Each run lasts at least three minutes of simulated time, so the night takes more wall time on the one Mac runner. The test suite alone, at one thousand seeds, was the other option. |
| D-427 | 2026-09-21 | Transition hitch budget | No frame on the Steam Deck is over two frames of the target rate across a floor transition. At the 90 frames per second of D-295, the budget is 22 milliseconds. If M-3 falls back to 60 frames per second, the budget is 33 milliseconds. | Owner answer, 2026-09-21. Resolves OQ-44. Applies D-295, which stands. Binds exit test 6 of PR-18. The budget follows the target rate. A fixed 33 milliseconds, and one frame of 11.1 milliseconds, were the other options. |
| D-428 | 2026-09-21 | The Deck build path | The build reaches the Steam Deck as the checkout, the Linux Godot .NET binary of D-294, and the .NET SDK of `global.json`, in the desktop mode of the Deck. One command in `CLAUDE.md` starts the bot session and writes the frame log. | Owner answer, 2026-09-21. Resolves OQ-161. Applies D-296, which stands. Binds exit test 7 of PR-13, exit test 6 of PR-18, and M-3. The export preset stays the work of PR-54. A Linux export preset in PR-18, and a move of the Deck test out of PR-18, were the other options. |
| D-429 | 2026-09-21 | The next floor worker | Core holds the worker as a pure function of the run seed and the floor number. The Game layer runs it on one task during the floor, and takes the grid at the transition. A headless run calls the function on the simulation thread. Core approves no threading type. | Owner answer, 2026-09-21. Applies D-72, D-207, and D-208, which stand. Binds PR-18. The grid is a pure result, so the two paths give one hash, and exit test 1 compares them. `System.Threading.Thread` and `Task` on the Core allowlists were the other options. |
| D-430 | 2026-09-21 | An early ascend | A bot run that ascends on floors 1 to 14 ends as `ascend`, the sixth end state. An ascend on floor 15 stays `bottom`. The bot gate and the night gate do not fail on an ascend, and the night record carries the count of ascends of each policy. | Owner answer, 2026-09-21. Revises in part D-270 and D-403, the end states only: the five earlier states stand. Binds PR-18 and exit test 5. One `ascend` state for every floor, and `bottom` with the floor number, were the other options. |
| D-431 | 2026-09-21 | The stairwell prompt | The prompt opens while the body stands on the stairwell cell, the test of the countdown pause. It closes when the body leaves the cell. The prompt does not hold the player, and the Overseer and the waves still attack. | Owner answer, 2026-09-21. Applies D-140 and D-417, which stand. Binds PR-18. A prompt that holds the player, and a prompt that holds and protects the player, were the other options. |
| D-432 | 2026-09-21 | The prompt timer test | Exit test 3 of PR-18 is a new test, `PromptIsUntimed`. It asserts that the prompt is open and the countdown holds on each tick that the prompt is open. The PR-17 test `TimerPausesAtStairwell` stays as it is. | Owner answer, 2026-09-21. Applies D-140. Binds PR-18. An extension of the PR-17 test was the other option. |
| D-433 | 2026-09-21 | The coward and progress | The coward policy promises progress. A coward run that does not reach the first stairwell before expiry ends as a softlock (D-420). | Owner answer, 2026-09-21. Applies D-420, which stands. Binds PR-18. A broken path to the stairwell then fails the gates. A coward that ends by the wander budget was the other option. |
| D-434 | 2026-09-21 | The coward in the sweeps | The PR bot job runs the coward at one hundred seeds, and the night sweep runs it at five thousand seeds, after the timer tester. | Owner answer, 2026-09-21. Applies D-430 and D-433, which stand. Binds PR-18. A coward run lasts one floor, so the cost is small, and the ascend count of the night record then reads a real policy. The tests alone, and the PR job alone, were the other options. |
| D-435 | 2026-09-21 | The Deck transition test | Exit test 6 of PR-18 is a bot session with the flag `--transitions <count>`. The session goes on for that count of descents, and the frame log records the slowest frame near each transition. The session exits 1 when one is over the budget of D-427. The owner runs the command in `CLAUDE.md` on the Deck (D-428). | Owner answer, 2026-09-21. Applies D-427 and D-428, which stand. Binds PR-18. Unit tests in CI cover the window logic. A longer M-3 table that a person reads was the other option. Revised in part by D-437: the session loads no enemy family. The flag, the window, and the exit code stand. |
| D-436 | 2026-09-21 | The smoke session order | The smoke session walks floor 1 with the greedy descender and descends first. The script of one thousand ticks then plays on floor 2 and ends the session, or a death ends it clean (D-403). | Owner answer, 2026-09-21. Applies D-114, D-149, and D-403, which stand. Binds PR-18 and exit test 7. On seed 1 the script dies to the scavengers at tick 273, before any walk after it reaches the stairwell. The descender clears floor 1 of seed 1. A walk after the script on content with no enemy, and another seed, were the other options. |
| D-437 | 2026-09-21 | The transition test with no enemies | A bot session with the transitions flag loads the content with no enemy family, so the greedy descender walks every floor of the count. | Owner answer, 2026-09-21. Revises in part D-435, the content of the session only: the flag, the frame window, and the exit code stand. Binds PR-18 and exit test 6. On seed 1 the descender dies on floor 2 to the enemies, so one run with them never reaches ten transitions. The hitch then covers the worker, the chunk swap, and the render, and the enemy nodes rebuild empty. The frame table of M-3 still measures the enemies. A new run after each death, and a smaller count, were the other options. |
| D-438 | 2026-09-21 | The night failure of the PR-17 merge | PR #85 fixes the full-clearer softlocks of the night of 2026-09-21, and a night then runs on the fix. | Owner answer, 2026-09-21. Applies D-420 and D-373, which stand. Binds PR-18. The night on `ad7589a` read a softlock of the full clearer on seeds 2100 and 2109, and the night gate of every PR then read red. A fix PR with a gate override first, and a merge of PR #85 past the red gate, were the other options. |
| D-439 | 2026-09-21 | The full clearer leaves in time | The full clearer stops the hunt for the rest of the floor when the timer left is no more than the walk that the floor still asks for. The walk is the path to the stairwell, or during a hunt the path to the enemy and from it to the stairwell, at 45 ticks for each cell plus ten seconds. The policy measures it once each second. | Owner answer, 2026-09-21. Applies D-420, which stands. Binds PR-18 and the full clearer. On seeds 2100 and 2109 the clear ended near 5,750 ticks, and the walk to the stairwell wound around the floor past expiry. On seed 2109 the walk back took about 37 ticks for each cell. A hunt order by path distance, and both changes, were the other options. |
| D-440 | 2026-09-21 | PR-18 merges with a red night gate | PR #85 merges although the `night-gate` job is red. The hand night on the branch at `bab19cc` and the local sweep of 5000 full-clearer seeds carry the proof of the fix of D-439. The first night on `main` after the merge makes the gate green again. | Owner choice, 2026-09-21. Applies D-275, D-373, and D-438, which stand. Binds PR-18. The same choice as D-372. A night on a branch writes no record, so no night of this branch can make the gate green, and every night on `main` fails until the fix merges. The hand night, run 35638438679, passed every step: the full clearer read 1039 bottoms, 3961 deaths, and no softlock, and the coward read 3908 ascends and 1092 deaths. |
| D-441 | 2026-09-21 | The HUD font | The HUD uses the default font of the engine. No font file enters the content. | Owner answer, 2026-09-21. Applies G-16, so no dependency entry. Binds PR-19. A pixel font from the content was the other option. The art pass of PR-62 can replace the font with a new decision. |
| D-442 | 2026-09-21 | The health display | The HUD shows the health of the player as a bar and a number, such as `72 / 100`, at the bottom left of the screen. | Owner answer, 2026-09-21. Applies D-36. Binds PR-19. A bar alone at the top left, and a bar and a number at the top left, were the other options. |
| D-443 | 2026-09-21 | The timer display | The HUD shows the countdown of the floor in minutes and seconds, such as `4:07`, at the top center. While the stairwell pauses the countdown, the timer dims and a paused mark shows under it. | Owner answer, 2026-09-21. Applies D-140 and D-431, which stand. Binds PR-19. A timer with no paused mark, and a timer at the top right, were the other options. |
| D-444 | 2026-09-21 | The damage numbers | A damage number shows for each hit that takes health: white for the health that an enemy loses, and red for the health that the player loses. The HUD reads the drop of health on each tick. Each number lives 0.8 seconds, rises and fades, and has a font size of 18 pixels at 800p. It stands above the screen box of the hit entity and never over it. | Owner answer, 2026-09-21. Applies D-36 and F-24. Binds PR-19. Numbers for the hits of the player alone, and larger numbers that live 1.2 seconds, were the other options. The Overseer holds no health (D-414), so a hit on it shows no number. |
| D-445 | 2026-09-21 | The boss bar placeholder | The boss bar is a wide bar with a name and a number, at the top center under the timer. It stays hidden in play until a boss exists. The screenshot fixture of PR-19 shows it with placeholder values. | Owner answer, 2026-09-21. Applies D-36. Binds PR-19 and the first boss PR. A bar at the bottom center, and a bar that always shows, were the other options. |
| D-446 | 2026-09-21 | The layout scale and the navigation fixture | The HUD and every later screen lay out on a base of 1280 by 800 pixels, and one scale fits the base to the screen: the smaller of the two ratios of the screen size to the base. The Deck gets the scale 1.0, and a screen of 1920 by 1080 gets 1.35. The fixture screen of the navigation test holds six buttons in a grid of three columns and two rows, and one slider under them. | Owner answer, 2026-09-21. Applies G-15 and D-90. Binds PR-19 and every later screen. A fixture of five buttons in one column, and a base of 1920 by 1080, were the other options. |
| D-447 | 2026-09-21 | The stairwell prompt place | The stairwell prompt stands at the lower center of the screen, clear of the health display. It names the buttons of the device of the last input: the keyboard keys, or the controller buttons. | Owner answer, 2026-09-21. Applies D-431, which stands. Binds PR-19. The screen center, and both devices at once, were the other options. |
| D-448 | 2026-09-21 | The ascend input | At the open stairwell prompt, a tap of the interact input descends, and a hold of one second ascends. The interact input is E on the keyboard and X on the controller (D-289). The Game layer sets the interact bit on the tick of the release of a tap, and the ascend bit on the tick that the hold reaches one second. A press that starts before the prompt opens does not count. | Owner answer, 2026-09-21. Applies D-257, D-289, and D-431, which stand. Binds PR-19. Core does not change, because the intent carries the bits (D-257). A new key and the D-pad, and a binding in a later PR, were the other options. Before PR-19, no key or button set the ascend bit. |
| D-449 | 2026-09-21 | The menu accept button | The A button of the controller activates the control with the focus on every screen. The boot adds it to the accept action of the engine, which has no controller input in Godot 4.7. | Owner answer, 2026-09-21. Applies D-15 and D-90. Binds PR-19 and every later screen. Menus take no play input, so A as jump in play (D-289) does not clash. The X button, and both A and X, were the other options. The smoke session of PR-19 found the gap. |
| D-450 | 2026-09-21 | The sound parameter format | One JSON file per sound holds the parameter set of sfxr: the wave type, the envelope, the frequency and its slides, the vibrato, the arpeggio, the duty and its sweep, the repeat, the phaser, the low-pass and high-pass filters with their sweeps, a volume, and a seed. A file holds one voice. | Owner answer, 2026-09-21. Resolves OQ-48. Applies D-92 and D-93, which stand. Binds PR-20. The set is well known and expressive, and a test checks each field and its range. The synthesizer writes 44.1 kHz 16-bit mono, as sfxr does. A layered file of 1 to 4 voices, and the flat set of the OQ-48 text, were the other options. Superseded by D-462. |
| D-451 | 2026-09-21 | Two-part sounds | A sound of two parts is two parameter files. The Game layer plays the two at once from one binding: the sword hit is a thump and a ring, and the hunter step is a tread and a whistle (D-409). | Owner answer, 2026-09-21. Applies D-450. Binds PR-20. The format stays pure sfxr, and each part takes its own tune. A list of voices in one file, and one voice for each sound, were the other options. Revised in part by D-458: the parts are a whoosh and a twang for the swing, a thump and a crunch for the hit, and a tread and a thump for the hunter step. The rule of two files for two parts stands. Superseded by D-462. |
| D-452 | 2026-09-21 | The audio buses | The mix has four buses: Master at 100 percent, Effects at 100 percent, Music at 70 percent, and UI at 80 percent. Each sound goes to Effects or UI, and Effects, Music, and UI go to Master. | Owner answer, 2026-09-21. Binds PR-20, PR-50 for the music, and PR-53 for the settings of OQ-69. The Music bus stays empty until PR-50. Three buses without UI, and five buses with separate player, world, and threat buses, were the other options. |
| D-453 | 2026-09-21 | The rendered sound files | The repository holds each rendered WAV file next to its parameter file under `content/audio/sfx/`. A test fails when a committed file differs from the synthesizer output, as for the atlas (D-305). Every content source skips `content/audio/`, and the Game layer reads the files at boot. | Owner answer, 2026-09-21. Applies D-305, which stands. Binds PR-20. The three CI legs prove that the render gives equal bytes on each platform. A render in the build, with the files outside git, was the other option. |
| D-454 | 2026-09-21 | The sound events | Core gives an action event on each tick for five gameplay facts: the player swing starts, the swing of the player hits a target, the player dodges, a hit lands on the player, and the countdown crosses a timer mark (D-456). The timer expiry and the hunter spawn keep their timer events. The Game layer plays the footstep and the hunter step on a stride rhythm from the Core positions, so Core holds no stride state. | Owner answer, 2026-09-21. Binds PR-20. The action events are not state, so the hash and the simulation version do not change (G-20). The fixture list of `EveryEventHasSound` holds the five action kinds, the expiry, and the hunter spawn. All nine sounds from Core events, and a Game layer that reads state changes alone, were the other options. Revised in part by D-469: the expiry has no sound of its own. |
| D-455 | 2026-09-21 | The hunter pitch | The pitch ratio of the hunter step is its speed over 3.5 meters per second, and the ratio stops at 2. The step is one octave higher at the sprint speed, 70 seconds after expiry (D-408), and it stays there. The stride rhythm follows the speed with no cap. | Owner answer, 2026-09-21. Applies D-408 and D-409, which stand. Binds PR-20 and `HunterPitchFollowsSpeed`. The sound rises while an escape is possible, and it stops before it gets shrill. A linear ratio with no cap, and one semitone for each meter per second, were the other options. |
| D-456 | 2026-09-21 | The timer marks | The countdown has ten marks: 60, 30, 10, 8, 6, 5, 4, 3, 2, and 1 seconds left. The alarm sounds at 60 and 30, and a short tick sounds at each other mark. Expiry keeps its own sound. A mark sounds only when the running countdown reaches it, so nothing sounds while the timer pauses at the stairwell (D-140), and a mark that passed does not sound again. | Owner answer, 2026-09-21. Applies D-140, which stands. Binds PR-20. The tick is a tenth sound of the first set. D-469 drops the expiry sound, and the tick reuses the alarm recording. One alarm at one minute, a repeat each 10 seconds, and one alarm with a tick each second in the last 10 seconds, were the other options. The same alarm at each mark, a rising pitch, and a replay of the last mark after the pause, were the other options for the sound and the pause. |
| D-457 | 2026-09-21 | The approval of the first sounds | For exit test 5 of PR-20, the session plays each sword sound and each hunter sound on the Mac of the owner with `afplay`, one at a time, and asks for an approval or a change of each. A play session then lets the owner hear the sounds in the game. A later decision records the approval, with the file name and the SHA-256 of each approved WAV file. | Owner answer, 2026-09-21. Binds PR-20. `afplay` alone, and a play session alone, were the other options. |
| D-458 | 2026-09-21 | The approved sword and hunter sounds | The owner approves the sword and hunter sounds of PR-20 (exit test 5), after the `afplay` review of D-457. The swing is a retro whoosh that rises and falls, with a square twang: `sword-swing-whoosh.wav` `534f6b898ca705ff395024b2cbff3c3b065334cb5578be59f247299fbf3d1f7c` and `sword-swing-twang.wav` `0f51653e7376e66be601ea129d956ec0251b5c11e8fc8e1c4d5901e607321601`. The hit is a short thud with a crunch: `sword-hit-thump.wav` `69a126940c3db2279bd5f67dc893d4b98bbc01f84b5332b55849580e8ee0a7e2` and `sword-hit-crunch.wav` `07c0c04c08236850abccdcf9df67570aa650b7715e2b904a94bf366e2af800d5`. The hunter spawn is a long pit whistle: `hunter-spawn.wav` `9046bed22bfaf93e90b4b6581f96767be4b20e79b04fa4361040743c7b9a1ff6`. The hunter step is a dark tread with a low sine thump: `hunter-tread.wav` `dc44d97d5a9c112a2621bfb9512fa34e1c2e398c4fdbd3ca5fccab405b08daf0` and `hunter-thump.wav` `540378efabaf92d07c1066ff47b082022621c819f700d86de746e8cca6e93730`. | Owner answer, 2026-09-21. Resolves PR-20 exit test 5. Applies D-450, D-455, and D-457, which stand. Revises in part D-409, where the pit whistle moves from the step to the spawn, and D-451, where the part pairs change. Each value is the SHA-256 of the committed file. The swing is a sound of two parts under D-451. The owner tried a darker whoosh, a sharper whoosh, resonant whooshes, and chiptune sweeps for the swing, a thump with a metal ring for the hit, a drone for the spawn, and a tread with a whistle or a growl for the step. The owner chose a retro style, with the rise and fall of the Minecraft sword sweep as the inspiration and no copy of it. Superseded by D-460. |
| D-459 | 2026-09-21 | The sound path | All sound stays generated in C# (D-89, D-93). A new synthesizer replaces the sfxr set: modal, noise, and tone layers. A fit command searches the parameters of each sound for the closest match to a reference recording, by spectrum and envelope. The references are for analysis alone, and the game never ships them. | Owner answer, 2026-09-21. Resolves OQ-182. Applies D-89 and D-93, which stand. Binds PR-20. D-462 supersedes D-450 for the format. The owner found the sfxr sounds poor, most of all the footstep: sfxr has held noise, crude filters, and one voice, and it cannot make real foley. CC0 recordings for foley with a synthesizer for stylized sounds, and offline AI generation, were the other options. Revised in part by D-464: analysis of the reference replaces the parameter search. The generated sound, the references, and the replacement of sfxr stand. |
| D-460 | 2026-09-21 | No approved sound stays | The new path of D-459 replaces every sound of the first set, and the sounds that D-458 approved too. No sfxr sound stays. | Owner answer, 2026-09-21. Supersedes D-458. D-457 stands, so the owner approves the new sword and hunter sounds with `afplay` and a play session, and a new decision records it. The revisions that D-458 made to D-409 and D-451 lapse with it. |
| D-461 | 2026-09-21 | The reference recordings | The references come from the CC0 packs of Kenney, and from Freesound, filtered to CC0, when Kenney has no match. The repository holds each reference under `WhatYouCarry.Tools/AudioSynth/References/` with its source and license. For each sound, the session plays two or three candidates with `afplay`, and the owner picks one by ear. The Freesound key is the variable `FREESOUND_API_KEY` of the shell profile of the owner, and never a file of the repository. | Owner answer, 2026-09-21. Applies D-459. Binds PR-20. G-16: Freesound is a data source of a Tools command, and no build or test reads it. The fit reruns exactly from the repository. A session pick by name, and a download with checksums outside git, were the other options. |
| D-462 | 2026-09-21 | The layered sound format | One JSON file per sound holds 1 to 4 layers, mixed, and a seed. A layer is a modal layer, a bank of damped resonances that a short noise burst strikes, or a noise layer, white, pink, or brown noise through a low-pass, high-pass, or band-pass filter with a cutoff envelope, or a tone layer, a sine, saw, square, or FM tone with a pitch envelope. Each layer has its own envelope and gain. A sound of parts is one file. | Owner answer, 2026-09-21. Applies D-459. Supersedes D-450 and D-451. Binds PR-20. The synthesizer writes 44.1 kHz 16-bit mono, and an unknown or absent field is an error (D-92). A prototype before the format choice was the other option. Revised in part by D-466: a fourth layer kind, the spectral layer of D-464. The three kinds, the limit of 4 layers, and one file per sound stand. |
| D-463 | 2026-09-21 | The footstep rhythm | The player footstep sounds 2.5 times each second at the walk speed of 4 meters per second, and 3 times each second at the sprint speed of 7 meters per second. Between the two speeds the rate follows a straight line. Below the walk speed a step stays 1.6 meters long, and above the sprint speed it stays 2.33 meters long. | Owner answer, 2026-09-21. Applies D-454, which stands. Binds PR-20. The step of half the walk cycle, 0.6 meters, gave 6.7 steps each second at the walk, and the owner found it too loud and too fast. Rates of 3.3 and 2 at the walk were the other options. |
| D-464 | 2026-09-22 | The spectral layer | A realistic sound comes from a spectral layer: the levels of 64 bands, every 128 samples, that the `audio-analyze` command measures from the CC0 reference of the sound. The synthesizer renders the levels as noise with random phases. The shipped WAV file is a new render, and the reference never ships (D-461). | Owner answer, 2026-09-22. Revises D-459 in part: the analysis becomes the parameters, and the parameter search goes. Applies D-93 and D-461, which stand. Binds PR-20. The search fit of the footstep sounded nothing like the reference, and the band-noise render of the same reference sounded close. A render of partial tracks with a noise residual, the direct use of CC0 recordings, and a better search were the other options. Revised in part by D-467: a pitched sound can ship as its CC0 recording, in a recording layer. The spectral layer stands for noisy sounds. |
| D-465 | 2026-09-22 | The controls of a spectral layer | A spectral layer has four controls: a trim to a length with a fade, a time stretch that keeps the pitch, a pitch shift in semitones, and a brightness tilt in decibels per octave. | Owner answer, 2026-09-22. Applies D-464. Binds PR-20. A sound then takes a change of length, speed, pitch, or color without a new reference. |
| D-466 | 2026-09-22 | The layered synthesizer stays | The layered synthesizer of D-462 stays for the stylized sounds, such as an alarm, a tick, or a whistle. One file can hold spectral layers and layered parts. | Owner answer, 2026-09-22. Revises D-462 in part: the spectral layer is a fourth kind of layer. Binds PR-20. A spectral layer for every sound was the other option. |
| D-467 | 2026-09-22 | Recordings for pitched sounds | A pitched sound, such as a horn, can ship as its CC0 recording. A recording layer plays a committed file under `content/audio/recordings/`, with a delay, a gain, a trim, and a fade, and `audio-synth` mixes it with the other layers into the one WAV file of the sound. CC0 needs no credit, and the repository keeps the source of each recording. | Owner answer, 2026-09-22. Revises D-89 and D-464 in part. Binds PR-20. The band noise of D-464 drops the pitch of a horn, and a render of partial tracks did not sound like the horn. Partial tracks, and a stylized horn from the layered synthesizer, were the other options. |
| D-468 | 2026-09-22 | The hunter spawn sound | The hunter spawn is a far mine horn over falling stones: the recording "horn 02" by Kostrava and the recording "Stones Falling" by iwanPlays, both CC0 on Freesound, with the horn louder than the stones. | Owner answer, 2026-09-22. Revises D-409 in part: the pit whistle goes. The tread and the look stand. Applies D-467. Binds PR-20. Three steam and train whistles, a siren, a fog horn, an air raid siren, and three other rockfalls were the other options. |
| D-469 | 2026-09-22 | The expiry has no sound | The timer expiry plays no sound of its own. The Overseer spawns on the tick of expiry, and its sound carries the moment (D-415, D-468). The timer tick plays the alarm recording at 0.7 of the level of the alarm. | Owner answer, 2026-09-22. Revises D-454 and D-456 in part: the expiry cue goes, and the marks stand. Binds PR-20. Two sounds on one tick say one thing twice. The stones for the expiry with the horn for the spawn, the same mix for both, and a gong under the spawn, were the other options. |
| D-470 | 2026-09-22 | The approved sword and hunter sounds | The owner approves the sword and hunter sounds of PR-20 (exit test 5), after the `afplay` review of D-457. The swing is `sword-swing.wav` `8fe02d941a5eb99f258349763878878c4ce1947a78fb33c146432da0db8a4c10`, from the CC0 recording "Swosh Sword Swing" by qubodup. The hit is `sword-hit.wav` `61254364b5b6844d554e129ec6fb78a80bbf22b3653fa27d2888ffba72d4ee96`, from "Deep Cut / Slash / Gash" by SypherZent. The spawn is `hunter-spawn.wav` `3f1510257264bb3251615460c9cefb8774b61b877a6d4cac3be19a5842a885fb`, the mine horn over falling stones of D-468. The step is `hunter-step.wav` `30cd989bbfcc98953a39b731d512b20c47dff6d24a50ae919883e0893e1f97b0`, from "HUTCH HD SFX 2014 0144" by leonelmail. | Owner answer, 2026-09-22. Resolves PR-20 exit test 5. Applies D-457, D-461, D-464, D-467, and D-468, which stand. Each value is the SHA-256 of the committed file. The swing and the hit are spectral renders of their references, and the spawn and the step are the recordings. The owner heard three to four candidates for each sound, and the sfxr sounds of D-458 before them. |
| D-471 | 2026-09-22 | The id of the CI skip | The CI skip of a documents head and the study of the CI duration take the roadmap id PR-71. The entry lives in the Phase 2 roadmap, after PR-20 and before the fixes of F-107 and F-108. | Owner answer, 2026-09-22. The owner asked for the work before the two enemy fixes. The PR holds two concerns by owner instruction, and its description records that exception to G-10. The recommendation stood. |
| D-472 | 2026-09-22 | The checks that never skip | `ste-check`, `doc-gate`, `review-gate`, `night-gate`, `det-lint`, and `asset-qa` run on each head. The CI skip of D-474 does not apply to them. | Owner answer, 2026-09-22. Each of `det-lint` and `asset-qa` takes about 30 seconds on a hosted Linux runner, so a skip saves almost nothing. The recommendation stood. A skip of `det-lint` and `asset-qa` too was the other option. |
| D-473 | 2026-09-22 | A push to main never skips | A push to `main` runs every job of every workflow, also when the commit changes documents alone. The CI skip of D-474 applies to a pull request event alone. | Owner answer, 2026-09-22. Every trunk commit keeps a full record, because F-102 showed that the trunk itself can go red. The recommendation stood. A skip on `main` too was the other option. |
| D-474 | 2026-09-22 | The two skip rules | The heavy jobs of a workflow skip a PR head when one of two rules holds. Rule 1: every path of the PR from the merge base is a document. Rule 2: every path of the push from the previous head is a document, and the newest run of the same workflow on the previous head completed with success. A new PR, a reopen, and a force push give no previous head. A run that did not pass, or no run, runs every job. The `ci-skip` command of Tools holds the rules. | Owner answers, 2026-09-22. Automates D-357, which stands: CI on a tip that follows a green head by metadata commits alone no longer runs again. The skip does not move the effective head of D-184. A run whose jobs skipped completes with success, so a second documents push skips too. The recommendation stood for both answers. The other options were rule 2 alone, and the diff from the PR base alone. |
| D-475 | 2026-09-22 | The skip set | A path is a document for the CI skip when it lies under `docs/` or `.claude/skills/`, or when it is `CLAUDE.md`, `AGENTS.md`, `README.md`, or `LICENSE`. Every other path runs every job, `content/`, `.github/`, and each project directory included. | Owner answer, 2026-09-22. The set is the eligible set of D-190 with `README.md` and `LICENSE` added, and D-190 stands with its own set. Neither added file feeds a build or a test. The owner chose it over the recommended D-190 set and over the metadata set of D-184. |
| D-476 | 2026-09-22 | The documents job | Each test class that reads a document of the checkout carries the test category `Documents`. The `documents` job of `ci.yml` runs that category on Linux on each head, and never skips. A guard test finds a class that reads a document and lacks the category. | Owner answer, 2026-09-22. About 20 tests read a document of the checkout: the byte ceilings of D-382 and D-384, the identical agent files of D-122, and the handoff rules of D-146 and D-379, for example. Without the job, a documents head that skips would pass a broken handoff in silence (T-2). The recommendation stood. A port of the rules into `ste-check`, and a skip of the metadata set alone, were the other options. |
| D-477 | 2026-09-22 | The workflows that skip | `ci.yml`, `bit-identity.yml`, `smoke.yml`, and `bots.yml` skip a documents head by the rules of D-474. | Owner answer, 2026-09-22. The first plan named the first three. `bots.yml` joins them, because it runs the Core bots for about 4 minutes, and a document cannot change its result. The recommendation stood. A bots job on each head was the other option. |
| D-478 | 2026-09-22 | The procgen class split | `ProcgenTests` holds three nested classes: the stairwell sweep, the tunnel sweeps, and the chamber sweeps. xUnit runs the tests of one class in sequence, so the nested classes run beside the reachability sweep. No test and no seed count changes. | Owner answer, 2026-09-22, in PR-71. Records F-109: the class summed to 549 seconds of the 550 seconds of a local run. The recommendation stood. A split in a later PR, and no split, were the other options. |
| D-479 | 2026-09-22 | The hosted job split | On the hosted Linux and Windows legs, `ci.yml` runs the tests in two jobs. One job runs the test category `Sweep`, and the other job runs every other test outside the category `Smoke`. The Mac leg keeps one job, because the Mac mini has one runner. | Owner answer, 2026-09-22, in PR-71. Revises in part D-277, the option of a job of its own for the sweep. The 5000 seeds of D-277 on `main` stand. The recommendation was a later PR, and the owner chose this PR. No split was the other option. |
| D-480 | 2026-09-22 | One fifth on a pull request | A pull request runs one fifth of each seed sweep: the reachability sweep at 1000 seeds, the procgen property sweeps at 200, `ReplayReproducesHash`, `CameraNeverInsideSolid`, and the enemy sweeps at 200, and `EveryPolicyEndsAtTheBottomOrByDeath` at 20 seeds per policy. A push to `main` and a local run keep the full count, and the night stays as D-116 sets it. The fixed regression floors and the rate tests keep their counts. | Owner answer, 2026-09-22, in PR-71. Revises in part D-116, the count per PR alone. The night count stands. Revises in part D-277, the PR count alone: 5000 seeds run on `main`. A fault that one seed in 5000 shows can pass a PR and fail on `main` or in the night. The recommendation stood. One tenth and one half were the other options. |
| D-481 | 2026-09-22 | The pull request signal | `ci.yml` sets `WYC_PR_SWEEP` to `1` on a pull request event and to `0` on a push. An absent variable gives the full count. Any other value fails the test and names the variable (T-2). | Owner answer, 2026-09-22, in PR-71. It follows the form of `WYC_NIGHT_SWEEP`. The recommendation stood. A variable that each run must set was the other option. |
| D-482 | 2026-09-22 | No cache of the NuGet packages | The CI jobs take no cache of the NuGet packages. | Owner answer, 2026-09-22, in PR-71. The build step, the restore included, takes 22 to 29 seconds of a leg of 18 to 21 minutes (D-109). The recommendation stood. |
| D-483 | 2026-09-22 | The id of the enemy walk fixes | The fixes of F-107 and F-108 take the roadmap id PR-72. The entry lives in the Phase 2 roadmap, after PR-71 and before PR-62. | Owner answer, 2026-09-22. PR-72 is the next free id. The recommendation stood. |
| D-484 | 2026-09-22 | One PR for F-107 and F-108 | One PR holds the fixes of F-107 and F-108. | Owner answer, 2026-09-22, in PR-72. The PR holds two concerns by owner instruction, and its description records that exception to G-10, as D-471 did for PR-71. The recommendation was two PRs with F-108 first. Two PRs with F-107 first was the other option. |
| D-485 | 2026-09-22 | The case of F-108 | Seed 1, floor 1 repeats F-108. With the player at the top of a ramp, the scavenger and the Overseer climbed the ramp in slow jumps. | Owner answer, 2026-09-22, in PR-72. Binds the regression test of PR-72. The play session of PR-20 ran `Main.FirstSeed`, which is 1. |
| D-486 | 2026-09-22 | The diagonal move | An enemy has as much freedom of movement as a player. A diagonal move to a cell is legal when two side moves reach that cell, in either order. It passes one solid corner, and the body slides along the block edge as a player does. No diagonal move passes between two solid corners at the height of the body. | Owner answer, 2026-09-22, in PR-72. The owner wrote "I want the enemies to have as much freedom of movement as a player". Revises in part D-165 and D-345, the move rule of the pathfinder only: a move of the search is one block up, any drop, a walk along a ramp, or a diagonal move of this rule. The box, the one-block jump, no automatic step-up, and the move rule of `Reachability` stand. The recommendation stood. A jump across a gap and a level diagonal alone were the other options. Revised in part by D-489 on 2026-09-22, the solid corner of a step up only: a diagonal step up passes no solid corner. The rest stands. Revised in part by D-545 on 2026-09-23, the drop only: a diagonal drop needs an open fall in the corner column. The rest stands. |
| D-487 | 2026-09-22 | The cost of a diagonal move | In the path search, a side move costs 10 and a diagonal move costs 14. The estimate to the goal is the octile distance in the same units. | Owner answer, 2026-09-22, in PR-72. The ratio is near the square root of 2, and the costs stay integers (G-9). The recommendation stood. Costs of 2 and 3, and of 1 and 1, were the other options. |
| D-488 | 2026-09-22 | The walkers of the diagonal move | The enemies, the Overseer, and the bots take the diagonal search, through the path follower that they share (D-111). `Reachability` keeps the four side moves. | Owner answer, 2026-09-22, in PR-72. Each diagonal move equals two side moves, so the floors that the generator digs do not change. The bot sweep numbers move with the rise of the simulation version (G-20). The recommendation stood. Enemies alone was the other option. |
| D-489 | 2026-09-22 | No diagonal step up past a corner | A diagonal move that needs a jump passes no solid corner: both side columns are open over the higher floor. A level move and a drop still pass one solid corner. | Owner answer, 2026-09-22, in PR-72. Revises in part D-486, the solid corner of a step up only. The rest of D-486 stands. A slide along the wall during the jump took the body past the top of its arc, so it landed short and jumped again: 13 of 431597 diagonal moves over 120 seeds, up to 121 ticks for one move. The recommendation stood. Two jumps for such a move was the other option. |
| D-490 | 2026-09-22 | The id of the documents rule | The rule of D-491 to D-495 is PR-73, the next free id. It runs now, after PR-72 and before PR-62. | Owner answer, 2026-09-22, in PR-73. Binds the order of `docs/design.md` section 7 and of the Phase 2 roadmap. |
| D-491 | 2026-09-22 | No full suite for documents | A change of documents alone runs no full test suite: not before a push of the author, not in a review or a repeat review, not in a review response, and not for a handoff. It runs `ste-check`, `doc-gate`, and the test category `Documents` in place of the suite. | Owner answer, 2026-09-22, in PR-73. Applies D-476, which stands: the `documents` job of CI runs the same category on each head. The category holds the byte ceilings, the identical agent files, and the handoff rules. F-102 shows that a documents change can break a test that `ste-check` does not read. The recommendation stood. `ste-check` alone, and `ste-check` with the category and no `doc-gate`, were the other options. |
| D-492 | 2026-09-22 | The documents of D-491 | A change is of documents alone when each path of the change lies in the skip set of D-475. | Owner answer, 2026-09-22, in PR-73. Applies D-475, which stands, so an agent and the CI skip read one set. The recommendation stood. The eligible set of D-190 and the metadata set of D-184 were the other options. |
| D-493 | 2026-09-22 | A change of code and documents | A change that holds a path outside the skip set runs the full suite, also when it holds documents too. | Owner instruction, 2026-09-22, in the prompt of PR-73: a push that changes code still runs the full suite. Applies D-492. |
| D-494 | 2026-09-22 | The test line of the PR gate | The PR gate line "Tests written and green (T-3)" adds a clause: a PR of documents alone needs `ste-check`, `doc-gate`, and the `Documents` category green in place of the suite (D-491). | Owner answer, 2026-09-22, in PR-73. T-3 stands for code. The recommendation stood. An unchanged line, and a line marked not applicable, were the other options. |
| D-495 | 2026-09-22 | The PR template | PR-73 does not change `.github/pull_request_template.md`. Its header points each line to the PR gate of the agent files, so the clause of D-494 applies through that header. | Owner answer, 2026-09-22, in PR-73. `.github/` is in the code set of D-190, and an edit there makes PR-73 a code PR, with the full CI and a cross-provider review. The recommendation stood. An edit of the template was the other option. |
| D-496 | 2026-09-22 | Meshy look reference | The Meshy images of 2026-09-22 under `artifacts/reference/meshy-miner-2026-09-22/` are a look reference for the body, and no mesh of Meshy loads. The body stays boxes in `content/models/player.bbmodel`, because the model reader reads cube and locator elements alone. A mesh import path is out of scope. The images stay out of the repository. | Owner choice, 2026-09-22, in the PR-62 session. Applies D-82, D-83, and D-339. Binds PR-74. The approved front concept is `02-concept-final-front.png`. The first face of `01-concept-first-face.png` failed D-83: eye whites and a flat pixel beard read as Minecraft. The body ignores these parts of the reference: shade painted into the texture (D-81), the front collar mirrored onto the back, the skin patches on the head sides, cut corners, and the brighter 3D texture. |
| D-497 | 2026-09-22 | Brow box | The body gains a brow box on the head bone: x -3 to 3, y 24.8 to 25.8, and 1 unit forward of the front face of the head box. It sits flush on the head face, a shared face and no penetration (D-301). It reads the hair material. Every head overlay of PR-22 encloses the brow box too (D-300). | Owner choice, 2026-09-22, in the PR-62 session. Applies D-82, D-83, D-300, D-301, and D-339. Binds PR-74 and PR-22. The owner measured about 6 by 1.2 units from the front concept. A lower edge at y 24.6 lands at 8.4 texels from the head top, so the owner chose 1 unit on the texel grid. A brow of 1.5 units, and the measured 1.2, were the other options. |
| D-498 | 2026-09-22 | Toe boxes | Each lower leg bone gains a toe box, flush against the front face of the lower leg box. It is as wide as the leg (4 units), about 3 units forward, and about 1.5 to 2 units high from the sole. The contact sheet of PR-74 settles the numbers. Every feet overlay of PR-22 encloses the toe box. | Owner choice, 2026-09-22, in the PR-62 session. Applies D-82, D-300, D-301, and D-339. Binds PR-74 and PR-22. Measured from the side views. The clip check of PR-57 tests the toe boxes against the other leg in the sword swing and the dodge. |
| D-499 | 2026-09-22 | Body proportions kept | Every current box of `player.bbmodel` keeps its size and place. The head is 8 by 8 by 8. The torso is 10 wide, 10.8 high, and 5 deep. Each arm is two boxes of 3 by 5.4 by 3. Each leg is two boxes of 4 by 5 by 4, with a 1-unit gap between the legs. The body is 28.8 units high, 1.8 m at 16 units to the meter. | Owner choice, 2026-09-22, in the PR-62 session. Applies D-82 and D-339. Binds PR-74. The animations and the sword locator stay valid. The concept is stockier: a torso of about 8.9 by 8.9, legs of 9.6, arms of 2.6, and 3.4 heads high. The owner chose the model proportions over it. |
| D-500 | 2026-09-22 | Body colors | The palette does not change. The shirt stays near rust 2 (base 17), the trousers and the boots near timber 1 (base 8), and the face and the hands near bone 2 (base 29). The hair, the beard, and the belt sit near timber 1. | Owner choice, 2026-09-22, in the PR-62 session. Applies D-304 and D-309, which stand. Binds PR-74. Sampled from the unlit front view of the 3D reference, which matches the current body materials. Revised in part by D-529 on 2026-09-23, the base steps only: each material takes the fine shade nearest the unlit 3D reference. The palette and the ramp of each part stand. Revised in part by D-530 on 2026-09-23, the ramp of the dark browns only: the trousers, the boots, the hair, the beard, and the belt move to umber. The shirt and the skin ramps stand. |
| D-501 | 2026-09-22 | Beard box | The body gains a beard box on the head bone: x -4 to 4, y 20.8 to 23.3, and 0.75 units forward of the front face of the head box, flush on that face. The mouth is a skin notch on the front face of the beard box. Every head overlay of PR-22 encloses the beard box too. | Owner choice, 2026-09-22, in the PR-62 session. Applies D-83, D-300, and D-301. Binds PR-74 and PR-22. A side view of the 3D reference shows the beard as a box. The contact sheet of PR-74 settles the numbers. A box 0.5 units deep, and a flat painted beard, were the other options. |
| D-502 | 2026-09-22 | Nose box | The body gains a nose box on the head bone: x -0.5 to 0.5, y 23.3 to 24.8, and 0.75 units forward of the front face of the head box. It reads skin. It touches the head, the brow, and the beard by shared faces alone (D-301). Every head overlay of PR-22 encloses the nose box too. | Owner choice, 2026-09-22, in the PR-62 session. Applies D-83, D-300, D-301, and D-497. Binds PR-74 and PR-22. A side view of the 3D reference shows the nose as a box. A painted nose alone was the other option. |
| D-503 | 2026-09-22 | Body noise | The owner picks the noise of the cloth and the leather on the contact sheet of PR-74, from 0.12, 0.20, and 0.30, beside the front concept. The skin keeps 0.12, so the face stays clear. | Owner choice, 2026-09-22, in the PR-62 session. Applies D-309, which stands until that pick. Binds PR-74. The Meshy texture shows more mottle than 0.12, and part of it is painted shade. Keep 0.12, and 0.25 now for the cloth and the leather, were the other options. Superseded by D-527 on 2026-09-23: the owner rejected 0.12, 0.20, and 0.30 on the contact sheet. |
| D-504 | 2026-09-22 | Split of the art pass | The art pass of D-339 becomes five PRs, in this order before M-3. PR-62 is the texture recipe system. PR-74 is the body. PR-75 is the sword. PR-76 is the enemy models of PR-16. PR-77 is the scene light and the edge smoothing of OQ-181. PR-62 keeps the look of every block, the body, and the sword. The approval of a contact sheet as finished art moves to PR-74. | Owner choice, 2026-09-22, in the PR-62 session. Applies G-10. Revises in part D-339, the scope of PR-62 only. The pass, its parts, and its place before Gate 2 stand. OQ-181 blocks PR-77, and not PR-62. The light and the edge smoothing share one frame time measurement on the Deck (D-295). One whole PR-62, four later PRs, and a sword with the enemies were the other options. |
| D-505 | 2026-09-22 | Texture recipe system | A recipe file under `content/textures/recipes/` is an ordered list of paint layers. A model names a recipe for each box face. The generator sizes each face at 32 texels per meter from its box, paints it, packs it into the atlas, and writes `content/textures/layout.json`. Game reads the atlas place of each face and each block from the layout, and not from the UVs of the model file. A test fails when the committed layout differs from the generator output. A recipe can extend another recipe with a color swap. A block is a recipe of 32 by 32 texels. | Owner choice, 2026-09-22, in the PR-62 session. The owner asked for one system that also serves the enemies, the items, the armor, the equipment, and props such as stalactites and rocks. Applies D-85, D-86, D-91, D-305, and D-308. Revises in part D-307 and D-330, the fixed tile places only. The materials and their colors stand (D-309). One layer list per tile with hand UVs, and a text grid per face, were the other options. |
| D-506 | 2026-09-22 | Atlas side | The atlas is 512 by 512 pixels. The generator stops with an error that names the recipe when the atlas has no room for it. A larger atlas needs a new decision. | Owner choice, 2026-09-22, in the PR-62 session. Revises in part D-308, the file side of 256 only. The density of 32 texels per meter stands. One body with its own faces takes about five tiles of 32 pixels, and the 256 atlas holds 64. A 1024 atlas, and 256 until the packer fails, were the other options. |
| D-507 | 2026-09-22 | First layer kinds | The first recipe format has five layer kinds: `fill` with noise, `edge`, `rect`, `band`, and the color swap of a recipe that extends another. Each layer names its `kind`. A later kind, such as a speckle, a stripe, a streak, or a stamp, comes with its first two users and no change of the format. An unknown kind is an error. | Owner choice, 2026-09-22, in the PR-62 session. Applies D-111, which stands. The users of `rect` and `band` are the face, the cuffs, the collar, the belt, and the boot band of PR-74. The users of the swap are the enemy families of PR-76. A full set of kinds with no user yet was the other option. Revised in part by D-527 on 2026-09-23, the format only: `fill` and `rect` gain a `shade` field, and the kinds `grain` and `gradient` join with their users. The other kinds and the swap stand. |
| D-508 | 2026-09-22 | Recipe binding file | A model names its recipes in a file next to it, `content/models/<model>.paint.json`. The file gives a recipe for each box, and a recipe for a single face where that face differs. | Owner choice, 2026-09-22, in the PR-62 session. Applies D-86 and D-298. Binds PR-62, PR-74, PR-75, PR-76, and PR-22. Blockbench opens and saves the model file with no loss, and an overlay or an enemy binds by the box name. A recipe field in each face of the model file was the other option. |
| D-509 | 2026-09-22 | Art skill for every asset | PR-62 adds the skill `asset-texture-creation`. It gives the five steps of the art of every asset. First, the agent writes a Meshy prompt for the 2D image. Second, the agent reviews the image, and approves it or suggests changes. Third, the owner makes the 3D model from the image. Fourth, the agent reviews screenshots from several angles, and approves them, asks for more angles, or suggests changes. Fifth, the agent builds the asset as boxes, recipes, and a paint file, as PR-74 builds the body. | Owner instruction, 2026-09-22, in the PR-62 session: "as part of this PR". Applies D-83, D-86, D-131, D-139, D-339, D-496, and D-505 to D-508. Binds PR-74, PR-75, PR-76, PR-22, and every later model PR. The owner chose to put the skill in PR-62, beside the recipe system that its fifth step uses (G-10). |
| D-510 | 2026-09-23 | PR-62 look kept | The owner confirms exit test 6 of PR-62: the contact sheet after the recipe system keeps the look of the sheet before it. The blocks and the ramps match pixel for pixel. The body and the sword keep their materials and noise amount, and the new draw of the noise on each face is the same look. | Owner answer, 2026-09-23, in the PR-62 session, after the review of PR #92. Closes exit test 6 of PR-62. Applies D-504 and D-505. A noise offset for each face in the paint file, which keeps the body pixels of PR-14, was the other option. |
| D-511 | 2026-09-23 | Codex review command | `make codex-review PR=<n>` starts the cross-provider review through the Codex CLI. The `codex-review` command of Tools holds the logic, and the Makefile target stays thin. The command passes the model `gpt-6-luna`, the reasoning effort `medium`, the approval policy, and the sandbox on the command line, and never takes them from the user configuration. It refuses to start when the CLI is missing or older than the minimum, the model probe fails, the PR is not open, the local branch differs from origin, the working tree is dirty, or the gitar pass of the head is not complete. Codex runs in a detached worktree at the PR head. The prompt is `Review PR #N`, a line to load `pr-review`, and a line to push the review record and the handoff entry as the metadata commit with `HEAD:<branch>`. After Codex exits, the command fetches, reads the record from origin, checks the head field against the effective head, and prints the verdict, the open finding ids, and the transcript path. The exit codes: 0 approve, 10 changes required or blocked, 11 three-strike stop, 1 fault, 3 refusal, and 2 usage. A fault is no record, a stale head, no pushed commit, a moved effective head, or a Codex error. | Owner direction, 2026-09-23. Automates the start of the review of T-4 and D-101, which stand. The review depth and the record format do not change. Revises in part D-103, the start of a reviewer session only: the author session starts it with this command. The rule of no scheduled agents stands. Revises in part D-375, the start of the reviewer session only: the Codex process is a session of its own, with the reviewer role. One invocation, one PR, and one role stand, and D-121 stands. The sandbox is `danger-full-access`, the value of the user configuration on 2026-09-23, because the reviewer runs `gh`, `git push`, and the build. The target name and the model id are tool configuration, not attribution (D-176). A review can take longer than the ten-minute tool limit of the author harness, so the author runs it in the background and waits for the completion notice. Binds PR-78. Revised in part by D-543 on 2026-09-23, the refusal on the gitar pass only: the flag `--skip-gitar-review` drops it. The rest stands. |
| D-512 | 2026-09-23 | Codex CLI source | The review uses the npm CLI `@openai/codex`. The target runs `npm install -g @openai/codex@latest` before each review, and the command refuses a CLI older than 0.156.1. The make variable `CODEX` names the binary, with the default `$(npm prefix -g)/bin/codex`. | Owner answer, 2026-09-23, to the start question on the binary and to the follow-up on a mandatory update. Verified 2026-09-23: Homebrew holds 0.39.0, which is too old for the current models. The ChatGPT app bundles 0.155.0-alpha.9.2, and only the Sparkle updater of the app replaces it. The npm CLI 0.156.1 answered a probe of `gpt-6-luna` at effort `medium`, and an unknown model slug failed with exit 1. The binary can change between two rounds of one PR. The app bundle with a minimum version, and a refusal with an update by hand, were the other options. Binds PR-78. |
| D-513 | 2026-09-23 | Three-strike stop | When a review returns the same finding for the third time, the fix loop stops and the owner decides. `make codex-review` exits 11. The author session turns off auto-merge with `gh pr merge N --disable-auto` and stops the fix loop. It asks the owner, and gives the finding, the evidence of the reviewer, the answers of the author, and the options. The answer goes into the response file, and an answer that sets a rule also gets a D-#. | Owner direction, 2026-09-23. Revises in part D-209, the count only: D-514 counts the rounds in which the id is open, and the command stops the loop. The scope rules of D-209 stand. Binds `pr-review`, `review-response`, and PR-78. |
| D-514 | 2026-09-23 | Same finding | A finding returns one time for each review round in which it is open. The same id counts, and a finding that one round fixed and a later round reopened counts each round in which it is open. Each finding of a review record has an `Open at:` line with the effective head of each round in which it is open, in backticks, and each head one time. An open finding whose line does not name the head of the round is a fault of the command. The three machine-read parts of the record do not change. | Owner answer, 2026-09-23, to the start question on the same finding. Applies D-179 and D-269, which stand. A count of consecutive open rounds, which a fix resets, was the other option. Binds `pr-review` and PR-78. |
| D-515 | 2026-09-23 | Strike scope | P0, P1, and P2 findings count toward the three-strike stop. A P3 finding never counts. An approving verdict always wins over a count. | Owner answer, 2026-09-23. A P3 finding never blocks the merge, so it never drives the fix loop. Every severity was the other option. Binds PR-78. |
| D-516 | 2026-09-23 | Gated auto-merge | A PR merges itself when the green light holds: CI is green, the Codex review approves the effective head, and every gitar comment has its answer. The author session runs `gh pr merge N --auto --squash` only when three conditions hold. The last metadata commit, with the review record and the handoff, is on origin. The gitar pass is complete. The record gives `Ready for owner merge` for the effective head. The session then runs one wait on the checks (D-380) and reads the state of the PR. When the PR merges, the session writes the transitional prompt at once, and it does not wait for `Merged PR #N`. The owner can still merge a PR, or turn auto-merge off. | Owner direction, 2026-09-23. Revises in part D-102 and D-126, the actor of the merge only: GitHub auto-merge merges a PR with the green light. Trunk, short branches, squash merges, conventional prefixes, and no trailers stand. Revises in part D-383, the trigger of the transitional prompt only: the merge itself, or `Merged PR #N`. The ruleset of D-522 makes GitHub hold the gate, and D-519 sets the first use. The verdict name `Ready for owner merge` stays, because `review-gate` reads it. The night gate or a Mac leg can hold a merge for hours, and a leg that ends "not acquired" gets a re-run (D-358). Binds PR-78. Revised in part by D-524 on 2026-09-23, the green light only: the owner confirms each merge after a summary of one paragraph. The rest stands. Revised in part by D-542 on 2026-09-23, the condition that the gitar pass is complete only, during the pause. The answer to each gitar comment stands. |
| D-517 | 2026-09-23 | Override auto-merge | A documents-only PR with the `review-override` label merges by auto-merge too. The session turns auto-merge on when `review-gate` is green by the label, the gitar pass is complete, and the handoff entry is on origin. | Owner answer, 2026-09-23. Applies D-188 and D-190, which stand. The owner lets the session apply the label, so such a PR merges with no owner step. An owner merge of such PRs by hand was the other option. Binds PR-78. Revised in part by D-524 on 2026-09-23, the merge with no owner step only: the owner confirms the merge. The auto-merge with the label stands. Revised in part by D-542 on 2026-09-23, the condition that the gitar pass is complete only, during the pause. The rest stands. |
| D-518 | 2026-09-23 | Handoff of a review run | Each Codex review run adds its own handoff entry, with the author field `Codex`, in the metadata commit that holds its review record. | Owner answer, 2026-09-23. Applies D-146 and D-182, which stand. The rounds of one PR fill the ten-entry window sooner, and `handoff-rotate` moves the older entries to the archive (D-379). A review record with no entry was the other option. Binds `pr-review` and PR-78. |
| D-519 | 2026-09-23 | PR-78 merge and setup | The owner merges PR-78 by hand. When the owner says `Merged PR #N`, the PR-78 session asks the owner to approve the setup. It then turns on `allow_auto_merge`, creates the ruleset from `.github/rulesets/main.json` on `main`, and shows that the live ruleset matches the file. Then it writes the transitional prompt. The first auto-merge is on the next PR. | Owner answer, 2026-09-23. A one-time step before the prompt of `merge-prompt.md`, so the rules are on `main` before the first auto-merge. A setup by the next session, and a setup before the merge with an auto-merge of PR-78, were the other options. Binds PR-78. |
| D-520 | 2026-09-23 | Ruleset bypass | The ruleset of `main` has one bypass: the repository admin role, through a pull request merge alone (`bypass_mode` `pull_request`). No one pushes to `main` directly. | Owner answer, 2026-09-23. The bypass clears a night gate deadlock, where a fix PR cannot green the gate. No bypass, and an admin bypass on a direct push too, were the other options. Binds PR-78. D-535 removed the bypass on 2026-09-23, and D-537 restored it the same day. The answer stands. |
| D-521 | 2026-09-23 | Review gate enforced | `.github/review-gate-mode` holds `enforced` from PR-78. A PR with no review record gets a `failure` from `review-gate`. | Owner answer, 2026-09-23. Revises in part D-180, the timing only: the repository is public, so the check turns enforced and required now, and not at launch. Revises in part D-185, the PR that changes the file only: PR-78, and not a Phase 5 PR. The mode source and the rules of both stand. The workflow reads the mode from the base branch, so it applies to the PRs after the merge. GitHub counts a neutral conclusion as a pass for a required check (F-51). The advisory mode with the `evaluate` job also required was the other option. Binds PR-78. |
| D-522 | 2026-09-23 | Ruleset of main | `.github/rulesets/main.json` holds the ruleset of `main`, and the live ruleset matches the file. It allows squash merges alone and requires resolved conversations. It requires 20 checks from the GitHub Actions app by name: `documents`, the five CI test jobs, the three Smoke jobs, the four Bit identity jobs, `bots`, `det-lint`, `asset-qa`, `ste-check`, `doc-gate`, `night-gate`, and `review-gate`. It does not require the newest `main` on the branch. Each platform job has a check name with the prefix of its workflow, such as `smoke-linux-x64`. The author answers a gitar thread and then resolves it, after a fix too. | Owner direction, 2026-09-23. Revises in part D-170, the deferral of branch protection only: the ruleset protects `main` from PR-78, and the rule that no session pushes to `main` stands. Revises in part D-250, the resolve of a comment with merit only: the ruleset needs each thread resolved, and the rest stands. Records F-110. A repository test binds the file to the workflows, and `docs/runbooks/main-ruleset.md` holds the commands. The PRs go one at a time, so a rule for the newest `main` forces only a rebase. Binds PR-78. |
| D-523 | 2026-09-23 | ChatGPT login alone | No `make codex-review` run uses API pricing. The command removes `OPENAI_API_KEY`, `CODEX_API_KEY`, and `CODEX_ACCESS_TOKEN` from the environment of every Codex process that it starts, the probe included. Each Codex call passes `forced_login_method="chatgpt"`. The command refuses to start unless `codex login status` gives `Logged in using ChatGPT`. | Owner instruction, 2026-09-23, during the first review round of PR #93. Applies D-511 and D-512, which stand. Verified 2026-09-23 on the npm CLI 0.156.1: the CLI reads a credential from each of the three variables, and `login status` gave `Logged in using ChatGPT` also with a fake `OPENAI_API_KEY` set, so the status alone does not prove the login of a run. The removal changes the child process alone. A run of another repository, such as the deck gate of decktome, keeps its own environment and its own key. A key revoked at the provider breaks such a run, and this decision revokes no key. Binds PR-78. |
| D-524 | 2026-09-23 | Merge confirmation | Before a merge, the session writes a summary of the PR in one paragraph and asks the owner to confirm the merge with `AskUserQuestion`. The session turns on auto-merge only after the owner confirms. For a PR that the owner merges by hand, the summary comes at the hand-over. | Owner instruction, 2026-09-23. Revises in part D-516, the green light only: the owner confirmation joins CI, the approving review, and the answered gitar comments. The rest of D-516 stands. Revises in part D-517, the merge with no owner step only: a documents-only PR needs the confirmation too, and the auto-merge with the label stands. Binds `one-pr-one-session`, `CLAUDE.md`, `AGENTS.md`, and PR-78. Revised in part by D-533 on 2026-09-23, the form of the summary only: four short sections in place of one paragraph. The confirmation stands. |
| D-525 | 2026-09-23 | Face paint layout | The front face of the head box, 16 by 16 texels from the top left, reads the face recipe on skin. Hair covers rows 0 to 2, a hair peak covers columns 7 and 8 of row 3, and a sideburn covers column 0 and column 15 from row 3 down. Each eye is 2 by 1 texels of timber 1 (base 8) at row 8, in columns 4 and 5 and in columns 10 and 11. A row of bone 1 (base 28) lies under each eye, at row 9. The eyes and the under-eye texels have no noise. The front face of the beard box, 16 by 5 texels, holds the mouth notch in skin: columns 5 to 10 of row 1, and columns 5 and 10 of row 2. | Owner choice, 2026-09-23, in the PR-74 session. Applies D-81, D-83, D-497, D-500, D-501, and D-502, which stand. Measured from `02-concept-final-front.png` and `09-3d-head-front-close.png`. The owner reads the under-eye bone 1 as a skin tone, and not as painted shade (D-81). Each feature is 2 texels wide or more, so it reads at game zoom. The layout is symmetric about the middle of the face. A straight hairline of 4 rows, the jagged line of the concept, wider eyes, eyes of 2 by 2, a socket beside each eye, no under-eye texels, a mouth of 4 texels, and an open mouth were the other options. Binds PR-74. |
| D-526 | 2026-09-23 | Body trim paint | The four side faces of each lower arm box, 6 by 11 texels, read skin in rows 7 to 10 and a hem of rust 1 (base 16) in row 6. The down face of each lower arm reads skin. The front face of the torso, 20 by 22 texels, holds the collar: skin in columns 5 to 14 of rows 0 and 1 and in columns 8 to 11 of row 2, inside a hem of rust 1 one texel wide at its sides and bottom. The belt is timber 1 (base 8) in rows 20 and 21 of the four side faces of the torso. The four side faces of each lower leg box, 8 by 10 texels, carry a band of the top 2 rows one step lighter, timber 2 (base 9). Every other face reads plain cloth, leather, or skin, the toe boxes included. | Owner choice, 2026-09-23, in the PR-74 session. Applies D-496, D-500, and D-507, which stand. Measured from `02-concept-final-front.png`. The trousers and the boots are timber 1, the darkest step of its ramp, so a darker band clamps to no change, and the boot band goes one step lighter. The back of the torso has no collar (D-496). Skin of 5 rows, skin of 4 rows with no hem, a collar with no hem, a plain collar, a belt of 3 rows, a band at the sole, and a seam of rock 1 were the other options. Binds PR-74. Revised in part by D-529 on 2026-09-23, the color of the boot band only: the band stays one step lighter than the boot, now timber 2 plus 1 shade. The rest stands. Revised in part by D-530 on 2026-09-23, the ramp of the belt and the boot band only: both are umber. The layout stands. |
| D-527 | 2026-09-23 | Texture detail approach | The recipe system gains the detail of the 3D reference, and the textures stay procedural (D-505). A `fill` and a `rect` name a fine shade with a `shade` field, from -3 to 3 shades of D-528 away from the step of `color`. The `grain` kind adds clustered noise: a lattice of smooth values with a spacing of `cell` texels, an `amount` in fine shades, a dither of each texel, and a `seed`. The `gradient` kind shifts the pixels within a `depth` of one `side` by up to `shift` fine shades, strongest at the side. The noise of `fill` and `rect`, `edge`, and `band` keep their steps of the palette. The painter uses whole numbers alone for the new kinds, so each platform paints the same bytes. The rework is part of PR-74, and exit test 4 stays there. | Owner choice, 2026-09-23, in the PR-74 session, after the noise sheets of D-503. The owner: "None of them match the level of detail that the 3D model screenshots have." Measured on `07-3d-front-unlit.webp`: the shirt varies by about 0.05 of lightness, and one palette step is about 0.10. The variation of the reference comes in clusters of 2 to 4 texels, and a broad grime darkens the shirt bottom. Supersedes D-503. Revises in part D-507, the format only. Applies D-81, D-111, and D-505, which stand. The body gives the users: cloth, leather, skin, and hair use `grain`, and the torso and the boot use `gradient`. The owner keeps D-525 as it stands, also where the brow hides the eye row from the camera of the sheet. A texel grid sampled from the 3D reference, and hand-painted texel grids, were the other options. Binds PR-74, PR-75, PR-76, and PR-22. |
| D-528 | 2026-09-23 | Fine shades | Each ramp of `content/textures/palette.json` gains a list of nine shades, three between each pair of its steps, at a quarter, a half, and three quarters of the way in linear light. The palette holds 104 colors. The 32 colors of D-304 keep their flat indices 0 to 31, and the shades take 32 to 103 in ramp order, so every block keeps its pixels. A recipe names a shade by its step and its `shade` offset (D-527). | Owner choice, 2026-09-23, in the PR-74 session. Revises in part D-304, the count of colors only. The 104 colors are unique. A fine shade is about 0.025 of lightness. A test checks each shade against the interpolation of its steps. Seven shades for each ramp, and a truecolor atlas, were the other options. Binds PR-74 and every later texture. |
| D-529 | 2026-09-23 | Measured base colors | Each body material takes the fine shade nearest the median of the unlit 3D reference in OKLab. The cloth is rust 2 plus 2 shades. The leather, the belt, and the beard are timber 1 plus 1 shade. The hair is timber 1. The skin is bone 2. | Owner choice, 2026-09-23, in the PR-74 session. Revises in part D-500, the base steps only. The ramps of D-500 stand. Measured on `07-3d-front-unlit.webp`: the shirt `#6c2c17`, the trousers `#2e2213`, the boots `#271d0f`, the belt `#2c2416`, the face skin `#a98768`, the hair `#1b1309`, and the beard `#281d0e`. The steps of D-500 alone, darker than the reference, were the other option. Binds PR-74. Revised in part by D-530 and D-531 on 2026-09-23: the dark browns move to umber, the hair sits one shade higher, and the skin two shades lower. The cloth stands. |
| D-530 | 2026-09-23 | Umber ramp | `content/textures/palette.json` gains a ninth ramp, umber: `#160e05`, `#2b1f11`, `#413221`, and `#5d4c39`, with nine shades in linear light (D-528). Its colors take the flat indices 32 to 35. The trousers, the boots, the toes, the belt, the hair, and the beard read umber. The blocks and the sword keep their ramps. | Owner choice, 2026-09-23, in the PR-74 session, after the sheet of the fine shades. Revises in part D-304, the count of ramps only, and D-500, the ramp of the dark browns only. The timber ramp is red-brown, and the reference browns are olive-brown. Measured on `07-3d-front-unlit.webp`: the median hue and chroma in OKLab of the texels of the trousers, the boots, the hair, the beard, and the belt, at a lightness of 0.17, 0.25, 0.33, and 0.43. The 117 colors and shades are unique. Timber kept, and the neutral shades of rock, were the other options. Binds PR-74. |
| D-531 | 2026-09-23 | Body tuning | The skin is bone 2 minus 2 shades. The hair is umber 1 plus 1 shade, with a grain of amount 3. The beard is umber 1 plus 3 shades, with a grain of amount 3. The trousers are umber 2, the boots umber 1 plus 3 shades, and the belt umber 2 plus 1 shade. The trousers and the boots carry two grains, one of cell 2 and one of cell 4, amount 2. The eyes stay timber 1 (D-525). | Owner answers, 2026-09-23, in the PR-74 session, after the sheet of the fine shades: approach B stays, the skin is "a little too light compared to the model", the hair starts one shade above the ramp bottom so the grain moves both ways, and the leather gets larger patches. Revises in part D-529, the hair, the skin, and the dark browns. The browns come from the nearest umber shade to the medians of D-529. The value of the skin settles on the contact sheet. Binds PR-74. |
| D-532 | 2026-09-23 | PR-74 body approved | The owner approves the contact sheet b2 of PR-74 as finished art: the boxes of D-497, D-498, D-501, and D-502, the paint of D-525 and D-526, and the texture of D-527 to D-531. | Owner answer, 2026-09-23, in the PR-74 session. Closes exit test 4 of PR-74 (D-504). The owner compared b2 with `07-3d-front-unlit.webp` and the earlier sheet b1. A warmer skin ramp and more tuning were the other options. The sheet light stays with PR-77. |
| D-533 | 2026-09-23 | Merge summary form | Before the owner confirms a merge (D-524), the session writes four sections of a few sentences each. What: the change. How: the method. CI: green or not, with each red check named. Codex review: the verdict, `Ready for owner merge`, `Blocked`, or `Changes required`. | Owner instruction, 2026-09-23, in the PR-74 session. Revises in part D-524, the form of the summary only. The next process PR writes the form into `one-pr-one-session` and its reference `review-and-merge.md`. The PR-74 session uses the form at once. Binds that PR. Revised in part by D-552 on 2026-09-23, the form only: the summary is four questions with their answers. The four parts stand. |
| D-534 | 2026-09-23 | Documents after approval | The effective head that `review-gate` and `make codex-review` read skips each commit whose paths all lie in the skip set of D-475. A green `review-gate` then stays green after a later documents commit, and no new cross-provider review is due. The gitar pass still reviews each push. | Owner answers, 2026-09-23, in the PR-74 session. Revises in part D-184, the paths that the review follows only. The metadata set of D-184 stays the set of the gitar pass. The skip set holds `docs/`, `.claude/skills/`, `CLAUDE.md`, `AGENTS.md`, `README.md`, and `LICENSE`. A documents commit can then change the agent files or a register with no second review, and the owner accepted that. The other options were `docs/` with the README, and `docs/` without the registers. The change goes in one new process PR in the next session, with D-533, and PR-74 keeps its scope. |
| D-535 | 2026-09-23 | No ruleset bypass | The ruleset of `main` has no bypass actor, so no merge button offers "Merge without waiting for requirements to be met". A night gate deadlock needs the owner to set the ruleset to `Disabled` for one merge and back to `Active`, by the procedure of `docs/runbooks/main-ruleset.md`. | Owner instruction, 2026-09-23, in the PR-74 session: "Remove this as an option". Supersedes D-520. The session updated the live ruleset at once, and PR-74 carries the same file, test, and runbook change, by owner choice. A change of the file with the live update after the merge, and the bypass kept, were the other options. Superseded by D-537 on 2026-09-23: the owner restored the bypass. |
| D-536 | 2026-09-23 | PR-74 review start | PR-74 skips the gitar pass. Its Codex review starts by hand with the exact invocation of `make codex-review`: the model, the effort, the approval policy, the sandbox, the ChatGPT login alone, the removed API variables, a detached worktree at the PR head, and the review prompt. The session then applies the verdict checks of the command to the pushed record. | Owner instruction, 2026-09-23, in the PR-74 session: "We can skip gitar for this PR." Gitar posted no check and no dashboard on any head of PR #94. The start check of `make codex-review` needs both, and the command has no skip. Applies D-511, D-523, and D-250 for this PR alone, and they stand for every other PR. A `--skip-gitar` flag in PR-74, and a gitar pass after all, were the other options. |
| D-537 | 2026-09-23 | Ruleset bypass restored | The ruleset of `main` keeps the bypass of D-520: the repository admin role, through a pull request merge alone. The live ruleset took the file again the same day. PR-74 carries no bypass change, and no later PR carries one. | Owner instruction, 2026-09-23, in the PR-74 session: "Removing the override checkbox was a mistake - restore that". Supersedes D-535. D-520 stands. The checkbox is the escape of a night gate deadlock, and the owner uses it for the manual merge of the night fix of D-538. |
| D-538 | 2026-09-23 | Night fix and branch nights | The PR after the process PR of D-533 and D-534 fixes the softlocks of the night of 2026-09-23: the greedy-descender policy softlocked 26 of 5000 seeds at commit `e069e16`, and the night of `170f08c` passed. The same PR changes the night gate: a success record of a night by hand on the PR branch, at the PR head, counts green for that PR. A night on a branch keeps its record apart from the record of `main`, so it never replaces the record that other PRs read. | Owner instruction, 2026-09-23, in the PR-74 session: "Manually running the night gate MUST count as green on CI as long as it passes. It also should not need to be part of main when it runs." Revises in part D-275, the ancestry rule for the record of the PR itself, from the merge of that PR. The 48-hour limit, and the rule for a record of `main`, stand. Today a hand night on `main` after a merge turns the gate green in about 30 minutes (D-274, D-275). A night on a feature branch does not count, and `night-results` holds one record, so a branch night replaces the record of `main`. PR-72 (`837902b`) is the likely cause, the only Core change between the two nights. Revised in part by D-544 on 2026-09-23, the order only: the night fix comes after PR-80. The fix and the branch nights stand. Revised in part by D-557 on 2026-09-23, the record of `main` only: a green branch night becomes the record of `main` at the merge commit of its PR (D-555). The branch nights and the gate read stand. |
| D-539 | 2026-09-23 | Override label head | The third condition of the override label in D-190 keeps the metadata set of D-184. The `review-gate` job compares the label event with the newest commit outside the metadata set, the work head. A documents commit after the label then needs the label again. | Owner answer, 2026-09-23, in the PR-79 session. Applies D-534, which changes the paths that the review follows only, and the label is not a review. D-190 stands. The other option was the skip set for the label too, and then the third condition never fails, because the eligible set of D-190 lies inside the skip set. Binds PR-79. |
| D-540 | 2026-09-23 | Documents-only review | A PR whose every commit lies in the skip set of D-475 has no effective head. The `review-gate` job fails its review path with a message that names the `review-override` label, also with an approving review record. `make codex-review` refuses such a PR with exit 3 and names the label. | Owner answer, 2026-09-23, in the PR-79 session. Applies D-534 and D-188. The `review-gate` job gave the same result to a PR of metadata alone before. The other option was a fallback to the newest commit outside the metadata set for such a PR, so that a cross-provider review still approves it. Binds PR-79. |
| D-541 | 2026-09-23 | Override covers README | The `review-override` label covers each path of the skip set of D-475: `docs/`, `.claude/skills/`, `CLAUDE.md`, `AGENTS.md`, `README.md`, and `LICENSE`. The `review-gate` job reads the one list of the CI skip, so the skip set, the effective head, and the eligible set never disagree. | Owner instruction, 2026-09-23, in the PR-79 session: "Ensure README.md and LICENSE are covered by the review-override tag". Resolves PR #95 review finding P2-1: a PR that changed only the root `README.md` or `LICENSE` had no effective head (D-540), and the label refused it. Revises in part D-190, the eligible set only. D-475 and D-540 stand. Binds PR-79. |
| D-542 | 2026-09-23 | Gitar pause | The owner pauses the gitar requirement of D-250. The automated pass stops as a gate of the hand-over, the auto-merge, and the PR gate, until a later PR of the owner ends the pause. The author still answers each gitar comment that comes in. When a gitar review holds feedback, the session stops at once and tells the owner. Feedback is a finding: a review thread of gitar, or a finding in its dashboard. The author does no push wait and posts no `Gitar review` comment. It reads the PR comments with the comment export before `make codex-review` and before the merge summary. Each text of the pause outside the registers names D-542, so the PR that ends the pause finds each one with `grep -rn 'D-542'`. | Owner instruction, 2026-09-23, in the PR-80 session: "we must pause required gitar reviews until I specifically tell you to re-enable them as part of a future PR. The changes we make to make gitar reviews not-required must be easily reversible. We should still address gitar feedback if it comes in, just do not require it. IF AT ANY POINT a gitar review comes in that contains feedback, immediately stop and alert me". The owner chose the flow of the pause from three options. The other two were a `Gitar review` request with no wait, and the push wait with no block. After an alert, the owner decides on the gitar rules. Revises in part D-250, the requirement only: the answer to each comment stands. Revises in part D-303 and D-374, the push wait and the `Gitar review` request only, for the pause. Revises in part D-516 and D-517, the condition that the gitar pass is complete only. Binds PR-80 and each later PR until the pause ends. Revised in part by D-550 on 2026-09-23, the answer to each gitar comment only: a gitar notice needs no answer. The rest stands. |
| D-543 | 2026-09-23 | Skip gitar flag | `make codex-review PR=<n> -- --skip-gitar-review` passes the flag `--skip-gitar-review` to the `codex-review` command. The flag drops the Gitar check run and the Gitar dashboard from the start checks. Every other start check stays, the check of unresolved review threads included (D-522). The author uses the flag on each review round while the pause of D-542 holds. The flag stays after the pause ends. | Owner instruction, 2026-09-23, in the PR-80 session: "add a —skip-gitar-review flag (invoked via ‘make codex-review PR=x -- --skip-gitar-review’) to the make codex-review command that we must use as long as the gitar requirement is paused - this flag will NOT be removed later when I tell you to re-enable the gitar requirement". Make reads each word after `--` as a goal. The target passes each goal that starts with `--` to the command, and a rule that does nothing keeps make from a stop. The ruleset of `main` requires resolved conversations, so the thread check stays. Revises in part D-511, the refusal on the gitar pass only: the flag drops it. The rest of D-511 stands. Binds PR-80. |
| D-544 | 2026-09-23 | Pause before night fix | PR-80, the gitar pause of D-542 and D-543, goes before the night fix of D-538. When `night-gate` stays red on PR-80, the owner merges it with the ruleset bypass of D-537. | Owner answer, 2026-09-23, in the PR-80 session. D-538 gave the night fix as "The PR after the process PR of D-533 and D-534", and the owner then named the gitar pause "Our next PR". The owner chose this order and the bypass merge from three options. The other two were a wait for a green night, and the night fix first. Revises in part D-538, the order only: the night fix comes after PR-80. The fix and the branch nights of D-538 stand. Binds PR-80. |
| D-545 | 2026-09-23 | Open fall of a diagonal drop | A diagonal drop falls straight down the corner column from the height of the start. The cells of the corner column between the landing and the start hold no solid part. A side move can reach a floor under an overhang of the corner column, and that floor is no landing of a diagonal move. | Owner answer, 2026-09-23, in the PR-81 session. Records F-111. Revises in part D-486, the legal diagonal move only: a drop also needs the open fall. The rest of D-486 and D-489 stands. The side moves and `Reachability` do not change (D-488). The simulation version rises to 16 (G-20). Seeds 2669 and 2879 of the greedy descender softlocked on this drop alone at `e069e16`. The recommendation stood. The overhang fix alone, and a revert of the diagonal moves, were the other options. Binds PR-81. |
| D-546 | 2026-09-23 | An arrival ends a wedge | `PathFollower` starts the count of ticks with no gain again at each arrival at a waypoint. A path that leads away from its goal for more than four seconds then reads no wedge, and the bot does not jump at each step of it. A body that a slope wedges still arrives at no waypoint, so the rule of F-105 stands. | Owner answer, 2026-09-23, in the PR-81 session, in the same choice as D-545. Records F-111. Applies D-405 and F-105, which stand. On 24 of the 26 softlock seeds of the night of `e069e16`, the count read a wedge on a detour of a diagonal path. The bot then jumped at each step, and a jump under a low ceiling or on a ramp held it in a loop. Binds PR-81. |
| D-547 | 2026-09-23 | Branch night at the effective head | The record of a branch night passes the `night-gate` job of its PR when its commit is the effective head of the PR, or a later commit of the PR (D-534). Every commit after the effective head changes documents alone, so a night on any of them ran the same code. A later code commit needs a new night. | Owner answer, 2026-09-23, in the PR-81 session. Applies D-538 and D-534, which stand. The first question named the metadata set of D-184 in its text and the effective head in its label. The owner then chose the effective head of D-534 over the work head. The rule also takes a night at a later documents commit, because a night on the branch runs at its tip. The exact PR head was the other option. Binds PR-81. |
| D-548 | 2026-09-23 | Branch night re-runs the gate | A branch night re-runs the newest `night-gate` run of its branch when it ends, so the check of the PR reads the new record. `night.yml` takes the permission `actions: write` for that step. A run that has not ended reads the new record when it runs, and a branch with no PR has no run. | Owner answer, 2026-09-23, in the PR-81 session. Applies D-538, which stands. The recommendation stood. A re-run of the failed job by the author, as D-358 does for a lost leg, was the other option. Binds PR-81. Revised in part by D-559 on 2026-09-23, the skip of `main` only: a night on `main` re-runs the newest `night-gate` run of each open PR. The re-run after a branch night stands. |
| D-549 | 2026-09-23 | The sweep reads the box of the caller | The sweep builds its box again after each axis, from the start and the whole displacement so far, in the form of the caller. A body sweep takes the feet, and it builds each box as `PlayerBody.Box` does. A plain box sweep builds `min + moved` and `max + moved`. The box after a move is then the box that the sweep read, bit for bit. No tolerance enters the rule. | Owner answer, 2026-09-23, in the PR-81 session. Records F-112. Applies D-235, which stands: the skin and the overlap check do not change. The local night of PR-81 crashed seed 4119 of the greedy descender on floor 3. The sweep moved its box to max x = 8.0, a contact, and left a block at x = 8 out of the Z move. The body box came to 8.000001 and overlapped it. The recommendation stood. A half-skin tolerance on the cross-axis cell ranges, and a PR of its own with a merge by bypass, were the other options. Binds PR-81. |
| D-550 | 2026-09-23 | Gitar notices need no answer | A gitar notice is a gitar comment that names no specific item to address: a status line such as "Gitar is working", a plan or quota notice, or a summary with no finding. The author does not answer a gitar notice. The reviewing provider ignores it, and a gitar notice with no answer never blocks a verdict, the hand-over, or the merge. A gitar comment with a specific item still gets its answer, and a gitar review with feedback still stops the session (D-542). | Owner instruction, 2026-09-23, in the PR-81 session: "It needs to know as part of the pr-review skill to IGNORE unaddressed gitar comments if they do not have specific items that need to be addressed. We should not address these type of comments either." Revises in part D-250, the answer to each comment only: a gitar notice needs none. Revises in part D-542, the answer to each gitar comment during the pause only. The rest of D-250 and D-542 stands. The owner put the change in PR-81, an exception to G-10, so round 2 of its review reads the rule. The next PR was the other option. Binds PR-81. The line of the PR template follows in the next PR (D-551). |
| D-551 | 2026-09-23 | Template line in the next PR | The gitar line of the PR gate in `.github/pull_request_template.md` takes the rule of D-550 in the PR after PR-81, and that PR holds no other change. The fixed seeds of the night come in the PR after it. PR-81 leaves the template as it is. | Owner answers, 2026-09-23, in the PR-81 session: "Drop it from PR-81, make it the very next PR we do by making the session-handoff-prompt include it". The template lies outside the skip set of D-475, so a change of it in PR-81 moved the effective head, and the branch night at `4282206` stopped counting (D-547). An earlier instruction of the same session named the fixed seeds as the focus of the next session. The owner then chose the template PR first and the seeds after it. One PR for both, and the seeds first, were the other options. Binds the next PR and PR-81. Revised in part by D-554 on 2026-09-23, the scope only: the PR holds the template line and its test. The rest stands. Revised in part by D-560 on 2026-09-23, the order only: the fixed seeds of the night come after PR-83. The template line of the next PR stands. |
| D-552 | 2026-09-23 | The merge summary in questions | The merge summary of D-533 is always four questions with their answers: what the PR changes, how it does it, whether CI is green, and what the Codex review said. A point that needs the owner comes last, as one more question. | Owner instruction, 2026-09-23, in the PR-81 session: "ALWAYS give me that summary in Q/A format." Revises in part D-533, the form only: the four parts and their content stand. The file `references/review-and-merge.md` of `one-pr-one-session` holds the form. Binds every merge summary from PR-81 on. |
| D-553 | 2026-09-23 | The id of the template line | The PR of D-551, the gitar line of the PR template, takes the roadmap id PR-82. The entry lives in the Phase 2 roadmap, after PR-81 and before PR-75. | Owner answer, 2026-09-23, in the PR-82 session. D-551 named no id. PR-82 is the next free id. The recommendation stood. Applies D-551, which stands. |
| D-554 | 2026-09-23 | A test of the template line | PR-82 adds one test, `PullRequestTemplateGitarLineMatchesThePrGate`. It asserts that the gitar line of the PR template is the gitar line of the PR gate in the agent files. It fails on the old template. | Owner answer, 2026-09-23, in the PR-82 session. T-3 asks for a test of each change, and the template lies outside the skip set of D-475. A grep in the exit tests, as in PR-80 and PR-81, was the other option. The recommendation stood. Revises in part D-551, the scope only: the PR holds the template line and its test. The rest of D-551 stands. |
| D-555 | 2026-09-23 | Promotion tree test | A success record of a branch night stands for a merge commit on `main` when the tree of the merge commit differs from the tree of the night commit only in paths of the skip set of D-475. The test compares trees, and not history, because a squash merge always makes a new commit. | Owner answer, 2026-09-23, in the PR-83 session. Resolves OQ-183. Applies D-475 and D-534, which stand. The PR-81 merge `2071cb6` differs from the night commit `4282206` only in `.claude/skills/`, `CLAUDE.md`, `AGENTS.md`, and `docs/`. The metadata set of D-184, and an equal tree, were the other options. The recommendation stood. Binds PR-83. |
| D-556 | 2026-09-23 | Promotion window | A promoted record keeps the end time of the branch night. The 48 hours of D-115 and D-177 count from that time, as for a branch night (D-547). A branch night older than 48 hours does not promote. | Owner answer, 2026-09-23, in the PR-83 session. Resolves OQ-184. Applies D-547, which stands. The time of the push to `main` was the other option. The recommendation stood. Binds PR-83. |
| D-557 | 2026-09-23 | Promotion job | A workflow on each push to `main` finds the merged PR of the commit, reads `night-branch/<head branch>`, and applies D-555, D-556, and D-558. When they hold, it writes a success record at the merge commit to `night-results`. The record names its source: the branch, the night commit, and the run. | Owner answer, 2026-09-23, in the PR-83 session. Resolves OQ-185. Revises in part D-373 and D-538, the writer of `night-results` only: a promotion writes it too. A night on another branch still writes `night-branch/<branch>`, and the ref guard of the night stands. PR #97 merged with a green branch night, and PR #98 then read the red night of `e069e16`. A read of the branch record by the gate, and a night by hand after each merge, were the other options. The recommendation stood. Binds PR-83. Revised in part by D-563 on 2026-09-23, the source field only: the record names the branch and the night commit, and the commit message names the run of the promotion. The job and the rules stand. |
| D-558 | 2026-09-23 | Promotion order | A promotion writes `night-results` only when that branch holds no record, or when the commit of its record is a strict ancestor of the merge commit. A record at the merge commit or at a later commit stays, success or failure, so a newer failed night on `main` always wins. A malformed record also stays, and the job names it. | Owner answer, 2026-09-23, in the PR-83 session. Resolves OQ-186. In the PR-81 case, the failed record of `e069e16` names an older commit than `2071cb6`, and the branch night tested the code of `2071cb6`. The order of the end times was the other option. The recommendation stood. Binds PR-83. Revised in part by D-569 on 2026-09-24, one more condition of the write: the branch night ran each failed seed of the record of `main`. The order rule stands. |
| D-559 | 2026-09-23 | Gate re-run after a new record | After each new record on `night-results`, from a night on `main` or from a promotion, the newest `night-gate` run of each open PR on `main` runs again. A run that has not ended reads the new record when it runs. | Owner answer, 2026-09-23, in the PR-83 session. Resolves OQ-187. Revises in part D-548, the skip of `main` only. The re-run after a branch night stands. The gate runs on `ubuntu-latest`, so a re-run puts nothing in the queue of the Mac runner. A re-run after a promotion alone, and a re-run by hand, were the other options. The recommendation stood. Binds PR-83. |
| D-560 | 2026-09-23 | The id and order of the promotion | The promotion takes the roadmap id PR-83. The entry lives in the Phase 2 roadmap after PR-82 and before PR-75. The fixed seeds of the night come in the PR after PR-83. PR-83 runs beside PR-82, and the PR that merges second changes the line of PR-82 that names the next PR. | Owner answer, 2026-09-23, in the PR-83 session. D-551 read: "The fixed seeds of the night come in the PR after it." Revises in part D-551, the order only. The template line of PR-82 stands. PR-83 after the seeds was the other option. The recommendation stood. Binds PR-83 and the PR of the fixed seeds. |
| D-561 | 2026-09-23 | No promotion of the PR-81 night | The promotion applies from the merge of PR-83 on, that merge included. No job and no hand write promotes the PR-81 night at `2071cb6`. PR #98 needs a green record of its own: a night on `main` or a branch night (D-275, D-538, D-547). | Owner answer, 2026-09-23, in the PR-83 session. The PR-81 record ends its window at 2026-09-25 23:34 UTC. A dispatch of the job for `2071cb6`, and a hand write before this PR, were the other options. The recommendation stood. Binds PR-83 and PR-82. |
| D-562 | 2026-09-23 | A night keeps a later record | The publish step of a night on `main` keeps the record of `main` when the commit of that record comes after the night commit. The night then writes no record, and its run keeps the result. The night replaces every other record, an absent or malformed one included. | Owner answer, 2026-09-23, in the PR-83 session. Resolves OQ-188. Applies D-558 in the other direction. The option read "absent, or its commit is an ancestor of the night commit". The check keeps a record at a later commit alone, because to keep a malformed record, or one off the history of `main`, blocks every later night. When the build fails, the check cannot run, and the failure record replaces the record of `main`. A night that always writes was the other option. The recommendation stood. Binds PR-83. |
| D-563 | 2026-09-23 | The source of a promoted record | The field `promotedFrom` of a promoted record names the branch and the night commit. The commit message on `night-results` names the run of the promotion. The form of a night record does not change. | Owner answer, 2026-09-23, in the PR-83 session. Resolves OQ-189. Revises in part D-557, the source field only. A branch record holds no run id. A run id in each night record, from a new option of `night-record`, was the other option. The recommendation stood. Binds PR-83. |
| D-564 | 2026-09-24 | Fixed seeds and a rotating slice | The fixed set of each night stays the gate: seeds 1 to 5000 of each bot policy, and seeds 1 to 100000 of the seed sweep. Each night also runs a rotating slice of seeds past the fixed range. The UTC date of the night start selects the window of the slice. The run log and the night record name the window, so each failure replays. | Owner answer, 2026-09-24, in the PR-84 session. Resolves OQ-190. Revises in part D-116, the night count only: the seed sweep runs its slice beside the 100000 fixed seeds. The fixed set alone, and a window in place of the fixed set, were the other options. The recommendation stood. Binds PR-84. |
| D-565 | 2026-09-24 | A slice failure blocks | A failure of a slice seed fails the night like a failure of the fixed set. The night record reads failure, and the next merge waits for a fix (D-115). | Owner answer, 2026-09-24, in the PR-84 session. Resolves OQ-191. Applies D-115, which stands. The recommendation was a green record with a warning and a finding. The owner chose the block. A block after seven days was the third option. D-566 keeps the block until a fix. Binds PR-84. |
| D-566 | 2026-09-24 | The size and window of the slice | The slice holds 500 seeds of each bot policy and 10000 seeds of the seed sweep, one tenth of each fixed set. For a fixed range that ends at F, the slice of day k holds the seeds from F + 1 + k × S to F + (k + 1) × S, with the slice size S. Day 0 is a fixed start date in the code, so no seed runs twice. | Owner answer, 2026-09-24, in the PR-84 session. Resolves OQ-192. Applies D-564. The nights of 2026-09-22 and 2026-09-24 took 2 h 0 min and 3 h 16 min, and the slice adds about 12 to 20 minutes. One fifth, and a slice in place of the top tenth of the fixed range, were the other options. The recommendation stood. Binds PR-84. |
| D-567 | 2026-09-24 | The seed of a slice failure | A failed night record of `main` names its failed slice seeds. Each later night on `main` runs them again until a night passes them. The fix PR adds each seed to a list of extra fixed seeds in the repository, and each night runs that list with the fixed set. | Owner answer, 2026-09-24, in the PR-84 session. Resolves OQ-193. Applies D-565, which stands: without the carry, the next window can pass with no fix and end the block. The branch night of the fix PR runs the seed from the list. The list alone, and a test in the suite with no list, were the other options. The recommendation stood. Binds PR-84. |
| D-568 | 2026-09-24 | The id and place of the fixed seeds | The fixed seeds of the night take the roadmap id PR-84. The entry lives in the Phase 2 roadmap after PR-83 and before PR-75. | Owner answer, 2026-09-24, in the PR-84 session. Applies D-560, which stands. After PR-75 was the other option. The recommendation stood. Binds PR-84. |
| D-569 | 2026-09-24 | A promotion needs the carried seeds | Each night record names the carried seeds that its night ran. A promotion writes only when the branch night ran each failed seed of the record of `main`. Otherwise the case is `carry-missing`, the promotion writes nothing, and the next night on `main` decides. | Owner answer, 2026-09-24, in the PR-84 session. Resolves OQ-194. Revises in part D-558, with one more condition of the write: the order rule of D-558 stands. Applies D-565 and D-567. A promoted record that keeps the failed seeds, and no change, were the other options. The recommendation stood. Binds PR-84. |
